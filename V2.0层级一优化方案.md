
---

# 层级一增补设计：

## 二、 标签化与动态分组 (Tagging & Dynamic Grouping)

### 2.1 设计目标
解决“物理目录（按代码结构）”与“业务测试（按业务板块）”的维度冲突。实现灵活的跨模块资产管理。

### 2.2 数据模型增补

在原有的 `api_definitions` 和 `api_groups` 基础上，完善 Tag 机制。

```sql
-- 标签元数据表 (用于管理标签颜色、描述，实现标签统一管理)
CREATE TABLE project_tags (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    project_id BIGINT NOT NULL,
    name VARCHAR(50) NOT NULL, -- 如 "核心链路", "结算中心"
    color VARCHAR(20), -- UI展示颜色，如 #f50
    category VARCHAR(20) DEFAULT 'business', -- business(业务) | maintain(维护状态)
    UNIQUE KEY uk_project_name (project_id, name)
);

-- 接口与标签关联表 (多对多)
-- 虽然 api_definitions 里有 tags JSON 字段作为快照，但关联表方便倒排索引查询
CREATE TABLE api_tag_relations (
    tag_id BIGINT,
    api_id BIGINT,
    PRIMARY KEY (tag_id, api_id)
);
```

### 2.3 交互界面设计：双视图切换

在**层级一：接口列表**页面，增加**视图切换器**。

#### 视图 A：目录视图 (Directory View) —— *默认视图*
*   **左侧导航：** 树状结构，对应 `api_groups` 表。
    *   📂 用户服务 (User Controller)
    *   📂 订单服务 (Order Controller)
*   **逻辑：** 严格对应 Swagger 的层级结构。
*   **适用场景：** 接口开发人员、新接口归档、代码对齐。

#### 视图 B：标签视图 (Tag View) —— *业务测试视图*
*   **左侧导航：** 标签云/标签列表，对应 `project_tags` 表。
    *   🏷️ **#核心交易链路** (包含 User/Order/Pay 下的关键接口)
    *   🏷️ **#VIP权益** (包含 User/Marketing 下的接口)
    *   🏷️ **#待修复** (临时标签)
*   **逻辑：** 点击某个标签，右侧列表聚合显示所有打上该标签的接口和用例。
*   **适用场景：** QA 进行特定业务板块测试、冒烟测试。

### 2.4 功能交互流程设计

#### 功能 1：标签管理与打标
1.  **自动打标 (Sync)：**
    *   在“智能同步”时，将 Swagger 的 `Tags` 字段自动映射为系统的 Tag。
2.  **批量打标 (Manual)：**
    *   在接口列表勾选多个接口 -> 点击“设置标签” -> 输入或选择 `#结算中心`。
    *   系统更新 `api_definitions.tags` 并维护 `api_tag_relations`。

#### 功能 2：基于标签的执行 (Tag-based Execution)
*   **场景：** 测试人员想要测“结算中心”板块。
*   **操作：**
    1.  切换到“标签视图”。
    2.  点击 `#结算中心` 标签，筛选出 20 个接口。
    3.  点击列表顶部的 **“批量执行 (Run All)”**。
    4.  系统弹窗：“选择执行环境”（自动调用 1.4 中的鉴权逻辑）。
    5.  执行完成后，报告显示：“业务板块【结算中心】测试结果：18 Pass, 2 Fail”。

---

## 三、 方案交互影响矩阵 (Interaction Matrix)

这两个新增方案如何影响现有的其他功能？

| 现有功能 | 交互影响点 | 处理逻辑 |
| :--- | :--- | :--- |
| **智能同步 (Smart Sync)** | **Tag 处理** | 同步时需将 Swagger 的 tag 字段 merge 进系统 Tag，注意去重。 |
| **在线调试 (Debug)** | **环境依赖** | 调试面板必须增加“环境选择”下拉框，否则调试无法通过鉴权。 |
| **AI 基准用例生成** | **数据依赖** | AI 生成用例时，入参无需关心 Token，只需关注业务参数。Token 由环境配置接管。 |
| **层级三：测试计划** | **圈选范围** | 创建测试计划时，选人不再只选“目录”，可以选“标签”。例如：`Include Tags: ['#P0', '#Core']`。 |

---

## 四、 总结

通过增加这两个设计细节：

1.  **环境与动态鉴权：** 你解决了“用例无法独立运行”的工程化难题，实现了**用例逻辑与鉴权逻辑的解耦**。
2.  **标签与动态分组：** 你解决了“代码结构与业务结构不一致”的管理难题，提供了**灵活的测试圈选维度**。

现在，层级一的设计已经具备了极高的落地性和实战价值。请将这些内容更新到你的主设计文档中。