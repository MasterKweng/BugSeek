
---

# 模块设计：环境管理与动态鉴权 (Environment & Dynamic Auth)

## 一、 设计目标
解决接口自动化测试中“环境差异化”与“身份认证依赖”的核心痛点，实现：
1.  **环境隔离：** 一套用例，通过切换环境配置，可无缝运行在 Dev/Test/Prod。
2.  **自动鉴权：** 执行用例前自动完成登录并注入 Token，用户无需手动抓包填 Token。
3.  **安全分离：** 登录逻辑（代码）与账号密码（敏感数据）分离存储。

---

## 二、 核心概念模型

采用 **“策略模板 + 环境变量”** 的分离设计模式：

1.  **全局鉴权策略 (Auth Strategy - Project Level):**
    *   定义 **“怎么登录”**。
    *   包含：登录接口 URL、请求体结构模板（含变量占位符）、Token 提取规则。
    *   *作用域：整个项目通用。*

2.  **环境配置 (Environment Config - Env Level):**
    *   定义 **“在哪登录”** 和 **“用谁登录”**。
    *   包含：Base URL、具体的账号/密码值（填充策略里的变量）。
    *   *作用域：特定环境（如 Test 环境）。*

---

## 三、 数据模型设计

### 3.1 环境表 (api_environments)
存储具体环境的基础信息和敏感变量值。

```sql
CREATE TABLE api_environments (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    project_id BIGINT NOT NULL,
    name VARCHAR(50) NOT NULL COMMENT '环境名称: Dev, Test, Prod',
    base_url VARCHAR(200) NOT NULL COMMENT '基础URL: https://api.test.com',
    
    -- 1. 全局 Header (JSON)
    -- 例: {"X-Client-Version": "2.0"}
    headers JSON, 
    
    -- 2. 环境变量 (JSON) - 存储具体的账号密码
    -- 例: {"auth_user": "admin", "auth_pass": "123456", "db_url": "..."}
    variables JSON, 
    
    is_default BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 3.2 项目鉴权配置表 (api_project_auth_config)
存储项目级别的登录逻辑模板。

```sql
CREATE TABLE api_project_auth_config (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    project_id BIGINT NOT NULL UNIQUE,
    
    -- 启用状态
    enabled BOOLEAN DEFAULT FALSE,
    auth_type VARCHAR(20) DEFAULT 'bearer', -- bearer | cookie | apikey
    
    -- 1. 登录接口定义 (模板)
    login_url VARCHAR(200) COMMENT '相对路径，如 /api/v1/login',
    login_method VARCHAR(10) DEFAULT 'POST',
    
    -- 登录请求体模板 (含变量占位符)
    -- 例: {"u": "{{auth_user}}", "p": "{{auth_pass}}"}
    login_body_template JSON, 
    
    -- 2. Token 提取与注入规则
    token_extract_expression VARCHAR(100) COMMENT 'JSONPath, 如 $.data.token',
    token_inject_header VARCHAR(50) DEFAULT 'Authorization', -- 注入到哪个Header
    token_inject_template VARCHAR(50) DEFAULT 'Bearer {token}' -- 值格式
);
```

---

## 四、 UI 交互设计流程

### 4.1 第一步：配置鉴权策略 (Project Settings)
*   **入口：** 项目设置 -> 鉴权配置。
*   **操作：**
    1.  **选择登录接口：** 从接口库中选择 `POST /login`。
    2.  **绑定变量：** 系统展示该接口的 Body 参数。用户将 `username` 字段的值改为变量 `{{auth_user}}`，将 `password` 改为 `{{auth_pass}}`。
    3.  **配置提取：** 输入 Token 在响应中的路径，如 `$.data.access_token`。
    4.  **保存：** 此时只定义了逻辑，没填真实密码。

### 4.2 第二步：配置环境凭证 (Environment Settings)
*   **入口：** 环境管理 -> 编辑“测试环境 (Test)”。
*   **操作：**
    1.  **基础信息：** 填写 Base URL (e.g., `http://10.0.0.1:8080`)。
    2.  **变量配置区：** 系统自动识别出鉴权策略里用到了 `{{auth_user}}` 和 `{{auth_pass}}`，自动生成两个输入框。
    3.  **填值：** 用户输入真实的测试账号 `test_admin` 和密码 `123456`。
    4.  **保存。**

### 4.3 第三步：执行与自动登录 (Runtime)
*   **入口：** 接口列表 -> 点击“执行” -> 选择“测试环境”。
*   **系统行为：**
    1.  **检测：** 发现“测试环境”开启了自动鉴权，且当前无缓存 Token。
    2.  **渲染：** 读取“鉴权策略模板”，用“测试环境”里的账号密码替换变量 `{{auth_user}}`。
    3.  **请求：** 向 `http://10.0.0.1:8080/api/v1/login` 发送带真实密码的请求。
    4.  **提取：** 从响应拿到 Token `eyJh...`。
    5.  **注入：** 将 Token 塞入用户要执行的接口 Header 中。
    6.  **执行：** 运行用户原本的测试用例。

---

## 五、 后端执行逻辑伪代码 (Python风格)

```python
def execute_test_case(case_id, env_id):
    # 1. 加载配置
    env = db.get_environment(env_id)
    auth_config = db.get_project_auth_config(env.project_id)
    
    # 2. 准备请求上下文
    session = requests.Session()
    session.headers.update(env.headers) # 应用全局Header
    
    # 3. 自动鉴权处理逻辑
    if auth_config.enabled:
        # 尝试从 Redis 获取该环境的缓存 Token
        token = redis.get(f"auth_token:{env_id}")
        
        if not token:
            # --- 执行登录流程 ---
            # A. 变量替换：用环境里的变量值，替换模板里的 {{key}}
            login_body = replace_variables(
                auth_config.login_body_template, 
                env.variables
            )
            
            # B. 发起登录请求
            login_url = urljoin(env.base_url, auth_config.login_url)
            resp = requests.request(
                method=auth_config.login_method,
                url=login_url,
                json=login_body
            )
            
            # C. 提取 Token
            token = jsonpath.extract(resp.json(), auth_config.token_extract_expression)
            
            # D. 缓存 Token (假设有效期 2 小时)
            redis.set(f"auth_token:{env_id}", token, ex=7200)
            
        # 4. 注入 Token 到当前 Session
        header_key = auth_config.token_inject_header # e.g., "Authorization"
        header_val = auth_config.token_inject_template.format(token=token) # e.g., "Bearer ey..."
        session.headers.update({header_key: header_val})

    # 5. 执行用户原本的测试用例
    case_url = urljoin(env.base_url, case.path)
    final_response = session.request(case.method, case_url, json=case.data)
    
    return final_response
```

---

## 六、 方案优势

1.  **多环境兼容：** Prod 环境用 Prod 账号，Test 环境用 Test 账号，逻辑完全隔离，互不干扰。
2.  **敏感数据安全：** 账号密码只存在于 `environments` 表中（建议加密存储），不会暴露在接口定义或代码仓库里。
3.  **配置复用：** 登录接口的逻辑改了（比如参数名变了），只需要在“项目设置”里改一次模板，所有环境自动生效。