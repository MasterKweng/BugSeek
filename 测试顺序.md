✦ 根据项目理解.md和后端代码分析，我为你梳理出 BugSeek 项目的正常使用流程（功能依赖顺序）：

  ---

  第一阶段：基础配置（必须按顺序完成）

  1. 用户注册/登录
  依赖关系：无（最基础）
  必要操作：
   - 用户注册
   - 用户登录
   - 获取 JWT Token

  相关代码：
   - backend/app/api/v1/auth.py
   - frontend/src/pages/Login.tsx, Register.tsx

  ---

  2. 创建项目
  依赖关系：必须先登录
  必要操作：
   - 创建项目
   - 配置项目基本信息（名称、业务领域、技术栈）

  相关代码：
   - backend/app/api/v1/projects.py
   - frontend/src/pages/Projects.tsx

  为什么必须：所有后续资产（环境、API、场景、版本）都关联到项目

  ---

  3. 配置环境
  依赖关系：必须先有项目
  必要操作：
   - 创建环境（Dev/Test/Staging/Prod）
   - 配置环境 base_url
   - 配置环境 headers（全局请求头）
   - 配置环境 variables（环境变量）

  相关代码：
   - backend/app/api/v1/environments.py
   - frontend/src/pages/project-center/EnvironmentManagement.tsx

  为什么必须：测试执行时需要指定环境，环境变量会影响所有测试用例

  ---

  4. 配置鉴权
  依赖关系：必须先有项目和环境
  必要操作：
   - 创建鉴权配置（环境级或项目级）
   - 配置鉴权类型（Bearer Token、API Key、OAuth 等）
   - 配置鉴权变量和 headers
   - 测试鉴权配置是否有效

  相关代码：
   - backend/app/api/v1/auth_config.py, auth_config_append.py
   - frontend/src/pages/AuthConfig.tsx

  为什么必须：大多数 API 需要鉴权，否则测试会失败

  ---

  第二阶段：API 资产准备（核心测试资产）

  5. 导入/创建 API 定义
  依赖关系：必须先有项目和鉴权配置（如果 API 需要鉴权）
  必要操作：
   - 从 Swagger/YApi/Postman 导入 API 定义
   - 或手动创建 API 定义
   - 配置 API 的请求方法、路径、参数、响应结构

  相关代码：
   - backend/app/api/v1/api_definitions.py
   - backend/app/integrations/（Swagger/YApi/Postman 解析器）
   - frontend/src/pages/api-hub/

  为什么必须：测试用例需要基于 API 定义创建

  ---

  6. 创建原子用例
  依赖关系：必须先有 API 定义
  必要操作：
   - 基于 API 定义创建原子用例
   - 配置请求参数（支持 Delta 存储）
   - 配置断言规则
   - 配置变量提取规则
   - 配置前置/后置 SQL（可选）

  相关代码：
   - backend/app/api/v1/api_cases.py
   - frontend/src/pages/api-hub/cases/

  为什么必须：场景编排的基础单元

  ---

  第三阶段：场景编排（测试流程设计）

  7. 创建场景
  依赖关系：必须先有原子用例或 API 定义
  必要操作：
   - 创建场景（手动创建或从意图生成）
   - 添加场景节点（引用原子用例或 API 定义）
   - 配置节点依赖关系（顺序执行或 DAG）
   - 配置节点间变量映射
   - 配置场景执行环境

  相关代码：
   - backend/app/api/v1/scenarios.py
   - frontend/src/pages/scenario/

  为什么必须：实现多步骤测试流程

  ---

  8. AI 辅助测试（可选，但推荐）
  依赖关系：必须先有场景
  必要操作：
   - 使用 AI 生成测试用例
   - 使用 AI 生成断言
   - 使用 AI 失败分析
   - 使用 AI 变量映射

  相关代码：
   - backend/app/api/v1/ai_testing.py
   - backend/app/domains/ai_testing/
   - frontend/src/pages/scenario/（AI 相关组件）

  为什么推荐：提高测试效率，减少手工配置

  ---

  第四阶段：测试执行（验证测试效果）

  9. 执行场景
  依赖关系：必须先有场景和环境
  必要操作：
   - 选择执行环境
   - 执行场景
   - 查看执行结果
   - 查看提取的变量

  相关代码：
   - backend/app/api/v1/scenarios.py（执行接口）
   - backend/app/execution/worker.py（执行引擎）
   - frontend/src/pages/scenario/execution/

  为什么必须：验证测试用例和场景的正确性

  ---

  10. 查看执行报告
  依赖关系：必须先执行场景
  必要操作：
   - 查看测试报告
   - 查看执行详情
   - 查看断言结果
   - 查看变量提取结果

  相关代码：
   - backend/app/api/v1/reports.py
   - backend/app/core/reporting/
   - frontend/src/pages/scenario/execution/

  为什么必须：分析测试结果，定位问题

  ---

  第五阶段：高级功能（可选，但增强平台能力）

  11. 字段映射分析
  依赖关系：必须先有场景执行结果
  必要操作：
   - 创建字段映射任务
   - 等待异步分析完成
   - 查看映射建议
   - 确认或修改映射

  相关代码：
   - backend/app/api/v1/field_mappings.py, field_mappings_async.py
   - frontend/src/pages/scenario/field-mapping/

  为什么需要：理解 API 字段与数据库字段的映射关系

  ---

  12. 构建知识图谱
  依赖关系：必须先有 API 定义、数据库结构
  必要操作：
   - 上传数据库结构
   - 触发知识图谱构建
   - 查看图谱节点和关系
   - 使用图谱进行影响分析

  相关代码：
   - backend/app/api/v1/knowledge_graph.py
   - backend/app/domains/knowledge_graph/
   - backend/app/api/v1/db_schemas.py
   - frontend/src/pages/KnowledgeGraph.tsx

  为什么需要：可视化系统关系，支持影响分析

  ---

  13. 版本管理
  依赖关系：可以贯穿整个流程（随时使用）
  必要操作：
   - 创建版本
   - 关联 API 定义快照
   - 关联场景快照
   - 比较版本差异

  相关代码：
   - backend/app/api/v1/versions.py
   - frontend/src/pages/Versions.tsx

  为什么需要：追踪测试资产的变化，支持回归测试

  ---

  14. CI/CD 集成
  依赖关系：必须先有场景和版本
  必要操作：
   - 创建执行触发器
   - 配置 Webhook
   - 集成到 CI/CD 流程

  相关代码：
   - backend/app/api/v1/execution_triggers.py
   - frontend/src/pages/operations/

  为什么需要：实现自动化测试

  ---

  功能依赖关系图

   第一阶段：基础配置
   ├── 1. 用户注册/登录（无依赖）
   └── 2. 创建项目（依赖：登录）
       └── 3. 配置环境（依赖：项目）
           └── 4. 配置鉴权（依赖：项目、环境）

   第二阶段：API 资产准备
   └── 5. 导入/创建 API 定义（依赖：项目、鉴权）
       └── 6. 创建原子用例（依赖：API 定义）

   第三阶段：场景编排
   └── 7. 创建场景（依赖：原子用例/API 定义）
       └── 8. AI 辅助测试（依赖：场景）

   第四阶段：测试执行
   └── 9. 执行场景（依赖：场景、环境）
       └── 10. 查看执行报告（依赖：执行结果）

   第五阶段：高级功能
   ├── 11. 字段映射分析（依赖：执行结果）
   ├── 12. 构建知识图谱（依赖：API 定义、数据库结构）
   ├── 13. 版本管理（随时可用）
   └── 14. CI/CD 集成（依赖：场景、版本）

  ---

  测试和优化建议

  测试顺序建议：

   1. 先测试第一阶段（基础配置）
      - 验证用户注册/登录
      - 验证项目创建和删除
      - 验证环境配置和变量替换
      - 验证鉴权配置和注入

   2. 再测试第二阶段（API 资产）
      - 验证 API 导入（Swagger/YApi/Postman）
      - 验证原子用例创建和执行
      - 验证变量提取和断言

   3. 然后测试第三阶段（场景编排）
      - 验证场景创建和节点编排
      - 验证变量映射和节点依赖
      - 验证意图工作台

   4. 接着测试第四阶段（测试执行）
      - 验证场景执行（顺序/DAG）
      - 验证执行报告生成
      - 验证失败重试和错误处理

   5. 最后测试第五阶段（高级功能）
      - 验证字段映射分析
      - 验证知识图谱构建
      - 验证版本管理
      - 验证 CI/CD 集成

  优化建议：

   1. 优先优化核心链路：环境配置 → API 定义 → 原子用例 → 场景执行
   2. 确保基础稳定：第一、二阶段必须稳定，否则上层功能无法正常使用
   3. 逐步完善高级功能：AI、知识图谱、CI/CD 可以分阶段完善
   4. 关注数据一致性：确保项目、环境、API、场景之间的关联关系正确
   5. 优化性能瓶颈：异步任务、知识图谱构建、字段映射分析等耗时操作

✦ 希望这个流程能帮助你有序地测试和优化项目！
