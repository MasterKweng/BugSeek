你好。作为算法老兵，我仔细审查了你的代码修改。

**总体评价**：
你的**算法逻辑（Algorithm Logic）**是非常清晰的，完美贯彻了我们商定的“Recall -> Gravity -> Re-rank”三段式策略。
但是，你的**工程实现（Engineering Implementation）**存在一个**致命的性能隐患**和一个**逻辑漏洞**。如果不修复，上线后第一波并发就会把你的服务器打垮。

以下是详细的 Code Review 和修复方案。

---

### 🚨 致命隐患：模型加载机制 (The Singleton Issue)

**问题代码**：
在 `app/api/v1/field_mappings.py` 的 `_generate_mapping_candidates_with_gravity` 函数中：
```python
# 你的代码
from app.utils.vector_index import VectorIndexManager
vector_manager = VectorIndexManager()  # <--- 致命错误在这里！！！
result = vector_manager.batch_search_with_gravity(field_names, top_k=20)
```

**后果分析**：
`VectorIndexManager` 的 `__init__` 方法里初始化了 `SentenceTransformer` 模型（约 80MB-100MB）并加载了 Pickle 缓存。
你把实例化放在了**请求处理函数**内部。这意味着：
1.  **每次** API 请求进来，都会重新加载一次 AI 模型和向量库。
2.  **耗时**：原本毫秒级的计算，变成了秒级（读取磁盘+模型初始化）。
3.  **OOM**：并发 10 个请求，就会在内存里加载 10 份模型，瞬间撑爆你的共享内存。

**修复方案**：
必须使用**单例模式（Singleton）**或在模块加载时初始化。

**修改建议**：
在 `app/utils/vector_index.py` 末尾添加全局实例，或使用 `lru_cache`。

```python
# app/utils/vector_index.py

# ... (类的定义保持不变) ...

# 添加全局单例
from functools import lru_cache

@lru_cache()
def get_vector_manager():
    # 确保只初始化一次
    manager = VectorIndexManager()
    # 如果索引未构建，尝试自动构建或加载
    if manager.column_vectors is None:
        manager.build_index() 
    return manager
```

然后修改 `app/api/v1/field_mappings.py`：
```python
# app/api/v1/field_mappings.py

from app.utils.vector_index import get_vector_manager  # 导入单例获取函数

def _generate_mapping_candidates_with_gravity(...):
    # ...
    try:
        # 获取单例，速度极快
        vector_manager = get_vector_manager() 
        result = vector_manager.batch_search_with_gravity(...)
```

---

### ⚠️ 逻辑漏洞：字段名冲突 (Key Collision)

**问题描述**：
如果一个 API 同时包含 `body.id` 和 `query.id`，你的 `field_names` 列表会变成 `["id", "id"]`。
在 `batch_search_with_gravity` 中：
```python
results = {}
for i, field in enumerate(api_fields):
    # ...
    results[field] = candidates # 如果 field 是 "id"，第二次循环会覆盖第一次
```
虽然在当前逻辑下，两个 `id` 的向量搜索结果是一样的，覆盖了似乎也没事。但**作为严谨的工程师，字典 Key 应该具有唯一性**，否则未来如果你想针对 `body.id` 和 `query.id` 做不同处理（比如 Body 权重更高），这里就会出错。

**修复方案**：
在 `vector_index.py` 的返回结果中，建议直接返回 List 顺序，或者由调用方维护映射关系。不过考虑到你现在的改动量，目前这个 Bug 是“软性”的（Soft Bug），暂时可以不改，但你要心中有数。

**更优的做法**是 `results` 的 Key 使用传入的 `api_fields` (即 path，如 `body.id`) 而不是 `split` 后的 `field_name`。但因为你在向量层只关心 `field_name`，所以这里需要小心。

---

### 💡 微调建议：分数归一化 (Score Clipping)

**问题**：
```python
if cand.get("db_table") == gravity_table:
    final_score += 0.3
if api_field_name.lower() == db_column.lower():
    final_score += 0.5
```
如果原始相似度是 0.9，加完分变成了 1.7。虽然 `sorted` 排序没问题，但前端展示“匹配度 170%”会很怪，甚至可能导致前端组件报错（如果前端以此画进度条）。

**修复**：
```python
cand["final_score"] = min(1.0, final_score) # 封顶 1.0
```

---

### ✅ 最终修正后的代码片段

请重点修改 **Model Singleton** 和 **Score Clipping**。以下是修正后的关键部分：

#### 1. `app/utils/vector_index.py` (修正 Score Clipping 和 单例)

```python
    def re_rank_candidates(
        self,
        candidates: List[Dict[str, Any]],
        gravity_table: str,
        api_field_name: str
    ) -> List[Dict[str, Any]]:
        # ... (前面的代码不变)
        
            # 规则2: 字段名完全匹配
            db_column = cand.get("db_column", "")
            if api_field_name.lower() == db_column.lower():
                final_score += 0.5
                if "reasons" not in cand:
                    cand["reasons"] = []
                cand["reasons"].append("字段名完全匹配")
            
            # --- 修正点：分数封顶 ---
            cand["final_score"] = min(1.0, final_score)
        
        # ... (排序代码不变)

# --- 修正点：文件末尾添加单例 ---
from functools import lru_cache

@lru_cache()
def get_vector_manager():
    """获取向量索引管理器的全局单例"""
    manager = VectorIndexManager()
    # 尝试加载索引（假设在应用启动时已经生成了索引，或者在这里惰性加载）
    if manager.column_vectors is None:
        try:
            manager.build_index()
        except Exception as e:
            # 记录日志，避免启动失败
            print(f"Vector index load failed: {e}")
    return manager
```

#### 2. `app/api/v1/field_mappings.py` (修正调用方式)

```python
# 导入修正
from app.utils.vector_index import get_vector_manager

def _generate_mapping_candidates_with_gravity(...):
    # ...
    try:
        # --- 修正点：使用单例 ---
        vector_manager = get_vector_manager()
        
        # 增加防御性检查：如果模型没加载成功，抛出异常触发回退
        if vector_manager.column_vectors is None:
            raise ValueError("Vector index not initialized")
            
        result = vector_manager.batch_search_with_gravity(field_names, top_k=20)
        # ... (后续逻辑不变)
```

**实施结论**：
除了上述提到的**单例模式是必须修改**的之外，其他代码逻辑都已经达到了生产级要求。你的方案在 Ryzen 7840HS 上运行起来会非常丝滑。请执行修改后即可进入测试。