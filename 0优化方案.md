# 🚀 字段映射引擎最终实施方案（生产增强版 - 完善版）

## 📋 核心设计理念

本方案采用**增量演进策略**，在不破坏现有五阶段代码骨架的前提下，通过三层安全网实现生产级增强：

1. **AI安全与柔性护栏**：防止幻觉，保留AI纠错能力
2. **结构增强与精准域推断**：精准圈定搜索范围，防止公共词污染
3. **全息可追溯体系**：建立完整审计日志，支持调参和自学习

## 🎯 解决的6个核心Issue

| Issue | 问题描述 | 解决方案 |
|-------|---------|---------|
| **Issue 1** | AI幻觉（造假） | 双层安全校验（物理存在+域合理性） |
| **Issue 2** | AI输出不规范 | 强制Prompt输出规范（confidence字段） |
| **Issue 3** | 公共词污染 | 权重分离的域推断 |
| **Issue 4** | 表名匹配失败 | 表名标准化处理（inflect） |
| **Issue 5** | 决策来源不明 | traces表增加decision_source |
| **Issue 6** | 搜索范围失控 | traces表增加in_allowed_tables |

---

## 🛠️ 实施计划（按Step顺序）

### **Step 1 (P0): AI 防护网**

#### 1.1 修改 `parse_json_safely` 确保提取 confidence

**目标**：从AI返回的JSON中提取confidence字段，如果缺失则默认赋值0.5

**需要修改的代码位置**：
- 文件：`backend/app/field_mapping/processor.py`
- 函数：`parse_json_safely` (约50-100行)

**改动点**：
- 在解析成功后，检查返回的字典是否包含`confidence`字段
- 如果不包含，添加默认值`0.5`
- 确保confidence在0.0-1.0范围内

#### 1.2 在 `_priority_ai_calling` 中增加双层校验

**目标**：实施物理存在校验（硬拒绝）和域合理性校验（软校验）

**需要修改的代码位置**：
- 文件：`backend/app/field_mapping/processor.py`
- 函数：`_priority_ai_calling` (约2186-2300行)

**改动点**：

**第一层：物理存在校验（硬拒绝）**
- 创建新方法`_column_exists(db_schema, table, column)`
- 检查AI返回的table是否在db_schema中
- 检查column是否在该表的列中
- 如果不存在，记录`AI_HALLUCINATION`，丢弃AI结果，回退到规则Top1

**第二层：域合理性校验（软校验）**
- 检查AI返回的table是否在`graph_context['valid_tables']`中
- 如果不在，记录Warning，将AI的confidence打折（*0.8）
- 不拒绝AI，保留AI纠错的可能性

---

### **Step 2 (P1): 数据库与 Trace 埋点**

#### 2.1 创建包含新字段的 `field_mapping_traces` 表

**目标**：在现有traces表基础上增加归因字段和域评分字段

**新增字段**：
- `decision_source`: 决策来源（'rule', 'ai', 'fallback', 'manual'）
- `in_allowed_tables`: 是否在DomainInferer圈定范围内

**完整表结构**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | PK | 主键 |
| `field_key` | STR | 关联的字段（如 `11:body.goods_id`） |
| `candidate` | STR | 候选表和列 (`order_items.goods_id`) |
| `stage` | STR | 处于哪个阶段 |
| `decision_source` | STR | 来源：`rule`, `ai`, `fallback`, `manual` |
| `in_allowed_tables` | BOOL | 是否在DomainInferer圈定范围内 |
| `is_anchor_table` | BOOL | 是否属于图谱计算出的核心表 |
| `s_vector` | FLOAT | 向量原始得分 |
| `s_exact` | FLOAT | 精确匹配得分 |
| `s_graph` | FLOAT | 图谱连通得分 |
| `final_score` | FLOAT | 最终加权得分 |

**需要创建**：
- Alembic迁移脚本：`backend/app/migrations/add_traces_decision_fields.py`

#### 2.2 在当前流程中注入写库代码

**目标**：在两个关键位置写入traces记录

**需要修改的代码位置**：

**位置1**：`processor.py` 的 `_batch_rule_scoring` 方法（约1000-1200行）
- 在`calculate_final_score_v2`后批量写入规则Traces
- 设置`decision_source = 'rule'`
- 设置`in_allowed_tables`（当前默认为True，Step 4后改为实际值）

**位置2**：`processor.py` 的 `_priority_ai_calling` 方法（约2186-2300行）
- 在AI处理完成后写入AI Traces
- 如果AI通过校验：`decision_source = 'ai'`
- 如果AI回退到规则：`decision_source = 'fallback'`

**需要创建**：
- 新方法：`_write_rule_traces(field_key, candidates, graph_context, allowed_tables)`
- 新方法：`_write_ai_traces(field_key, ai_candidate, is_hallucination, in_valid_tables)`

---

### **Step 3 (P1): 表名标准化工具**

#### 3.1 实现表名标准化工具函数

**目标**：将表名规范化，支持 `t_purchase_orders` 匹配到 `purchase_order`

**需要创建的新文件**：
- 文件：`backend/app/utils/name_normalizer.py`

**核心功能**：
```python
import inflect
import re

p = inflect.engine()

def normalize_name(name: str) -> str:
    """
    表名标准化处理
    
    步骤：
    1. 转小写
    2. 去除ERP常见前缀 (t_, sys_, erp_)
    3. 单词单复数归一化 (orders -> order)
    """
    # 1. 转小写
    name = name.lower()
    # 2. 去除前缀
    name = re.sub(r'^(t_|sys_|erp_)', '', name)
    # 3. 单复数归一化
    singular = p.singular_noun(name)
    return singular if singular else name
```

#### 3.2 安装依赖

**需要修改**：
- 文件：`backend/requirements.txt`
- 新增依赖：`inflect==6.0.4`

#### 3.3 编写单元测试

**需要创建**：
- 文件：`backend/tests/test_name_normalizer.py`

**测试用例**：
- `t_purchase_orders` → `purchase_order`
- `sys_users` → `user`
- `orders` → `order`
- `order` → `order`（单数不变）

---

### **Step 4 (P2): 域约束注入**

#### 4.1 实现带权重的 `DomainInferer`

**目标**：采用分离权重策略推断业务域

**需要创建的新文件**：
- 文件：`backend/app/field_mapping/domain_inferer.py`

**核心算法**：
```python
class DomainInferer:
    def infer_domain(self, api_path: str, fields: List[str]) -> str:
        """
        推断业务域
        
        权重分配：
        - 路径推断：0.7
        - 字段前缀统计：0.3
        """
        # 1. 路径推断 (权重 0.7)
        path_domain = self.extract_from_path(api_path)
        
        # 2. 字段前缀统计 (权重 0.3)
        field_prefixes = self.count_field_prefixes(fields)
        
        # 3. 加权合并
        return self.weighted_merge(path_domain, field_prefixes)
    
    def extract_from_path(self, api_path: str) -> str:
        """从API路径提取业务域"""
        # 例如：/api/purchase_orders/create -> purchase_orders
        
    def count_field_prefixes(self, fields: List[str]) -> Dict[str, float]:
        """统计字段前缀（忽略common_words）"""
        # 例如：order_id, order_date -> order: 2
        
    def weighted_merge(self, path_domain: str, field_prefixes: Dict[str, float]) -> str:
        """加权合并得出Top域"""
```

#### 4.2 将域推断集成到并发处理流程

**目标**：在API并发处理时，为每个API推断业务域，圈定allowed_tables

**需要修改的代码位置**：
- 文件：`backend/app/field_mapping/processor.py`
- 函数：`_process_api_with_semaphore`（约1560-1800行）

**⚠️ 重要：调用时机修正（解决暗雷3）**

**原始设计问题**：
- 原计划在`_extract_and_deduplicate_fields`之后调用
- 但现有并发流是按API切分的，在外部调用需要映射上下文，非常别扭

**修正后的调用位置**：
- 在`_process_api_with_semaphore`内部的第一步调用

**改动点**：
```python
async def _process_api_with_semaphore(self, definition, semaphore, graph, db_schema):
    async with semaphore:
        logger.info(f"[{self.trace_id}] 处理API: {definition.method} {definition.path}")

        # 1. 提取该API的所有字段
        all_fields, api_context = self._extract_all_fields_v2(definition)

        # 2. [新增] 推断业务域（修正：在内部调用，而非外部）
        domain_inferer = DomainInferer(db_schema)
        allowed_tables = domain_inferer.infer(
            api_path=definition.path,
            fields=[f.split('.')[-1] for f in all_fields]
        )

        # 3. [新增] 降级兜底（解决暗雷2）
        if not allowed_tables:
            logger.warning(f"[{self.trace_id}] 域推断失败，降级为全库搜索")
            allowed_tables = list(db_schema['tables'].keys())

        # 4. 带域约束的向量搜索...
```

#### 4.3 修改向量搜索调用

**目标**：仅在DomainInferer圈定的allowed_tables中进行向量检索

**需要修改的代码位置**：
- 文件：`backend/app/field_mapping/processor.py`
- 函数：`_process_api_with_semaphore`（约1560-1800行）

**改动点**：
- 将原来的：
  ```python
  candidates = vector_manager.search(field_info.field_name, top_k=10)
  ```
- 改为：
  ```python
  candidates = vector_manager.search_in_tables(
      field_info.field_name,
      allowed_tables,  # 来自DomainInferer
      top_k=10
  )
  ```

#### 4.4 修改Stage 5合并逻辑（解决暗雷1）

**目标**：让AI分数和规则分数进行"同台竞技"，避免软校验折扣被无脑覆盖

**需要修改的代码位置**：
- 文件：`backend/app/field_mapping/processor.py`
- 函数：`_merge_results`（约2400-2500行）

**⚠️ 原始设计问题**：
- 现有逻辑：`if field_key in ai_results: candidates = ai_results[field_key]`（直接无脑覆盖）
- 问题：Step 1.2中把AI的confidence打折（*0.8）就毫无意义了

**修正后的合并逻辑**：
```python
def _merge_results(
    self,
    field_registry: Dict[str, FieldInfo],
    ai_results: Dict[str, List[FieldMappingCandidate]],
    db_schema: Dict[str, Any]
) -> List[FieldMappingSuggestion]:
    """
    合并规则候选和AI候选，生成最终建议列表
    """
    suggestions = []
    
    for field_key, field_info in field_registry.items():
        # 合并候选：AI和规则同台竞技（修正：避免无脑覆盖）
        if field_key in ai_results:
            ai_top = ai_results[field_key][0]
            rule_top = field_info.rule_candidates[0] if field_info.rule_candidates else None
            
            # 让打折后的AI分数与规则分数PK
            if rule_top and ai_top.get('confidence', 0) < rule_top.get('final_score', 0):
                logger.info(f"[{self.trace_id}] AI置信度({ai_top.get('confidence', 0)})低于规则({rule_top.get('final_score', 0)})，回退到规则")
                candidates = field_info.rule_candidates
                decision_source = 'fallback'
            else:
                candidates = ai_results[field_key]
                decision_source = 'ai'
        else:
            candidates = field_info.rule_candidates or []
            decision_source = 'rule'
        
        # 生成建议
        suggestion = FieldMappingSuggestion(
            definition_id=field_info.apis[0]['definition_id'],
            definition_method=field_info.apis[0]['method'],
            definition_path=field_info.apis[0]['path'],
            api_field_path=field_info.field_path,
            candidates=[c.model_dump() for c in candidates[:3]],  # Top3
            status='pending',
            decision_source=decision_source  # 新增：记录最终决策来源
        )
        
        suggestions.append(suggestion)
    
    # 写入数据库建议表
    self._save_suggestions_to_db(suggestions)
    
    return suggestions
```

#### 4.5 更新traces写入逻辑

**目标**：在写入traces时，记录`in_allowed_tables`字段

**需要修改的代码位置**：
- 文件：`backend/app/field_mapping/processor.py`
- 函数：`_write_rule_traces`（新增）

**改动点**：
- 在写入traces时，检查候选表是否在`allowed_tables`中
- 设置`in_allowed_tables`字段的实际值（True/False）

---

## 💣 排雷记录（3个逻辑暗雷）

### 暗雷 1：Stage 5 的合并逻辑会"吃掉"软校验折扣

**问题描述**：
- Step 1.2中，如果AI的表不在域内，把AI的confidence乘以0.8（软校验）
- 但现有Stage 5逻辑直接无脑优先使用AI结果，导致分数打折毫无意义

**排雷方案**：
- 修改`_merge_results`，让AI分数和规则分数"同台竞技"
- 如果打折后的AI confidence低于规则final_score，回退到规则

### 暗雷 2：DomainInferer 导致 `allowed_tables` 为空的崩溃风险

**问题描述**：
- 对于奇葩API（如`/api/v1/do_something_weird/`），DomainInferer可能推断不出任何表
- 导致`allowed_tables = []`，向量库报错或返回空结果

**排雷方案**：
- 在`_process_api_with_semaphore`中增加降级兜底逻辑
- 如果`allowed_tables`为空，降级为全库搜索

### 暗雷 3：DomainInferer 的调用时机问题

**问题描述**：
- 原计划在`_extract_and_deduplicate_fields`之后调用
- 但现有并发流按API切分，在外部调用需要映射上下文，非常别扭

**排雷方案**：
- 将调用位置移到`_process_api_with_semaphore`内部的第一步
- 在并发处理每个API时，立即推断其业务域

---

## 📊 实施优先级总结

| Step | 优先级 | 涉及文件 | 预估工时 | 风险等级 | 备注 |
|------|--------|---------|---------|---------|------|
| **Step 1** | P0 | processor.py | 2小时 | 低 | AI安全网 |
| **Step 2** | P1 | migrations.py, processor.py | 3小时 | 中 | 数据库升级 |
| **Step 3** | P1 | name_normalizer.py (新) | 2小时 | 低 | 表名标准化 |
| **Step 4** | P2 | domain_inferer.py (新), processor.py | 4小时 | 中 | 域约束注入 |

**总工时**：约11小时

---

## ✅ 验证计划

### Step 1验证
- 运行一个包含AI调用的任务
- 检查日志中是否有`AI_HALLUCINATION`记录
- 验证AI返回的confidence是否正确提取
- 验证域合理性校验是否正确打折（*0.8）

### Step 2验证
- 检查`field_mapping_traces`表是否创建成功
- 运行任务后，查询traces表，验证是否有记录
- 验证`decision_source`字段是否正确（rule/ai/fallback）
- 验证`in_allowed_tables`字段是否正确（True/False）

### Step 3验证
- 运行单元测试`test_name_normalizer.py`
- 验证各种表名格式的标准化结果：
  - `t_purchase_orders` → `purchase_order`
  - `sys_users` → `user`
  - `orders` → `order`
  - `order` → `order`（单数不变）

### Step 4验证
- 运行完整任务
- 检查日志中的域推断结果
- 验证向量搜索是否只在`allowed_tables`范围内
- 验证降级兜底逻辑是否生效（允许空列表降级）
- 验证Stage 5合并逻辑是否正确（AI和规则同台竞技）
- 查询traces表，验证`in_allowed_tables`字段的准确性

---

## 🎯 最终目标

通过这4个Step的实施，将实现：

1. ✅ **AI零幻觉**（双层校验：物理存在+域合理性）
2. ✅ **精准域推断**（权重分离策略：路径0.7+字段0.3）
3. ✅ **表名智能匹配**（标准化处理：小写+去前缀+单复数归一化）
4. ✅ **完整审计追踪**（traces表升级：decision_source+in_allowed_tables）
5. ✅ **降级兜底**（域推断失败时自动降级为全库搜索）
6. ✅ **智能决策**（AI和规则同台竞技，避免无脑覆盖）

**系统将从"能跑"升级为"生产级健壮"，具备持续优化和自学习的基础。**

---

## 📝 关键技术亮点

1. **柔性护栏**：AI校验采用"硬拒绝+软校验"双层设计，既防止幻觉，又保留纠错能力
2. **权重分离**：域推断采用"路径0.7+字段0.3"分离权重，避免单一来源偏差
3. **表名标准化**：引入`inflect`库处理单复数，是做ERP系统的神来之笔
4. **同台竞技**：Stage 5合并时AI和规则公平竞争，避免软校验折扣被浪费
5. **降级兜底**：域推断失败时自动降级，确保系统不会崩溃
6. **全息可追溯**：traces表记录决策来源和域范围，支持后续调参和自学习

---

## 🚀 实施建议

1. **按Step顺序执行**：Step 1 → Step 2 → Step 3 → Step 4
2. **每个Step完成后验证**：确保当前Step的功能正常后再进入下一个Step
3. **保留回滚方案**：每个Step实施前，确保可以快速回滚到上一个稳定版本
4. **监控关键指标**：
   - AI幻觉率（`AI_HALLUCINATION`日志数量）
   - 域推断成功率（`allowed_tables`非空率）
   - 决策来源分布（rule/ai/fallback比例）
   - traces表写入成功率

---

## 📞 技术支持

如果实施过程中遇到以下问题，随时寻求支持：
- 并发锁问题
- SQL写入性能问题
- 向量库查询优化
- 域推断算法调优

**祝开发顺利！** 🎉