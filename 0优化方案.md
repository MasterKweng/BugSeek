🔍 根本问题分析（3 个“拦路虎”）
1. 关键词投毒依然存在（根源问题）
看日志：
提取关键词: ['POST', '测试登录流程']
[阶段一] 关键词召回完成: found=122 candidates
老兵诊断：
你之前的修复方案中，关于**“过滤 HTTP 方法关键词”的逻辑没有生效！系统依然把 POST 作为关键词去数据库里搜，导致召回了一大堆不相关的接口。这说明，你虽然设计了方案，但实际代码可能没有合进去**。
2. 向量检索的“语义漂移”（数据质量问题）
看日志：
向量检索成功了！found=50 results，这证明你的新架构跑通了！
但是，RRF 融合后的 Top10 依然是“扫码收货”、“退货入库”、“Webhook”...
老兵诊断：
这说明你的 API 数据（特别是 summary 和 description）语义质量不高。对于模型来说，“登录”这个词，和“扫码收货”（receive）、“退货”（return）在某些场景下可能确实有语义关联（比如都涉及权限、状态变更）。
解决办法： 我们必须给向量检索加一道硬核过滤（Hard Filter）。
3. 大模型“越狱”（Prompt 与解析问题）
看日志：
ERROR | json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)
AI 返回的不是纯 JSON，而是一大段 Markdown 文本：### 阶段一：业务步骤识别...
老兵诊断：
这说明你之前加的 _parse_ai_response（JSON 净水器）在最后的 intent_scenario_generation 这一步没有被调用！
你可能只在 intent_api_selection 后面加了，但忘了在第二步 AI 调用后也加上。同时，我们给 AI 的候选集太差了（全是不相关的 API），导致它彻底放弃，开始“自由发挥”写分析报告。
🛠️ 老兵的修复指令（三步彻底解决）
第一步：彻底铲除“关键词投毒”
打开 backend/app/services/api_retrieval.py，定位到 retrieve_apis_by_intent_lite 方法。在调用 self._keyword_recall(...) 之前，必须加入关键词清理逻辑。我强烈建议用我之前给的正则表达式：
code
Python
import re

# ... 在方法开头 ...
logger.info(f"[{self.trace_id}] 开始轻量级混合检索: intent='{user_intent}'")

# 1. 过滤掉干扰关键词（修复关键词投毒 Bug）
clean_intent = re.sub(r'(?i)\b(GET|POST|PUT|DELETE|PATCH)\b', '', user_intent).strip()
# 如果清理后为空，降级回原始意图
keyword_intent = clean_intent if clean_intent else user_intent

# 2. 关键词召回 (使用清理过的意图)
keyword_candidates = self._keyword_recall(
    user_intent=keyword_intent, 
    project_id=project_id
)
第二步：给向量检索加上“必杀规则”
打开 backend/app/utils/vector/api_strategy.py（或者你实现向量搜索的地方）。在执行向量搜索后，加上硬核过滤。
code
Python
# 在 APIVectorManager 的 search 方法里
async def search(self, query_text, project_id, top_k):
    # ... 原有向量计算逻辑 ...
    
    results = # ... 从向量库拿到的原始结果
    
    # === 新增：硬核过滤 ===
    # 如果意图里有“登录”、“认证”、“token”等词，优先只保留路径或摘要里包含这些词的 API
    login_keywords = ['login', 'auth', 'token', 'signin', 'session']
    if any(keyword in query_text.lower() for keyword in login_keywords):
        filtered_results = []
        for res in results:
            # 检查 path 或 summary
            if any(keyword in res['path'].lower() for keyword in login_keywords) or \
               any(keyword in res.get('summary', '').lower() for keyword in login_keywords):
                filtered_results.append(res)
        
        # 如果过滤后还有结果，就用过滤后的；如果一个都没，说明可能语义匹配上了但文本没写对，用原始的
        if filtered_results:
            results = filtered_results

    return results[:top_k]
这能保证，只要用户提到了“登录”，那些库存、订单接口连第一轮海选都过不了！
第三步：在所有 AI 调用后都加上“净水器”
打开 backend/app/api/v1/intent_workbench.py，找到 generate_scenario_from_intent 方法。
你肯定在 ai_selection 后面加了 _parse_ai_response，现在请在最后的 ai_result 后面也加上！
code
Python
# ... 阶段四：生成场景结构 ...
ai_result = await ai_service.execute(...)

# === 在这里加上净水器和强校验！ ===
try:
    # 先用净水器把 Markdown 洗掉
    parsed_result = _parse_ai_response(ai_result["result"])
    ai_result["result"] = parsed_result # 更新为纯净的 dict

    # 再用你之前写的校验逻辑
    validated_result = _validate_scenario_result(
        ai_result,
        selected_api_ids
    )
    
    logger.info(f"...")
    return ApiResponse(data={...})

except (ValueError, json.JSONDecodeError) as e:
    logger.error(f"[{trace_id}] JSON 解析或校验失败: {str(e)}, AI原始返回={ai_result['result'][:500]}")
    raise HTTPException(...)
这样，无论 AI 返回什么牛鬼蛇神，都会被你的净水器和校验器挡在外面，系统就不会再因为 JSONDecodeError 而崩溃了。