可以。
我按你**当前 V2.0 的真实代码结构**，把这 5 个 phase 拆成“怎么改、为什么这样改、靠什么机制实现目标、每阶段交付什么”。

先把基线说清楚：

当前 `FieldMappingAppService` 的主链是
**API/DB 提取 → history/lexical/vector/runtime 召回 → DeterministicRules → Minimal ranker → AI enricher → DecisionArtifact**，还没有 SQL/code lineage 召回器 。
当前排序器就是 `Minimal ranker for Phase 2`，本质仍是线性加权 。
当前规则层已经有 runtime/history/feedback/weak evidence 这些 hard reject/boost，但还没到高风险裁决层 。
当前契约层已经有 `ApiFieldSpec / CandidateEvidence / DecisionArtifact`，说明骨架够稳，可以继续扩展，不需要推翻 。
当前 runtime 证据仍是“表 prior + SchemaMapper.map_field()”这一路，不是 SQL AST/列级血缘 。
当前历史先验也主要是“当前项目、当前版本、confirmed/feedback”这一级别 。

---

# 总体改造原则

这 5 个 phase 里，真正拉高上限的顺序应该是：

**Phase 1 强证据层**
先补最缺的“血缘证据”。

**Phase 2 裁决层**
把“候选排序”升级成“可靠裁决”。

**Phase 3 规则层**
给高风险字段和复杂字段加硬边界。

**Phase 4 召回层增强**
让 history/vector/lexical 不再是简单相似度工具。

**Phase 5 运行时验证层**
把 runtime 从“表优先级”升级成“字段验证器”。

如果让我改，我会严格按这个顺序做。
因为这是**收益最大、返工最少、能持续验收**的顺序。

---

# Phase 1：补强证据层

## 目标

把当前系统从：

* lexical/vector/history/runtime 混合猜测

升级为：

* **lineage 强证据 + 其他证据补强**

这是上限版里收益最大的阶段。

---

## 你当前代码的问题

当前 `services.py` 里没有：

* `sql_lineage_recaller`
* `code_lineage_recaller`

而 `runtime_recaller.py` 只是基于 `ImpactRepository + SchemaMapper` 把 runtime table prior 转成候选，不是真正的列级 lineage  。

---

## 这一阶段具体要改什么

## 1）新增 SQL lineage 解析层

### 新增目录/文件

```text id="ylv3ok"
backend/app/domains/data_impact/
    sql_lineage_parser.py
    orm_lineage_parser.py
    lineage_repository.py
    lineage_service.py
```

### 要实现的能力

#### `sql_lineage_parser.py`

负责：

* 解析 SQL
* 找出 projection 列
* 识别 alias
* 展开 join path
* 提取 expression source columns
* 输出列级 lineage edge

#### `orm_lineage_parser.py`

负责：

* 解析 ORM / resultMap / mapper mapping
* 建立 DTO 字段与 DB 列的映射边

### 推荐实现方式

* SQL 层：用 SQL AST 解析器做 projection/join/expression 提取
* ORM 层：针对你实际项目里最常见的 ORM / Mapper 配置做针对性解析
* 先不要追求“全 SQL 方言通杀”，先覆盖你项目真实路径

### 新增落库表

建议新增：

```text id="1f9q2j"
sql_lineage_edges
- id
- project_id
- version_id
- definition_id
- source_sql_id
- source_table
- source_column
- target_alias
- expression_type
- join_path_json
- confidence
- payload_json
- created_at
```

---

## 2）新增 code lineage 解析层

### 新增目录/文件

```text id="wk4cd7"
backend/app/domains/data_impact/
    code_lineage_parser.py
```

### 要实现的能力

针对这些模式做解析：

* DTO / VO 字段赋值
* Assembler / Converter
* MapStruct 映射
* BeanUtils copy 后的显式覆盖
* 手写 `setXxx(getYyy())`

### 目标输出

```json id="pfs6p4"
{
  "definition_id": 123,
  "api_field_path": "response.userName",
  "db_table": "user",
  "db_column": "name",
  "evidence_type": "code_assignment",
  "confidence": 0.97
}
```

### 新增表

```text id="6k8tb0"
code_lineage_edges
- id
- project_id
- version_id
- definition_id
- api_field_path
- source_object
- source_field
- target_object
- target_field
- db_table
- db_column
- confidence
- payload_json
- created_at
```

---

## 3）新增两个召回器接入主链

### 新增文件

```text id="jlwmof"
backend/app/domains/field_mapping_engine/recall/
    sql_lineage_recaller.py
    code_lineage_recaller.py
```

### 它们返回什么

返回的仍然是你当前系统兼容的候选结构：

```python id="jlwmz9"
{
    "db_table": "...",
    "db_column": "...",
    "features": {
        "f_sql_lineage_exact": 1.0,
        "f_join_path_match": 0.9
    },
    "recall_sources": ["sql_lineage"],
    "explanations": ["SQL投影命中字段血缘"],
    "raw_payload": {...}
}
```

这样可以直接复用你现在的 `_merge_candidate_evidence()` 机制 。

---

## 4）改 `services.py`

### 现在怎么改

在 `FieldMappingAppService.__init__()` 里新增：

```python id="56sn4h"
self.sql_lineage_recaller = SQLLineageRecaller(db)
self.code_lineage_recaller = CodeLineageRecaller(db)
```

### 修改 `build_recall_artifacts()`

当前你 merge 的是：

* lexical
* vector
* runtime 

改成：

* history prior
* lexical
* vector
* runtime
* sql lineage
* code lineage

### 同时新增 item 级上下文

在 recall item 中增加：

* `domain_anchor`
* `field_risk_level`
* `api_summary_digest`
* `lineage_context`

---

## 这一阶段怎么实现目标

靠的是：

### 目标 1：提高 top1 准确率

方式：

* 用 SQL/code lineage 提供强候选，不再只靠“像不像”

### 目标 2：减少误杀

方式：

* lineage hit 的候选，优先级天然高于 vector/lexical 候选

### 目标 3：让 high-risk field 不再被弱证据带偏

方式：

* 强证据层成为后续 Phase 2/3 的基础

---

## 这一阶段完成后的验收

达到下面 3 条就算成功：

1. `services.py` 主链里已接入 `sql_lineage_recaller` 和 `code_lineage_recaller`
2. `candidate_evidence` 中开始出现 `f_sql_lineage_* / f_code_*`
3. SQL/代码强命中的字段，Top1 不再被 lexical/vector 轻易抢走

---

# Phase 2：替换排序与校准

## 目标

把当前“线性打分器”升级成：

## **硬规则短路 + 强特征排序 + 独立置信度校准**

---

## 你当前代码的问题

当前 `ranker.py` 自己已经写得很清楚：
它只是 `Minimal ranker for Phase 2`，主要还是小特征线性求和，再减 penalty 。

这对可用版够了，对上限版不够。

---

## 这一阶段具体要改什么

## 1）重写 `ranker.py`

### 不改类名，改内部实现

保留 `CandidateRanker` 这个入口，避免外部改动过大。
把内部拆成三段：

### A. `pre_rank_short_circuit()`

先处理：

* SQL lineage exact hit
* code assignment exact hit
* runtime field hit + exact name
* history confirmed + exact + same domain anchor

命中后：

* `short_circuit_accept = True`
* score >= 0.98

### B. `calculate_rich_score()`

把特征扩展成两类：

#### 强正特征

* `f_sql_lineage_exact`
* `f_sql_projection_hit`
* `f_join_path_match`
* `f_code_assignment_hit`
* `f_domain_anchor_match`
* `f_field_position_match`
* `f_sibling_context_match`
* `f_enum_dictionary_match`
* `f_data_type_match`

#### 负特征

* `f_domain_conflict_penalty`
* `f_high_risk_conflict_penalty`
* `f_join_impossible_penalty`
* `f_type_mismatch_penalty`
* `f_enum_conflict_penalty`

### C. `post_rank_margin_adjustment()`

看：

* top1-top2 margin
* strongest evidence count
* negative evidence count

margin 太小，则 confidence 不得太高。

---

## 2）新增 `confidence_calibrator.py`

### 新增文件

```text id="8i8u3n"
backend/app/domains/field_mapping_engine/ranking/confidence_calibrator.py
```

### 作用

把你当前 `services.py` 里的 `_calibrate_confidence()` 抽出去，并升级成独立校准器。

### 输入

* top1 score
* top2 score
* margin
* decision_source
* lineage strength
* runtime strength
* risk level
* negative evidence count

### 输出

* `confidence`
* `confidence_bucket`
* `review_policy`

例如：

* `auto_accept`
* `manual_review`
* `reject_auto`

---

## 3）改 `services.py`

### 现在要改的点

把：

* `_calibrate_confidence()`
* `_infer_relation_type()`

从 `services.py` 拆出去。

`services.py` 只负责 orchestrate，不再直接承担复杂裁决逻辑 。

---

## 4）新增 `relation_classifier.py`

### 新增文件

```text id="855334"
backend/app/domains/field_mapping_engine/evidence/relation_classifier.py
```

### 让 relation_type 不再只分：

* direct
* fk
* derived 

而是升级成：

* `direct`
* `alias`
* `fk`
* `joined`
* `derived`
* `enum_transform`
* `conditional`
* `runtime_verified`
* `uncertain`

---

## 这一阶段怎么实现目标

靠的是：

### 目标 1：让 score 真正可比较

方式：

* 更强特征
* margin-aware 排序
* 负证据显式进入裁决

### 目标 2：让 confidence 真正可信

方式：

* 分离“排名分”和“最终置信度”
* 建 review policy

### 目标 3：让 relation_type 成为真实业务语义

方式：

* 不再依赖 field suffix 的粗判断
* 基于 lineage / join / expression / dictionary 做分类

---

## 这一阶段完成后的验收

1. `ranker.py` 不再是 minimal formula
2. `confidence` 与 `score` 彻底分离
3. 每条 decision 有明确 `review_policy`
4. `relation_type` 明显比现在细

---

# Phase 3：升级规则层

## 目标

把当前 `DeterministicRules` 从“基础规则层”升级成：

## **高风险字段的硬边界守门员**

---

## 你当前代码的问题

当前规则已经有：

* exact boost
* history boost
* runtime conflict reject
* feedback reject
* weak evidence reject
* exact+strong evidence short-circuit 

这已经很好，但还缺“高风险”和“复杂字段”的专用规则。

---

## 这一阶段具体要改什么

## 1）扩展 `DeterministicRules`

### 增加风险识别

先做一个字段风险分层：

#### high risk

* amount
* balance
* status
* role
* permission
* deleted
* user_id
* create_by/update_by
* time fields

#### medium risk

* code/name/display/statusText
* foreign references

#### low risk

* 普通描述字段

### 实现方式

新增：

```python id="rzqzls"
def classify_field_risk(field_item) -> str:
    ...
```

并把结果写进 recall item：

* `risk_level = high|medium|low`

---

## 2）增加位置规则

### 规则内容

* `path.id` 优先主表主键
* `query.status` 更偏过滤条件字段
* `body.xxxName` 不应轻易判为主键/外键
* `response.*Count/Total/Display*` 不得轻易判 `direct`

### 实现方式

在 `DeterministicRules.apply_from_dict()` 中新增：

* `f_field_position_match`
* `f_position_conflict_penalty`

---

## 3）增加派生字段规则

### 规则内容

对这些字段：

* `*Count`
* `*Total`
* `*Amount`
* `fullName`
* `displayStatus`

若没有 expression lineage / runtime verified：

* 不允许判成 `direct`
* 降到 `derived` 或 `manual_review`

---

## 4）增加 domain anchor hard boundary

### 规则内容

如果：

* API path
* runtime table prior
* history prior
* module/tag

已经把 anchor 锚定在 `order` 域，
那 `user` 域、`role` 域这些泛字段不能只凭向量相似挤进 top1。

### 实现方式

新增：

```text id="jly6c8"
backend/app/domains/field_mapping_engine/evidence/domain_anchor.py
```

给每个 recall item 算：

* `allowed_tables`
* `anchor_score`

再在 `DeterministicRules` 中使用：

* `f_domain_anchor_match`
* `f_domain_anchor_conflict_penalty`

---

## 这一阶段怎么实现目标

靠的是：

### 目标 1：控制高风险误配

方式：

* 没强证据就不许 auto accept

### 目标 2：控制派生字段误判

方式：

* 没表达式血缘就不许 direct

### 目标 3：控制泛字段污染

方式：

* 引入 domain anchor hard boundary

---

## 这一阶段完成后的验收

1. 高风险字段的 auto_accept 显著减少误判
2. `Count/Total/Display*` 不再被大量判成 `direct`
3. runtime / domain anchor 冲突字段不会轻易进 top1

---

# Phase 4：升级 history / vector / lexical

## 目标

让弱证据层从“简单相似度工具”升级成“高质量补召回层”。

---

## 你当前代码的问题

当前：

* `HistoryRecaller` 主要只看当前项目当前版本 confirmed/feedback 
* `VectorRecaller` 仍属于字段名+描述语义召回
* `LexicalRecaller` 仍是基本名称/注释匹配

也就是说，这三层还没有“上下文感知”。

---

## 这一阶段具体要改什么

## 1）升级 `HistoryRecaller`

### 改造目标

从：

* 当前项目当前版本

升级为：

* 当前版本
* 跨版本
* 同业务域
* 同 path pattern
* 同 source_type（path/query/body）

### 新增方法

```python id="qbse6y"
build_cross_version_prior_map()
build_domain_prior_map()
build_path_pattern_prior_map()
build_feedback_weight_map()
```

### 关键机制

* accepted > modified > rejected
* 新反馈权重大于旧反馈
* 高质量 reviewer 权重大于普通 reviewer
* rejected 不能无条件压过 lineage strong hit

---

## 2）升级 `LexicalRecaller`

### 改造目标

把它从“字符串相似度”升级成“词法结构召回器”。

### 新增能力

* acronym expansion
* token role matching
* prefix/suffix semantic rules
* enum code/name pair recognition
* sibling co-occurrence

例如：
`userId + userName` 同时出现时，
候选 `user.id + user.name` 应该互相加强，而不是独立打分。

### 实现方式

在 lexical recall 阶段先构建：

* field family context
* sibling token clusters

---

## 3）升级 `VectorRecaller`

### 改造目标

让向量召回变成“带上下文约束的召回器”。

### 现在建议输入

不再只喂：

* field_name
* field_path
* description

改成喂：

* method
* path resource nouns
* field_name
* field_path
* description
* sibling_paths
* api summary
* domain anchor
* allowed_tables

### 关键约束

vector 只能：

* 扩召回
* 提供弱语义证据

不能：

* 单独做 high confidence
* 绕过 domain anchor
* 绕过 lineage/runtime conflict

---

## 这一阶段怎么实现目标

靠的是：

### 目标 1：弱证据更聪明

方式：

* 加上下文，不再单字段孤立召回

### 目标 2：减少向量误导

方式：

* vector 被 lineage/runtime/domain 限制

### 目标 3：让历史反馈更像“经验库”

方式：

* 跨版本、跨 path、跨 domain 的 prior 进入系统

---

## 这一阶段完成后的验收

1. `HistoryRecaller` 不再只认当前版本
2. `LexicalRecaller` 能利用 sibling/context
3. `VectorRecaller` 能接受 richer context + allowed_tables
4. 低证据字段的 top3 覆盖率明显上升，但误判不明显增加

---

# Phase 5：接入更强 runtime verification

## 目标

把现在的 runtime 从：

## “表优先级 + SchemaMapper 猜列”

升级为：

## “真实执行路径下的字段验证器”

---

## 你当前代码的问题

当前 `RuntimeEvidenceRecaller` 主要做两件事：

1. 读 API 对表的 impact
2. 用 `SchemaMapper.map_field(table, field_name)` 猜具体列 

这在生产里很有用，但它还是偏弱，因为：

* 它没有证明“这个响应字段真的来自这列”
* 它也没有证明“SQL projection 最终映射到了这个字段”

---

## 这一阶段具体要改什么

## 1）扩展 runtime evidence 类型

当前 `FieldMappingRuntimeEvidence` 已经有表，可以继续用。
新增 evidence_type：

* `runtime_table_prior`
* `runtime_column_verified`
* `response_value_match`
* `sql_projection_verified`
* `code_assignment_verified`

---

## 2）新增 response verifier

### 新增文件

```text id="vxx0vd"
backend/app/domains/field_mapping_engine/runtime/
    response_field_verifier.py
```

### 它做什么

对真实执行结果中的 API response leaf value，
与候选 DB 列值做“值级比对”。

### 不能直接拿原值怎么办

建议用：

* hash/fingerprint
* pattern signature
* enum value match
* timestamp shape match
* numeric range + uniqueness

这样能减少隐私/敏感数据问题。

---

## 3）新增 SQL projection verifier

### 新增文件

```text id="f4jkuw"
backend/app/domains/field_mapping_engine/runtime/
    sql_projection_verifier.py
```

### 它做什么

结合：

* `SqlTrace`
* SQL lineage parse result
* response field

验证：

* 这个 response 字段是否来自该 SQL projection
* 是否只是 join 出来的 label
* 是否来自 expression

---

## 4）改 `RuntimeEvidenceRecaller`

### 现在怎么改

让它不只生成：

* `f_runtime_table_hit`
* `f_runtime_field_hit`

而是新增：

* `f_runtime_column_verified`
* `f_response_value_match`
* `f_sql_projection_verified`
* `f_code_assignment_verified`

### 规则

这些特征一旦 >= 某阈值：

* 可进入 short-circuit
* 或大幅抬升 confidence

---

## 5）新增“运行时验证任务”

### 推荐做法

不要每次 suggest 时都现场跑验证。
改成：

* 离线采样真实请求/回放请求
* 产出 runtime verification evidence
* suggest 阶段只消费 evidence

这样性能和稳定性都更好。

---

## 这一阶段怎么实现目标

靠的是：

### 目标 1：让 runtime 成为列级验证证据

方式：

* 不只看表 impact，看 column verified

### 目标 2：让 response 字段不再只是“推断”

方式：

* 用值级匹配 + SQL projection 验证

### 目标 3：让 high-risk 字段 confidence 真正站得住

方式：

* 高风险字段只有 runtime/code/sql 强证据才允许高置信

---

## 这一阶段完成后的验收

1. `RuntimeEvidenceRecaller` 不再只靠 `SchemaMapper.map_field()`
2. runtime evidence 中出现 `runtime_column_verified / response_value_match`
3. 有 runtime verified 的字段，top1 和 confidence 明显更稳
4. 高风险字段的误判率进一步下降

---

# 五个 phase 做完后的系统形态

做完后，字段映射引擎会从现在的：

## **生产可用版 V2.0**

升级为：

## **高可靠上限版 V3 Ultimate**

它的特征会是：

* **lineage 是主骨架**
* **runtime 是验证器**
* **history/vector/lexical 是补召回层**
* **rules 是高风险守门层**
* **ranker + calibrator 是最终裁决层**
* **AI 只是受控补全层**

---

# 如果是我来改，我会这样排期

## 第一阶段先做什么

先做 **Phase 1 + Phase 2**。
因为这两步最直接决定“上限”。

## 第二阶段做什么

再做 **Phase 3**。
因为高风险字段和 domain anchor 必须尽早拉住误判。

## 第三阶段做什么

再做 **Phase 4 + Phase 5**。
这是持续优化与验证层，会让准确率更稳、更可解释。

---

# 最后给你一个非常实际的判断

如果你问我：

## “这 5 个 phase 里，哪个最值钱？”

答案是：

### **Phase 1**

因为你现在最缺的不是更多 AI，不是更多向量，而是：

## **真正的列级血缘证据**

如果你问：

## “哪个 phase 做完后，系统最像企业级？”

答案是：

### **Phase 2 + Phase 3**

因为那时它才真正具备：

* 可靠裁决
* 高风险守门
* 可解释 confidence

如果你问：

## “哪个 phase 做完后，效果最稳？”

答案是：

### **Phase 5**

因为 runtime verification 会把大量“看起来对”变成“真正被验证过”。

---
