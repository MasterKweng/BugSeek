1、新增项目版本时，通过API文档不同版本的对比，自动保留对应的接口测试用例、自动化测试用例以及后续所有模块的内容。
2、项目在新建版本时，分为全新版本和迭代版本两个类型：前者要求上传全量的API文档，上传后全新版本只会关联全量API中的接口；后者可选择上传全量或部分文档，自动比对后，将旧版本未变动的接口（以及对应的用例等）自动提到新版本中。但是二者均支持手动从之前版本中添加接口。
3、项目的某个版本允许上传多次文档，但后续只会新增/修改发生了变更的接口，如果未发生变更，则之前的接口信息不会改变。
4、让AI理解整个开发项目，保留下来AI的结果，用来场景组装是，作为项目背景介绍。
5、推送接口自动化测试代码之前，应当允许用户查看代码结构及细节，可以手动修改，然后一键推送
6、CI/CD 对应配置应当支持github、gitee、gitlab本地部署、云效等等主流平台地址
7、CI/CD 对应配置应当支持自动生成pipeline，保存pipeline，以及和接口自动化测试代码之间的交叉组合
8、脚本管理页面，支持接口层面查看执行记录



急需修改：
1、增加链路详情查看接口、页面；让AI设计这个链路详情的具体细节
2、将链路独立出列表，参考分组和接口页面的逻辑；分组对应的操作按钮需要重新设计