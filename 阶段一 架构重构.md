下面我给你的是**BugSeek V2 阶段一：架构重构（Architecture Refactor）完整工程方案**。
这是**平台级重构方案**，目标是：

**不推倒重写，90%代码保留，但把系统升级成可持续演进的架构。**

这一步的本质是：

> **把当前“技术分层架构”重构为“领域驱动架构（Domain Architecture）”。**

同时为后续三大能力铺路：

* Knowledge Graph
* Data Impact
* AI Testing Engine

---

# 一、阶段一目标（Architecture Refactor）

阶段一只做三件事：

### 1 代码结构重组

把当前结构：

```
api
services
core
utils
```

改为：

```
domains
platform
integrations
ai
execution
```

---

### 2 执行引擎解耦

当前：

```
core/test_execution.py
```

未来：

```
execution/
    engine
    scheduler
    worker
```

---

### 3 AI模块分层

当前：

```
ai/
    service
    prompts
```

未来：

```
ai/
    llm gateway
    embeddings
    adapters

ai_testing/
    agents
    pipelines
```

---

# 二、重构后的项目结构

后端结构升级为：

```
backend/
│
├── app/
│
│   ├── api/                # HTTP路由
│   │   └── v1/
│
│   ├── domains/            # 业务领域 ⭐
│   │
│   │   ├── auth/
│   │   ├── project/
│   │   ├── environment/
│   │   ├── version/
│   │
│   │   ├── api_hub/
│   │   │   ├── definitions/
│   │   │   ├── cases/
│   │   │   └── sync/
│   │
│   │   ├── scenario/
│   │   │   ├── models/
│   │   │   ├── services/
│   │   │   └── executor/
│   │
│   │   ├── execution/      # 执行层
│   │
│   │   ├── data_mapping/
│   │
│   │   └── test_data/
│
│   ├── platform/           # 平台基础设施
│   │
│   │   ├── db/
│   │   ├── cache/
│   │   ├── queue/
│   │   └── config/
│
│   ├── integrations/       # 外部系统
│   │
│   │   ├── openapi/
│   │   ├── postman/
│   │   ├── yapi/
│   │   └── database/
│
│   ├── execution/          # 执行引擎 ⭐
│   │
│   ├── ai/                 # LLM基础能力
│   │
│   └── utils/
```

---

# 三、目录迁移方案

现在我们逐个模块迁移。

---

# 1 API Router

当前：

```
app/api/v1/
```

保持不变。

但是 router 不再直接调用 service。

改为调用：

```
domain service
```

例：

```
api/v1/api_cases.py
```

调用：

```
domains/api_hub/cases/service.py
```

---

# 2 services 拆分

当前：

```
app/services/
```

问题：

```
services 变成万能垃圾桶
```

解决：

按领域拆分。

例如：

### API Cases

当前：

```
services/api_cases_service.py
```

迁移到：

```
domains/api_hub/cases/service.py
```

---

### API Definitions

当前：

```
services/api_definitions_service.py
```

迁移：

```
domains/api_hub/definitions/service.py
```

---

### Scenarios

当前：

```
services/scenario_service.py
```

迁移：

```
domains/scenario/services/scenario_service.py
```

---

# 四、Execution Engine 重构

当前：

```
core/test_execution.py
```

这是一个风险点。

必须拆分。

---

## 新结构

```
execution/

engine.py
scheduler.py
worker.py
context_bus.py
assertion_engine.py
result_store.py
```

---

## Engine

负责：

```
执行计划
```

```
class ExecutionEngine:

    def run_scenario(self, scenario_id):
        plan = self.build_execution_plan(scenario_id)
        scheduler.schedule(plan)
```

---

## Scheduler

负责：

```
调度任务
```

支持：

```
parallel
retry
timeout
```

---

## Worker

负责：

```
执行HTTP请求
```

---

# 五、Scenario Engine 重构

当前：

```
scenario executor
```

建议结构：

```
domains/scenario/

models/
services/

executor/

    dag_executor.py
    node_runner.py
```

---

## DAG Executor

负责：

```
拓扑排序
节点依赖
```

---

## Node Runner

负责：

```
执行单个API节点
```

---

# 六、Field Mapping 模块

当前：

```
field_mapping/
```

迁移到：

```
domains/data_mapping/
```

结构：

```
data_mapping/

extractor.py
vector_search.py
reasoner.py
ranking.py
```

---

# 七、向量系统重构

当前：

```
utils/vector
```

迁移：

```
platform/vector/
```

结构：

```
vector/

embedding_service.py
vector_store.py
hybrid_search.py
```

---

# 八、AI 模块拆分

当前：

```
ai/
```

问题：

AI逻辑和业务混合。

---

## 新结构

```
ai/

gateway.py
embedding_service.py

adapters/
    openai_adapter.py
    qwen_adapter.py
```

只负责：

```
调用模型
```

---

## AI Testing

新模块：

```
domains/ai_testing/
```

结构：

```
ai_testing/

agents/
pipelines/
analyzers/
```

---

# 九、Parser 重构

当前：

```
parsers/
```

迁移：

```
integrations/
```

结构：

```
integrations/

openapi/
postman/
yapi/
database/
```

---

# 十、Celery 任务结构

当前：

```
celery/tasks.py
```

拆分：

```
celery/tasks/

sync_tasks.py
field_mapping_tasks.py
ai_tasks.py
```

---

# 十一、数据库访问层

当前：

```
db/
```

升级：

```
platform/db/

session.py
repositories/
migrations/
```

---

# 十二、日志系统

升级：

```
platform/logging/

logger.py
```

支持：

```
structured logging
```

---

# 十三、配置系统

统一：

```
platform/config/
```

---

# 十四、重构步骤

按顺序执行：

### Step1

创建新目录结构。

---

### Step2

迁移 services → domains。

---

### Step3

拆分 execution engine。

---

### Step4

拆分 ai module。

---

### Step5

迁移 parser → integrations。

---

# 十五、代码迁移原则

必须遵守：

### 原则1

**不修改业务逻辑**

只移动代码。

---

### 原则2

每次迁移一个模块。

---

### 原则3

保证测试通过。

---

# 十六、重构周期

预计时间：

```
2-3 周
```

---

# 十七、阶段一完成后的架构

完成后系统变成：

```
BugSeek V2

Domain Layer
Execution Layer
AI Layer
Knowledge Layer
Infrastructure Layer
```

这时系统就具备：

**平台级扩展能力。**

---

# 十八、真实工程建议

在做这个重构前一定要做：

```
git tag v1
```

因为：

**架构重构一定会出 bug。**

---

如果你愿意，我可以继续给你一份**非常关键的工程文档**：

**BugSeek V2 数据库架构（40+表完整设计）**

这一步会决定：

> **AI 自动测试是否真正可实现。**
