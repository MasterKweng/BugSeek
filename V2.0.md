这是基于**“资产归一、编排统一”**理念重新设计的**AI测试平台核心架构方案**。

这个方案将原有的破碎流程整合为**三层金字塔架构**，彻底消除了冗余的脚本维护成本，并让 AI 在“数据映射”和“断言生成”上发挥最大价值。

---

# AI 自动化测试平台 v2.0 核心设计方案

## 〇、 核心架构图谱

```mermaid
graph TD
    User[用户] --> L1
    User --> L2
    User --> L3

    subgraph L1 [层级一：API 资产库 (API Hub)]
        D1[智能同步/Diff]
        D2[API 调试]
        D3[原子用例 (Atomic Cases)] -->|替代原测试脚本| D2
        AI1[AI: 断言生成 & 变更修复]
    end

    subgraph L2 [层级二：场景编排工作室 (Scenario Studio)]
        S1[可视化画布 (Flow Editor)]
        S2[逻辑控制器 (If/Loop/Wait)]
        S3[上下文总线 (Context Bus)]
        AI2[AI: 智能连线 & 步骤推荐]
    end

    subgraph L3 [层级三：计划与执行 (Plan & Insight)]
        P1[测试计划/集合]
        P2[CI/CD 触发器]
        P3[深度分析报告]
    end

    L2 -->|引用| D3
    L3 -->|调度| L2
```

---

## 一、 层级一：API 资产库 (API Hub)

**定位：** 唯一的“真理来源”（Single Source of Truth）。
**核心变革：** 废弃独立的“测试脚本”模块，将“测试用例”作为 API 的**衍生属性**直接挂载在接口定义下。

### 1.1 功能设计

#### 功能 1：智能同步与版本 Diff (Smart Sync)
*   **设计思路：** 解决“文档与代码不一致”的痛点。
*   **交互逻辑：**
    1.  用户点击“同步”（来源 Swagger/YApi）。
    2.  **AI 介入：** 系统对比库中现有接口，计算 **Diff**。
    3.  **变更预警：**
        *   **新增接口：** 标记 `New`。
        *   **变更接口：** 标记 `Changed`。如果某个字段被删除，系统自动扫描引用了该字段的所有“用例”和“场景”，并标记为“需修复”。
        *   **AI 自动修复：** 对于简单的字段改名（如 `userId` -> `user_id`），AI 尝试自动更新所有关联用例的映射关系。

#### 功能 2：原子用例管理 (Atomic Case Management)
*   **设计思路：** 一个接口对应 N 个原子用例。
*   **页面结构：** 左侧接口详情，右侧用例列表（Tab页）。
*   **用例类型：**
    *   **基准用例 (Default Case)：** 导入时由 AI 根据 Schema 自动生成，包含全字段的基础断言（非空校验、类型校验）。
    *   **业务用例 (Business Cases)：** 用户手动创建，如“登录-密码错误”、“登录-账号锁定”。
*   **配置项：**
    *   **入参覆写：** 继承接口定义的参数，但允许修改特定字段的值（支持参数化 `{{username}}`）。
    *   **后置操作 (Post-Actions)：** 提取变量（Extract）、断言（Assert）、数据库操作（SQL）。

### 1.2 数据模型 (Schema)

```sql
-- 接口主表 (原 api_endpoint)
TABLE api_definitions (
    id BIGINT PRIMARY KEY,
    project_id BIGINT,
    method VARCHAR(10),
    path VARCHAR(255),
    schema_snapshot JSON, -- 存储导入时的原始结构快照，用于比对 Diff
    version_hash VARCHAR(64)
);

-- 原子用例表 (替代原 test_scripts)
TABLE api_cases (
    id BIGINT PRIMARY KEY,
    definition_id BIGINT, -- 关联接口
    name VARCHAR(100), -- 如：正常下单、库存不足下单
    priority VARCHAR(10), -- P0/P1
    -- 核心配置 --
    request_data JSON, -- 具体的请求参数值
    assertion_rules JSON, -- 断言规则集合
    extraction_rules JSON, -- 变量提取规则
    ai_generated BOOLEAN -- 是否由 AI 生成
);
```

---

## 二、 层级二：场景编排工作室 (Scenario Studio)

**定位：** 全能的业务组装车间。
**核心变革：** 统一“模块内链路”和“跨模块组合”，采用**图（Graph）**结构而非列表结构。

### 2.1 功能设计

#### 功能 1：可视化流编排 (Flow Editor)
*   **设计思路：** 类似 Node-RED 或 GitHub Actions 的可视化编辑器。
*   **组件面板 (Palette)：**
    *   **API 用例：** 从左侧拖拽“API 资产库”里的某个“原子用例”到画布。
    *   **逻辑节点：** `Condition` (判断), `Loop` (循环), `Delay` (等待), `SQL` (数据准备/清理)。
    *   **子场景 (Sub-Flow)：** 将另一个已编排好的场景作为一个“黑盒节点”拖入（实现复用，如复用“登录流程”）。

#### 功能 2：AI 智能接线 (Auto-Wiring) —— **核心亮点**
*   **痛点：** 步骤 A 的输出 `id` 传递给步骤 B 的输入 `user_id`，手动配置非常繁琐。
*   **AI 交互：**
    1.  用户将 **Step A (创建订单)** 和 **Step B (支付订单)** 放入画布。
    2.  点击 **“AI 自动连接”** 按钮。
    3.  **AI 分析：**
        *   Step A 的 Response 包含 `data.orderId`。
        *   Step B 的 Request 需要 `body.oid`。
        *   语义分析认为 `orderId` ≈ `oid`。
    4.  **结果：** 自动画线，并生成变量提取规则：`StepA_Var_orderId = StepA.res.data.orderId`，Step B 入参自动填入 `${StepA_Var_orderId}`。

#### 功能 3：上下文总线 (Context Bus)
*   **设计思路：** 解决变量作用域问题。
*   **机制：**
    *   **全局变量：** 环境级别（Environment），如 `host`, `db_url`。
    *   **场景变量：** 仅在当前场景生命周期有效，由各个步骤的“提取操作”产生。
    *   **调试面板：** 在调试执行时，右侧实时显示“当前总线上的所有变量及其值”，方便排查数据传递问题。

### 2.2 数据模型 (Schema)

```sql
-- 场景表
TABLE scenarios (
    id BIGINT PRIMARY KEY,
    project_id BIGINT,
    name VARCHAR(100),
    graph_data JSON, -- 核心：存储画布的节点(Nodes)和连线(Edges)信息
    variables_schema JSON, -- 该场景需要的入参定义（如果被当作子场景调用）
    status VARCHAR(20)
);

/* graph_data JSON 结构示例:
{
  "nodes": [
    { "id": "step_1", "type": "api_case", "data": { "case_id": 101 } },
    { "id": "step_2", "type": "condition", "data": { "expression": "${code} == 200" } }
  ],
  "edges": [
    { "source": "step_1", "target": "step_2", "mapping": { "output.id": "input.user_id" } }
  ]
}
*/
```

---

## 三、 层级三：计划与执行 (Plan & Execution)

**定位：** 持续集成的调度中心。

### 3.1 功能设计

#### 功能 1：测试计划 (Test Plan)
*   **设计思路：** 场景的集合，支持策略配置。
*   **配置项：**
    *   **包含场景：** 选择多个 Scenarios。
    *   **运行环境：** Test / Stage / Prod。
    *   **并发策略：** 串行执行 / 并行执行（Worker 数量）。
    *   **失败策略：** 遇到失败立即停止 / 忽略失败继续。
    *   **定时任务/Webhook：** 类似 Jenkins Cron 表达式。

#### 功能 2：深度分析报告 (Insight Report)
*   **设计思路：** 不止是 Pass/Fail，而是给出“质量健康度”。
*   **报告内容：**
    *   **基础数据：** 通过率、耗时 P99。
    *   **错误聚类：** “本次失败中，50% 是因为 `502 Bad Gateway`，30% 是因为断言失败”。
    *   **高频挂点：** “最近 7 天，【支付模块】的报错率最高”。

---

## 四、 核心工作流重塑 (User Journey)

### 场景：从零构建一个“电商下单”自动化测试

1.  **资产准备 (Asset)：**
    *   进入 **API 资产库**，从 Swagger 同步最新接口。
    *   找到 `POST /login`，AI 自动生成了基准用例。用户微调入参，保存为“账号A登录”。
    *   找到 `POST /order/create`，AI 生成基准用例。

2.  **编排场景 (Studio)：**
    *   进入 **场景工作室**，新建场景“核心下单流程”。
    *   拖拽“账号A登录”用例。
    *   拖拽“创建订单”用例。
    *   点击 **“AI 自动接线”**。系统自动提取登录接口的 `token` 并放入 Header，自动提取订单接口需要的商品数据。
    *   拖入 **If 控制器**：判断 `order_id` 是否非空。
    *   拖入 **SQL 节点**：`DELETE FROM orders WHERE id = ${order_id}` (数据清理)。

3.  **执行与集成 (Plan)：**
    *   点击“调试运行”，看到每一步变成绿色，右侧变量总线显示数据流转正常。
    *   将该场景加入“冒烟测试计划”。
    *   复制 Webhook URL，配置到 GitLab CI 中。每当后端代码提交，自动触发该计划。

---

## 五、 落地建议与迁移策略

鉴于你已经开发了一部分代码，建议按以下优先级进行重构：

1.  **第一阶段（止损与合并）：**
    *   停止开发独立的 `test_scripts` 页面。
    *   在 `ModuleDetail.tsx`（接口详情）中增加一个 Tab 叫“测试用例”，把原来写脚本的功能搬进去。
    *   数据库层面，建立 `api_cases` 表，迁移旧数据。

2.  **第二阶段（统一引擎）：**
    *   开发新的“场景执行引擎”（后端），支持解析 Graph/JSON 结构的流程。
    *   前端开发 `Scenario Studio` 画布，暂时只支持串行拖拽，先跑通“API A -> API B”的数据传递。

3.  **第三阶段（AI 增强）：**
    *   接入 LLM（如 GPT/Claude/通义千问）。
    *   实现“智能断言生成”和“智能参数映射”这两个 ROI 最高的 AI 功能。

这个方案去掉了“中间商赚差价”（繁琐的脚本层、分割的模块编排层），让测试资产的流动更加直接高效，这才是大厂级别的平台设计。