
---

### 总体架构设计

我们将彻底抛弃原本的 $O(M \times N)$ 暴力循环，改为 **“倒排索引召回 + 向量语义重排 + 全局重心决策”** 三段式架构。

*   **核心理念**：先用向量（语义）找“像谁”，再用重心（统计）定“是谁”。
*   **资源预估**：
    *   **内存**：约增加 150MB~200MB（模型+向量库），完全在 Ryzen 7 的核显共享内存承受范围内。
    *   **CPU**：利用 Zen4 的 AVX-512 指令集加速向量运算，毫秒级响应。
    *   **存储**：向量缓存文件约 10-20MB。

---

### 实施阶段一：环境准备与向量化基座 (Day 1-2)

**目标**：建立语义搜索能力，将数据库的“死”字符串变成“活”的向量。

#### 1. 引入轻量级模型
在 WSL2 环境中，为了避免 Python 依赖地狱，建议使用 `sentence-transformers` 库，并选用 `all-MiniLM-L6-v2` 模型。
*   **模型大小**：80MB（极小，加载快）。
*   **性能**：在 Ryzen 7840HS 上单次推理仅需几毫秒。

#### 2. 数据库列的“特征工程”与缓存
不要每次请求都读库。我们需要一个后台任务（Background Task）在服务启动时做一次全量索引。

**代码实现逻辑 (Python)**：
```python
import numpy as np
import os
import pickle
from sentence_transformers import SentenceTransformer

class VectorIndexManager:
    def __init__(self, db_schema):
        # 1. 初始化模型 (只会占用 ~100MB 显存/内存)
        self.model = SentenceTransformer('all-MiniLM-L6-v2')
        self.cache_path = "/home/project/data/db_embeddings.pkl" # 放在 WSL Linux 目录
        self.db_schema = db_schema
        self.column_vectors = None
        self.column_meta = [] # 存储列的元数据 (table, name, type)

    def build_index(self):
        # 检查缓存是否存在且未过期
        if os.path.exists(self.cache_path):
            self._load_from_cache()
            return

        texts = []
        for table in self.db_schema['tables']:
            for col in table['columns']:
                # 特征工程：组合 表名 + 列名 + 注释
                # 例如: "users name user real name"
                text = f"{table['name']} {col['name']} {col.get('comment', '')}"
                texts.append(text)
                self.column_meta.append({
                    'table': table['name'],
                    'column': col['name'],
                    'type': col['type'],
                    'is_pk': col.get('is_pk', False)
                })
        
        # 批量计算向量 (Ryzen 7840HS 会跑满所有核心，极快)
        self.column_vectors = self.model.encode(texts, convert_to_numpy=True)
        
        # 持久化缓存 (解决 WSL IO 慢的问题，下次启动秒读)
        with open(self.cache_path, 'wb') as f:
            pickle.dump((self.column_vectors, self.column_meta), f)

    def search(self, api_field_name, top_k=20):
        # 实时计算 API 字段的向量
        query_vec = self.model.encode([api_field_name], convert_to_numpy=True)
        
        # 向量点积计算相似度 (矩阵运算，Zen4 强项)
        # scores shape: (1, N)
        scores = np.dot(self.column_vectors, query_vec.T).flatten()
        
        # 获取 Top-K 索引
        top_indices = np.argpartition(scores, -top_k)[-top_k:]
        top_indices = top_indices[np.argsort(scores[top_indices])[::-1]]
        
        return [
            {**self.column_meta[i], 'vector_score': float(scores[i])} 
            for i in top_indices
        ]
```

---

### 实施阶段二：核心算法逻辑重构 (Day 3-4)

**目标**：用“重心表算法”替换现有的线性加权算法。

#### 1. 算法流程设计
当一个 API 请求体（Body）进来时，包含字段列表 `[user_id, order_no, total_fee, create_time]`。

**步骤 A：全量初筛 (Recall)**
遍历每个字段，调用 `VectorIndexManager.search(field, top_k=10)`。
*   得到一个二维列表：`candidates_matrix`。
*   此时 `total_fee` 可能匹配到 `orders.fee` (0.85) 和 `products.fee` (0.82)。

**步骤 B：计算“重心表” (Gravity Calculation)**
这是优化的灵魂。统计所有字段的 Top 10 候选列中，哪个表出现的频次最高、累积得分最高。

```python
def calculate_gravity_table(all_fields_candidates):
    table_scores = {}
    
    for field_candidates in all_fields_candidates:
        for idx, cand in enumerate(field_candidates):
            tbl = cand['table']
            score = cand['vector_score']
            
            # 衰减权重：排名越靠后，对表权重的贡献越小
            rank_decay = 1.0 / (idx + 1) 
            
            if tbl not in table_scores: table_scores[tbl] = 0
            table_scores[tbl] += score * rank_decay

    # 返回得分最高的表名，作为“重心表”
    return max(table_scores, key=table_scores.get)
```

**步骤 C：重排序 (Re-ranking)**
利用重心表对每个字段的候选集进行加权修正。

```python
def re_rank(candidates, gravity_table):
    for cand in candidates:
        final_score = cand['vector_score']
        
        # 规则 1: 属于重心表，大幅加分
        if cand['table'] == gravity_table:
            final_score += 0.3  # 强关联
            
        # 规则 2: 名字完全一样 (Exact Match)，直接封顶
        if api_field_name.lower() == cand['column'].lower():
            final_score += 0.5
            
        cand['final_score'] = final_score
    
    # 重新按 final_score 排序
    return sorted(candidates, key=lambda x: x['final_score'], reverse=True)
```

---

### 实施阶段三：AI 兜底与 RAG 集成 (Day 5)

**目标**：处理“硬骨头”，但要省 Token。

#### 1. 触发机制
只有当 Step C 重排序后，Top 1 候选的 `final_score < 0.7`（置信度阈值）时，才触发 AI。

#### 2. Prompt 模板 (Prompt Engineering)
你需要构建一个极简的 Prompt，把计算过的“中间结果”喂给 AI。

```text
角色: 资深数据库架构师
任务: 将 API 字段映射到数据库列

[上下文]
- 当前接口推测主表: {gravity_table} (置信度高)
- 待映射字段: {api_field_name}

[候选列表 - 已按相关性排序]
1. 表:{c1.table}, 列:{c1.column}, 类型:{c1.type}, 语义分:{c1.vector_score:.2f}
2. 表:{c2.table}, 列:{c2.column}, ...
3. ... (只给 Top 5)

[要求]
请分析字段语义和主表上下文。
- 如果候选 1-5 中有正确的，请返回其序号。
- 优先选择归属于 '{gravity_table}' 的列。
- 如果都不匹配，返回 None。
只返回 JSON 格式: {"selected_index": 0, "reason": "..."}
```

---

### 实施阶段四：性能压测与 WSL2 调优 (Day 6)

**目标**：确保在你的 AMD Ryzen 7 上跑得飞快。

1.  **Warm-up（预热）**：
    *   在应用启动（Startup）阶段，强制加载 Embedding 模型和 `numpy` 缓存。
    *   不要在第一次 API 请求进来时才加载模型，那会造成用户感知的“首屏卡顿”。

2.  **异步并发**：
    *   Python 如果用 FastAPI，确保 `model.encode` 这种 CPU 密集型任务在 `run_in_executor` 线程池中运行，避免阻塞 Event Loop。
    *   因为 `numpy` 底层会自动利用多核，所以线程池大小不需要设太大（例如设为 4 即可）。

---

### 总结：实施路线图

| 阶段 | 核心任务 | 关键产出 | 预期效果 |
| :--- | :--- | :--- | :--- |
| **Day 1-2** | **基建**：集成 `sentence-transformers`，实现 Schema 向量化缓存。 | `VectorIndexManager` 类，`.pkl` 缓存文件。 | 摆脱 O(M×N) 循环，查询速度从 500ms 降至 10ms。 |
| **Day 3-4** | **算法**：实现“重心表”投票逻辑与重排序。 | `Gravity` 算法模块。 | 解决“同名词”跨表匹配错误问题（如 `price` 归属）。 |
| **Day 5** | **智能**：集成 AI RAG 兜底。 | AI 调用模块，Prompt 模板。 | 解决缩写、生僻词映射，提升召回率。 |
| **Day 6** | **调优**：异步线程池封装，WSL2 资源限制。 | 压测报告，启动脚本。 | 无 OOM，无卡顿，CPU 利用率健康。 |

这套方案完全为你现在的环境量身打造：**向量计算吃 CPU（你有 7840HS），缓存和索引吃少量内存（避开 OOM），重心算法补足语义缺失。** 祝你重构顺利！