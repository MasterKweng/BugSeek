下面给出可落地的“字段相似度算法公式”（用于 api_field_path ↔ db_table/db_column 匹配评分），包含标准化、分词、相似度计算与综合评分，并给出推荐参
  数。

  ———

  # 1) 标准化（Normalization）

  对字段名统一处理：

  normalize(s):
    s = lower(s)
    s = trim("_")
    return s
  例：
  - orderId → order_id

  ———


  按 _ 拆分，同时做轻量词形还原：

  tokens = split(normalize(s), "_")
  例：

  - order_id → tokens = ["order"]

  ———


  统一语义词：
  synonyms = {
    "usr": "user",
    "acct": "account",
    "org": "organization",
    "dept": "department"
  }

  ———
  # 4) 相似度计算（核心公式）
  ## 4.1 词集合 Jaccard 相似度
  J = |A ∩ B| / |A ∪ B|

  ## 4.2 编辑距离相似度（Levenshtein）

  L = 1 - (levenshtein(a, b) / max(len(a), len(b)))



  ———
  # 5) 字段相似度评分输出（推荐范围）

  score_field(api_field, db_column):
    b = normalize(db_column)

    if a == "id" and b in ["id","*_id"]: return 0.9

    A = tokens(a)
    J = jaccard(A,B)


  ———


  当 API 字段有语义主体时：

  score_table(api_field, table):
    A = tokens(api_field)
    B = tokens(table)
    return jaccard(A,B)

  ———

  # 7) 综合建议（示例）

  字段：order_id
  候选：orders.id

  field_sim = 0.9
  table_sim = 1.0
  final = 0.7*field_sim + 0.3*table_sim = 0.93

  字段：order_id
  候选：order_items.order_id

  field_sim = 1.0
  table_sim = 0.5
  final = 0.85

  ———

  # 8) 阈值建议

  - ≥0.85 高置信度
  - 0.60~0.85 人工确认
  - <0.60 不建议