• 下面把方案拆分成三部分：接口清单 + UI 原型 + 数据结构建议。不改代码，只给设计。

  ———

  ## 一、接口清单（建议新增/扩展）

  ### 1) 生成映射建议

  POST /api/v1/field-mappings/suggest
  Query：project_id, version_id
  Body：

  {
    "include_paths": true,
    "include_query": true
  }

  {
    "code": 0,
    "message": "success",
    "data": {
      "items": [
        {
          "definition_id": 123,
          "definition_method": "POST",
          "definition_path": "/orders/{order_id}/items",
          "api_field_path": "path.order_id",
          "candidates": [
            {
              "db_table": "orders",
              "db_column": "id",
              "score": 0.86,
              "reasons": ["path匹配", "主键优先"]
            },
            {
              "db_table": "order_items",
              "db_column": "order_id",
              "score": 0.63,
              "reasons": ["字段名匹配"]
            }
          ]
        }
    }



  POST /api/v1/field-mappings/batch-apply
  Body：

  {
    "version_id": 10,
    "items": [
        "definition_id": 123,
        "api_field_path": "path.order_id",
        "db_table": "orders",
        "db_column": "id",
        "relation_type": "direct",
        "source": "ai"
    ]


  ———

  ### 3) 更新映射状态（人工审核）
  PUT /api/v1/field-mappings/{id}/status



  ### 4) 拉取待审核建议

  GET /api/v1/field-mappings/pending
  Query：project_id, version_id
  用于审核列表

  ———
  ### 5) 版本继承（可选）
  POST /api/v1/field-mappings/clone

  {
    "from_version_id": 9,
  }
  ———
  ## 二、UI 原型（页面建议）

  ### 入口：版本中心 → 字段映射


  - 过滤器：接口、字段名、状态（proposed/confirmed/rejected）

  ———


  - 接口（method + path）
  - API 字段路径
  - 置信度
  - 原因（tag）
  - 操作：确认 / 驳回 / 查看候选

  ———

  ### B. 候选详情抽屉

  展示：
  - 所有候选映射
  - 每个候选的评分与原因
  - 一键选择最优候选
  ———
  ### C. 批量确认

  - “只确认 ≥ 0.85 的推荐项”
  - “仅当前筛选条件下确认”

  ———

  ### D. 状态提示

  - 映射生成结果统计
      - 高置信度 xx 条
      - 需人工确认 xx 条



  ### 1) 扩展现有 api_field_mappings

  新增字段：

  - status：proposed | confirmed | rejected
  - confidence：AI 置信度
  - source：manual | ai
  - reasons：JSON（用于解释）
  - created_by/updated_by（已有）

  ———
  ### 2) 新增建议缓存表（可选）

  如果不想直接写入映射表，可使用建议池：

  api_field_mapping_suggestions

  - definition_id
  - api_field_path
  - candidate_list (JSON)
  - best_candidate (JSON)
  - confidence
  - status (pending/accepted/rejected)
  - created_at

  ———

  ### 3) 全局字段字典表（可选）

  project_field_dictionary

  - project_id
  - field_name
  - db_table
  - db_column
  - priority
  - source

  ———

  ## 结论

  这套拆分能做到：

  - AI 自动生成建议
  - 规则校验降误判
  - 人工只审核少量低置信度项
  - 版本继承减少重复工作