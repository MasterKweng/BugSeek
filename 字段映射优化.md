可以。基于我刚才对 `V2.0` 的检查，最后还差的不是“大重构”，而是**主链路接线**。

先给一句结论：

> 你现在是 **“最终版组件已齐，主流程未完全切换”**。
> 只要把下面这几处接上，才算真正达到“最终版标准”。

---

# 达到最终版标准的最后接线清单

## P0-1：把新证据源真正接入 `services.py`

这是最关键的一步。

你现在的 `FieldMappingAppService` 仍然只初始化并使用：

* `history_recaller`
* `lexical_recaller`
* `vector_recaller`
* `runtime_recaller`
* `deterministic_rules`
* `ranker`
* `ai_enricher`

主链还是旧的 `history + lexical + vector + runtime`，没有把新补的 lineage 能力接进来 。

### 要改的文件

`backend/app/domains/field_mapping_engine/services.py`

### 要做的事

在 `__init__()` 里新增并正式实例化：

* `SQLLineageRecaller`
* `CodeLineageRecaller`
* `FeatureBuilder`
* `RelationClassifier`
* `ConfidenceCalibrator`
* `RiskPolicy`
* `RuntimeVerificationService`

### 接线目标

让主流程从现在的：

```text
extract -> history/lexical/vector/runtime -> rules -> rank -> ai
```

升级成：

```text
extract
-> history/lexical/vector/runtime/sql_lineage/code_lineage
-> feature_builder
-> deterministic_rules
-> ranker
-> runtime_verification
-> ai_enricher
-> relation_classifier
-> confidence_calibrator
-> risk_policy
```

---

## P0-2：改 `build_recall_artifacts()`，把 SQL/code lineage 合并进正式候选池

当前 `build_recall_artifacts()` 里只 merge 了：

* lexical candidates
* vector candidates
* runtime candidates 

而你新增的：

* `SQLLineageRecaller`
* `CodeLineageRecaller`

还没进正式召回链。虽然文件已经存在，而且实现不是空壳  ，但没进主链就不算生效。

### 要改的文件

`backend/app/domains/field_mapping_engine/services.py`

### 要做的事

在 `build_recall_artifacts()` 里新增：

* `sql_lineage_candidates = self.sql_lineage_recaller.recall_from_dict(...)`
* `code_lineage_candidates = self.code_lineage_recaller.recall_from_dict(...)`

然后把当前的 `_merge_candidate_evidence(...)` 扩成：

* lexical
* vector
* runtime
* sql_lineage
* code_lineage

### 最好顺手做的事

把每个 field item 增补这些上下文：

* `allowed_tables`
* `domain_anchor`
* `risk_level`
* `source_type`

否则后面的 `FeatureBuilder`、`RiskPolicy` 很难发挥作用。

---

## P0-3：把 `FeatureBuilder` 接到排序前

你已经有 `FeatureBuilder`，而且已经能补这些很关键的特征：

* `f_field_position_match`
* `f_domain_anchor_match`
* `f_data_type_match`
* `f_sibling_context_match`
* `f_lineage_strength` 

但现在它没有进入正式候选处理链。

### 要改的文件

`backend/app/domains/field_mapping_engine/services.py`

### 要做的事

在 `rank_recall_items()` 里，`deterministic_rules.apply_from_dict()` 之前或之后都可以，但必须加一层：

```python
for candidate in candidate_evidence:
    self.feature_builder.enrich_candidate(field_item=item, candidate=candidate)
    self.deterministic_rules.apply_from_dict(item, candidate)
```

### 目标

让排序器真正吃到：

* lineage strength
* domain anchor match
* field position match
* sibling context match

否则你虽然有这些特征定义，但排序分不会反映出来。

---

## P0-4：把 `RelationClassifier` 和 `ConfidenceCalibrator` 接管最终决策

你现在已经有：

* `RelationClassifier.classify()` 
* `ConfidenceCalibrator.calibrate()` 

但主流程仍然在用 `services.py` 里的内联方法：

* `_infer_relation_type()`
* `_calibrate_confidence()` 

这说明“新组件已存在，旧逻辑仍在生效”。

### 要改的文件

`backend/app/domains/field_mapping_engine/services.py`

### 要做的事

在 `_build_decision_artifact()` 里，把：

* `relation_type = self._infer_relation_type(...)`
* `confidence = self._calibrate_confidence(...)`

替换成：

* `relation_type = self.relation_classifier.classify(field_item=item, top_candidate=top_candidate)`
* `calibration = self.confidence_calibrator.calibrate(field_item=item, ranked_candidates=final_candidates, decision_source=...)`

然后把这些值写回：

* `relation_type`
* `confidence`
* `confidence_bucket`
* `review_policy`

### 要删或降级的内容

* `_infer_relation_type()` 改成兼容壳，内部直接调 classifier
* `_calibrate_confidence()` 改成兼容壳，内部直接调 calibrator
* 后续稳定后可删掉旧内联逻辑

---

## P0-5：把 `RiskPolicy` 变成正式门禁，而不是辅助工具

你已经有 `RiskPolicy`，并且已经定义了：

* 高风险字段识别
* 高风险字段 auto accept 条件 

但现在它还没进入正式裁决链。

### 要改的文件

* `backend/app/domains/field_mapping_engine/services.py`
* `backend/app/domains/field_mapping_engine/ranking/deterministic_rules.py`
* `backend/app/domains/field_mapping_engine/ranking/confidence_calibrator.py`

### 要做的事

让 `RiskPolicy` 在两个点真正生效：

#### 1）排序前门禁

对高风险字段，如果没有以下任一强证据：

* `f_sql_lineage_exact >= 0.9`
* `f_code_assignment_hit >= 0.9`
* `f_runtime_field_hit >= 1.0`
* `f_history_prior >= 0.95`

就禁止短路 auto accept。

#### 2）决策后审查策略

由 `RiskPolicy` 决定：

* `auto_accept`
* `manual_review`
* `reject_auto`

### 目标

高风险字段不能再只靠词法/向量/AI 进入高置信自动确认。

---

## P0-6：把 `RuntimeVerificationService` 接入最终校验链

你已经有 `RuntimeVerificationService`，而且里面已经组合了：

* `ResponseFieldVerifier`
* `SQLProjectionVerifier` 

但当前 `services.py` 主链还没用它。

### 要改的文件

`backend/app/domains/field_mapping_engine/services.py`

### 要做的事

在 `rank_recall_items()` 或 `optimize_ranked_items()` 之前，加一层 runtime verification enrichment。

推荐位置：

* 规则排序后
* AI 前

也就是：

```text
recall -> feature -> rules -> rank -> runtime_verification -> ai -> decision
```

### 作用

把 runtime verification 补成真正的：

* `f_runtime_column_verified`
* `f_runtime_projection_verified`
* response value match evidence

而不是只停留在 runtime table prior。

---

## P1-1：让 `LineageService` 真正进入数据生成链，而不是只做读服务

你现在已有：

* `SQLLineageParser` 
* `ORMLineageParser` 
* `LineageService` 
* `LineageRepository` 

但目前更偏“能查能转”，还没看到它被明确接入“构建 lineage 证据”的主任务链。

### 要改的文件

* `backend/app/domains/data_impact/engine.py`
* `backend/app/domains/data_impact/impact_analyzer.py`
* 或专门新增 Celery task / build step

### 要做的事

在以下时机自动生成 lineage evidence：

#### 1）导入 SQL/ORM 资产时

* 解析 SQL
* 解析 mapper/result mapping
* 生成 lineage edges
* 落到 repository

#### 2）运行时 trace 到达时

* 对 SQL trace 再做解析
* 补 lineage edge
* 落库

### 目标

不是“等字段映射时才临时解析”，而是把 lineage 变成独立资产。

---

## P1-2：把 lineage 存储从 `FieldMappingRuntimeEvidence` 里独立出来

当前 `LineageRepository` 还是把 lineage evidence 存在现有 `FieldMappingRuntimeEvidence` 里，代码自己就写了：

> Persist lineage evidence using the existing runtime evidence table 

这对过渡期没问题，但如果你要严格对齐“最终版标准”，这一步最好独立。

### 要改的文件

* `backend/app/platform/db/base.py`
* `backend/app/domains/data_impact/lineage_repository.py`

### 要新增的表

* `sql_lineage_edges`
* `code_lineage_edges`

### 目标

把：

* runtime verification evidence
* lineage edge evidence

从资产语义上切开。

这对后续：

* 查询效率
* 审计
* 指标统计
* 资产治理

都更清晰。

---

## P1-3：把 `ORMLineageParser` 和 `CodeLineageParser` 从“payload parser”升级到“资产自动抽取”

这是最终版里最容易被忽略的一步。

当前：

* `ORMLineageParser` 本质上在解析 `mapping_payload["mappings"]` 
* `CodeLineageParser` 主要在解析 `assignments` 或 assignment-like text 

这说明它们更像：

* 标准化器
* 轻量 helper

还不是：

* 真实仓库扫描器
* 真正 ORM / mapper 资产抽取器

### 要做的事

后续把它们接入到：

* 代码仓库扫描任务
* ORM 资产导入任务
* mapper/resultMap 解析任务

### 最低标准

至少做到：

* 能批量读取 mapper/config 文件
* 能自动提取 result mapping
* 能自动提取常见赋值链
* 而不是只靠外部先组好 payload 再喂进 parser

---

# 最后给你一个“最短闭环版”

如果你只想用最少动作达到“最终版标准”，那就只做下面 6 件：

## 最短闭环版 6 步

1. `services.py` 接入 `SQLLineageRecaller`
2. `services.py` 接入 `CodeLineageRecaller`
3. `services.py` 接入 `FeatureBuilder`
4. `services.py` 改用 `RelationClassifier`
5. `services.py` 改用 `ConfidenceCalibrator`
6. `services.py` / 决策层接入 `RiskPolicy` + `RuntimeVerificationService`

### 做完这 6 步后

我会把你的状态从：

## “最终版组件已齐，主流程未切换”

升级成：

## **“最终版主流程已生效，基本达到最终版标准”**

---

# 一句话总结

> **你现在离最终版标准，差的不是新组件，而是把这些新组件真正接进 `services.py` 主链，并让 lineage / risk / calibrator / runtime verification 成为正式决策依据。**
