可以。下面我直接按**可执行的重构方案**给你。

先给总结论：

## 结论

**保留“数据资产 + 接口契约 + 任务入口”，删除“现有字段映射核心引擎”，新建一套独立的字段映射内核。**

也就是：

* **保留壳子**
* **删除内核**
* **新建内核**
* **最后平滑切换**

之所以这么判断，是因为当前实现里，字段映射核心域反向依赖 API 层，且阶段持久化与续跑加载的对象语义不一致，这不是普通 bug，而是架构和状态模型本身有问题 。另外当前评分公式也过于简化，本质上不足以支撑你要的“最终效果最好”目标 。

---

# 一、保留什么

## 1）保留数据库表

这些表是有价值的，**不要删**，它们可以继续作为业务资产层：

* `api_field_mappings`
* `field_mapping_suggestions`
* `field_mapping_traces`
* `async_tasks`

这些表已经把“确认映射”“建议结果”“决策追踪”“异步任务”四类核心资产分开了，这个方向是对的 。

### 具体建议

* 表名不改
* 主键不改
* 已有索引尽量保留
* 允许**新增字段**
* 允许**新增辅助表**
* 不建议推翻现有表再迁移数据

---

## 2）保留 HTTP 接口路径

前端兼容层尽量保留，尤其是这些路径：

* `/field-mappings`
* `/field-mappings/suggest`
* `/field-mappings/batch-apply`
* `/field-mappings/suggestions`
* `/field-mappings/suggest-task`
* `/async-tasks/{task_id}`
* `/field-mappings/tasks/{task_id}/...`

这些接口现在已经承担了前端工作台职责，直接删会把前后端一起拖进重构。最佳做法是：

> **路径保留，内部实现全部换掉。**

当前同步接口和异步接口都已经成型，只是内部核心逻辑不值得继续沿用  。

---

## 3）保留 Celery 任务入口名

建议保留：

* `app.celery.tasks.execute_field_mapping_task`

原因很简单：

* 任务调用方不用改
* 调度器不用改
* 前端轮询不用改

但任务函数内部逻辑要重写。目前它只是直接实例化 `FieldMappingProcessor` 并跑完整流程，这种结构需要替换掉 。

---

## 4）保留少量真正有用的基础工具

这些可以保留，但只作为**基础工具**，不要再让它们承担核心决策：

* `app/utils/name_normalizer.py`
  这个文件很小，职责单纯，可保留 
* `app/domains/data_mapping/constants.py`
  阶段枚举、状态常量、结果 key 的思路可以保留，但阶段设计本身要改 
* `app/domains/data_mapping/exceptions.py`
  异常类型可保留 
* `app/domains/data_impact/schema_mapper.py`
  这个工具比较干净，可以复用到运行时证据归一化里 

---

## 5）保留“运行时影响分析”这条证据链

如果只追求最终效果，不看成本，那么一定要把运行时证据接进来。

仓库里已经有：

* `api_execution_traces`
* `sql_traces`
* `api_table_impacts`

这些表说明系统已经开始具备运行时数据影响分析基础设施，这对字段映射准确率上限非常重要 。

---

# 二、删除什么

这里我说“删除”，是指**删除旧实现，不是删除功能能力**。

## 1）删除整个旧的字段映射核心引擎

### 直接删掉旧实现内容：

* `backend/app/domains/data_mapping/processor.py`
* `backend/app/domains/data_mapping/scoring.py`
* `backend/app/domains/data_mapping/validation.py`
* `backend/app/domains/data_mapping/domain_inferer.py`
* `backend/app/domains/data_mapping/graph_builder.py`

这些文件的核心问题不是“某几行写错”，而是：

* 职责过载
* 旧逻辑 / V2 逻辑混杂
* 规则、AI、图谱、任务状态、trace、DB 持久化全耦合在一起
* 核心域反向依赖 API 层
* 阶段结果保存的是摘要，续跑时却当运行态对象加载

这一套继续改，最后只会变成“更复杂的旧系统”，不会变成“更好的新系统” 。

---

## 2）删除 `field_mappings.py` 里的“核心算法函数”

这个文件应该只当 controller，不应该内置算法。

应该删掉或迁出的内容：

* `_extract_api_fields`
* `_extract_field_descriptions`
* `_generate_mapping_candidates_with_gravity`
* 任何候选生成、评分、AI fallback 逻辑

理由：

* 现在 API 层里混了太多业务内核
* `processor.py` 还反过来 import 这里的函数和模型，结构已经坏掉了 
* 这里的同步 suggest 路径本身还有明显实现风险，比如回退调用了未展示定义的 `_generate_mapping_candidates`，说明这条链路不可靠 

---

## 3）删除当前“断点续跑”实现

这个必须删。

现有 `process()` 的阶段保存 / 恢复不是“粗糙”，而是“语义错误”：

* 阶段1保存的是统计摘要
* 续跑时却把它当 `field_registry`
* 阶段2保存的是评分统计
* 续跑时却把它当 `rule_results`
* 阶段3/4/5 同类问题

也就是说，当前断点续跑不是不稳定，而是**理论上就不成立** 。

---

## 4）删除当前评分公式

当前 `scoring.py` 里是：

* `0.45 * 向量分`
* `0.25 * 精确分 * 词权重`
* `0.30 * 图谱分`

这在 demo 里可以，在生产里不够。原因：

* 证据太少
* 没有运行时证据
* 没有历史确认映射证据
* 没有 comment/schema/type/fk/path hierarchy 等系统性特征
* 没有校准机制

所以这个文件**不值得修**，应直接废弃重做 。

---

## 5）删除“核心域直接依赖 Pydantic DTO”

`processor.py` 直接 import：

* `_extract_api_fields`
* `_extract_field_descriptions`
* `FieldMappingCandidate`
* `FieldMappingSuggestion`

而这些都来自 `app.api.v1.field_mappings`。这会导致：

* 领域层不能独立测试
* API 改 DTO 会伤核心引擎
* 核心逻辑无法复用到离线批处理 / CLI / 回归任务

这个依赖方向必须彻底砍掉 。

---

# 三、新方案的总体结构

我建议你新建一个**全新的核心包**，不要在旧包上继续堆。

## 新目录建议

```text
backend/app/domains/field_mapping_engine/
    __init__.py
    contracts.py
    extractor/
        api_schema_extractor.py
        db_schema_extractor.py
        runtime_evidence_extractor.py
    recall/
        lexical_recaller.py
        vector_recaller.py
        history_recaller.py
        domain_recaller.py
        runtime_recaller.py
    evidence/
        feature_builder.py
        relation_classifier.py
        confidence_calibrator.py
    ranking/
        deterministic_rules.py
        ranker.py
    ai/
        candidate_expander.py
        ai_guardrail.py
    persistence/
        artifact_store.py
        suggestion_writer.py
        trace_writer.py
    orchestration/
        job_runner.py
        stage_manager.py
        resume_manager.py
```

---

# 四、新方案的职责分层

## 1）Controller 层

只做：

* 参数校验
* 调用 service
* 返回结果

不做：

* 字段提取
* 候选召回
* 评分
* AI 判断
* trace 拼装

### 对应改造

* `field_mappings.py` 保留路径，内部改成调用 `FieldMappingAppService`
* `field_mappings_async.py` 保留路径，内部改成调用 `FieldMappingJobService`

---

## 2）Application Service 层

负责：

* 组装一次映射任务
* 读取项目 / 版本 / schema / definitions
* 触发 job runner
* 管理同步请求和异步请求的统一入口

这个层不要有算法，只做编排。

---

## 3）Core Engine 层

这是新系统真正的核心。

它只处理四件事：

### A. 提取对象

把输入转成统一结构：

* `ApiFieldSpec`
* `ApiDefinitionContext`
* `DbTableSpec`
* `DbColumnSpec`
* `RuntimeEvidence`

### B. 候选召回

多路召回，不做最终拍板。

### C. 证据构造

为每个候选构造成熟的 feature/evidence。

### D. 排序与裁决

输出：

* final candidate
* relation_type
* confidence
* evidence_trace
* decision_source

---

# 五、新方案的核心算法

这是最关键的部分。

## 总原则

你的目标不是“猜一个最像的”，而是：

> **先找强证据，再做弱证据补全，再做裁决。**

---

## 第一层：强证据

这是最重要的层。

### 1. 历史确认映射证据

来自 `api_field_mappings` 里已经确认过的数据。

用途：

* 同项目、同领域、同 path pattern 下优先复用
* 作为 high-priority prior

### 2. 运行时 SQL / data impact 证据

如果某 API 在真实执行中能观测到：

* 访问了哪些表
* 影响了哪些列
* SQL trace 里出现了哪些字段

那么这些证据优先级要高于纯向量匹配。

### 3. 明确 schema/comment 映射证据

例如：

* 字段描述直接命中列注释
* 列注释包含明确别名
* 码表字段有固定注释结构

---

## 第二层：结构证据

### 1. 字段名标准化匹配

保留规范化能力，但只作为 feature：

* snake/camel 统一
* 同义词归一
* 单复数归一
* 缩写展开

### 2. 表域约束

保留“allowed_tables”这个思想，但不要再用当前这种过于脆弱的实现。

新的表域推断应来自：

* path resource
* API tag
* 历史 confirmed mapping
* runtime table impact
* graph proximity

### 3. 图谱/外键证据

图构建能力可以保留“思想”，但实现要重写。
当前 `graph_builder.py` 里有大量硬编码猜测和软外键推断，这种东西不能做强证据，只能做弱结构证据 。

---

## 第三层：语义证据

### 1. 向量召回

继续保留，但只做召回和 feature。

### 2. 注释语义匹配

* API 字段 description
* DB column comment
* table comment
* historical notes

### 3. 相邻字段上下文

这个思路是对的：

* 一个字段不应孤立判断
* 同一个 request body 的兄弟字段、同一路径上的字段组合很重要

但当前实现是拼进去的，新版本要把它变成正式 feature，而不是补丁逻辑。

---

## 第四层：AI 补全

AI 只做两件事：

### A. 候选扩展

当结构证据和语义证据都不足时，让 AI 提供补充候选。

### B. 原因解释

让 AI 帮你整理“为什么是它”，但不让 AI 直接拍板。

### 绝对禁止

* AI 直接决定最终 mapping
* AI 绕过物理 schema 校验
* AI 绕过 allowed table/domain 校验

当前代码已经开始做 AI 安全兜底，比如物理列存在校验、域外置信度折扣，这个方向是对的，但要内聚到独立 guardrail 模块里，不要继续嵌在巨型 processor 里 。

---

# 六、新的评分与裁决机制

## 1）不要再用单公式直接拍板

不要再做那种一条公式算完就结束的设计。

新方案应该分成：

### A. 确定性规则

一旦命中，直接短路：

* 历史 confirmed exact hit
* runtime verified exact hit
* exact normalized name + exact domain + exact comment alias

### B. 排序模型

对剩余候选做 rank。

### C. 置信度校准

排序分数不等于置信度，必须单独做 calibration。

---

## 2）建议的证据字段

每个 candidate 至少要有这些 feature：

* `f_name_exact`
* `f_name_token_overlap`
* `f_name_edit_distance`
* `f_comment_similarity`
* `f_table_domain_match`
* `f_graph_distance`
* `f_history_prior`
* `f_runtime_table_hit`
* `f_runtime_column_hit`
* `f_vector_similarity`
* `f_type_compatibility`
* `f_field_position_match`（path/query/body）

---

## 3）最终 confidence 规则

建议三段式：

* `0.90+`：可自动确认
* `0.70 ~ 0.89`：进入建议池，待人工确认
* `<0.70`：不自动输出 final，只输出候选列表

---

## 4）relation_type 要扩展

现有只有 `direct/fk/derived` 不够。

建议改成：

* `direct`
* `alias`
* `foreign_key`
* `joined`
* `derived`
* `enum_transform`
* `conditional`
* `runtime_verified`
* `uncertain`

这个字段会直接决定后续可解释性质量。

---

# 七、新的阶段设计

这是替代现有“坏掉的断点续跑”的关键。

## 新阶段不要只存摘要，要存**真实工件**

### Stage 1：输入快照

保存：

* definition ids
* api schema snapshot
* db schema snapshot
* version context
* config snapshot

### Stage 2：字段提取结果

保存：

* `ExtractedFieldArtifact[]`

每条包含：

* field_key
* definition_id
* method
* path
* field_path
* source_type
* field_name
* description
* sibling_paths

### Stage 3：候选召回结果

保存：

* `CandidateArtifact[]`

每条包含：

* field_key
* candidates[]
* recall_sources[]
* recall_scores[]

### Stage 4：证据与评分结果

保存：

* `ScoredCandidateArtifact[]`

每条包含：

* all features
* rank score
* confidence_pre_calibration

### Stage 5：AI 补全结果

保存：

* ai_prompt_digest
* ai_candidates
* guardrail_rejections
* hallucination flags

### Stage 6：最终裁决结果

保存：

* final candidate
* relation_type
* confidence
* decision_source
* trace summary

### Stage 7：持久化结果

写：

* `field_mapping_suggestions`
* `field_mapping_traces`

这样续跑时恢复的是真实中间产物，而不是错误的统计摘要。

---

# 八、建议新增的表

现有表要保留，但为了最终效果，我建议新增 3 张表。

## 1）`field_mapping_stage_artifacts`

存阶段工件。

字段建议：

* `id`
* `task_id`
* `stage`
* `artifact_type`
* `artifact_key`
* `payload_json`
* `created_at`

用途：

* 断点恢复
* 调试
* 回归对比

---

## 2）`field_mapping_feedback`

存人工反馈。

字段建议：

* `id`
* `project_id`
* `definition_id`
* `api_field_path`
* `db_table`
* `db_column`
* `feedback_type` (`accept/reject/modify`)
* `source_task_id`
* `created_by`
* `created_at`

用途：

* 学习历史偏好
* 建立 confirmed prior
* 提升后续召回和排序

---

## 3）`field_mapping_runtime_evidence`

存运行时映射证据。

字段建议：

* `id`
* `project_id`
* `definition_id`
* `execution_id`
* `api_field_path`
* `table_name`
* `column_name`
* `evidence_type` (`sql_trace/impact/response_match`)
* `confidence`
* `payload_json`

用途：

* 让“运行时验证”正式进入内核

---

# 九、文件级别的改造清单

## A. 保留文件名，但重写内容

### `backend/app/api/v1/field_mappings.py`

保留：

* 路径
* request/response 契约

删除：

* 提取函数
* 候选生成
* 同步评分
* gravity/AI fallback

改成：

* 纯 controller

---

### `backend/app/api/v1/field_mappings_async.py`

保留：

* 路径
* 查询任务、分页、重试、重放这些交互能力

删除：

* 任何阶段语义判断逻辑
* 任何自己解释旧阶段结果的逻辑

改成：

* 调 `FieldMappingJobService`

---

### `backend/app/celery/tasks/field_mapping_tasks.py`

保留：

* 任务函数名
* task_id 入参

删除：

* 直接调用旧 `FieldMappingProcessor`

改成：

* 调新 `JobRunner`

---

### `backend/app/celery/tasks/_common.py`

保留：

* 保存建议表这个职责

删除：

* 与旧 result 结构强绑定的部分

改成：

* 接收统一 `DecisionArtifact[]`
* 一次性落 suggestions + traces

---

## B. 直接删旧文件，换新实现

这些文件建议整个替换：

* `backend/app/domains/data_mapping/processor.py`
* `backend/app/domains/data_mapping/scoring.py`
* `backend/app/domains/data_mapping/validation.py`
* `backend/app/domains/data_mapping/domain_inferer.py`
* `backend/app/domains/data_mapping/graph_builder.py`

---

## C. 可保留但迁移

### `backend/app/utils/field_mapping_utils.py`

建议不要继续当核心逻辑库用。

处理方式：

* 保留最基础的 `normalize/tokenize`
* 删除评分相关函数
* 删除“映射总分计算”类方法
* 拆到新 `recall/lexical_recaller.py`

---

# 十、新方案的执行顺序

## Phase 0：冻结旧核心

先冻结旧 `data_mapping` 核心，不再往里加逻辑。

原则：

* 不再修算法
* 只修阻断型 bug
* 不再往 `processor.py` 里堆任何新功能

---

## Phase 1：建新内核目录

新建：

* `field_mapping_engine`

先把最小骨架搭起来：

* contracts
* extractor
* recall
* ranking
* persistence
* orchestration

---

## Phase 2：先做“无 AI 版本”

第一版先不接 AI，先做：

* API 字段提取
* DB schema 拉平
* lexical recall
* history recall
* domain recall
* runtime recall
* feature builder
* deterministic rule
* ranker
* suggestion writer
* trace writer

原因：

* 先把确定性骨架做稳
* AI 永远是补充层，不应成为第一阶段依赖

---

## Phase 3：接入运行时证据

这一阶段非常关键。

把以下证据接进来：

* SQL trace
* table impact
* execution trace
* data impact schema mapper

这个阶段做完，准确率会有实质提升。

---

## Phase 4：接入 AI 补全

只接到以下位置：

* ambiguous candidate expansion
* natural language explanation

不允许 AI 绕过：

* schema existence check
* domain constraint
* runtime contradiction
* history negative feedback

---

## Phase 5：双跑验证

这一步不能省。

### 方式

旧引擎和新引擎同时跑：

* 同一个 task
* 同一个 schema
* 同一批 definitions

比较：

* top1 命中率
* top3 覆盖率
* 自动确认准确率
* 人工驳回率
* hallucination 数量
* trace 完整性

### 切换条件

只有当新引擎在这些指标上稳定优于旧引擎，才切默认。

---

## Phase 6：切流

切换顺序建议：

1. 新任务默认走新引擎
2. 老任务查询兼容旧数据
3. suggestions/workbench 全部读新结果
4. 一段时间后删旧引擎

---

# 十一、我建议你最后保留的“最小旧资产”

如果你要我给一个极度明确的保留清单，那就是：

## 保留

* `platform/db/base.py` 里的字段映射相关表
* `api/v1/field_mappings.py` 的路由路径
* `api/v1/field_mappings_async.py` 的路由路径
* `celery/tasks/field_mapping_tasks.py` 的任务名
* `utils/name_normalizer.py`
* `data_impact/schema_mapper.py`
* `data_mapping/constants.py`
* `data_mapping/exceptions.py`

## 删除并重写

* `data_mapping/processor.py`
* `data_mapping/scoring.py`
* `data_mapping/validation.py`
* `data_mapping/domain_inferer.py`
* `data_mapping/graph_builder.py`
* `field_mappings.py` 里的内部算法函数
* 当前断点续跑机制
* 当前评分与 AI 合并机制

---

# 十二、最终判断再压缩成一句话

> **保留接口、表、任务壳；删除字段映射旧内核；新建一个“提取—召回—证据—排序—AI补全—裁决—审计”的独立引擎，并用运行时证据把准确率拉到上限。**

这才是只考虑最终效果时，最正确的做法。

你下一步最应该先做的是：**先把“新引擎目录结构 + 新阶段工件模型 + 新表设计”定下来，再开始写代码。**
