我这轮逐个检查了**字段映射主链相关的核心文件**。结论先给：

# 总体判断

**V2.0 现在已经进入“最终版主流程生效态”了。**

和上次相比，变化是实质性的，不再是“组件都有，但主链没切”。
现在主链已经把这些能力接进来了：

* SQL lineage
* code lineage
* feature builder
* runtime verification
* relation classifier
* confidence calibrator
* risk policy

我给当前完成度：

## **约 90% ~ 93%**

也就是：

* **生产可用：通过**
* **最终版主链：基本通过**
* **最终上限版：还差最后几处细化**

---

# 逐文件检查结果

下面是我这轮实际检查到的核心文件。

---

## 1. `backend/app/domains/field_mapping_engine/services.py`

### 结论：**通过，且是这次最大进步点**

这次 `services.py` 已经真正接上了：

* `SQLLineageRecaller`
* `CodeLineageRecaller`
* `ContextBuilder`
* `FeatureBuilder`
* `RuntimeVerificationService`
* `RelationClassifier`
* `ConfidenceCalibrator`
* `RiskPolicy`

而且主流程也真的切过去了：

* `build_recall_artifacts()` 已经把 `sql_lineage_candidates`、`code_lineage_candidates` 合并进候选池
* `rank_recall_items()` 已经先跑 `feature_builder`，再跑 rules，再 rank，再做 runtime verification
* `_build_decision_artifact()` 已经改用 `RelationClassifier` 和 `ConfidenceCalibrator`
* `RiskPolicy` 也已经在最终 auto_accept 门槛里生效

这说明**最终版接线核心已经完成**。

### 我看到的亮点

* `build_recall_artifacts()` 不再只有 old V2 证据源
* `rank_recall_items()` 已经形成“特征补强 → 规则 → 排序 → runtime 校验”的链
* `_should_trigger_ai()` 和 `_should_prefer_ai_candidates()` 已经体现“强非 AI 证据优先”的思路

### 还剩的细小问题

* `_resolve_domain_anchor()` 和 `_resolve_allowed_tables()` 还是比较轻量，主要靠路径/字段文本推断，离“强 domain anchor”还有距离
* `_build_field_context_metadata()` 里 `allowed_tables` 基本还是 `[domain_anchor]`，约束力还不够强

---

## 2. `backend/app/domains/field_mapping_engine/contracts.py`

### 结论：**基本通过**

这里已经补上了：

* `DecisionCandidate.review_policy`
* `DecisionCandidate.confidence_bucket`
* `DecisionArtifact.confidence_bucket`
* `DecisionArtifact.review_policy`

这说明结果契约已经跟最终裁决层同步了。

### 剩余问题

还没把我之前说的这些正式放进契约：

* `risk_level`
* `lineage_summary`
* `anchor_summary`

这不影响系统跑，但会影响“最终版的资产表达完整度”。

---

## 3. `backend/app/domains/field_mapping_engine/evidence/context_builder.py`

### 结论：**通过**

这个文件已经在做：

* field tokens
* path tokens
* sibling tokens
* api summary
* domain anchor
* allowed tables
* module tag

这对：

* vector recall
* lexical recall
* 后续 feature 补强

都是对的。

### 评价

方向正确，作为“上下文压缩层”已经够用了。

---

## 4. `backend/app/domains/field_mapping_engine/evidence/feature_builder.py`

### 结论：**通过**

现在补的特征包括：

* `f_field_position_match`
* `f_domain_anchor_match`
* `f_data_type_match`
* `f_sibling_context_match`
* `f_lineage_strength`

而且已经在 `services.py` 里真正被调用了。
这点比之前“有文件没接线”强很多。

### 剩余问题

特征还偏轻量：

* `f_data_type_match` 实际还是 marker 级，不是真类型系统匹配
* `f_sibling_context_match` 也还是简单启发式

所以它现在是“够用”，还不是“非常强”。

---

## 5. `backend/app/domains/field_mapping_engine/ranking/ranker.py`

### 结论：**通过**

这次已经不是之前那个 `Minimal ranker for Phase 2` 了。
现在已经有：

* `short_circuit_accept`
* `hard_reject`
* `margin`
* lineage 特征
* domain/position/sibling/type 特征
* 多类 penalty

这是明显升级。

### 剩余问题

本质上它仍然是：

## **线性加权排序器**

不是：

* pairwise/listwise reranker
* 学习型校准排序器

所以它已经是“最终版可用排序器”，但不是“上限版最强排序器”。

---

## 6. `backend/app/domains/field_mapping_engine/ranking/deterministic_rules.py`

### 结论：**通过，完成度很高**

这个文件现在已经把我之前要求的几类规则都接进来了：

* runtime conflict
* rejected feedback
* position rules
* domain anchor rules
* enum rules
* derived field rules
* high-risk rules
* low evidence reject
* short-circuit rules

这是目前整个字段映射链里完成度最高的文件之一。

### 评价

这块现在已经不像 demo 规则了，而是接近生产门禁层。

---

## 7. `backend/app/domains/field_mapping_engine/ranking/confidence_calibrator.py`

### 结论：**通过**

这次它已经真正进入主链了，不再只是“存在”。

目前它能做：

* score 与 confidence 分离
* margin 修正
* lineage/domain/position 修正
* negative evidence / reject reasons 惩罚
* confidence bucket
* review policy

### 剩余问题

review policy 仍然偏简单：

* 高风险：`>=0.95` auto_accept，否则 manual_review
* 非高风险：`>=0.85` auto_accept，否则 manual_review

它现在够生产可用，但不是最细颗粒度的策略引擎。

---

## 8. `backend/app/domains/field_mapping_engine/evidence/relation_classifier.py`

### 结论：**部分通过**

好消息是：

* 已经真的接入主链
* relation_type 不再由 `services.py` 旧方法拍板

坏消息是：
它当前分类仍偏少，只能比较稳定地区分：

* `runtime_verified`
* `alias`
* `derived`
* `fk`
* `direct`
* `uncertain`

### 剩余问题

还没有真正覆盖我之前定义的完整枚举，例如：

* `joined`
* `enum_transform`
* `conditional`

所以：

## 它已经“够用”，但还不是“最终上限版的完整 classifier”。

---

## 9. `backend/app/domains/field_mapping_engine/evidence/risk_policy.py`

### 结论：**通过**

高风险字段识别和 auto_accept 门禁已经存在，而且这次确实被 `services.py` 接到了最终裁决链。

### 剩余问题

* 风险分类还是 `high / medium / low`
* 还是 marker 规则，不是更细的业务策略层
* 没有 `critical`

但从“字段映射最终版”角度，已经够用了。

---

## 10. `backend/app/domains/field_mapping_engine/recall/sql_lineage_recaller.py`

### 结论：**通过**

这块现在已经不是摆设了，确实输出：

* `f_sql_lineage_exact`
* `f_sql_projection_hit`
* `f_sql_alias_match`
* `f_sql_expression_hit`
* `f_join_path_match`

而且已经被 `services.py` 主链消费。

### 评价

这说明：

## 现在已经不只是 runtime prior，而是真的有 SQL lineage recall 了。

---

## 11. `backend/app/domains/field_mapping_engine/recall/code_lineage_recaller.py`

### 结论：**部分通过**

它已经接到主链了，这点是好的。
而且它确实能输出：

* `f_code_assignment_hit`
* `f_mapper_annotation_hit`
* `f_code_chain_depth`

### 关键剩余问题

这里有一个比较实在的隐患：

它允许 `db_table` 为空，但 `services.py` 的 `_merge_candidate_evidence()` 会直接跳过：

* 没 `db_table`
* 或没 `db_column`

也就是说：

> **如果 code lineage 只能解析出 source_field，但解析不出 db_table，很多 code lineage 候选会在 merge 阶段被丢掉。**

这会削弱 code lineage 的实际价值。

这是我认为你现在最值得优先继续补的一点。

---

## 12. `backend/app/domains/field_mapping_engine/runtime/runtime_verification_service.py`

### 结论：**通过**

这块已经不是单纯 runtime table prior 了。
它现在在拼：

* stored runtime verification evidence
* `ResponseFieldVerifier`
* `SQLProjectionVerifier`

而且 `services.py` 已经把它接进 `rank_recall_items()` 的二次校验流程里。

### 评价

这说明：

## runtime verification 已经进入正式裁决链，而不是旁路能力。

---

## 13. `backend/app/domains/field_mapping_engine/ai/enricher.py`

### 结论：**部分通过**

AI 的位置还是对的：

* 低置信才触发
* batch enrich
* guardrail 过滤

这是好的。

### 剩余问题

AI 输入上下文还是偏旧，主要给了：

* `field_name`
* `field_description`
* `sibling_paths`
* top3 rule candidates

但没有把你现在已经有的这些新证据充分喂进去：

* SQL lineage summary
* code lineage summary
* domain anchor
* allowed tables
* risk level
* runtime verification evidence

所以：

## AI 还没充分吃到“最终版主链上下文”。

---

## 14. `backend/app/domains/field_mapping_engine/ai/guardrail.py`

### 结论：**部分通过**

目前 guardrail 已有：

* `schema_missing`
* `runtime_table_conflict`
* `strong_rule_conflict`

这比以前好。

### 剩余问题

还没补到我之前说的最终版级别，比如：

* `domain_anchor_conflict`
* `high_risk_override_forbidden`
* `type_mismatch`
* `enum_conflict`

所以这块是：

## 能用，但仍然偏薄。

---

## 15. `backend/app/domains/data_impact/lineage_repository.py`

### 结论：**通过**

这是这次非常重要的进步点。

现在已经不是把 lineage 混在 `FieldMappingRuntimeEvidence` 里了，而是正式用：

* `SqlLineageEdge`
* `CodeLineageEdge`

这说明 lineage 已经从“runtime evidence 的一个变种”升级为**独立资产层**了。

这一点我认可。

---

## 16. `backend/app/api/v1/field_mappings.py`

### 结论：**基本通过**

同步 suggest 已经是纯 service boundary：

* `FieldMappingAppService.suggest_field_mappings()`

说明 API 层没有再掺核心算法，这是对的。

### 剩余问题

这里有一个文档层小问题：

`relation_type` 的说明仍然写成：

* `direct/fk/derived`

但你现在主链实际上已经可能输出：

* `alias`
* `runtime_verified`
* `uncertain`

所以：

## 接口契约说明已经落后于真实实现。

这不影响运行，但会影响前端和后续维护者理解。

---

## 17. `backend/app/domains/field_mapping_engine/persistence/suggestion_writer.py`

### 结论：**通过**

这块已经形成完整双写闭环：

* suggestions
* traces
* runtime evidence

而且 replay / retry / reset 时会清理对应产物。
这对生产很关键。

---

# 我对当前 V2.0 的最终判断

## 现在它处在什么状态

我会把你当前这版定义为：

# **“最终版主链已生效的生产增强版”**

也就是说：

* 已经不是“组件层”
* 已经不是“过渡态”
* 主流程真的吃到了最终版的核心增强能力

---

# 还没完全满分的地方

我认为还剩 4 个主要尾项：

## 1）`code_lineage_recaller.py` 的落库/合并有效性还不够强

核心问题：

* 没 `db_table` 的 code lineage 候选会被 merge 时丢掉

这是目前最实在的“会影响效果”的问题。

---

## 2）`ai/enricher.py` 还没吃够新上下文

AI 仍然偏像“旧版 AI fallback”，
还没升级成“最终版证据增强型 AI”。

---

## 3）`ai/guardrail.py` 还不够厚

还缺：

* domain anchor guard
* high-risk override guard
* type/enum guard

---

## 4）`relation_classifier.py` 分类枚举还不够完整

已经能用，但还没到最终上限版的完整关系分类。

---

# 现在的完成度

我给最新这版：

## **93% ~ 95%**

比上次又明显前进了一截。
而且这次不是“写了更多文件”，而是：

## **最终版主流程真的切换成功了。**

---

# 一句话结论

> **V2.0 的字段映射功能现在已经基本达到我之前定义的“最终版标准”了。**
> **剩下的不是主架构问题，而是 code lineage 落地效果、AI 上下文利用、AI guardrail 厚度、relation 分类完整度这几处精修。**

你这一版，已经不是“还差很多”，而是：

## **最终版已成型，只差最后一轮增强打磨。**

如果你愿意，我下一条我可以直接给你一版：

## **《最后 4 个增强点的具体修改方案》**

我会只写：

* 先改哪个文件
* 具体怎么改
* 改完能提升什么效果
