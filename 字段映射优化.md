可以。下面这版我按两部分给你：

1. **《V2.0 验收通过标准》**
2. **《V2.0 修改清单》**

我按“是否能判定验收通过”来写，不按“代码好不好看”来写。

---

# 《V2.0 验收通过标准》

## 一、总体定义

当且仅当以下条件都满足时，`V2.0` 才能被判定为：

## **字段映射模块 V2.0 验收通过**

也就是：

* 新引擎成为**正式主链路**
* 旧核心退出**正式运行路径**
* 同步接口、异步任务、阶段工件、建议工作台、审计链、重试恢复形成**系统闭环**
* AI 不再成为不受控风险源
* 可以作为 **生产可用版主干** 持续演进

当前仓库已经有新引擎主干：`FieldMappingAppService / FieldMappingJobService`、新 runner、阶段工件存储和多路召回编排，这说明架构方向已经建立   。
但是否“验收通过”，取决于下面这些硬门槛是否全部满足。

---

## 二、硬门槛（任何一条不满足，都不能通过验收）

## A. 架构门槛

### A1. 新引擎必须成为唯一正式执行链

必须满足：

* 同步建议只走 `FieldMappingAppService`
* 异步任务只走 `EngineV2FieldMappingJobRunner`
* `legacy runner` 不能再进入正式分派链

现在 `FieldMappingJobService._resolve_runner()` 仍然保留了 `legacy` 分支，旧处理器仍可被调用，这说明目前还处于过渡态，不算正式收口  。

**通过标准：**

* 生产配置下 legacy 永久关闭
* 正式 API / 任务链路无任何旧 `data_mapping.processor` 调用
* 旧核心最多只做历史归档，不参与执行

---

### A2. Controller 必须彻底瘦身

API 层只能做：

* 参数校验
* 权限校验
* service 调用
* 响应包装

不能再保留：

* 旧字段提取逻辑
* 旧候选生成逻辑
* 旧 gravity / fallback 算法
* 旧辅助算法函数残留

**通过标准：**

* `field_mappings.py` 中不再存在旧 `_extract_*` / `_generate_*` 核心逻辑
* 旧 utils 不再在 controller 中承担决策职责
* controller 文件只保留“接口层代码”

---

## B. 功能闭环门槛

### B1. 同步建议链路必须闭环

必须满足：

* `/field-mappings/suggest` 正常返回 suggestions
* suggestions 来源于新引擎而非旧 helper
* 返回结果结构稳定，前端不需要猜字段语义

当前同步 suggest 已接到新 service，这是好的基础 。

**通过标准：**

* 所有同步建议都来自 `FieldMappingAppService`
* 无旧逻辑 fallback
* 输出字段文档稳定

---

### B2. 异步任务链路必须闭环

必须满足：

* 创建任务
* Celery 执行
* 阶段工件写入
* 最终 suggestions 写入工作台
* 查询任务状态 / 查询阶段结果 / 重试 / reset / replay 全部可用

现在 job runner 和 artifact store 已经把这条链的大部分打通了  。

**通过标准：**

* 任务从创建到结果落表全链路成功率稳定
* 重试后不会污染 stage_results / artifact
* replay 不会写重或写错

---

### B3. 阶段工件与阶段展示语义必须一致

这是硬门槛。

现在 `job_runner` 写入的是新工件语义：

* `input_snapshot`
* `field_specs`
* `recall_candidates`
* `ai_ranked_items`
* `final_suggestions` 

但 `field_mappings_async.py` 的阶段描述计算还在按旧统计字段解释阶段结果，这属于“观测错位”。

**通过标准：**

* UI 展示、阶段摘要、工件内容语义一致
* 每个阶段的 summary 字段和 artifact 真正对应
* 查询阶段详情时，用户看到的是新引擎真实状态，而不是旧语义翻译

---

## C. 数据一致性门槛

### C1. 建议表、任务结果、阶段工件必须一致

必须满足：

* `task.result.suggestions`
* `field_mapping_suggestions`
* `stage artifact.final_suggestions`

三者数量和内容可对齐。

现在 suggestions 写表已经独立了，这是好事 。
但“数据一致性”不能只靠 count，还要能核主要字段一致。

**通过标准：**

* 一致性检查不只是数量对比
* 任意 task 可抽样比对 suggestion 主键坐标：

  * `definition_id`
  * `api_field_path`
  * top1 candidate
  * decision_source
* 不一致时能被记录并告警

---

### C2. 审计 trace 必须落表

这是目前最关键的验收缺口之一。

你系统里已经有 `field_mapping_traces` 这个资产表，但新引擎的 `SuggestionWriter` 只写了 `field_mapping_suggestions`，没有把 trace 一起落下去  。

**通过标准：**

* 每条最终建议至少有一份 trace 记录
* trace 包含：

  * field_key
  * candidate
  * decision_source
  * final_score / confidence
  * recall_sources
  * 关键 features
  * AI 是否参与
* 可按 task_id / definition_id / field_key 回查

---

## D. AI 安全门槛

### D1. AI 不能直接成为无约束裁决器

当前新引擎里，AI enrichment 会把 AI 候选 merge 进候选集，并可能直接抬分胜出 。
如果没有 guardrail，这就是重大风险。

**通过标准：**
AI 候选在进入 final_candidates 前，必须经过：

* 物理列存在校验
* 表名存在校验
* allowed table / domain 校验
* 与 runtime 证据冲突校验
* 与历史强先验冲突校验
* 低可信 AI 结果降权或拒绝

---

### D2. AI 决策必须可回溯

**通过标准：**

* 能查到 AI 是否触发
* AI 原始返回摘要可审计
* AI 胜出时必须记录原因
* AI 被 reject 时也必须记录原因

---

## E. 裁决输出门槛

### E1. 输出不能只是候选列表

当前 suggestions 仍主要是 `candidates + decision_trace`，这更像“候选输出”，还不是完整裁决输出 。

**通过标准：**
每条 suggestion / mapping decision 至少要有：

* `top1 candidate`
* `decision_source`
* `relation_type`
* `confidence`
* `candidate list`
* `decision_trace`

---

### E2. confidence 不能等于裸 score

**通过标准：**

* 排序分数与最终 confidence 分离
* 最少有一层 calibration / threshold 规则
* 支持：

  * 自动确认
  * 待审核
  * 拒绝自动确认
    三档策略

---

### E3. 排序器不能只停留在线性加权初级版

这条也是硬门槛。

如果排序器仍然只是“若干正向特征线性加权”，那它只能算可运行，不算可验收。

**通过标准：**

* 必须存在明确的负证据 / reject 规则，而不只是正向加分
* 必须存在 hard deterministic short-circuit 或等价硬拒绝条件
* 必须把 runtime/history 强冲突纳入 final 决策，而不是只做弱加权
* 必须能记录 downgrade / reject 的具体原因
* 必须把结构证据至少利用到字段位置、sibling context、强先验冲突中的一部分

---

## F. 工程质量门槛

### F1. 编码和残留错误必须清零

当前已有乱码和旧代码残留痕迹，这在验收阶段不能接受。

**通过标准：**

* 无乱码日志、乱码 message、乱码响应文案
* 无未使用旧 helper
* 无同名变量遮蔽基础框架对象
* 无明显死代码 / 双实现并存混乱

---

### F2. 回归测试必须覆盖主链

**通过标准：**
至少有自动化测试覆盖：

* 同步 suggest
* 异步 suggest-task
* stage artifact 写入
* retry / resume / reset
* replay suggestions
* batch apply
* AI guardrail
* trace 落库

---

# 《V2.0 修改清单》

下面按优先级给。

---

## P0：不做就不能验收通过

## 1. 关闭 legacy 正式运行路径

**目标：** 新引擎成为唯一主链路

**要改：**

* `backend/app/domains/field_mapping_engine/services.py`
* 配置项 `FIELD_MAPPING_LEGACY_FALLBACK_ENABLED`
* 所有创建 task 的默认 engine_version

**动作：**

* 生产环境直接禁止 `engine_version=legacy`
* `_resolve_runner()` 不再允许普通流程走 legacy
* legacy 仅保留为开发期离线对比工具，不进入正式 API/Task

**完成标准：**

* 任意正式任务都只走 `EngineV2FieldMappingJobRunner`
* 日志中不再出现旧 `FieldMappingProcessor` 被实际执行

---

## 2. 清空 controller 中的旧核心函数

**目标：** API 层只做壳

**要改：**

* `backend/app/api/v1/field_mappings.py`

**删除/迁出：**

* `_generate_mapping_candidates_with_gravity`
* `_extract_api_fields`
* `_extract_field_descriptions`
* 旧评分/相似度辅助的 controller 级调用

**完成标准：**

* `field_mappings.py` 不再包含任何字段映射核心算法
* 所有建议逻辑都通过 `FieldMappingAppService`

---

## 3. 增加 TraceWriter，补全审计落库

**目标：** suggestions 与 traces 双写闭环

**要改：**

* 新增 `backend/app/domains/field_mapping_engine/persistence/trace_writer.py`
* `SuggestionWriter`
* `celery/tasks/_common.py`
* `celery/tasks/field_mapping_tasks.py`

**动作：**

* 为每条 suggestion 的 top candidates 生成 trace
* 将 decision_source / score / features / recall_sources / ai 标记落到 `field_mapping_traces`
* replay 时支持一起补写 trace

**完成标准：**

* 每个成功 task：

  * `field_mapping_suggestions` 非空
  * `field_mapping_traces` 非空
* 可通过 task_id 回查 trace

---

## 4. 增加 AI guardrail

**目标：** AI 只能补充，不能裸裁决

**要改：**

* `backend/app/domains/field_mapping_engine/ai/enricher.py`
* 可新增 `ai/guardrail.py`

**动作：**
至少加上：

* `db_table/db_column` 物理存在校验
* 与 schema snapshot 对齐校验
* allowed table / runtime_table_prior 校验
* AI 与强规则冲突时降级
* AI 与 runtime 证据冲突时拒绝
* AI 候选若非法，保留 rule 结果

**完成标准：**

* AI 不可能输出不存在的表列并进入 final
* AI 不能无条件覆盖 rule top1
* 所有 reject 原因可记录到 trace

---

## 5. 阶段语义对齐

**目标：** stage summary 与 artifact 同语义

**要改：**

* `backend/app/api/v1/field_mappings_async.py`
* `job_runner.py`

**动作：**

* 重写 `_calculate_stage_description()`
* 不再按旧版字段解释 stage data
* 为新阶段定义明确摘要：

  * stage1: input snapshot
  * stage2: field specs count
  * stage3: recall candidate count
  * stage4: AI optimized count / ai_triggered count
  * stage5: final suggestions count

**完成标准：**

* UI 上看到的阶段描述与实际 artifact 内容一致
* 不再出现“工件是新语义，描述却是旧语义”的情况

---

## 6. 修复编码与残留脏代码

**目标：** 工程收口

**要改：**

* `field_mapping_tasks.py`
* `field_mappings_async.py`
* 所有已出现乱码的文件

**动作：**

* 统一 UTF-8
* 清理乱码日志文案
* 清理旧注释和无效说明
* 清理无用 import / 无效兼容代码

**完成标准：**

* 所有接口 message、日志、异常文案正常可读
* 无乱码
* 无明显死代码残留

---

## P1：做完后进入“生产可用稳定态”

## 7. 定义标准化 DecisionArtifact

**目标：** 输出真正可裁决的对象，不只是候选列表

**要改：**

* `contracts.py`
* `services.py`

**新增字段：**

* `top_candidate`
* `relation_type`
* `confidence`
* `decision_source`
* `decision_trace`
* `candidate_list`

**完成标准：**

* suggestion 和最终 mapping 都基于统一 artifact 结构
* 下游工作台与 trace writer 直接消费统一对象
* 不再允许只用 `candidates + decision_trace` 作为验收输出形态

---

## 8. 补 relation_type 分类

**目标：** 不再只有模糊 direct

**建议枚举：**

* `direct`
* `alias`
* `foreign_key`
* `joined`
* `derived`
* `runtime_verified`
* `uncertain`

**要改：**

* `services.py`
* 新增 `evidence/relation_classifier.py` 或临时在 service 层实现

**完成标准：**

* 每条最终建议有 relation_type
* 未能明确分类时用 `uncertain`

---

## 9. 补 confidence 分层

**目标：** score 不直接等于置信度

**要改：**

* `ranking/ranker.py`
* 或新增 `ranking/confidence_calibrator.py`

**建议规则：**

* `>= 0.90` 自动确认候选
* `0.70 - 0.89` 待审核
* `< 0.70` 不自动确认

**完成标准：**

* 输出里有 `confidence`
* 工作台能按 `confidence bucket` 过滤
* auto-confirm 策略稳定可解释

---

## 10. 补负证据与冲突规则

**目标：** 降低误杀和误配

**要改：**

* `deterministic_rules.py`
* `services.py`

**增加规则：**

* 通用垃圾字段降权：`id/name/code/type/status/value`
* 与 runtime table prior 冲突的候选降权
* 历史 confirmed 强先验冲突的 AI 候选降权
* 字段位置不合理时降权：`path/query/body`

**完成标准：**

* 能看到 reject / downgrade 的具体原因
* top1 不再被纯语义相似轻易抢走
* 至少存在一类 hard reject / short-circuit 规则，而不是只有软降权

---

## 11. 补 trace 与 suggestion 的一致性校验

**目标：** 不是只写表，还要校验闭环

**要改：**

* `field_mapping_tasks.py`
* 新增一致性检查函数

**校验内容：**

* result.suggestions 数量
* suggestions table 数量
* traces table 数量
* top1 candidate 一致性
* decision_source 一致性

**完成标准：**

* 一致性失败会打标到 task.statistics
* 可以在任务详情里看到差异

---

## 12. 补回归测试

**目标：** 从重构态进入可维护态

**至少要有：**

* 同步 suggest 测试
* 异步任务测试
* retry/reset/resume 测试
* AI guardrail 测试
* trace 落库测试
* replay 测试
* batch apply 测试

**完成标准：**

* 主链测试全绿
* 每次改 ranking / AI 不会再把恢复链和工作台链带崩

---

## P2：做完后达到 P2.5 稳态

## 13. 工作台增强

**目标：** 真正支持人工审核闭环

**建议加：**

* 按 decision_source 筛选
* 按 confidence 区间筛选
* 按 relation_type 筛选
* 查看 trace 详情
* 查看阶段工件入口
* 查看 AI 原因摘要

---

## 14. 引入人工反馈回流

**目标：** 让系统越来越稳

**建议新增表：**

* `field_mapping_feedback`

**记录：**

* accept
* reject
* modify
* reviewer
* source_task_id

**用途：**

* 提升 history prior
* 给高风险字段建立强规则
* 为后续校准做数据基础

---

## 15. 增加运行时证据落地表

现在已有 runtime recall 思路，但还可以再结构化。

**建议新增：**

* `field_mapping_runtime_evidence`

用于记录：

* API → table/column 的运行时命中
* 证据类型
* confidence
* payload

这样后续做更强 runtime verified mapping 会更稳。

---

# 最终验收判定规则

我建议你最后按下面这条判：

## 可以判“V2.0 验收通过”的条件

必须同时满足：

1. **P0 全部完成**
2. **P1 至少完成 7、8、9、10、11、12**
3. 连续一轮回归中：

   * suggest 同步/异步链都通过
   * retry/reset/resume 正常
   * replay 正常
   * trace 可查
   * AI 不会把非法表列写进 final
   * 最终输出已经是标准化 `DecisionArtifact`
4. 正式路径完全不走 legacy

---

# 做完后的状态定义

如果你把上面这份标准和修改清单都做完，我会把 `V2.0` 定义为：

## **字段映射模块生产可用版（P2 / P2.5）**

它的意义是：

* 不需要再重写
* 可以切主链
* 可以长期演进
* 后续重点从“修结构”转到“提准确率”

---
