可以。
我直接按**你当前 V2.0 的实际代码**来讲，不讲空架构。

先给结论：

# 一、当前 V2.0 到了哪里

你现在这版已经是一个**生产可用的多证据字段映射引擎底座**，不是玩具版了。
它已经具备：

* 新主链路：`FieldMappingAppService -> recall -> rule -> rank -> AI -> suggestion` 
* 多路召回：`history / lexical / vector / runtime` 
* 基础规则：`DeterministicRules` 已有负证据、硬拒绝、短路接受 
* 基础排序：`CandidateRanker` 仍是线性加权模型，文件自己都写着 `Minimal ranker for Phase 2` 
* AI 受控补全：AI 只对低置信样本触发，并且有 guardrail  
* 持久化闭环：suggestion / trace / runtime evidence / feedback / stage artifact 都已落表    

所以：

> **上限版不是推倒重来。**
> **上限版是在你当前 V2.0 的骨架上，把“强证据层、裁决层、可观测层”继续做厚。**

---

# 二、当前 V2.0 距离“最终上限版”差什么

核心差 5 件事：

## 1. 没有真正的 SQL lineage / 代码 lineage

你现在的 `RuntimeEvidenceRecaller` 用的是：

* `ImpactRepository.get_impacts_by_api()`
* `FieldMappingRuntimeEvidence`
* `SchemaMapper.map_field()` 

而 `ImpactRepository` 当前存的是：

* `SqlTrace`
* `TableImpact`
* `FieldImpact`
* `ApiTableImpact` 

这说明你现在只有：

> **运行时表影响 + 字段变化证据**

没有：

> **SQL AST 级列血缘**
> **代码赋值链级 DTO → SQL列 → 物理列 的静态血缘**

这就是你距离“效果上限最高”的最大缺口。

---

## 2. 排序器仍然太轻

当前 `CandidateRanker` 是标准的 Phase 2 线性公式：

* `f_name_similarity`
* `f_table_similarity`
* `f_history_prior`
* `f_comment_similarity`
* `f_name_exact`
* `f_vector_similarity`
* `f_runtime_table_hit`
* `f_runtime_field_hit`
* 若干 rule boost / penalty 

这能用，但它还不是上限版，因为：

* 特征太少
* 没有 lineage 特征
* 没有 field context / sibling context 深特征
* 没有 high-risk field policy
* 没有 margin-aware calibration
* 没有 pairwise / listwise rerank

---

## 3. 规则层还没有“领域级/高风险级裁决”

当前 `DeterministicRules` 很不错，已经有：

* runtime conflict
* rejected feedback
* weak evidence hard reject
* exact+strong evidence 短路接受 

但它还缺：

* 高风险字段单独规则
* 枚举码值/状态字段专门机制
* path/query/body 位置强约束
* 聚合/派生字段专门拒绝机制
* domain anchor 级 hard boundary

---

## 4. relation_type 仍然偏简化

你现在 `_infer_relation_type()` 只大致区分：

* `fk`
* `direct`
* `derived` 

这对生产够用，但对“上限版”不够。

上限版至少要稳定区分：

* `direct`
* `alias`
* `fk`
* `joined`
* `derived`
* `enum_transform`
* `conditional`
* `runtime_verified`
* `uncertain`

---

## 5. history prior 还是“同项目当前版本”的简单 prior

`HistoryRecaller` 现在主要吃：

* 当前项目当前版本 confirmed mappings
* accepted / modified feedback
* rejected feedback 

这很好，但上限版还需要：

* 跨版本迁移 prior
* 同业务域 prior
* 同 path pattern prior
* 同 tag / module prior
* 负反馈时间衰减
* 用户团队反馈权重

---

# 三、最终上限版的目标定义

我给你一个明确目标：

# **V3 Ultimate = 静态血缘 + SQL lineage + 运行时验证 + 多证据裁决 + 高风险策略 + 可解释闭环**

在你当前 V2.0 上，这个“最终上限版”不是换架构，而是加 4 层能力：

1. **静态血缘层**
2. **更强证据特征层**
3. **更强裁决层**
4. **高风险策略层**

---

# 四、基于当前代码，具体怎么改

下面我直接按你的现有目录和文件来落方案。

---

## A. 在 `field_mapping_engine/recall/` 下新增两类召回器

你现在已经有：

* `lexical_recaller.py` 
* `vector_recaller.py` 
* `history_recaller.py` 
* `runtime_recaller.py` 

### 现在要新增：

### 1）`sql_lineage_recaller.py`

用途：

* 从 SQL AST / ORM 查询树里直接找字段来源
* 产出比 runtime 更强的候选

#### 输入

* `definition_id`
* SQL 文本 / MyBatis XML / ORM query metadata
* schema snapshot

#### 输出候选特征

* `f_sql_lineage_exact`
* `f_sql_alias_match`
* `f_join_path_match`
* `f_expression_match`
* `f_select_projection_hit`

#### 你要新增的数据

不是只存 `SqlTrace.sql_text`，而是增加一层解析结果表，例如：

```text
sql_lineage_edges
- id
- definition_id
- api_field_path (nullable)
- sql_source_type (select/projection/join/where/insert/update)
- source_table
- source_column
- target_alias
- expression
- confidence
- payload_json
```

### 2）`code_lineage_recaller.py`

用途：

* 扫 DTO/VO/Assembler/MapStruct/BeanUtils/手写赋值链
* 把 `response.userName <- userDO.name` 这类证据直接变成强候选

#### 输出特征

* `f_code_assignment_hit`
* `f_mapper_annotation_hit`
* `f_dto_field_path_hit`
* `f_entity_property_hit`

---

## B. 在 `domains/data_impact/` 下新增真正的 lineage 解析层

你现在 `ImpactRepository` 只负责“存”，不负责“解析血缘” 。

### 需要新增：

#### 1）`sql_lineage_parser.py`

负责：

* SQL AST 解析
* SELECT alias 提取
* projection lineage
* join path 提取
* subquery 展开
* expression source columns 提取

#### 2）`orm_lineage_parser.py`

负责：

* JPA / SQLAlchemy / MyBatis resultMap / mapper 层转义
* result field → db column 映射

#### 3）`lineage_service.py`

负责统一输出：

```json
{
  "definition_id": 123,
  "api_field_path": "body.userName",
  "candidate_columns": [
    {
      "db_table": "user",
      "db_column": "name",
      "evidence_type": "sql_projection",
      "confidence": 0.98
    }
  ]
}
```

---

## C. 改 `services.py`，把主编排升级成“六路召回”

你现在 `FieldMappingAppService.__init__()` 里挂的是：

* `api_extractor`
* `db_extractor`
* `history_recaller`
* `lexical_recaller`
* `vector_recaller`
* `runtime_recaller`
* `deterministic_rules`
* `ranker`
* `ai_enricher` 

### 最终上限版要改成：

```python
self.history_recaller
self.lexical_recaller
self.vector_recaller
self.runtime_recaller
self.sql_lineage_recaller
self.code_lineage_recaller
self.domain_anchor_recaller
```

### 然后修改 `build_recall_artifacts()`

现在这里只 merge：

* lexical
* vector
* runtime 

要改成 merge：

* lexical
* vector
* runtime
* sql_lineage
* code_lineage
* domain_anchor

并新增字段：

```python
items.append({
    ...
    "domain_anchor": ...,
    "lineage_context": ...,
    "risk_level": ...,
    "candidate_evidence": ...
})
```

---

## D. 替换 `ranker.py`，不要再用当前这个 Minimal ranker

当前文件名字已经把问题说死了：`Minimal ranker for Phase 2` 。

### 上限版改法

不是删除接口，而是保留 `CandidateRanker` 这个类名，内部改成三段式：

## 第一段：硬规则短路

优先级最高：

* lineage exact hit
* code assignment exact hit
* runtime field hit + exact name
* history confirmed + exact + same anchor

命中则：

* `short_circuit_accept = True`
* score 直接 >= 0.98

## 第二段：强特征排序

把特征扩成下面这些：

### 新增强特征

* `f_sql_lineage_exact`
* `f_sql_projection_hit`
* `f_join_path_match`
* `f_code_assignment_hit`
* `f_code_mapper_hit`
* `f_field_position_match`
* `f_sibling_context_similarity`
* `f_domain_anchor_match`
* `f_data_type_match`
* `f_enum_dictionary_match`

### 新增负特征

* `f_high_risk_conflict_penalty`
* `f_domain_anchor_conflict_penalty`
* `f_type_mismatch_penalty`
* `f_enum_conflict_penalty`
* `f_join_impossible_penalty`

## 第三段：独立 confidence calibration

现在 `_calibrate_confidence()` 在 `services.py` 里是规则修正型 。

上限版应该抽出去：

### 新增文件

`ranking/confidence_calibrator.py`

输入：

* top1 score
* top1-top2 margin
* decision_source
* lineage strength
* risk level
* negative evidence count

输出：

* calibrated confidence
* confidence_bucket

---

## E. 升级 `DeterministicRules`

当前这层已经不错，但还不够“上限版” 。

### 你要增加的规则类型：

## 1）高风险字段规则

对于这些字段：

* amount
* status
* balance
* role
* permission
* user_id
* deleted
* create_by / update_by
* time fields

规则变严：

* 没 lineage / runtime / strong history，不允许 auto accept
* 语义像但缺强证据时，直接压到人工审核

## 2）path/query/body 强位置规则

例如：

* `path.id` 优先主表主键
* `query.status` 更可能是过滤条件字段
* `body.xxxName` 更可能是展示/别名字段而不是主键

## 3）派生/聚合字段拒绝规则

比如：

* `xxxCount`
* `totalAmount`
* `displayStatus`
* `fullName`

如果没有表达式血缘，不允许判成 `direct`

## 4）domain anchor hard boundary

如果 API 路径/模块/运行时 impact 已经明确 anchor 在 `order` 域，
那 `user.*`、`role.*` 这类泛字段就不能靠向量相似轻易抢走 top1。

---

## F. 升级 `HistoryRecaller`

当前 `HistoryRecaller` 只吃：

* 当前项目当前版本 confirmed
* accepted/modified feedback
* rejected feedback 

### 上限版改法

#### 1）扩展 prior source

新增：

* 同项目跨版本 confirmed
* 同业务域 confirmed
* 同 path pattern confirmed
* 同字段位置（path/query/body）confirmed

#### 2）引入时间衰减

近期反馈权重大于老反馈。

#### 3）引入反馈质量分层

* accepted > modified > rejected
* reject 只在没有强 lineage 证据时生效
* 高质量 reviewer 可加权

---

## G. 升级 `VectorRecaller`

当前向量查询输入是：

* field_name
* field_path
* description 

这太弱了。

### 上限版改法

把 query 上下文改成：

* method
* path resource nouns
* field_name
* field_path
* field_description
* sibling_paths
* api summary/description
* detected domain anchor

并且把 vector 召回改成：

* **只召回，不拍板**
* **vector score 不能单独抬成高 confidence**
* allowed_tables 不只是 runtime table prior，还要结合 domain anchor

---

## H. 升级 `LexicalRecaller`

当前 `LexicalRecaller` 的特征是：

* name similarity
* table similarity
* exact name
* history prior
* comment similarity 

### 上限版改法

新增：

* token role match
* acronym expansion match
* field suffix/prefix match
* enum/code/name pair match
* sibling co-occurrence match

例如：

* `userName` 和 `userId` 同时出现时，`user.name` 和 `user.id` 应该形成上下文协同，而不是单点匹配。

---

## I. 新增 `relation_classifier.py`

现在 relation type 还是在 `services.py` 里手写判断 。
上限版要把它独立出来。

### 新文件

`evidence/relation_classifier.py`

### 输入

* field_name
* candidate features
* lineage evidence
* SQL projection / expression
* runtime evidence

### 输出

* `direct`
* `alias`
* `fk`
* `joined`
* `derived`
* `enum_transform`
* `conditional`
* `runtime_verified`
* `uncertain`

### 规则示例

* SQL projection alias -> `alias`
* join path hit -> `joined`
* expression contains concat/sum/case -> `derived`
* enum dictionary match -> `enum_transform`
* runtime field hit + exact field = `runtime_verified`

---

## J. 修改 `job_runner.py`，不要改阶段数，但要升级每阶段工件

你现在阶段设计已经是好的，不要推翻 。

### 仍保留 5 阶段

1. input snapshot
2. field specs
3. recall candidates
4. ai ranked items
5. final suggestions

### 但要扩展 artifact 内容

#### Stage 2 field_specs

增加：

* domain anchor
* field risk level
* sibling context
* api summary/description digest

#### Stage 3 recall_candidates

增加：

* lineage candidates
* code lineage candidates
* reject reasons by source
* merged evidence graph

#### Stage 4 ai_ranked_items

增加：

* calibrated confidence
* relation classification
* high-risk downgrade reason

#### Stage 5 final_suggestions

增加：

* review_policy (`auto_accept/manual_review/reject`)
* confidence_bucket
* risk_level

---

## K. 数据表层需要新增两张表

### 1）`sql_lineage_edges`

存 SQL/ORM 血缘边。

### 2）`code_lineage_edges`

存 DTO/Assembler/Mapper 代码血缘边。

你当前已有：

* `FieldMappingTrace`
* `FieldMappingStageArtifact`
* `FieldMappingFeedback`
* `FieldMappingRuntimeEvidence` 

这些继续保留，不动。
新表是补强“强证据层”。

---

## L. AI 层不要再增强“拍板能力”，只增强“补全能力”

你现在 AI 的位置是对的：低置信触发 + guardrail  。

### 上限版只做两件增强：

#### 1）更好的 prompt 上下文

把 lineage evidence、domain anchor、runtime prior、history prior 都喂进去。

#### 2）更严格的回写策略

AI 产出的 candidate 必须附：

* why
* supporting fields
* expected relation_type

并继续受 guardrail 约束。

### 不要做的事

* 不要让 AI 绕过 lineage
* 不要让 AI 在高风险字段上覆盖强规则
* 不要让 AI 在无 schema 证据时生成 final

---

# 五、最终上限版的具体改造顺序

我给你最合理的顺序：

## Phase 1：补强证据层

先做：

* `sql_lineage_recaller.py`
* `code_lineage_recaller.py`
* `sql_lineage_parser.py`
* `orm_lineage_parser.py`

这是提升上限最大的部分。

---

## Phase 2：替换排序与校准

做：

* 重写 `ranker.py`
* 新增 `confidence_calibrator.py`
* 新增 `relation_classifier.py`

这一步会把“候选引擎”真正变成“高可靠裁决引擎”。

---

## Phase 3：升级规则层

重写 `DeterministicRules`，加入：

* 高风险字段策略
* domain anchor hard boundary
* 派生字段拒绝机制
* 位置约束

---

## Phase 4：升级 history/vector/lexical

做：

* cross-version prior
* richer vector query context
* richer lexical context features

---

## Phase 5：接入更强 runtime verification

把 runtime evidence 从“table prior”升级成：

* response-field verified mapping
* execution-time column hit verification

---

# 六、最终我给你的落地判断

一句话讲清：

> **你当前的 V2.0 已经是一个很好的生产基座；**
> **最终上限版不需要换骨架，只需要把“强证据层 + 裁决层 + 高风险策略层”做厚。**

如果再压缩成工程动作：

## 你下一步最该做的 4 件事

1. **补 SQL lineage / code lineage**
2. **替换 Phase 2 ranker 为强特征 ranker + confidence calibrator**
3. **把 relation_type 独立成 classifier**
4. **给高风险字段加专门裁决策略**

这 4 件事做完，你的字段映射能力就会从：

## “生产可用版”

升级成：

## **“高可靠上限版”**