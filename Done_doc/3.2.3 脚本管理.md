# 脚本管理模块整体改动方案

## 一、数据库改动

### 新增表

#### 1. `test_types` - 测试类型配置表

```python
class TestType(Base, TimestampMixin):
    """测试类型配置表"""
    __tablename__ = "test_types"
    
    id = Column(Integer, primary_key=True, index=True)
    project_id = Column(Integer, ForeignKey("projects.id"), nullable=False)
    name = Column(String(50), nullable=False)
    code = Column(String(50), nullable=False, unique=True)
    description = Column(Text)
    is_preset = Column(Boolean, default=False)
    is_active = Column(Boolean, default=True)
    sort_order = Column(Integer, default=0)
    
    __table_args__ = (
        Index('ix_test_types_project_id', 'project_id'),
        UniqueConstraint('project_id', 'code', name='uq_project_type_code'),
    )
```

**用途**：管理预设和自定义测试类型

---

#### 2. `script_generations` - 脚本生成记录表

```python
class ScriptGeneration(Base, TimestampMixin):
    """脚本生成记录表"""
    __tablename__ = "script_generations"
    
    id = Column(Integer, primary_key=True, index=True)
    project_id = Column(Integer, ForeignKey("projects.id"), nullable=False)
    endpoint_id = Column(Integer, ForeignKey("api_endpoints.id"), nullable=False)
    test_types = Column(JSON, nullable=False)
    generated_count = Column(Integer, default=0)
    status = Column(String(20), default="completed")
    error_message = Column(Text)
    
    __table_args__ = (
        Index('ix_script_generations_project_id', 'project_id'),
        Index('ix_script_generations_endpoint_id', 'endpoint_id'),
    )
```

**用途**：记录每次生成的配置，便于追溯

---

#### 3. `script_executions` - 脚本执行记录表

```python
class ScriptExecution(Base, TimestampMixin):
    """脚本执行记录表"""
    __tablename__ = "script_executions"
    
    id = Column(Integer, primary_key=True, index=True)
    project_id = Column(Integer, ForeignKey("projects.id"), nullable=False)
    script_id = Column(Integer, ForeignKey("api_test_scripts.id"), nullable=False)
    endpoint_id = Column(Integer, ForeignKey("api_endpoints.id"), nullable=False)
    environment_id = Column(Integer, ForeignKey("environments.id"), nullable=False)
    
    status = Column(String(20), default="pending")
    duration_ms = Column(Integer)
    
    request_url = Column(String(500))
    request_method = Column(String(10))
    request_body = Column(JSON)
    
    response_status_code = Column(Integer)
    response_body = Column(JSON)
    response_time_ms = Column(Integer)
    
    assertion_results = Column(JSON)
    error_message = Column(Text)
    
    # 关系定义
    script = relationship("ApiTestScript", foreign_keys=[script_id])
    endpoint = relationship("ApiEndpoint", foreign_keys=[endpoint_id])
    environment = relationship("Environment", foreign_keys=[environment_id])
    
    __table_args__ = (
        Index('ix_script_executions_project_id', 'project_id'),
        Index('ix_script_executions_script_id', 'script_id'),
        Index('ix_script_executions_endpoint_id', 'endpoint_id'),
        Index('ix_script_executions_environment_id', 'environment_id'),
    )
```

**用途**：记录脚本执行历史，关联到接口层面

---

### 修改表

#### `api_test_scripts` - 测试脚本表

**无需修改**，现有结构保持不变

---

## 二、后端 API 改动

### 新增接口

#### 1. 测试类型管理

- `GET /api/v1/api-integration/test-types` - 获取测试类型列表
- `POST /api/v1/api-integration/test-types` - 创建自定义测试类型
- `PUT /api/v1/api-integration/test-types/{id}` - 更新测试类型
- `DELETE /api/v1/api-integration/test-types/{id}` - 删除测试类型

---

#### 2. 脚本执行

- `POST /api/v1/api-integration/scripts/{script_id}/execute` - 执行单个脚本
- `POST /api/v1/api-integration/endpoints/{endpoint_id}/execute-all` - 执行接口的所有脚本
- `GET /api/v1/api-integration/endpoints/{endpoint_id}/executions` - 获取接口的执行记录
- `GET /api/v1/api-integration/executions/{execution_id}` - 获取执行详情

---

### 修改接口

#### 1. `GET /api/v1/api-integration/scripts` - 获取脚本列表

**改动**：返回树形结构（分组 → 接口 → 脚本）

**新响应格式**：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "groups": [
      {
        "group_id": 1,
        "group_name": "用户管理",
        "description": "用户相关接口",
        "endpoints": [
          {
            "endpoint_id": 101,
            "path": "/api/users",
            "method": "POST",
            "summary": "创建用户",
            "scripts": [
              {
                "id": 1,
                "name": "正向测试：正常创建用户",
                "test_type": "positive",
                "status": "active"
              }
            ]
          }
        ]
      }
    ],
    "total_scripts": 150,
    "total_groups": 5
  }
}
```

---

#### 2. `POST /api/v1/api-integration/scripts/generate` - 生成脚本

**改动**：支持自定义测试类型和类型描述

**新请求参数**：
```json
{
  "endpoint_ids": [101, 102],
  "test_types": ["positive", "negative", "performance"],
  "custom_type_descriptions": {
    "performance": "测试接口在高并发下的响应时间"
  }
}
```

**新响应格式**：
```json
{
  "code": 0,
  "message": "成功生成 9 个测试脚本",
  "data": {
    "generation_id": 123,
    "endpoints_count": 2,
    "scripts_count": 9,
    "test_types_used": ["positive", "negative", "performance"],
    "scripts": [...]
  }
}
```

---

#### 3. AI Prompt 模板

**改动**：动态构建测试类型配置，传递给 AI

**新 Prompt 格式**：
```
请为以下接口生成测试脚本：

项目信息：
- 项目名称：{project_name}
- 技术栈：{tech_stack}

接口信息：
- 路径：{path}
- 方法：{method}
- 描述：{description}

请求参数 Schema：
{request_schema}

响应参数 Schema：
{response_schema}

测试类型配置：
{test_types_config}

请根据以上测试类型配置，为每个类型生成对应的测试脚本。

返回格式（JSON）：
{{
  "scripts": [
    {{
      "name": "测试用例名称",
      "test_type": "positive|negative|boundary|exception|performance",
      "test_type_description": "该测试类型的具体要求",
      "description": "测试描述",
      "request_body": {{...}},
      "assertions": [...]
    }}
  ]
}}
```

**测试类型配置格式**：
```json
{
  "positive": {
    "name": "正向测试",
    "description": "使用符合业务语义的正常参数测试",
    "requirements": [
      "使用真实业务场景的参数",
      "验证返回状态码为 200",
      "验证核心业务字段正确"
    ]
  },
  "performance": {
    "name": "性能测试",
    "description": "测试接口在高并发下的响应时间",
    "requirements": [
      "并发请求 100 次",
      "响应时间应小于 500ms",
      "成功率应大于 95%"
    ]
  }
}
```

---

## 三、前端页面改动

### 1. 脚本列表页面 (`Scripts.tsx`)

**改动**：
- 使用 Tree 组件展示树形结构（分组 → 接口 → 脚本）
- 每个脚本添加"执行"按钮
- 支持按分组生成脚本

**界面设计**：
```
┌─────────────────────────────────────────────────┐
│ 测试脚本                    [按分组生成] [执行]  │
├─────────────────────────────────────────────────┤
│ ▼ 用户管理 (15个脚本)                           │
│   ▼ 创建用户 (5个脚本)                          │
│     ✓ 正向测试：正常创建用户                   │
│     ✓ 逆向测试：缺少必填字段                   │
│     ✓ 边界测试：用户名长度边界                 │
│   ▼ 获取用户详情 (3个脚本)                      │
│     ✓ 正向测试：正常获取                       │
│     ✓ 逆向测试：用户不存在                     │
│ ▼ 订单管理 (20个脚本)                          │
│   ▼ 创建订单 (7个脚本)                          │
└─────────────────────────────────────────────────┘
```

---

### 2. 生成脚本弹窗

**改动**：
- 显示预设测试类型（可勾选）
- 支持添加自定义测试类型
- 支持编辑自定义类型的描述
- 显示预计生成的脚本数量

**界面设计**：
```
┌─────────────────────────────────────────┐
│ 生成测试脚本                              │
├─────────────────────────────────────────┤
│ 预设测试类型：                            │
│ ☑ 正向测试                                │
│ ☑ 逆向测试                                │
│ ☐ 边界测试                                │
│ ☐ 异常测试                                │
│                                          │
│ 自定义测试类型：                            │
│ [+ 添加自定义类型]                          │
│ ☑ 性能测试                                │
│    描述：测试高并发下的响应时间和稳定性      │
│    [编辑] [删除]                           │
│ ☑ 安全测试                                │
│    描述：测试 SQL 注入和 XSS 防护           │
│    [编辑] [删除]                           │
│                                          │
│ 预计生成：9 个脚本（3个接口 × 3种类型）     │
│                                          │
│ [取消] [开始生成]                          │
└─────────────────────────────────────────┘
```

---

### 3. 接口列表页面 (`Endpoints.tsx`)

**改动**：
- 每个接口添加"执行"按钮
- 添加"执行记录"按钮
- 支持查看接口的历史执行记录

**界面设计**：
```
┌─────────────────────────────────────────┐
│ 接口定义                    [刷新]       │
├─────────────────────────────────────────┤
│ 接口路径    | 方法 | 摘要       | 操作   │
├─────────────────────────────────────────┤
│ /api/users | POST | 创建用户    | [执行] │
│ /api/users/{id} | GET | 获取用户 | [执行] │
└─────────────────────────────────────────┘
```

---

### 4. 新增页面/组件

#### 环境选择弹窗
- 选择执行环境
- 显示环境的基础 URL

#### 执行记录弹窗
- 按环境筛选执行记录
- 显示执行结果（成功/失败）
- 点击查看详情

#### 执行详情抽屉
- 显示完整的请求/响应
- 显示断言结果
- 显示错误信息

---

## 四、核心改动总结

### 数据层

| 改动 | 说明 |
|------|------|
| 新增 `test_types` 表 | 管理测试类型 |
| 新增 `script_generations` 表 | 记录生成配置 |
| 新增 `script_executions` 表 | 记录执行历史 |

---

### 后端层

| 改动 | 说明 |
|------|------|
| 新增 4 个测试类型管理接口 | CRUD 操作 |
| 新增 4 个脚本执行接口 | 执行、查询详情 |
| 修改脚本列表接口 | 返回树形结构 |
| 修改生成脚本接口 | 支持自定义类型 |
| 修改 AI Prompt | 动态构建类型配置 |

---

### 前端层

| 改动 | 说明 |
|------|------|
| 脚本列表改为树形展示 | 分组 → 接口 → 脚本 |
| 优化生成脚本弹窗 | 支持自定义类型 |
| 接口列表添加执行功能 | 直接执行所有脚本 |
| 新增环境选择弹窗 | 选择执行环境 |
| 新增执行记录弹窗 | 查看历史记录 |
| 新增执行详情抽屉 | 查看详细结果 |

---

## 五、实施优先级

### 第一阶段：基础功能（当前执行）

1. 数据库表创建
   - `test_types` 表
   - `script_generations` 表
   
2. 测试类型管理接口
   - `GET /api/v1/api-integration/test-types`
   - `POST /api/v1/api-integration/test-types`
   
3. 修改生成脚本接口
   - 支持自定义测试类型
   - 支持类型描述
   
4. 前端生成脚本弹窗优化
   - 显示预设类型
   - 支持添加自定义类型

---

### 第二阶段：树形展示

1. 修改脚本列表接口（返回树形结构）
2. 前端脚本列表改为 Tree 组件
3. 按分组生成脚本功能

---

### 第三阶段：执行功能

1. 创建 `script_executions` 表
2. 脚本执行接口
3. 接口执行接口
4. 执行记录查询接口
5. 前端执行功能（环境选择、执行记录、详情展示）