# 阶段二执行计划（细化版）

以下计划基于“阶段二 详细设计.md”，细化为“具体待改函数列表 + 执行顺序 + 回归测试清单”。

## 一、具体待改函数列表（按模块）

### 数据库与模型
- `backend/app/platform/db/base.py`
  - 新增 SQLAlchemy 模型类：
    - `class GraphNode(Base)`
    - `class GraphEdge(Base)`
- `backend/migrations/xxxx_create_graph_nodes_edges.py`
  - 迁移脚本内：
    - `upgrade()`：创建 `graph_nodes`、`graph_edges` 表与索引
    - `downgrade()`：删除表与索引

### Graph 领域模型
- `backend/app/domains/knowledge_graph/entity_models.py`
  - 新增：
    - `class GraphNodeDTO`
    - `class GraphEdgeDTO`
    - `enum NodeType`
    - `enum RelationType`

### Repository
- `backend/app/domains/knowledge_graph/repository.py`
  - 新增（至少）：
    - `create_nodes(nodes: List[GraphNodeDTO])`
    - `create_edges(edges: List[GraphEdgeDTO])`
    - `get_node_by_id(node_id)`
    - `get_edges_by_source(source_id, relation_type=None)`
    - `get_edges_by_target(target_id, relation_type=None)`
    - `find_node(node_type, source_id | name)`
    - `upsert_node(node)`
    - `upsert_edge(edge)`

### Graph Builder
- `backend/app/domains/knowledge_graph/builder.py`
  - `build_api_nodes()`
  - `build_table_nodes()`
  - `build_field_nodes()`
  - `build_api_field_edges_from_mapping()`
  - `build_api_table_edges_from_execution()`

### Graph Service
- `backend/app/domains/knowledge_graph/graph_service.py`
  - `build_from_openapi(project_id, version_id)`
  - `build_from_db_schema(project_id, version_id)`
  - `build_from_field_mapping(project_id, version_id)`
  - `update_from_execution(project_id, scenario_id)`
  - `get_node(node_id)`
  - `get_tables_written_by_api(api_id)`
  - `get_apis_operating_on_table(table_name)`

### Query Engine
- `backend/app/domains/knowledge_graph/query_engine.py`
  - `get_tables_written_by_api(api_id)`
  - `get_apis_operating_on_table(table_name)`
  - `get_api_fields(api_id)`（文档里“API→FIELD”链路需要）

### API 接口
- `backend/app/api/v1/knowledge_graph.py`
  - `get_node(id)`
  - `get_tables_by_api(id)`
  - `get_apis_by_table(name)`
- `backend/app/api/v1/__init__.py`
  - 注册路由

### 接入点（触发构建/更新）
- API 文档解析完成处（OpenAPI 同步）
  - 末尾调用：`graph_service.build_from_openapi(...)`
- DB Schema 同步完成处
  - 末尾调用：`graph_service.build_from_db_schema(...)`
- Field Mapping 完成处
  - 结果落库后调用：`graph_service.build_from_field_mapping(...)`
- 执行日志写入处
  - 执行完成后调用：`graph_service.update_from_execution(...)`

### Celery 任务
- `backend/app/celery/tasks/knowledge_graph_tasks.py`
  - `build_graph_from_api(project_id, version_id)`
  - `build_graph_from_schema(project_id, version_id)`
  - `update_graph_from_execution(project_id, scenario_id)`
- `backend/app/celery/tasks/__init__.py`
  - 注册任务

### 前端
- `frontend/src/services/knowledgeGraph.ts`
  - `getNode(id)`
  - `getTablesByApi(id)`
  - `getApisByTable(name)`
- `frontend/src/pages/KnowledgeGraph.tsx`
  - 图谱展示组件（G6）
- `frontend/src/routes/...`
  - 注册路由 `/knowledge-graph`

## 二、执行顺序（严格依赖顺序）
1. 数据库迁移（GraphNode/GraphEdge 表）
2. SQLAlchemy 模型落地（`GraphNode`/`GraphEdge`）
3. Graph DTO 与枚举（`entity_models.py`）
4. Repository CRUD（Graph 存取）
5. Graph Builder 基础结构（API/DB/Field）
6. Graph Service（统一入口）
7. Query Engine（查询方法）
8. API 路由（Graph 查询接口）
9. Celery 任务（异步构建/更新）
10. 接入点埋钩（OpenAPI/Schema/FieldMapping/Execution）
11. 前端页面（Graph UI + API 连接）
12. 测试补齐

## 三、回归测试清单

### 后端新增测试
1. `backend/tests/test_knowledge_graph_repository.py`
   - `test_create_and_get_node`
   - `test_create_and_get_edge`
   - `test_upsert_node`
   - `test_upsert_edge`
2. `backend/tests/test_knowledge_graph_builder.py`
   - `test_build_api_nodes`
   - `test_build_table_nodes`
   - `test_build_field_nodes`
   - `test_build_api_field_edges_from_mapping`
3. `backend/tests/test_knowledge_graph_query_engine.py`
   - `test_get_tables_written_by_api`
   - `test_get_apis_operating_on_table`
4. `backend/tests/test_knowledge_graph_api.py`
   - `test_get_node`
   - `test_get_tables_by_api`
   - `test_get_apis_by_table`

### 后端现有回归建议
- `backend/tests/test_field_mapping_processor.py`（FieldMapping → Graph edge 关联时）
- `backend/tests/test_scenario_executor.py`（Execution logs → Graph 更新时）

### 前端手工回归
- `/knowledge-graph` 页面打开
- 按 API 查询，能看到对应表节点
- 按 Table 查询，能看到 API 节点
