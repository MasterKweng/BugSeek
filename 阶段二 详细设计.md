下面给你的是 **BugSeek V2 阶段二：Knowledge Graph（系统知识图谱）完整工程方案**。
这一阶段其实是 **AI 自动化测试平台最关键的能力**，甚至比 AI 本身更重要。

原因很简单：

> **AI 不知道你的系统结构，AI 就无法生成正确的测试。**

Knowledge Graph 的作用就是：

**让系统理解系统本身。**

我会从工程角度把方案拆成 **12个核心部分**：

1. 为什么必须有 Knowledge Graph
2. Knowledge Graph 在 BugSeek 中的位置
3. Graph 数据模型设计
4. Graph 节点模型
5. Graph 边关系模型
6. Graph 数据库设计
7. Graph 构建流程
8. Graph 更新机制
9. Graph 查询引擎
10. Graph 推理能力
11. Graph 与 AI 的结合
12. 实施步骤

下面开始。

---

# 一、为什么测试平台必须有 Knowledge Graph

传统测试平台数据结构是：

```
API
Test Case
Scenario
```

但 AI 测试需要的是：

```
UI → API → Service → Table → Field
```

举个例子：

```
CreateOrder API
    ↓
OrderService
    ↓
orders table
    ↓
order_status field
```

如果 AI 不知道这条链：

AI 就无法：

* 自动生成断言
* 自动生成测试
* 自动分析失败

所以必须建立：

```
System Knowledge Graph
```

---

# 二、BugSeek V2 中 Knowledge Graph 的位置

系统架构：

```
AI Testing
     │
     │
Knowledge Graph
     │
     │
API Hub + DB Schema
```

Graph 其实是：

```
系统结构的统一视图
```

---

# 三、Graph 数据模型设计

Graph 的核心只有两类对象：

```
Node
Edge
```

结构：

```
Node(id, type, properties)

Edge(source, target, relation, properties)
```

例如：

```
Node: API:createOrder
Node: TABLE:orders

Edge:
createOrder → writes → orders
```

---

# 四、Graph Node 设计

需要的 Node 类型：

```
API
SERVICE
TABLE
FIELD
UI_PAGE
TEST_CASE
SCENARIO
```

完整 Node Schema：

```
graph_nodes

id (uuid)
node_type
name
display_name
source_id
properties
created_at
updated_at
```

例：

```
node_type = API
name = createOrder
properties = {
    method: POST
    path: /api/orders
}
```

---

# 五、Graph Edge 设计

Edge 类型：

```
CALLS
IMPLEMENTS
READS
WRITES
CONTAINS
USES
TESTS
```

Schema：

```
graph_edges

id
source_node_id
target_node_id
relation_type
properties
```

例：

```
createOrder API → WRITES → orders table
```

---

# 六、Graph 数据库存储方案

有两个选择：

### 方案1（推荐）

使用 PostgreSQL。

优点：

* 简单
* 不引入新系统
* 已有 PG

表结构：

```
graph_nodes
graph_edges
```

加索引：

```
node_type index
relation_type index
```

---

### 方案2

使用 Neo4j。

优点：

Graph 查询非常强。

缺点：

运维复杂。

---

**BugSeek 建议先用 PostgreSQL。**

---

# 七、Graph 构建流程

Graph 的数据来自四个来源：

```
API文档
数据库Schema
代码分析
执行日志
```

流程：

```
OpenAPI Parser
       ↓
API Nodes

DB Schema Parser
       ↓
Table + Field Nodes

Field Mapping
       ↓
API → Field Edges

Execution Logs
       ↓
API → Table Impact
```

最终 Graph：

```
UI
 ↓
API
 ↓
Service
 ↓
Table
 ↓
Field
```

---

# 八、Graph Builder 模块

模块结构：

```
domains/knowledge_graph/

builder.py
entity_models.py
repository.py
graph_service.py
```

---

### Graph Builder

负责构建 Graph。

```
class GraphBuilder:

    def build_api_nodes(self):
        pass

    def build_table_nodes(self):
        pass

    def build_field_nodes(self):
        pass
```

---

# 九、Graph 更新机制

Graph 不是一次生成。

必须支持：

```
增量更新
```

触发条件：

```
API同步
DB Schema更新
字段映射更新
测试执行
```

例如：

```
API同步完成
→ 更新 API Node
```

---

# 十、Graph 查询引擎

Graph 查询用于 AI 推理。

结构：

```
query_engine.py
```

常见查询：

### API影响表

```
get_tables_written_by_api(api_id)
```

---

### 表对应API

```
get_apis_operating_on_table(table_name)
```

---

### API字段链

```
API → FIELD
```

---

# 十一、Graph 推理能力

Graph 不是简单存储。

它要支持：

```
推理
```

例如：

```
UI → API → Table
```

AI 可以自动生成：

```
UI测试 → DB断言
```

---

# 十二、Graph 与 AI 结合

AI 调用 Graph：

```
AI Prompt
    ↓
Graph Query
    ↓
Context Injection
    ↓
LLM
```

示例 Prompt：

```
API createOrder writes table orders.
Suggest assertions.
```

Graph 提供：

```
orders.status
orders.amount
orders.user_id
```

AI 才能生成断言。

---

# 十三、Graph 与 Field Mapping

你的系统已经有：

```
field_mapping
```

Graph 可以直接复用。

生成：

```
API Field → DB Field
```

关系：

```
MAPS_TO
```

---

# 十四、Graph API 设计

Graph API：

```
GET /graph/node/{id}

GET /graph/api/{id}/tables

GET /graph/table/{name}/apis
```

---

# 十五、Graph UI

前端建议新增：

```
/knowledge-graph
```

使用：

```
G6
```

展示：

```
API → TABLE → FIELD
```

---

# 十六、Graph 性能优化

关键索引：

```
node_type
relation_type
source_node_id
target_node_id
```

---

# 十七、Graph 构建任务

Celery 任务：

```
build_graph_from_api
build_graph_from_schema
update_graph_from_execution
```

---

# 十八、Graph 规模预估

典型系统：

```
API      500
TABLE    200
FIELD    3000
```

Edges：

```
10k+
```

PG 完全可以承受。

---

# 十九、阶段二实施步骤

步骤：

### Step1

创建 Graph Schema。

```
graph_nodes
graph_edges
```

---

### Step2

实现 Graph Builder。

---

### Step3

API 文档 → Graph。

---

### Step4

DB Schema → Graph。

---

### Step5

Field Mapping → Graph。

---

### Step6

Graph Query Engine。

---

# 二十、阶段二完成后的能力

系统就拥有：

```
系统结构理解能力
```

AI 可以：

```
理解 API
理解 DB
理解数据流
```

这是：

**AI 自动测试的核心基础。**

---

如果你愿意，我可以继续给你写 **阶段三：Data Impact Engine（数据库影响分析）完整工程方案**。

那一部分会真正让平台实现：

```
API执行 → 自动生成数据库断言
```

这是 **AI 自动化测试最关键的能力之一**。
