• 下面继续细化三个关键部分：评分规则细节 + 阈值建议 + UI 交互流程图（文字版）。

  ———

  # 一、评分规则细节（可解释评分）


  ## 1) 结构合法性（硬性过滤）

  - 字段不存在 → 直接淘汰

  ## 2) 评分因子（建议权重）

  | 字段名相似度 | user_id ↔ users.id | 0~0.30 |
  | 路径语义匹配 | /orders/{order_id} 指向 orders | 0~0.20 |
  | 全局字典/历史复用 | 项目字典中已有映射 | 0~0.20 |
  ### 评分例子
  字段 path.order_id，候选：
  - orders.id
      - 字段名匹配 0.30
      - 表名语义匹配 0.20
      - 路径语义匹配 0.20
      - 类型匹配 0.05
      - score=0.90
  - order_items.order_id
      - 字段名匹配 0.25
      - 路径语义匹配 0.05
      - 类型匹配 0.05
      - score=0.60

  ———

  # 二、阈值建议（自动/人工分界）

  ### 推荐阈值

  - 0.60 ~ 0.85 → 建议人工确认

  ### 冲突处理

  - 如果最高候选与次高候选分差 < 0.10
    → 强制人工确认（避免误选）

  ———

  # 三、UI 交互流程图（文字版）
  ### 入口流程
     ↓
  点击「生成建议」
     ↓
  后端返回建议列表
  列表展示：高置信度/待确认/无建议
  ———

  ### 确认流程

  查看候选详情（含评分与理由）
     ↓
  选择最优候选 → 确认
     ↓
  保存映射（status=confirmed）

  ———

  ### 批量确认流程

  点击「批量确认」
     ↓
  弹窗选择：
     ↓


  ### 驳回流程
     ↓
  标记 status=rejected
     ↓

  ———

  # 四、算法校验逻辑示例（伪代码）
      score += name_similarity(api_field, db_column) * 0.30
      score += table_semantic_match(api_field, db_table) * 0.25
      if api_path_contains_resource():
          score += 0.20
          reasons.append("path匹配")
      if is_primary_key(db_column):
          score += 0.15
          reasons.append("主键优先")
      if type_compatible(api_type, db_type):
          score += 0.10
          reasons.append("类型匹配")
      if in_project_dictionary(api_field):
          score += 0.20
          reasons.append("历史映射复用")

  ———

  # 五、推荐落地顺序（最小成本）

  1. 先实现规则打分和建议列表 UI
  2. 再实现批量确认/驳回
  3. 最后引入 AI 生成候选
