1. 无鉴权 (No Auth)
收起下方所有的字段
2. Basic Auth
原理： 它是 HTTP 协议最古老的鉴权方式。将 用户名:密码 组合后进行 Base64 编码，放在 Header 的 Authorization 字段中。
操作： 输入 Username 和 Password。Postman 会自动生成 Header：Authorization: Basic dXNlcm5hbWU6cGFzc3dvcmQ=。
3. Bearer Token
原理： 只要你持有这个“票据（Token）”，你就有权访问。它是 OAuth2 和 JWT（JSON Web Token）最常用的承载方式。
操作： 只需要输入一个字符串（Token）。Postman 会生成 Header：Authorization: Bearer <Your_Token>。
实际例子（现代 ERP 标配）：
JWT 鉴权： 你通过登录接口获得一串很长的字符（JWT），里面包含了你的用户 ID、过期时间等加密信息。之后请求“创建入库单”接口时，就在 Header 里带上这个 Bearer Token。
移动端 App： App 登录后，本地持久化存储这个 Token，每次调用 API 时自动带上。
4. API Key
保持现有逻辑
5. Session / Cookie
原理： 主要用于浏览器环境。用户登录成功后，服务器在内存或 Redis 中存储 Session 信息，并通过 Header Set-Cookie 将 SessionID 发给浏览器。浏览器之后每次请求都会自动带上这个 Cookie。
操作： 在 Postman 中，这通常是自动处理的（如果你通过接口登录成功，Postman 会自动存下 Cookie 并在下次请求时带上）。
实际例子：
传统的 ERP 网页版： 比如早期的 Odoo 或 SAP 网页后台。只要你浏览器没关，你刷新页面不需要重新登录，就是因为 Cookie 中保存了 session_id。
安全特性： 容易受到 CSRF（跨站请求伪造）攻击，所以通常需要配合 CSRF Token 使用（这可能就是你最初遇到 403 的原因）。