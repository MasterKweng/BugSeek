可行，而且应该采用“AI 生成 + 规则/算法校验 + 人工确认”的混合方案，避免 644 个接口的人工成本与误判风险。下面给出完整可落地的方案设计（不改代码，仅
  设计）：

  ———

  ## 一、总体目标

  - AI 负责提出候选映射（API 字段路径 → 表/字段）

  ———
  ## 二、输入与依赖

  必需数据：

  1. API 定义（method、path、request_schema）
  2. 当前版本的数据库结构（schema_snapshot）
  3. 现有映射字典（项目/版本已有 field mappings）
  4. 字段命名规则/词典（如 user/order/tenant 等)

  可选增强：

  - FK 约束或注释（如果 schema_snapshot 内含）
  - 业务字段字典（全局映射模板）

  ———

  ### 1) 发现“待映射字段”

  - path 中的 {xxx}
  - request_body 中的 *_id, id, xxxId
  - query 中的 *_id
  → 形成“待映射变量列表”（如 order_id, user_id）

  ———

  AI 看到：

  - 端点语义：POST /orders/{order_id}/items
  - 字段名：order_id
  - schema 中表/字段：orders.id, order_items.order_id, orders.order_id...

  输出多个候选并给出解释+置信度：

  order_id -> orders.id (0.86)  // 路径语义匹配
  ———

  ### 3) 规则/算法校验与打分（关键）

  对 AI 候选做硬性过滤与打分：

  硬性校验（不通过即淘汰）

  - 表是否存在
  - 字段是否存在
  - API/路径字段名与 DB 字段不相关（完全不匹配）

  打分因子（加权）
  - 路径语义（/orders/{order_id}  → orders.id）
  - 主键优先（优先 id 或主键字段）
  - 外键兼容（order_id 更倾向 FK 字段）
  - 已有字典优先（全局模板）
  - 历史映射复用（同名字段在其他接口已确认）

  最终得分：0~1，区间划分：

  - <0.60：不建议映射

  ———

  ### 4) 冲突处理

  当一个字段出现多个候选：

  - 选择最高分
  - 若差值 < 0.1 → 标记冲突，要求人工确认

  ### 5) 生成结果落库（含状态）
  映射记录新增字段建议：
  - source: ai
  - confidence: 0.86
  - status: proposed/confirmed/rejected

  ———

  ## 四、输出与落库策略

  ### 1) 自动落库（可选）


  ### 2) 人工审核

  前端提供：

  - 批量通过/驳回

  ———

  ## 五、接口与模块建议（与现有结构一致）

  ### 后端建议新增 API

  1. 生成建议

  POST /field-mappings/suggest?project_id=&version_id=
  2. 批量应用

  POST /field-mappings/batch-apply
  body: { items: [{...}], mode: confirm|propose }
  3. 审核状态更新

  PUT /field-mappings/{id}/status



  - 入口放在「版本中心-字段映射」页
  - 展示：
      - API 字段路径
      - 推荐表/字段
      - 置信度
      - 冲突候选列表

  ———
  ## 七、与现有 pre_sql 生成的衔接
  已有逻辑是：
  - 如果用例有 {{order_id}}
  - 系统根据映射生成 pre_sql

  所以只要映射质量高，pre_sql 就自动可靠。

  ———

  ## 八、风险与约束

  - 误匹配可能带来错误数据 → 必须保留人工确认层
  - 结构不完整（缺 PK/FK）会降低准确率
  - 多表同名字段冲突要强制人工确认

  ———

  ## 九、落地阶段建议

  阶段1：最小可用

  - AI 生成候选 + 规则校验 + 人工确认页面
  - 不做自动落库

  阶段2：半自动化

  - ≥0.85 自动写入，其他待审
  - 加批量导入/导出

  阶段3：智能提升

  - 引入历史映射学习
  - 结合实际执行数据修正置信度

  ———

  ### 结论

  AI 生成 + 算法校验方案完全可行，且是目前唯一能在大规模接口场景下把人工成本降到可控的方案。
  如果你认可，我可以把该设计拆成“接口清单 + UI 原型 + 数据结构建议”。