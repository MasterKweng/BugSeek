# 接口与集成模块（API Integration Hub）完整功能设计

## 一、模块概述

**功能定位**：后端服务验证，把控 Gate 2
**核心价值**：从文档到测试全流程自动化，支持 CI/CD 集成与精准回归

## 二、核心功能设计

### 2.1 文档导入与解析

**功能描述**：支持多种接口文档格式的导入与解析

**子功能**：
- Swagger/OpenAPI 文档导入（URL 或文件）
- YApi 文档导入
- Postman Collection 导入
- 文档版本管理
- 文档变更检测

**数据模型**：
```python
api_documents
- id, project_id, name, source_type, source_url, content, version, created_at, updated_at
```

**核心接口**：
```
POST   /api/v1/api-integration/documents/import
GET    /api/v1/api-integration/documents
GET    /api/v1/api-integration/documents/{id}
DELETE /api/v1/api-integration/documents/{id}
```



### 2.2 接口定义管理

**功能描述**：管理解析后的接口定义

**子功能**：
- 接口列表展示
- 接口详情查看
- 接口搜索与过滤
- 接口标签管理
- 接口分组管理

**数据模型**：
```python
api_endpoints
- id, project_id, document_id, path, method, summary, description, request_schema, response_schema, tags, group_id
```

**核心接口**：
```
GET    /api/v1/api-integration/endpoints
GET    /api/v1/api-integration/endpoints/{id}
PUT    /api/v1/api-integration/endpoints/{id}
GET    /api/v1/api-integration/endpoints/search
```

---

### 2.3 测试脚本自动生成

**功能描述**：基于接口定义自动生成测试脚本

**子功能**：
- 单接口测试脚本生成
- 参数化测试生成（边界值、异常值）
- 断言逻辑自动生成
- 脚本模板自定义
- 脚本预览与编辑

**生成策略**：
- 正向测试：正常参数、必填参数
- 逆向测试：缺失必填参数、错误类型参数
- 边界测试：最大值、最小值、空值
- 异常测试：超长字符串、特殊字符

**数据模型**：
```python
api_test_scripts
- id, project_id, endpoint_id, name, script_content, test_type, generated_at
```

**核心接口**：
```
POST   /api/v1/api-integration/scripts/generate
GET    /api/v1/api-integration/scripts
GET    /api/v1/api-integration/scripts/{id}
PUT    /api/v1/api-integration/scripts/{id}
DELETE /api/v1/api-integration/scripts/{id}
```

---

### 2.4 智能场景组装

**功能描述**：AI 分析接口依赖关系，自动生成业务链路测试

**子功能**：
- 接口依赖关系分析
- 依赖图可视化展示
- 业务链路自动识别
- 链路测试脚本生成
- 变量传递自动处理

**依赖分析算法**：
1. 解析接口请求/响应结构
2. 识别参数依赖关系（A 的输出 → B 的输入）
3. 构建有向依赖图（networkx）
4. 拓扑排序确定执行顺序
5. 生成链路测试脚本

**数据模型**：
```python
api_dependencies
- id, project_id, source_endpoint_id, target_endpoint_id, mapping_rule, dependency_type

api_scenarios
- id, project_id, name, description, endpoint_ids, execution_order, variables
```

**核心接口**：
```
POST   /api/v1/api-integration/scenarios/analyze
GET    /api/v1/api-integration/scenarios
POST   /api/v1/api-integration/scenarios/generate
GET    /api/v1/api-integration/scenarios/{id}/graph
```

---

### 2.5 Mock 服务管理

**功能描述**：根据接口定义自动生成 Mock 服务

**子功能**：
- Mock 服务自动生成
- Mock 规则自定义
- Mock 数据持久化
- Mock 服务启停控制
- Mock 调用日志查看

**Mock 策略**：
- 静态 Mock：返回固定数据
- 动态 Mock：根据请求参数动态生成
- 随机 Mock：返回随机数据（基于类型）
- 智能 Mock：基于示例数据智能生成

**数据模型**：
```python
mock_services
- id, project_id, endpoint_id, mock_url, mock_response, mock_rules, status, created_at

mock_logs
- id, mock_service_id, request_data, response_data, timestamp
```

**核心接口**：
```
POST   /api/v1/api-integration/mock-services/create
GET    /api/v1/api-integration/mock-services
PUT    /api/v1/api-integration/mock-services/{id}
DELETE /api/v1/api-integration/mock-services/{id}
POST   /api/v1/api-integration/mock-services/{id}/start
POST   /api/v1/api-integration/mock-services/{id}/stop
GET    /api/v1/api-integration/mock-services/{id}/logs
```

---

### 2.6 测试套件管理

**功能描述**：将多个测试用例组成测试套件，支持全流程回归

**子功能**：
- 测试套件创建与编辑
- 测试用例批量添加
- 执行顺序配置（串行/并行）
- 套件标签管理
- 套件版本管理

**套件类型**：
- 烟烟测试套件：核心接口快速验证
- 回归测试套件：全量接口回归
- 场景测试套件：业务链路测试
- CI/CD 测试套件：流水线集成

**数据模型**：
```python
test_suites
- id, project_id, name, description, test_case_ids, execution_order, tags, version, created_at
```

**核心接口**：
```
POST   /api/v1/api-integration/test-suites
GET    /api/v1/api-integration/test-suites
GET    /api/v1/api-integration/test-suites/{id}
PUT    /api/v1/api-integration/test-suites/{id}
DELETE /api/v1/api-integration/test-suites/{id}
```

---

### 2.7 测试执行引擎

**功能描述**：执行接口测试，支持单接口、场景、套件执行

**子功能**：
- 单接口测试执行
- 场景链路测试执行
- 套件批量执行
- 并行执行控制
- 执行超时控制

**执行模式**：
- 同步执行：立即返回结果
- 异步执行：返回执行 ID，支持轮询
- 定时执行：按计划自动执行

**数据模型**：
```python
test_executions
- id, project_id, suite_id, scenario_id, script_id, status, started_at, finished_at, duration, total, passed, failed, skipped

test_results
- id, execution_id, test_case_id, status, response_time, response_data, error_message, assertion_results
```

**核心接口**：
```
POST   /api/v1/api-integration/executions/single
POST   /api/v1/api-integration/executions/scenario
POST   /api/v1/api-integration/executions/suite
GET    /api/v1/api-integration/executions/{id}
GET    /api/v1/api-integration/executions/{id}/results
```

---

### 2.8 测试报告生成

**功能描述**：生成多种格式的测试报告

**子功能**：
- JSON 格式报告
- JUnit XML 格式报告
- HTML 可视化报告
- Allure 报告
- 自定义报告模板

**报告内容**：
- 测试概览（总数、通过率、失败数）
- 失败用例详情
- 响应时间统计
- 接口覆盖率
- 历史趋势对比

**核心接口**：
```
GET    /api/v1/api-integration/executions/{id}/report?format=json
GET    /api/v1/api-integration/executions/{id}/report?format=junit
GET    /api/v1/api-integration/executions/{id}/report?format=html
GET    /api/v1/api-integration/executions/{id}/report?format=allure
```

---

## 三、与模块 5 的交互接口（冗余设计）

### 3.1 CI/CD 批量执行接口

```
POST /api/v1/api-integration/test-suites/{suite_id}/execute
```

**用途**：模块 5 触发全流程接口回归测试

**请求参数**：
```json
{
  "trigger_type": "ci_cd",
  "pipeline_id": "jenkins-123",
  "environment": "staging",
  "parallel": true,
  "timeout": 300
}
```

**响应**：
```json
{
  "execution_id": "exec-20240121-001",
  "status": "running",
  "test_count": 50,
  "estimated_duration": 120
}
```

---

### 3.2 执行状态查询接口

```
GET /api/v1/api-integration/executions/{execution_id}/status
```

**用途**：模块 5 轮询测试执行状态

**响应**：
```json
{
  "execution_id": "exec-20240121-001",
  "status": "completed",
  "progress": {
    "total": 50,
    "passed": 45,
    "failed": 5,
    "skipped": 0
  },
  "started_at": "2024-01-21T10:00:00Z",
  "finished_at": "2024-01-21T10:02:30Z",
  "duration": 150
}
```

---

### 3.3 测试结果报告接口（CI/CD 专用）

```
GET /api/v1/api-integration/executions/{execution_id}/report
```

**用途**：模块 5 获取标准测试报告

**查询参数**：
- `format`: `json` | `junit` | `html` | `allure`

---

### 3.4 质量门禁决策接口

```
POST /api/v1/api-integration/executions/{execution_id}/gate-decision
```

**用途**：模块 5 根据测试结果决策是否放行

**请求参数**：
```json
{
  "gate_rules": {
    "pass_rate_threshold": 95,
    "critical_failure_threshold": 0,
    "coverage_threshold": 80
  }
}
```

**响应**：
```json
{
  "decision": "block",
  "score": 90,
  "reasons": [
    "通过率 90% 低于阈值 95%",
    "存在 2 个严重失败"
  ],
  "recommendation": "修复失败用例后重新执行"
}
```

---

### 3.5 TIA 影响分析接口

```
POST /api/v1/api-integration/tia/analyze
```

**用途**：模块 5 分析代码变更影响的接口测试用例

**请求参数**：
```json
{
  "git_diff": {
    "commit_hash": "abc123",
    "changed_files": [
      "backend/api/user.py",
      "backend/api/order.py"
    ]
  }
}
```

**响应**：
```json
{
  "affected_test_cases": [
    {
      "test_case_id": "tc-001",
      "name": "POST /api/users",
      "priority": "high",
      "reason": "直接依赖 user.py"
    }
  ],
  "total_affected": 12,
  "recommended_execution": true
}
```

---

### 3.6 代码-用例映射管理

```
POST   /api/v1/api-integration/tia/mappings
GET    /api/v1/api-integration/tia/mappings
DELETE /api/v1/api-integration/tia/mappings/{id}
```

**用途**：模块 5 管理代码与测试用例的映射关系

**数据模型**：
```python
tia_mappings
- id, project_id, file_path, function_name, test_case_ids, dependency_type, created_at
```

---

### 3.7 CI/CD 配置接口

```
POST   /api/v1/api-integration/ci-cd/configs
GET    /api/v1/api-integration/ci-cd/configs
PUT    /api/v1/api-integration/ci-cd/configs/{id}
```

**用途**：模块 5 配置 CI/CD 系统集成

**请求参数**：
```json
{
  "platform": "jenkins",
  "webhook_url": "https://jenkins.example.com/webhook",
  "secret_token": "xxx",
  "auto_trigger": true,
  "trigger_events": ["push", "pull_request"]
}
```

---

### 3.8 Webhook 回调接口

```
POST /api/v1/api-integration/webhooks/{platform}/callback
```

**用途**：接收模块 5 的 Webhook 回调

**请求参数**（Jenkins）：
```json
{
  "build": {
    "number": 123,
    "status": "SUCCESS",
    "url": "https://jenkins.example.com/job/123"
  },
  "project": {
    "name": "bugseek-project"
  }
}
```

---

## 四、数据模型汇总

```python
# 文档管理
api_documents
- id, project_id, name, source_type, source_url, content, version, created_at, updated_at

# 接口定义
api_endpoints
- id, project_id, document_id, path, method, summary, description, request_schema, response_schema, tags, group_id

# 测试脚本
api_test_scripts
- id, project_id, endpoint_id, name, script_content, test_type, generated_at

# 依赖关系
api_dependencies
- id, project_id, source_endpoint_id, target_endpoint_id, mapping_rule, dependency_type

# 业务场景
api_scenarios
- id, project_id, name, description, endpoint_ids, execution_order, variables

# Mock 服务
mock_services
- id, project_id, endpoint_id, mock_url, mock_response, mock_rules, status, created_at

# Mock 日志
mock_logs
- id, mock_service_id, request_data, response_data, timestamp

# 测试套件
test_suites
- id, project_id, name, description, test_case_ids, execution_order, tags, version, created_at

# 测试执行
test_executions
- id, project_id, suite_id, scenario_id, script_id, status, started_at, finished_at, duration, total, passed, failed, skipped

# 测试结果
test_results
- id, execution_id, test_case_id, status, response_time, response_data, error_message, assertion_results

# TIA 映射
tia_mappings
- id, project_id, file_path, function_name, test_case_ids, dependency_type, created_at

# CI/CD 配置
ci_cd_configs
- id, project_id, platform, webhook_url, secret_token, auto_trigger, trigger_events
```

---

## 五、核心流程

### 5.1 文档到测试流程
```
导入文档 → 解析接口 → 生成脚本 → 创建套件 → 执行测试 → 生成报告
```

### 5.2 CI/CD 集成流程
```
代码变更 → TIA 分析 → 触发回归 → 轮询状态 → 获取报告 → 门禁决策
```

### 5.3 依赖分析流程
```
接口定义 → 解析结构 → 识别依赖 → 构建图 → 拓扑排序 → 生成链路
```

---

## 六、技术栈

| 类型 | 技术选型 | 说明 |
|------|---------|------|
| 文档解析 | prance | OpenAPI 规范解析 |
| HTTP 客户端 | httpx | 异步 HTTP 请求 |
| 依赖分析 | networkx | 图算法 |
| Mock 服务 | FastAPI | 动态路由 |
| 脚本生成 | jinja2 | 模板引擎 |
| 测试框架 | pytest | 测试执行 |
| 报告生成 | allure-pytest | Allure 报告 |

---

## 七、核心价值

1. **效率提升**：从文档到测试全自动，效率提升 50-100 倍
2. **测试全面**：智能场景组装，覆盖业务链路，覆盖率提升 30-50%
3. **开发解耦**：Mock 服务支持前后端并行开发，联调时间减少 50-70%
4. **CI/CD 集成**：完整的 CI/CD 接口，支持精准回归和质量门禁
5. **易于维护**：代码-用例映射，自动维护，降低维护成本