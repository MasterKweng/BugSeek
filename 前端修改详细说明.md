# 前端修改详细说明 - 阶段三

## 问题说明

由于工具限制，无法直接修改前端代码。以下是详细的修改说明，请手动应用这些修改。

## 文件位置

`frontend/src/pages/FieldMappingSuggestions.tsx`

## 修改 1：添加分页状态

### 位置
在第 68 行附近，`includeBody` 状态声明之后。

### 原代码
```typescript
const [includeBody, setIncludeBody] = useState(true);

// ==================== 异步任务相关状态 ====================
const [taskModalVisible, setTaskModalVisible] = useState(false);
```

### 修改后
```typescript
const [includeBody, setIncludeBody] = useState(true);

// ==================== 新增：分页状态 ====================
const [pagination, setPagination] = useState({
  current: 1,
  pageSize: 20,
  total: 0
});

// ==================== 异步任务相关状态 ====================
const [taskModalVisible, setTaskModalVisible] = useState(false);
```

---

## 修改 2：修改 loadTaskResults 函数

### 位置
在第 282 行附近。

### 原代码
```typescript
// 加载任务结果（使用缓存优化）
const loadTaskResults = async (taskId: number) => {
  try {
    // 使用带缓存的服务函数
    const suggestions = await fieldMappingService.getFieldMappingSuggestionsCached(taskId);
    setSuggestions(suggestions || []);
  } catch (error: any) {
    console.error('加载任务结果失败:', error);
    message.error(error.message || '加载结果失败');
  }
};
```

### 修改后
```typescript
// 加载任务结果（使用缓存优化 + 服务器分页）
const loadTaskResults = async (taskId: number, page: number = 1, size: number = 20) => {
  try {
    const response = await fieldMappingService.getFieldMappingSuggestions(taskId, {
      page,
      page_size: size
    });
    
    if (response.code === 0 && response.data) {
      setSuggestions(response.data.items || []);
      setPagination({
        current: page,
        pageSize: size,
        total: response.data.total || 0
      });
    }
  } catch (error: any) {
    console.error('加载任务结果失败:', error);
    message.error(error.message || '加载结果失败');
  }
};
```

---

## 修改 3：修改 Table 组件

### 位置
搜索 `Table` 组件，找到显示建议列表的 Table。

### 原代码（可能的样子）
```typescript
<Table
  dataSource={suggestions}
  rowKey="definition_id"
  // ... 其他 props
/>
```

### 修改后
```typescript
<Table
  dataSource={suggestions}
  rowKey="definition_id"
  pagination={{
    current: pagination.current,
    pageSize: pagination.pageSize,
    total: pagination.total,
    showSizeChanger: true,
    showTotal: (total) => `共 ${total} 条`,
    onChange: (page, pageSize) => {
      loadTaskResults(taskId!, page, pageSize);
    },
    onShowSizeChange: (current, size) => {
      loadTaskResults(taskId!, 1, size);
    }
  }}
  // ... 其他 props
/>
```

---

## 修改 4：确保 services/fieldMapping.ts 中的类型定义正确

### 位置
`frontend/src/services/fieldMapping.ts`

### 需要确保的函数签名
```typescript
export const getFieldMappingSuggestions = async (
  taskId: number,
  params?: { page?: number; page_size?: number }
): Promise<ApiResponse<FieldMappingSuggestionResponse>> => {
  return api.get('/field-mappings/suggestions', { 
    params: { task_id: taskId, ...params } 
  })
}
```

---

## 验证步骤

1. 保存所有修改的文件
2. 重启前端开发服务器（如果正在运行）
3. 打开字段映射页面
4. 创建一个新的任务或查看已完成的任务
5. 检查：
   - 列表是否显示数据
   - 分页控件是否出现
   - 总数据量是否正确
   - 切换页码是否正常
   - 修改每页显示数量是否正常

---

## 预期效果

修改完成后，前端应该：
1. 只加载当前页的数据（20 条）
2. 显示正确的分页控件
3. 显示总数据量
4. 支持切换页码
5. 支持修改每页显示数量

---

## 常见问题

### Q1: 如果列表仍然为空？
A1: 检查后端是否正确返回数据，使用浏览器开发者工具查看网络请求。

### Q2: 如果分页不工作？
A2: 确保 `taskId` 有值，并且 `loadTaskResults` 函数被正确调用。

### Q3: 如果缓存导致问题？
A3: 清除浏览器缓存或使用无痕模式测试。

---

## 回滚方案

如果出现问题，可以：
1. 回退到修改前的代码
2. 暂时使用客户端分页（去掉 page 参数，一次性加载所有数据）