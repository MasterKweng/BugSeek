# 层级一：API 资产库 (API Hub) 详细设计方案

## 一、定位与核心理念

### 1.1 定位
**API 资产库**是整个测试平台的"真理来源"（Single Source of Truth），是所有测试活动的基础数据源。

### 1.2 核心变革
- **废弃独立的"测试脚本"模块**：将测试用例作为 API 的衍生属性，直接挂载在接口定义下
- **消除冗余维护成本**：不再维护独立的脚本文件，测试用例与接口定义同生命周期
- **AI 赋能**：在断言生成、变更修复、智能同步等环节引入 AI 能力

### 1.3 设计原则
- **资产归一**：所有接口定义和测试用例统一管理
- **版本可追溯**：记录接口变更历史，支持 Diff 对比
- **自动化优先**：AI 自动生成基准用例，减少手工配置
- **智能化修复**：接口变更时自动检测影响范围并尝试修复

---

## 二、功能架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    API 资产库 (API Hub)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  功能 1：智能同步与版本 Diff (Smart Sync)                │   │
│  │  ├─ 多源导入 (Postman/Swagger/YApi)                     │   │
│  │  ├─ AI 变更检测 (Diff 计算)                              │   │
│  │  ├─ 变更预警 (新增/变更/删除)                            │   │
│  │  ├─ 影响分析 (扫描关联用例和场景)                        │   │
│  │  └─ AI 自动修复 (字段改名自动映射)                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  功能 2：接口定义管理 (API Definition)                   │   │
│  │  ├─ 接口列表 (分页、筛选、搜索)                          │   │
│  │  ├─ 接口详情 (方法、路径、参数、响应)                    │   │
│  │  ├─ 在线调试 (请求构造、响应查看)                        │   │
│  │  ├─ Mock 服务 (自动生成 Mock 接口)                       │   │
│  │  └─ 版本快照 (存储原始 Schema)                           │   │
│  └─────────────────────────────────────────────────────────┘   │
│                          ↓                                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  功能 3：原子用例管理 (Atomic Case Management)           │   │
│  │  ├─ 基准用例 (AI 自动生成)                              │   │
│  │  ├─ 业务用例 (用户手动创建)                              │   │
│  │  ├─ 入参覆写 (参数化、变量引用)                          │   │
│  │  ├─ 后置操作 (提取、断言、SQL)                           │   │
│  │  └─ 用例执行 (单用例执行、批量执行)                      │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 三、功能详细设计

### 3.1 功能 1：智能同步与版本 Diff (Smart Sync)

#### 3.1.1 设计思路
解决"文档与代码不一致"的痛点，通过 AI 智能对比接口变更，自动预警和修复影响范围。

#### 3.1.2 交互流程

**步骤 1：创建同步任务**
1. 用户进入 API 资产库页面
2. 点击"新建同步"按钮
3. 填写同步配置：
   - 项目选择
   - 文档来源（Postman/Swagger/YApi）
   - 文档 URL 或文件上传
   - 版本标识（如 v1.2.3）
   - 同步描述
4. 提交任务，系统返回任务 ID

**步骤 2：AI 智能对比**
1. 系统异步解析文档，提取接口定义
2. AI 对比库中现有接口，计算 Diff：
   - 对比接口路径和方法
   - 对比请求参数（字段名、类型、必填性）
   - 对比响应结构（字段名、类型、嵌套关系）
   - 生成版本哈希值用于快速比对

**步骤 3：变更预警**
系统识别三类变更并标记：

- **新增接口**（标记为 `New`）
  - 显示新增的接口列表
  - AI 自动生成基准用例
  - 提示用户是否需要创建业务用例

- **变更接口**（标记为 `Changed`）
  - 显示变更详情：
    - 新增字段：`+ field_name`
    - 删除字段：`- field_name`
    - 修改字段：`~ field_name (old_type → new_type)`
  - AI 自动扫描影响范围：
    - 查找引用了删除字段的用例
    - 查找引用了删除字段的场景
    - 标记为"需修复"状态

- **删除接口**（标记为 `Deleted`）
  - 显示已删除的接口列表
  - 提示关联的用例和场景
  - 建议用户确认是否删除

**步骤 4：AI 自动修复**
对于简单的变更，AI 尝试自动修复：

- **字段改名**（如 `userId` → `user_id`）
  - AI 识别语义相似性
  - 自动更新所有关联用例的映射关系
  - 自动更新所有关联场景的变量引用
  - 生成修复报告

- **类型变更**（如 `string` → `integer`）
  - AI 检查用例中的参数值是否兼容
  - 自动转换兼容的值
  - 标记不兼容的用例供人工确认

**步骤 5：变更确认**
1. 用户查看变更报告
2. 逐项确认变更：
   - 接受变更并应用修复
   - 拒绝变更（保留旧版本）
   - 手动调整修复方案
3. 提交确认，系统更新数据库

#### 3.1.3 界面设计

**同步任务列表**
```
┌─────────────────────────────────────────────────────────────────┐
│  智能同步                                                         │
├─────────────────────────────────────────────────────────────────┤
│  [+ 新建同步]                                                     │
├─────────────────────────────────────────────────────────────────┤
│  来源        │ 版本    │ 状态    │ 变更数  │ 时间        │ 操作   │
│  Swagger     │ v1.2.3  │ ✅ 完成 │ 15     │ 2026-02-03  │ [查看] │
│  Postman     │ v1.2.2  │ ⚠️ 变更 │ 8      │ 2026-02-01  │ [查看] │
│  YApi        │ v1.2.1  │ ⏳ 进行中│ -      │ 2026-02-03  │ [查看] │
└─────────────────────────────────────────────────────────────────┘
```

**变更详情弹窗**
```
┌─────────────────────────────────────────────────────────────────┐
│  变更详情 - Swagger v1.2.3                                      │
├─────────────────────────────────────────────────────────────────┤
│  变更统计：新增 5 个，变更 8 个，删除 2 个                        │
├─────────────────────────────────────────────────────────────────┤
│  [新增接口] [变更接口] [删除接口]                                 │
│                                                                  │
│  变更接口列表：                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ POST /api/v1/order/create                                 │ │
│  │ 状态：⚠️ 变更                                              │ │
│  │ 变更内容：                                                  │ │
│  │   + response.data.orderStatus (新增字段)                   │ │
│  │   ~ request.productId (string → integer)                   │ │
│  │ 影响范围：                                                  │ │
│  │   - 用例：正常下单 (需修复)                                │ │
│  │   - 场景：核心下单流程 (需修复)                            │ │
│  │ AI 建议：自动修复类型变更                                   │ │
│  │ [接受] [手动修复] [忽略]                                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  [应用所有变更] [取消]                                           │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.1.4 AI 能力设计

**Diff 计算算法**
```python
# 伪代码
def calculate_api_diff(old_schema, new_schema):
    diff = {
        'added': [],
        'changed': [],
        'removed': []
    }
    
    # 对比路径和方法
    if old_schema.path != new_schema.path:
        diff['changed'].append({
            'field': 'path',
            'old': old_schema.path,
            'new': new_schema.path
        })
    
    # 对比请求参数
    old_params = extract_fields(old_schema.request_schema)
    new_params = extract_fields(new_schema.request_schema)
    
    for field in new_params - old_params:
        diff['added'].append({'field': field, 'location': 'request'})
    
    for field in old_params - new_params:
        diff['removed'].append({'field': field, 'location': 'request'})
    
    for field in old_params & new_params:
        if old_params[field] != new_params[field]:
            diff['changed'].append({
                'field': field,
                'location': 'request',
                'old': old_params[field],
                'new': new_params[field]
            })
    
    # 对比响应结构（类似逻辑）
    # ...
    
    return diff
```

**影响范围扫描**
```python
# 伪代码
def scan_impact(api_definition_id, changed_fields):
    impact = {
        'affected_cases': [],
        'affected_scenarios': []
    }
    
    # 扫描用例
    cases = query_cases_by_api(api_definition_id)
    for case in cases:
        for field in changed_fields:
            if field_in_case(case, field):
                impact['affected_cases'].append({
                    'case_id': case.id,
                    'case_name': case.name,
                    'affected_field': field
                })
    
    # 扫描场景
    scenarios = query_scenarios_by_api(api_definition_id)
    for scenario in scenarios:
        for field in changed_fields:
            if field_in_scenario(scenario, field):
                impact['affected_scenarios'].append({
                    'scenario_id': scenario.id,
                    'scenario_name': scenario.name,
                    'affected_field': field
                })
    
    return impact
```

**AI 自动修复**
```python
# 伪代码
def auto_fix_renamed_field(old_name, new_name, affected_items):
    fixed_items = []
    
    for item in affected_items:
        # 替换用例中的字段引用
        if item.type == 'case':
            new_case_data = replace_field_in_case(item.data, old_name, new_name)
            fixed_items.append({
                'item_id': item.id,
                'item_type': 'case',
                'old_data': item.data,
                'new_data': new_case_data,
                'confidence': 0.95
            })
        
        # 替换场景中的变量引用
        elif item.type == 'scenario':
            new_scenario_data = replace_field_in_scenario(item.data, old_name, new_name)
            fixed_items.append({
                'item_id': item.id,
                'item_type': 'scenario',
                'old_data': item.data,
                'new_data': new_scenario_data,
                'confidence': 0.90
            })
    
    return fixed_items
```

---

### 3.2 功能 2：接口定义管理 (API Definition)

#### 3.2.1 设计思路
提供完整的接口定义管理功能，支持查看、编辑、调试、Mock 等操作。

#### 3.2.2 交互流程

**步骤 1：查看接口列表**
1. 进入 API 资产库页面
2. 查看接口列表：
   - 显示接口基本信息（方法、路径、分组）
   - 显示接口状态（正常/废弃/变更）
   - 显示关联用例数量
3. 使用筛选和搜索：
   - 按分组筛选
   - 按方法筛选（GET/POST/PUT/DELETE等）
   - 按状态筛选
   - 按关键词搜索（路径、描述）

**步骤 2：查看接口详情**
1. 在列表中点击接口
2. 进入接口详情页面：
   - **基本信息**：方法、路径、描述、标签、状态
   - **请求参数**：
     - Path 参数（表格展示：参数名、类型、必填、描述）
     - Query 参数（同上）
     - Header 参数（同上）
     - Body 参数（JSON Schema 预览，支持折叠展开）
   - **响应定义**：
     - 状态码列表（200/400/500等）
     - 响应头
     - 响应体（JSON Schema 预览，支持折叠展开）
   - **版本快照**：
     - 显示当前版本哈希
     - 查看历史版本
     - 对比版本差异

**步骤 3：在线调试**
1. 在接口详情页点击"在线调试"
2. 填写测试参数：
   - 选择测试环境（开发/测试/预发布/生产）
   - 填写 Path 参数
   - 填写 Query 参数
   - 填写 Header 参数
   - 填写 Body 参数（支持 JSON 格式化，带语法高亮）
3. 发送请求：
   - 点击"发送"按钮
   - 显示请求进度（Loading 动画）
4. 查看响应结果：
   - 响应状态码（带颜色标识：2xx 绿色，4xx 橙色，5xx 红色）
   - 响应时间（毫秒）
   - 响应头（表格展示）
   - 响应体（JSON 格式化高亮）
5. 保存调试结果：
   - 保存为新的用例（点击"保存为用例"）
   - 导出响应数据（JSON/CSV）

**步骤 4：Mock 服务**
1. 在接口详情页查看 Mock 地址
2. 复制 Mock 地址到剪贴板
3. 调用 Mock 接口：
   - 使用 HTTP 客户端（如 Postman）调用 Mock 地址
   - 获取随机生成的 Mock 数据
4. 自定义 Mock 数据：
   - 点击"编辑 Mock 数据"
   - 使用 Mock.js 语法编写模板：
     ```javascript
     {
       "id|+1": 1,
       "name": "@cname",
       "age|18-60": 20,
       "email": "@email",
       "created_at": "@datetime"
     }
     ```
   - 保存 Mock 模板
5. Mock 规则配置：
   - 配置不同状态码的返回数据
   - 配置延迟响应时间（模拟网络延迟）
   - 配置错误率（模拟接口不稳定）

#### 3.2.3 界面设计

**接口列表**
```
┌─────────────────────────────────────────────────────────────────┐
│  接口列表                                                         │
├─────────────────────────────────────────────────────────────────┤
│  [筛选: 全部分组 ▼] [筛选: 全部方法 ▼] [搜索: 请输入关键词...]  │
├─────────────────────────────────────────────────────────────────┤
│  方法    │ 路径                    │ 分组    │ 用例数 │ 状态 │ 操作 │
│  POST    │ /api/v1/users/login     │ 用户模块 │ 5      │ ✅ 正常│ [详情]│
│  GET     │ /api/v1/users/:id       │ 用户模块 │ 2      │ ✅ 正常│ [详情]│
│  POST    │ /api/v1/orders/create   │ 订单模块 │ 8      | ⚠️ 变更│ [详情]│
│  DELETE  │ /api/v1/orders/:id       │ 订单模块 │ 1      │ 🗑️ 废弃│ [详情]│
└─────────────────────────────────────────────────────────────────┘
```

**接口详情**
```
┌─────────────────────────────────────────────────────────────────┐
│  接口详情 - POST /api/v1/users/login                            │
├─────────────────────────────────────────────────────────────────┤
│  [返回列表] [在线调试] [编辑] [Mock服务]                         │
│                                                                  │
│  基本信息                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 方法：POST                                                  │ │
│  │ 路径：/api/v1/users/login                                 │ │
│  │ 描述：用户登录接口                                          │ │
│  │ 状态：✅ 正常                                              │ │
│  │ 标签：#认证 #用户                                          │ │
│  │ 版本：v1.2.3 (hash: abc123...)                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  请求参数                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Body (application/json)                                    │ │
│  │ {                                                           │ │
│  │   "username": "string (必填)",                             │ │
│  │   "password": "string (必填)",                             │ │
│  │   "rememberMe": "boolean (可选)"                           │ │
│  │ }                                                           │ │
│  │ [查看完整 Schema]                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  响应定义                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 200 OK                                                      │ │
│  │ {                                                           │ │
│  │   "code": 0,                                               │ │
│  │   "message": "success",                                    │ │
│  │   "data": {                                                │ │
│  │     "token": "string",                                     │ │
│  │     "userId": "integer",                                   │ │
│  │     "userInfo": {                                          │ │
│  │       "username": "string",                                │ │
│  │       "avatar": "string"                                   │ │
│  │     }                                                      │ │
│  │   }                                                        │ │
│  │ }                                                           │ │
│  │ [查看完整 Schema]                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  Tab: [用例列表] [版本历史] [调用记录]                          │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 用例列表 (5)                                                │ │
│  │  [正常登录] [账号不存在] [密码错误] [账号锁定] [Token过期]  │ │
│  │ [+ 新建用例]                                                │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

**在线调试**
```
┌─────────────────────────────────────────────────────────────────┐
│  在线调试 - POST /api/v1/users/login                            │
├─────────────────────────────────────────────────────────────────┤
│  环境: [开发环境 ▼]                                              │
│                                                                  │
│  请求参数                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ Body                                                        │ │
│  │ {                                                           │ │
│  │   "username": "test_user",                                 │ │
│  │   "password": "123456",                                    │ │
│  │   "rememberMe": true                                       │ │
│  │ }                                                           │ │
│  │ [格式化] [清空] [使用默认值]                                │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  [发送请求] [保存为用例]                                         │
│                                                                  │
│  响应结果                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 状态码：200 ✅  (452ms)                                    │ │
│  │                                                             │ │
│  │ Response Body:                                             │ │
│  │ {                                                           │ │
│  │   "code": 0,                                               │ │
│  │   "message": "success",                                    │ │
│  │   "data": {                                                │ │
│  │     "token": "eyJhbGciOiJIUzI1NiIs...",                    │ │
│  │     "userId": 1001,                                        │ │
│  │     "userInfo": {                                          │ │
│  │       "username": "test_user",                             │ │
│  │       "avatar": "https://cdn..."                          │ │
│  │     }                                                      │ │
│  │   }                                                        │ │
│  │ }                                                           │ │
│  │                                                             │ │
│  │ [复制] [导出] [保存为用例]                                  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

### 3.3 功能 3：原子用例管理 (Atomic Case Management)

#### 3.3.1 设计思路
将测试用例作为接口的衍生属性，一个接口对应多个原子用例，支持 AI 自动生成和用户手动创建。

#### 3.3.2 交互流程

**步骤 1：查看用例列表**
1. 在接口详情页切换到"用例列表"Tab
2. 查看该接口的所有用例：
   - 显示用例名称
   - 显示用例优先级（P0/P1/P2）
   - 显示用例来源（AI生成/用户创建）
   - 显示用例状态（正常/废弃）
   - 显示最后执行时间和结果

**步骤 2：创建基准用例（AI 生成）**
1. 点击"AI 生成基准用例"按钮
2. AI 分析接口定义：
   - 解析请求参数结构
   - 解析响应结构
   - 识别关键字段（如 id、status、timestamp）
3. AI 生成基准用例：
   - **入参生成**：
     - 必填字段：生成合理的默认值
     - 可选字段：根据类型生成示例值
     - 参数化：支持变量引用（如 `{{username}}`）
   - **断言生成**：
     - 状态码断言：断言 2xx 状态码
     - 字段非空断言：关键字段非空
     - 类型断言：字段类型正确
     - 值范围断言：如状态码为 0 或 1
4. 用户审核用例：
   - 查看生成的入参
   - 查看生成的断言
   - 调整不合适的部分
5. 保存用例

**步骤 3：创建业务用例（手动）**
1. 点击"新建用例"按钮
2. 填写用例基本信息：
   - 用例名称（如"密码错误登录"）
   - 用例描述
   - 优先级（P0/P1/P2）
3. 配置入参覆写：
   - 继承接口定义的参数
   - 修改特定字段的值：
     - `username`: "wrong_user"
     - `password`: "wrong_password"
   - 支持参数化：使用 `{{variable_name}}` 格式
4. 配置后置操作：
   - **提取变量**：
     - 选择提取字段（如 `data.token`）
     - 设置变量名（如 `login_token`）
     - 设置提取路径（如 `$.data.token`）
   - **断言配置**：
     - 添加状态码断言（如 401）
     - 添加响应体断言（如 `code` 等于 1001）
     - 添加响应头断言
     - 添加响应时间断言（如 < 500ms）
   - **SQL 操作**：
     - 前置 SQL：数据准备
     - 后置 SQL：数据清理
5. 保存用例

**步骤 4：编辑用例**
1. 在用例列表中点击用例
2. 进入用例编辑页面：
   - 修改用例名称、描述、优先级
   - 调整入参覆写
   - 添加/删除断言
   - 添加/删除变量提取
   - 添加/删除 SQL 操作
3. 保存修改

**步骤 5：执行用例**
1. 在用例列表中点击"执行"按钮
2. 选择执行环境
3. 系统执行用例：
   - 应用入参覆写
   - 发送请求
   - 检查断言
   - 提取变量
   - 执行 SQL 操作
4. 查看执行结果：
   - 执行状态（成功/失败）
   - 执行时间
   - 响应数据
   - 断言结果（通过/失败）
   - 提取的变量
5. 查看失败详情：
   - 查看失败的断言
   - 查看期望值和实际值
   - 查看错误信息

**步骤 6：批量执行**
1. 勾选多个用例
2. 点击"批量执行"按钮
3. 选择执行环境
4. 系统批量执行：
   - 并行执行多个用例
   - 显示执行进度
   - 实时更新执行状态
5. 查看批量执行结果：
   - 执行统计（总数/成功/失败）
   - 查看每个用例的执行结果
   - 导出执行报告

#### 3.3.3 界面设计

**用例列表**
```
┌─────────────────────────────────────────────────────────────────┐
│  用例列表 (5)                                                     │
├─────────────────────────────────────────────────────────────────┤
│  [AI 生成基准用例] [+ 新建用例] [批量执行]                       │
├─────────────────────────────────────────────────────────────────┤
│  用例名称          │ 优先级 │ 来源     │ 最后执行 │ 状态 │ 操作  │
│  正常登录          │ P0     │ AI生成   │ 5分钟前  │ ✅   │ [执行]│
│  账号不存在        │ P1     │ 用户创建 │ 1小时前  │ ✅   │ [执行]│
│  密码错误          │ P1     │ 用户创建 │ 2小时前  │ ✅   │ [执行]│
│  账号锁定          │ P2     │ 用户创建 │ 未执行   │ ⏳   │ [执行]│
│  Token过期         │ P1     │ 用户创建 │ 未执行   │ ⏳   │ [执行]│
└─────────────────────────────────────────────────────────────────┘
```

**用例编辑**
```
┌─────────────────────────────────────────────────────────────────┐
│  编辑用例 - 正常登录                                             │
├─────────────────────────────────────────────────────────────────┤
│  [保存] [取消]                                                   │
│                                                                  │
│  基本信息                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 用例名称：正常登录                                           │ │
│  │ 描述：使用正确的用户名和密码登录                             │ │
│  │ 优先级：[P0 ▼]                                              │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  入参覆写                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ {                                                           │ │
│  │   "username": "test_user",    ← 可编辑                     │ │
│  │   "password": "123456",        ← 可编辑                     │ │
│  │   "rememberMe": true           ← 可编辑                     │ │
│  │ }                                                           │ │
│  │ [格式化] [使用接口默认值]                                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  后置操作                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ [+ 提取变量] [+ 添加断言] [+ 添加SQL]                       │ │
│  │                                                             │ │
│  │ 提取变量：                                                   │ │
│  │ • 变量名：login_token                                       │ │
│  │   提取路径：$.data.token                                    │ │
│  │   [删除]                                                     │ │
│  │                                                             │ │
│  │ 断言：                                                       │ │
│  │ • 状态码：200 [删除]                                         │ │
│  │ • 响应体：$.code == 0 [删除]                                │ │
│  │ • 响应时间：< 500ms [删除]                                  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

**AI 生成基准用例**
```
┌─────────────────────────────────────────────────────────────────┐
│  AI 生成基准用例                                                 │
├─────────────────────────────────────────────────────────────────┤
│  AI 正在分析接口定义...                                          │
│  ✓ 解析请求参数（3个字段）                                       │
│  ✓ 解析响应结构（嵌套3层）                                       │
│  ✓ 识别关键字段（token, userId）                                 │
│                                                                  │
│  AI 已生成基准用例，请审核：                                     │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 入参：                                                       │ │
│  │ {                                                           │ │
│  │   "username": "ai_test_user",                              │ │
│  │   "password": "Test@123456",                               │ │
│  │   "rememberMe": false                                      │ │
│  │ }                                                           │ │
│  │                                                             │ │
│  │ 断言：                                                       │ │
│  │ • 状态码：200                                               │ │
│  │ • 响应体：$.code == 0                                      │ │
│  │ • 响应体：$.data.token 非空                                │ │
│  │ • 响应体：$.data.userId 非空                               │ │
│  │ • 响应体：$.data.userInfo.username 非空                     │ │
│  │                                                             │ │
│  │ 提取变量：                                                   │ │
│  │ • login_token = $.data.token                               │ │
│  │ • user_id = $.data.userId                                  │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  [确认保存] [修改后保存] [取消]                                  │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.3.4 AI 能力设计

**AI 生成基准用例**
```python
# 伪代码
def ai_generate_base_case(api_definition):
    # 分析接口定义
    request_schema = api_definition.request_schema
    response_schema = api_definition.response_schema
    
    # 生成入参
    request_data = generate_request_data(request_schema)
    
    # 生成断言
    assertions = generate_assertions(response_schema)
    
    # 生成变量提取
    extractions = generate_extractions(response_schema)
    
    return {
        'name': f'{api_definition.path} 基准用例',
        'priority': 'P0',
        'request_data': request_data,
        'assertion_rules': assertions,
        'extraction_rules': extractions,
        'ai_generated': True
    }

def generate_request_data(schema):
    # 根据类型生成合理的默认值
    field_values = {}
    for field in schema.fields:
        if field.type == 'string':
            field_values[field.name] = f'ai_test_{field.name}'
        elif field.type == 'integer':
            field_values[field.name] = 1001
        elif field.type == 'boolean':
            field_values[field.name] = True
        elif field.type == 'array':
            field_values[field.name] = []
        # ... 其他类型
    
    return field_values

def generate_assertions(schema):
    assertions = []
    
    # 状态码断言
    assertions.append({
        'type': 'status_code',
        'operator': 'in',
        'value': [200, 201, 204]
    })
    
    # 字段非空断言
    key_fields = identify_key_fields(schema)
    for field in key_fields:
        assertions.append({
            'type': 'body',
            'path': f'$.data.{field}',
            'operator': 'not_empty'
        })
    
    # 类型断言
    for field in schema.fields:
        assertions.append({
            'type': 'body',
            'path': f'$.data.{field.name}',
            'operator': 'type',
            'value': field.type
        })
    
    return assertions
```

---

## 四、数据模型设计

### 4.1 接口定义表 (api_definitions)

```sql
CREATE TABLE api_definitions (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    project_id BIGINT NOT NULL,
    group_id BIGINT,
    
    -- 接口基本信息
    method VARCHAR(10) NOT NULL COMMENT 'HTTP方法: GET/POST/PUT/DELETE',
    path VARCHAR(500) NOT NULL COMMENT '接口路径',
    summary VARCHAR(200) COMMENT '接口摘要',
    description TEXT COMMENT '接口描述',
    tags JSON COMMENT '标签数组',
    
    -- 版本信息
    version_hash VARCHAR(64) COMMENT '版本哈希，用于快速比对',
    source_type VARCHAR(20) COMMENT '来源类型: swagger/postman/yapi/manual',
    source_url VARCHAR(500) COMMENT '文档来源URL',
    source_version VARCHAR(50) COMMENT '文档版本标识',
    
    -- Schema 数据
    schema_snapshot JSON COMMENT '导入时的原始Schema快照',
    request_schema JSON COMMENT '请求参数Schema',
    response_schema JSON COMMENT '响应Schema',
    
    -- Mock 数据
    mock_data JSON COMMENT 'Mock数据模板',
    mock_rules JSON COMMENT 'Mock规则配置',
    
    -- 状态信息
    status VARCHAR(20) DEFAULT 'active' COMMENT '状态: active/deprecated/archived',
    sync_status VARCHAR(20) COMMENT '同步状态: synced/changed/deleted',
    last_sync_at TIMESTAMP COMMENT '最后同步时间',
    
    -- 元数据
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by BIGINT,
    updated_by BIGINT,
    
    -- 索引
    INDEX idx_project_group (project_id, group_id),
    INDEX idx_method_path (method, path),
    INDEX idx_status (status),
    INDEX idx_version_hash (version_hash)
) COMMENT='接口定义表';
```

### 4.2 原子用例表 (api_cases)

```sql
CREATE TABLE api_cases (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    definition_id BIGINT NOT NULL COMMENT '关联接口ID',
    project_id BIGINT NOT NULL,
    
    -- 用例基本信息
    name VARCHAR(100) NOT NULL COMMENT '用例名称',
    description TEXT COMMENT '用例描述',
    priority VARCHAR(10) DEFAULT 'P2' COMMENT '优先级: P0/P1/P2',
    case_type VARCHAR(20) DEFAULT 'business' COMMENT '用例类型: base/business',
    
    -- 请求配置
    request_data JSON COMMENT '请求参数覆写',
    environment_id BIGINT COMMENT '默认环境ID',
    
    -- 断言配置
    assertion_rules JSON COMMENT '断言规则数组',
    
    -- 变量提取配置
    extraction_rules JSON COMMENT '变量提取规则数组',
    
    -- SQL 操作
    pre_sql TEXT COMMENT '前置SQL',
    post_sql TEXT COMMENT '后置SQL',
    
    -- AI 信息
    ai_generated BOOLEAN DEFAULT FALSE COMMENT '是否AI生成',
    ai_confidence DECIMAL(3,2) COMMENT 'AI置信度',
    ai_suggestions JSON COMMENT 'AI建议',
    
    -- 状态信息
    status VARCHAR(20) DEFAULT 'active' COMMENT '状态: active/deprecated',
    fix_status VARCHAR(20) COMMENT '修复状态: none/needed/fixed',
    
    -- 元数据
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by BIGINT,
    updated_by BIGINT,
    
    -- 索引
    INDEX idx_definition (definition_id),
    INDEX idx_project (project_id),
    INDEX idx_priority (priority),
    INDEX idx_type (case_type)
) COMMENT='原子用例表';
```

### 4.3 同步任务表 (sync_tasks)

```sql
CREATE TABLE sync_tasks (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    project_id BIGINT NOT NULL,
    
    -- 同步配置
    source_type VARCHAR(20) NOT NULL COMMENT '来源类型',
    source_url VARCHAR(500) COMMENT '文档URL',
    source_version VARCHAR(50) COMMENT '版本标识',
    description TEXT COMMENT '同步描述',
    
    -- 同步结果
    status VARCHAR(20) DEFAULT 'pending' COMMENT '状态: pending/running/completed/failed',
    total_count INT DEFAULT 0 COMMENT '总接口数',
    new_count INT DEFAULT 0 COMMENT '新增接口数',
    changed_count INT DEFAULT 0 COMMENT '变更接口数',
    deleted_count INT DEFAULT 0 COMMENT '删除接口数',
    
    -- Diff 数据
    diff_data JSON COMMENT '变更详情数据',
    impact_analysis JSON COMMENT '影响分析数据',
    fix_data JSON COMMENT 'AI修复数据',
    
    -- 元数据
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    started_at TIMESTAMP,
    completed_at TIMESTAMP,
    created_by BIGINT,
    
    -- 索引
    INDEX idx_project (project_id),
    INDEX idx_status (status),
    INDEX idx_created_at (created_at)
) COMMENT='同步任务表';
```

### 4.4 版本快照表 (version_snapshots)

```sql
CREATE TABLE version_snapshots (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    definition_id BIGINT NOT NULL,
    project_id BIGINT NOT NULL,
    
    -- 版本信息
    version_hash VARCHAR(64) NOT NULL COMMENT '版本哈希',
    version_tag VARCHAR(50) COMMENT '版本标签',
    source_version VARCHAR(50) COMMENT '文档版本',
    
    -- 快照数据
    schema_snapshot JSON COMMENT 'Schema快照',
    snapshot_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    -- 元数据
    created_by BIGINT,
    
    -- 索引
    INDEX idx_definition (definition_id),
    INDEX idx_version_hash (version_hash),
    UNIQUE KEY uk_definition_version (definition_id, version_hash)
) COMMENT='版本快照表';
```

---

## 五、核心工作流

### 5.1 完整使用流程

**场景：从零构建一个"用户下单"接口的测试用例**

```
1. 同步文档
   ├─ 进入 API 资产库
   ├─ 点击"新建同步"
   ├─ 选择 Swagger 来源
   ├─ 输入文档 URL
   ├─ 提交同步任务
   └─ 等待同步完成

2. 查看 Diff
   ├─ 同步完成后查看变更报告
   ├─ AI 检测到 15 个接口变更
   ├─ 查看变更详情
   └─ 应用变更

3. 查看接口
   ├─ 在接口列表中找到 "POST /api/v1/orders/create"
   ├─ 点击查看详情
   └─ 查看接口定义

4. AI 生成基准用例
   ├─ 在接口详情页点击"AI 生成基准用例"
   ├─ AI 分析接口定义
   ├─ AI 生成入参和断言
   ├─ 用户审核生成结果
   └─ 保存基准用例

5. 创建业务用例
   ├─ 点击"新建用例"
   ├─ 命名为"库存不足下单"
   ├─ 配置入参覆写（设置库存不足的商品ID）
   ├─ 添加断言（断言返回错误码）
   └─ 保存用例

6. 在线调试
   ├─ 点击"在线调试"
   ├─ 选择测试环境
   ├─ 填写测试参数
   ├─ 发送请求
   └─ 查看响应结果

7. 执行用例
   ├─ 在用例列表中点击"执行"
   ├─ 选择执行环境
   └─ 查看执行结果

8. 批量执行
   ├─ 勾选所有用例
   ├─ 点击"批量执行"
   ├─ 查看执行统计
   └─ 导出执行报告
```

### 5.2 接口变更修复流程

```
1. 检测变更
   ├─ 创建新的同步任务
   ├─ AI 对比接口定义
   └─ 识别字段变更

2. 影响分析
   ├─ AI 扫描关联用例
   ├─ AI 扫描关联场景
   └─ 生成影响报告

3. AI 自动修复
   ├─ AI 识别字段改名（userId → user_id）
   ├─ AI 自动更新用例映射
   ├─ AI 自动更新场景变量引用
   └─ 生成修复报告

4. 人工确认
   ├─ 用户查看修复报告
   ├─ 逐项确认修复
   ├─ 调整不合适的修复
   └─ 应用修复

5. 执行验证
   ├─ 执行修复后的用例
   ├─ 执行修复后的场景
   └─ 确认修复成功
```

---

## 六、技术实现要点

### 6.1 AI 能力集成

**Diff 计算**
- 使用 JSON Schema 对比算法
- 识别字段级别的变更
- 生成结构化的 Diff 数据

**语义分析**
- 使用 LLM 分析字段语义相似性
- 识别字段改名（userId → user_id）
- 识别字段合并/拆分

**自动修复**
- 基于语义相似性自动映射字段
- 更新用例中的参数引用
- 更新场景中的变量引用

**断言生成**
- 基于 Schema 自动生成基础断言
- 识别关键字段并添加非空断言
- 生成类型断言和范围断言

### 6.2 数据同步策略

**增量同步**
- 只同步变更的接口
- 记录版本哈希用于快速比对
- 保留历史版本快照

**冲突解决**
- 检测手动修改与文档更新的冲突
- 提示用户选择保留哪个版本
- 支持"保留我的修改"选项

### 6.3 性能优化

**分页加载**
- 接口列表分页加载
- 用例列表分页加载
- 支持虚拟滚动（大数据量场景）

**缓存策略**
- 缓存接口定义数据
- 缓存用例执行结果
- 缓存 Mock 数据

**异步处理**
- 同步任务异步执行
- AI 分析异步执行
- 批量操作异步执行

---

## 七、风险与漏洞修补（Critical Risks）

这是落地时最容易"烂尾"的地方，请务必仔细评估：

### 7.1 "同步"与"手工修改"的冲突处理 (The Conflict)

#### 问题描述
用户觉得 Swagger 定义的参数类型不对，手动在平台里把 `age` 从 `string` 改成了 `int`。第二天，Swagger 同步了，里面还是 `string`。

#### 风险
同步逻辑会覆盖用户的修改吗？还是报错？

#### 解决方案：锁定机制
在 `api_definitions` 表增加 `lock_status` 字段：
- 如果用户手动修改了接口定义，自动锁定该字段
- 下次同步时**跳过**该字段的更新，并发出警告
- 或者采用 Git 的思路：**"Accept Yours" vs "Accept Theirs"**

### 7.2 脏数据与幂等性 (Dirty Data & Idempotency)

#### 问题描述
AI 生成了一个基准用例 `POST /users`，入参是固定的 `username: "ai_test_user"`。第一次运行成功，第二次运行必挂，报错"用户名已存在"。

#### 风险
用例无法重复执行，影响自动化测试的稳定性。

#### 解决方案：动态随机函数
AI 生成逻辑必须引入**动态随机函数**：
- 生成的基准用例参数不要写死 `"ai_test_user"`
- 要写成 `ai_test_{{timestamp}}` 或 `ai_test_{{random_string}}`
- 在"原子用例"的解析引擎中内置这套函数库

### 7.3 复杂参数的覆盖逻辑 (Deep Merge)

#### 问题描述
Body 是一个深层嵌套 JSON：
```json
{ "user": { "info": { "address": "Beijing" }, "role": "admin" } }
```
业务用例只想改 `address` 为 "Shanghai"。`request_data` 存的是全量 JSON 还是 Diff？

#### 风险
- 如果存全量 JSON：接口增加新字段后，业务用例会丢失新字段
- 如果存 Diff：需要实现复杂的 Deep Merge 逻辑

#### 解决方案：Deep Merge
- **存储**：建议存 **Diff (Delta)**，即只存 `{"user": {"info": {"address": "Shanghai"}}}`
- **执行**：运行时进行 **Deep Merge**
- **优势**：接口增加 `user.email` 字段后，基准用例更新后，业务用例能自动继承这个新字段

---

## 八、数据模型优化 (Schema Refinement)

针对 SQL 设计的性能和扩展性优化建议：

### 8.1 `api_definitions` 表优化

#### 增加 `content_hash` 字段
```sql
ALTER TABLE api_definitions ADD COLUMN content_hash VARCHAR(64) COMMENT '内容哈希，用于快速比对';
```

**说明**：
- `version_hash` 通常指文档的版本号
- 有时候版本号没变，但内容变了
- `content_hash = MD5(method + path + request_schema + response_schema)`
- 同步时，先比对 `content_hash`，不一样再启动昂贵的 Diff 计算

### 8.2 `api_cases` 表优化

#### 标准化断言和提取规则存储
不要只存 JSON，建议设计明确的结构：

**断言规则标准化格式**：
```json
[
  {
    "source": "body",
    "property": "data.id",
    "operator": "not_null",
    "expected": null,
    "description": "订单ID不能为空"
  },
  {
    "source": "header",
    "property": "Content-Type",
    "operator": "contains",
    "expected": "json",
    "description": "响应头必须包含json"
  }
]
```

**变量提取规则标准化格式**：
```json
[
  {
    "variable_name": "order_id",
    "source": "body",
    "json_path": "$.data.orderId",
    "description": "提取订单ID"
  }
]
```

**优势**：
- 便于前端渲染表单
- 便于后端统一校验
- 便于统计分析断言类型分布

---

## 九、交互体验微调 (UX Polish)

### 9.1 变更预警的展示

#### 优化点
不要只在"同步任务"里看变更预警。

#### 强提醒设计
1. **接口列表页**：变更的接口应该有明显的**红点**或**高亮行**
2. **Diff View**：用户点进去后，应该有一个 **"Diff View" (左右分栏对比)**，就像 GitHub 的 PR 界面一样，直观看到：
   - 左边是旧定义
   - 右边是新定义
   - 变更部分高亮显示

### 9.2 调试与用例的转化

#### 优化点
调试成功后，"保存为用例" 是一个高频操作。

#### 所见即所得的断言生成
保存时，自动把刚刚调试通过的 Response 中的关键值提取出来，填入断言中：
- 例如，刚才返回了 `code: 0`
- 保存为用例时，自动勾选"断言 code 等于 0"
- 这叫**"所见即所得的断言生成"**

### 9.3 AI 生成用例的参数化

#### 优化点
AI 生成的基准用例参数必须支持参数化，避免脏数据问题。

#### 动态参数生成
AI 生成时应该使用动态变量：
- 用户名：`ai_test_user_{{random_string(8)}}`
- 时间戳：`{{timestamp}}`
- 随机数：`{{random_int(1000, 9999)}}`
- UUID：`{{uuid()}}`

这样生成的用例可以重复执行，不会因为数据冲突而失败。

---

## 十、界面设计规范

### 7.1 色彩规范
- **成功**：绿色 (#52c41a)
- **警告**：橙色 (#fa8c16)
- **错误**：红色 (#f5222d)
- **信息**：蓝色 (#1890ff)
- **中性**：灰色 (#8c8c8c)

### 7.2 交互规范
- **按钮**：主要操作使用实心按钮，次要操作使用虚线按钮
- **弹窗**：重要操作使用模态弹窗，辅助操作使用抽屉
- **表单**：必填项用红色星号标识，错误信息显示在字段下方
- **表格**：支持多选、排序、筛选，操作列固定在右侧

### 7.3 响应式设计
- **桌面端**：支持 1920x1080 及以上分辨率
- **平板端**：支持 768x1024 及以上分辨率
- **移动端**：支持 375x667 及以上分辨率

---

## 十二、任务规划 (Implementation Roadmap)

### 12.1 阶段一：基础架构搭建（2周）

#### 任务 1.1：数据库设计与迁移
- [ ] 设计并创建 `api_definitions` 表（包含所有字段）
- [ ] 设计并创建 `api_cases` 表（包含所有字段）
- [ ] 设计并创建 `sync_tasks` 表
- [ ] 设计并创建 `version_snapshots` 表
- [ ] 编写数据库迁移脚本
- [ ] 执行数据库迁移，验证表结构

#### 任务 1.2：后端基础框架搭建
- [ ] 创建 API 资产库模块目录结构
- [ ] 实现 API 定义 CRUD 接口
- [ ] 实现原子用例 CRUD 接口
- [ ] 实现同步任务 CRUD 接口
- [ ] 实现版本快照管理接口
- [ ] 编写 API 文档（Swagger）
- [ ] 单元测试覆盖核心接口

#### 任务 1.3：前端基础页面搭建
- [ ] 创建 API 资产库主页面
- [ ] 实现接口列表页面（分页、筛选、搜索）
- [ ] 实现接口详情页面（Tab切换）
- [ ] 实现用例列表页面
- [ ] 实现用例编辑页面
- [ ] 实现同步任务列表页面
- [ ] 实现变更详情弹窗

### 12.2 阶段二：文档同步功能（3周）

#### 任务 2.1：文档解析器开发
- [ ] 实现 Swagger 解析器
- [ ] 实现 Postman 解析器
- [ ] 实现 YApi 解析器
- [ ] 编写解析器单元测试
- [ ] 处理解析异常情况

#### 任务 2.2：同步任务管理
- [ ] 实现同步任务创建接口
- [ ] 实现异步任务执行逻辑
- [ ] 实现任务进度查询接口
- [ ] 实现任务失败重试机制
- [ ] 实现任务取消功能

#### 任务 2.3：Diff 算法实现
- [ ] 实现 JSON Schema 对比算法
- [ ] 实现请求参数 Diff 计算
- [ ] 实现响应结构 Diff 计算
- [ ] 实现变更类型识别（新增/变更/删除）
- [ ] 生成结构化的 Diff 数据
- [ ] 编写 Diff 算法测试用例

#### 任务 2.4：变更预警功能
- [ ] 实现变更检测逻辑
- [ ] 实现影响范围扫描
- [ ] 实现关联用例扫描
- [ ] 实现关联场景扫描
- [ ] 实现变更报告生成
- [ ] 前端展示变更预警

### 12.3 阶段三：AI 能力集成（4周）

#### 任务 3.1：AI 基础设施搭建
- [ ] 选择并接入 LLM（GPT/Claude/通义千问）
- [ ] 实现 AI 服务接口封装
- [ ] 实现 Prompt 管理机制
- [ ] 实现 AI 调用日志记录
- [ ] 实现 AI 调用限流和成本控制

#### 任务 3.2：AI 基准用例生成
- [ ] 实现 Schema 分析逻辑
- [ ] 实现请求参数生成算法
- [ ] 实现断言规则生成算法
- [ ] 实现变量提取规则生成算法
- [ ] 实现动态随机函数（timestamp/random_string/uuid）
- [ ] 前端实现 AI 生成用例界面
- [ ] 实现 AI 生成结果审核流程

#### 任务 3.3：AI 自动修复
- [ ] 实现字段语义相似性分析
- [ ] 实现字段改名识别算法
- [ ] 实现用例映射自动更新
- [ ] 实现场景变量引用自动更新
- [ ] 实现修复报告生成
- [ ] 前端实现修复确认界面

#### 任务 3.4：AI 断言生成
- [ ] 实现关键字段识别算法
- [ ] 实现非空断言生成
- [ ] 实现类型断言生成
- [ ] 实现范围断言生成
- [ ] 实现所见即所得断言生成
- [ ] 前端实现断言配置界面

### 12.4 阶段四：在线调试与 Mock（2周）

#### 任务 4.1：在线调试功能
- [ ] 实现调试参数配置界面
- [ ] 实现环境选择功能
- [ ] 实现请求发送逻辑
- [ ] 实现响应数据展示
- [ ] 实现响应格式化和高亮
- [ ] 实现调试历史记录
- [ ] 实现保存为用例功能

#### 任务 4.2：Mock 服务
- [ ] 实现 Mock 地址生成
- [ ] 实现 Mock 数据生成逻辑
- [ ] 集成 Mock.js 库
- [ ] 实现 Mock 模板编辑功能
- [ ] 实现 Mock 规则配置（延迟、错误率）
- [ ] 实现 Mock 接口路由

### 12.5 阶段五：用例执行引擎（3周）

#### 任务 5.1：用例执行引擎
- [ ] 实现单用例执行逻辑
- [ ] 实现请求参数覆写逻辑
- [ ] 实现断言检查逻辑
- [ ] 实现变量提取逻辑
- [ ] 实现前置/后置 SQL 执行
- [ ] 实现执行结果记录

#### 任务 5.2：批量执行
- [ ] 实现批量用例选择
- [ ] 实现并行执行逻辑
- [ ] 实现执行进度跟踪
- [ ] 实现执行统计
- [ ] 实现批量执行报告生成

#### 任务 5.3：Deep Merge 机制
- [ ] 实现 Delta 数据存储
- [ ] 实现 Deep Merge 算法
- [ ] 处理嵌套 JSON 合并
- [ ] 处理数组类型字段
- [ ] 编写 Deep Merge 测试用例

#### 任务 5.4：动态变量函数
- [ ] 实现 `{{timestamp}}` 函数
- [ ] 实现 `{{random_string}}` 函数
- [ ] 实现 `{{random_int}}` 函数
- [ ] 实现 `{{uuid()}}` 函数
- [ ] 实现变量解析引擎
- [ ] 编写函数库文档

### 12.6 阶段六：冲突解决与优化（2周）

#### 任务 6.1：锁定机制
- [ ] 在 `api_definitions` 表增加 `lock_status` 字段
- [ ] 实现手动修改自动锁定逻辑
- [ ] 实现同步时跳过锁定字段逻辑
- [ ] 实现锁定状态警告提示
- [ ] 实现 "Accept Yours" vs "Accept Theirs" 冲突解决

#### 任务 6.2：Content Hash 优化
- [ ] 在 `api_definitions` 表增加 `content_hash` 字段
- [ ] 实现 Content Hash 计算逻辑
- [ ] 实现快速比对机制
- [ ] 优化 Diff 计算性能
- [ ] 性能测试和调优

#### 任务 6.3：标准化存储
- [ ] 标准化断言规则存储格式
- [ ] 标准化变量提取规则存储格式
- [ ] 实现格式验证逻辑
- [ ] 迁移旧数据到新格式
- [ ] 更新前端表单渲染逻辑

### 12.7 阶段七：用户体验优化（2周）

#### 任务 7.1：Diff View 界面
- [ ] 实现左右分栏对比界面
- [ ] 实现变更高亮显示
- [ ] 实现差异统计展示
- [ ] 实现代码折叠展开
- [ ] 优化界面响应速度

#### 任务 7.2：变更预警强提醒
- [ ] 实现接口列表红点标记
- [ ] 实现变更接口高亮显示
- [ ] 实现变更统计徽章
- [ ] 实现一键查看变更功能
- [ ] 优化提示时机和频率

#### 任务 7.3：所见即所得断言生成
- [ ] 实现调试成功后自动提取关键值
- [ ] 实现自动勾选断言逻辑
- [ ] 实现断言模板生成
- [ ] 实现断言快速编辑
- [ ] 优化用户体验流程

### 12.8 阶段八：测试与上线（1周）

#### 任务 8.1：功能测试
- [ ] 编写所有功能的测试用例
- [ ] 执行功能测试
- [ ] 修复测试发现的 Bug
- [ ] 达到测试覆盖率目标

#### 任务 8.2：性能测试
- [ ] 进行接口列表性能测试
- [ ] 进行用例执行性能测试
- [ ] 进行 Diff 计算性能测试
- [ ] 进行 AI 调用性能测试
- [ ] 优化性能瓶颈

#### 任务 8.3：文档完善
- [ ] 编写用户使用手册
- [ ] 编写 API 接口文档
- [ ] 编写部署文档
- [ ] 编写故障排查指南
- [ ] 录制操作演示视频

#### 任务 8.4：灰度发布
- [ ] 配置灰度发布策略
- [ ] 选择灰度测试用户
- [ ] 收集用户反馈
- [ ] 修复灰度发现的问题
- [ ] 全量发布

### 12.9 任务优先级说明

**P0 任务（必须完成）**：
- 数据库设计与迁移
- 后端基础框架搭建
- 文档解析器开发
- 同步任务管理
- Diff 算法实现
- 基础 AI 能力集成

**P1 任务（重要但不紧急）**：
- AI 自动修复
- 在线调试功能
- Mock 服务
- 用例执行引擎
- Deep Merge 机制
- 锁定机制

**P2 任务（优化和增强）**：
- Content Hash 优化
- 标准化存储
- Diff View 界面
- 变更预警强提醒
- 所见即所得断言生成

### 12.10 预估时间线

```
阶段一：基础架构搭建（2周）
├─ 第 1 周：数据库设计、后端框架
└─ 第 2 周：前端基础页面

阶段二：文档同步功能（3周）
├─ 第 3 周：文档解析器开发
├─ 第 4 周：同步任务管理
└─ 第 5 周：Diff 算法和变更预警

阶段三：AI 能力集成（4周）
├─ 第 6 周：AI 基础设施
├─ 第 7-8 周：AI 基准用例生成
└─ 第 9 周：AI 自动修复和断言生成

阶段四：在线调试与 Mock（2周）
├─ 第 10 周：在线调试功能
└─ 第 11 周：Mock 服务

阶段五：用例执行引擎（3周）
├─ 第 12 周：执行引擎和批量执行
├─ 第 13 周：Deep Merge 和动态变量
└─ 第 14 周：测试和优化

阶段六：冲突解决与优化（2周）
├─ 第 15 周：锁定机制和 Content Hash
└─ 第 16 周：标准化存储

阶段七：用户体验优化（2周）
├─ 第 17 周：Diff View 和变更预警
└─ 第 18 周：所见即所得断言生成

阶段八：测试与上线（1周）
└─ 第 19 周：功能测试、性能测试、文档完善、灰度发布

总计：19 周（约 5 个月）
```

### 12.11 风险与应对

#### 风险 1：AI 能力接入失败
**应对**：提前准备多个 AI 服务商的备选方案，实现快速切换

#### 风险 2：Diff 算法性能问题
**应对**：实现 Content Hash 快速比对，只对变更的接口进行详细 Diff

#### 风险 3：Deep Merge 复杂度高
**应对**：先支持简单的字段覆盖，后续迭代逐步支持复杂嵌套

#### 风险 4：用户接受度低
**应对**：提供详细的使用文档和培训，收集用户反馈持续优化

---

## 十三、总结

层级一（API 资产库）是整个测试平台的基础，通过以下设计实现了核心目标：

1. **资产归一**：统一管理接口定义和测试用例，消除冗余维护
2. **智能同步**：AI 驱动的文档同步和变更检测，自动预警影响范围
3. **自动化生成**：AI 自动生成基准用例和断言，减少手工配置
4. **智能化修复**：接口变更时自动检测影响范围并尝试修复

通过层级一的完善，为层级二（场景编排工作室）和层级三（计划与执行）提供了坚实的数据基础。