# 鉴权配置实施阶段规划

## 阶段一：数据库层（1-2天）

### 1.1 数据库表结构创建
- 创建 `auth_configs` 主表
- 创建 `auth_input_mappings` 映射表
- 创建 `auth_extract_rules` 提取规则表
- 建立外键关系和索引

### 1.2 数据迁移脚本
- 编写表结构迁移脚本
- 添加必要的索引和约束

**交付物：**
- `migrations/create_auth_config_tables.py`
- 数据库表已创建并验证

---

## 阶段二：后端数据模型（1天）

### 2.1 SQLAlchemy 模型定义
在 `app/db/base.py` 中创建三个模型类：
- `AuthConfig`
- `AuthInputMapping`
- `AuthExtractRule`

### 2.2 Pydantic 模型定义
在 `app/api/v1/auth_config.py` 中定义：
- `InjectionConfig`
- `InputMapping`
- `ExtractRule`
- `AuthConfigCreate`
- `AuthConfigUpdate`
- `AuthConfigResponse`

**交付物：**
- 完整的数据模型定义
- 类型验证和序列化支持

---

## 阶段三：后端 API 开发（2-3天）

### 3.1 CRUD 基础接口
- `GET /projects/{id}/auth-config` - 获取配置
- `POST /projects/{id}/auth-config` - 创建配置
- `PUT /projects/{id}/auth-config` - 更新配置
- `DELETE /projects/{id}/auth-config` - 删除配置

### 3.2 测试接口
- `POST /projects/{id}/auth-config/test-acquisition` - 测试登录和提取

### 3.3 路由注册
在 `app/api/v1/__init__.py` 中注册路由

**交付物：**
- 完整的 API 接口
- 接口文档（Swagger 自动生成）
- 单元测试

---

## 阶段四：后端执行引擎（2-3天）

### 4.1 鉴权中间件实现
创建 `app/core/auth_middleware.py`：
- `AuthMiddleware` 类
- `apply_auth()` 方法
- `_get_auth_value()` 方法
- `_perform_login()` 方法
- `_extract_from_response()` 方法
- `_inject_to_request()` 方法

### 4.2 通用提取器
- JSONPath 提取（Body）
- Header 提取
- Cookie 提取

### 4.3 隔离执行机制
- 添加 `_internal_no_auth` 标记
- 防止递归调用

### 4.4 缓存管理
- Token 缓存存储
- 过期检查
- 自动刷新

**交付物：**
- 完整的鉴权中间件
- 单元测试覆盖

---

## 阶段五：前端 UI 开发（3-4天）

### 5.1 类型定义
创建 `frontend/src/types/auth.ts`：
- TypeScript 接口定义
- 枚举类型

### 5.2 主弹窗组件
修改 `frontend/src/pages/Projects.tsx`：
- 鉴权类型选择下拉框
- 根据类型动态显示配置项
- 注入规则配置（Header Key、Value Template）
- 来源模式选择（静态/动态）

### 5.3 右侧抽屉组件
创建 `frontend/src/components/AuthAcquisitionDrawer.tsx`：
- 登录接口选择器（从 API 资产库）
- 参数映射配置
- 提取规则配置（支持多规则）
- 测试运行功能

### 5.4 API 服务
在 `frontend/src/services/` 中添加鉴权配置 API 调用

**交付物：**
- 完整的 UI 组件
- API 集成
- 响应式设计

---

## 阶段六：集成测试与优化（1-2天）

### 6.1 集成测试
- Basic Auth 测试
- Bearer Token 测试
- API Key 测试
- Session/Cookie 测试
- Custom 模式测试

### 6.2 边界情况测试
- 登录接口失败处理
- Token 过期自动刷新
- 递归调用防护
- 并发请求处理

### 6.3 性能优化
- 缓存策略优化
- 数据库查询优化
- 前端渲染优化

### 6.4 文档完善
- API 文档
- 使用手册
- 最佳实践指南

**交付物：**
- 测试报告
- 性能测试结果
- 完整文档

---

## 总计时间估算：10-15天

### 关键依赖关系
1. 阶段一 → 阶段二（依赖表结构）
2. 阶段二 → 阶段三（依赖数据模型）
3. 阶段三 + 阶段四 → 阶段五（前后端并行开发）
4. 所有阶段 → 阶段六（集成测试）

### 建议开发顺序
**并行开发路径：**
- 路径 A：阶段一 → 阶段二 → 阶段三 → 阶段四 → 阶段六
- 路径 B：阶段五（可在阶段三开始时同步进行）

### 风险点
1. **数据模型变更**：早期设计需充分验证，避免后期返工
2. **提取规则复杂度**：JSONPath 表达式校验可能比较复杂
3. **并发控制**：多请求同时触发登录需要仔细处理

### 里程碑检查点
- **里程碑 1**（第 3 天）：数据库和数据模型完成
- **里程碑 2**（第 6 天）：后端 API 和中间件完成
- **里程碑 3**（第 10 天）：前端 UI 完成
- **里程碑 4**（第 15 天）：集成测试通过，文档完善