✦ 实施阶段一详细理解

  核心目标

  建立语义搜索能力，将数据库的"死"字符串变成"活"的向量，为后续的向量化映射打基础。

  详细理解

  1. 技术选型与模型选择

  选择 `sentence-transformers` + `all-MiniLM-L6-v2` 的原因：

   - 轻量级：模型仅 80MB，适合在 Ryzen 7840HS 上快速加载
   - 性能优秀：单次推理仅需几毫秒，适合实时处理
   - 语义理解强：能够理解字段名、表名、注释之间的语义关联
   - WSL2 兼容性好：避免 Python 依赖地狱

  为什么不选更大的模型？
   - 当前环境是 Ryzen 7840HS + 共享显存，大模型会导致 OOM
   - 实际应用中，字段映射不需要极复杂的语义理解
   - 小模型 + 重心算法 = 更好的性价比

  2. 数据库列的"特征工程"

  关键设计：组合 表名 + 列名 + 注释

  示例：
   # 原始数据
   {
     "table": "users",
     "column": "name",
     "type": "varchar",
     "comment": "user real name"
   }

   # 特征工程后的文本
   text = "users name user real name"

  为什么这样组合？
   - 表名上下文：users 提供业务领域上下文
   - 列名核心：name 是映射的关键
   - 注释增强：user real name 提供额外语义信息
   - 向量优势：模型能理解这种组合的语义关联

  3. 缓存机制设计

  缓存文件：`db_embeddings.pkl`

  缓存策略：
   1. 启动时检查：服务启动时检查缓存文件是否存在
   2. 首次构建：首次启动时全量计算所有列的向量
   3. 持久化存储：将向量和元数据序列化到磁盘
   4. 快速加载：后续启动直接从缓存加载，避免重复计算

  为什么需要缓存？
   - 避免重复计算：595个字段 × 80MB模型 = 需要时间
   - WSL2 IO 慢：每次全量计算会拖慢启动速度
   - 内存优化：避免每次请求都重新加载模型

  4. 向量索引结构

  数据结构设计：

   # column_vectors: numpy.ndarray
   # 形状: (N, 384) - N为数据库列总数，384为向量维度
   # 示例: (595, 384)

   # column_meta: List[Dict]
   # 每个元素包含:
   {
     'table': 'users',           # 表名
     'column': 'name',           # 列名
     'type': 'varchar',          # 数据类型
     'is_pk': False             # 是否主键
   }

  内存占用估算：
   - 向量矩阵：595 × 384 × 4字节 ≈ 0.9MB
   - 元数据：595 × 200字节 ≈ 0.1MB
   - 总计：约 1MB（实际缓存文件会更大，包含序列化开销）

  5. 向量搜索算法

  核心算法：余弦相似度（点积）

   # 1. 计算 API 字段的向量
   query_vec = self.model.encode([api_field_name])  # 形状: (1, 384)

   # 2. 向量点积计算相似度
   # column_vectors: (N, 384)
   # query_vec.T: (384, 1)
   # scores: (1, N)
   scores = np.dot(self.column_vectors, query_vec.T).flatten()

   # 3. Top-K 选择
   top_indices = np.argpartition(scores, -top_k)[-top_k:]
   top_indices = top_indices[np.argsort(scores[top_indices])[::-1]]

  算法复杂度：
   - 时间复杂度：O(N)，N为数据库列总数
   - 空间复杂度：O(1)，使用原地计算
   - 优势：numpy 的矩阵运算会自动使用 AVX-512 指令集加速

  为什么用点积而不是余弦相似度？
   - 假设向量已经归一化，点积 = 余弦相似度
   - numpy 的 np.dot 会自动利用 CPU 向量指令
   - 比 cosine_similarity 函数更快

  6. 实施细节

  文件路径设计：
   cache_path = "/home/project/data/db_embeddings.pkl"

  为什么用 `/home/project/data/`？
   - WSL2 的 Linux 文件系统比 Windows 文件系统快
   - 避免跨文件系统访问的性能损耗
   - 便于权限管理

  启动流程：
   # 应用启动时
   vector_manager = VectorIndexManager(db_schema)
   vector_manager.build_index()  # 自动检查缓存，或构建索引

   # 每次请求时
   candidates = vector_manager.search("user_name", top_k=10)

  7. 与现有系统的集成

  集成点：

   1. 替换现有的 `_generate_mapping_candidates` 函数
      - 原函数：嵌套循环遍历所有表和列
      - 新函数：向量搜索直接找到 Top-K 候选

   2. 性能对比：
      - 原算法：O(M × N)，M为字段数，N为数据库列数
      - 新算法：O(M)，每次字段查询都是 O(N) 的向量搜索
      - 预期提升：从 500ms 降至 10ms

   3. 兼容性：
      - 返回格式保持不变：List[FieldMappingCandidate]
      - 可以与现有的规则评分逻辑并存
      - 支持渐进式迁移

  8. 关键注意事项

  内存管理：
   - 模型加载：~100MB
   - 向量缓存：~10MB（pkl 文件）
   - 运行时内存：~150MB
   - 总计：在 Ryzen 7840HS 的 16GB 内存中完全可控

  并发处理：
   - numpy 的 encode 函数是 CPU 密集型
   - 需要在 FastAPI 的线程池中运行
   - 避免阻塞事件循环

  错误处理：
   - 缓存文件损坏：重新构建索引
   - 模型加载失败：降级到原算法
   - 向量计算异常：返回空列表

  预期效果

   1. 性能提升：查询速度从 500ms 降至 10ms
   2. 准确率提升：语义相似度比字符串匹配更准确
   3. 扩展性：后续可以轻松添加新的数据库表
   4. 资源优化：CPU 利用率更高，内存占用可控

✦ 这个阶段是整个向量化方案的基础设施，后续的重心算法和 AI 兜底都建立在这个向量索引之上。