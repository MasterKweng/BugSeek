可以。按你现在这条链，**最合适的回写控制面应该放在 `job_runner.py`，重活子进度放在 `DataImpactEngine` / `LineageService`，字段处理类进度放在 `FieldMappingAppService` 这一层。**

我建议这样落。

**总原则**
- **总进度和阶段切换**：由 [job_runner.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/orchestration/job_runner.py) 控
- **阶段内重活子进度**：由 [engine.py](/D:/code/BugSeek/backend/app/domains/data_impact/engine.py) 和 [lineage_service.py](/D:/code/BugSeek/backend/app/domains/data_impact/lineage_service.py) 回传
- **字段规格/召回/排序/AI/落库的批次进度**：由 [services.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/services.py) 这一层的步骤函数回传

---

# 1. 字段提取阶段
文件：
[services.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/services.py)  
[job_runner.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/orchestration/job_runner.py)

## 当前实际入口
- `EngineV2FieldMappingJobRunner.run()`
- `app_service.extract_field_specs(...)`

## 推荐回写点

### 1.1 任务初始化
位置：
[job_runner.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/orchestration/job_runner.py) 的 `_init_stages()`

回写：
- `5%`
- `初始化 engine_v2 阶段任务`

### 1.2 开始提取字段
位置：
`run()` 里，调用 `extract_field_specs(...)` 之前

回写：
- `10%`
- `开始提取接口字段规格`

### 1.3 已加载 definition
位置：
`extract_field_specs_step.run(...)` 内部  
也就是 [services.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/services.py) 调到的 application step 里

回写：
- `15%`
- `已加载 644 个 definition，开始提取 path/query/body 字段`

### 1.4 输入快照保存完成
位置：
现有 `_mark_stage_complete(... Stage.FIELD_EXTRACTION ...)`

保留，但文案改成更清晰：
- `20%`
- `字段提取完成，输入快照已保存`

---

# 2. 规则评分阶段
文件：
[job_runner.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/orchestration/job_runner.py)

这是现在最关键的阶段，因为 42 就卡在这里。

## 2.1 开始规则评分前
位置：
`extract_field_specs` 结束后、`rebuild_lineage_before_run` 判断前

回写：
- `22%`
- `开始规则评分前准备`

---

## 2.2 重建 lineage 资产
文件：
[job_runner.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/orchestration/job_runner.py)  
[engine.py](/D:/code/BugSeek/backend/app/domains/data_impact/engine.py)  
[lineage_service.py](/D:/code/BugSeek/backend/app/domains/data_impact/lineage_service.py)

### 2.2.1 开始重建
位置：
`_rebuild_lineage_assets(...)` 一进入

回写：
- `24%`
- `开始重建血缘资产`

### 2.2.2 workspace 准备完成
位置：
`_resolve_workspace_root(...)` 成功返回后

回写：
- `25%`
- `代码仓工作区已准备完成`

### 2.2.3 SQL lineage 是否跳过
位置：
`_rebuild_lineage_assets(...)` 里，根据 `execution_ids` 判断

回写二选一：

有 execution_ids：
- `26%`
- `开始构建 SQL lineage`

无 execution_ids：
- `26%`
- `未提供 execution_ids，跳过 SQL lineage`

### 2.2.4 SQL lineage 批次进度
位置：
`_rebuild_lineage_assets(...)` 内层循环  
`for definition_id in definition_ids` + `for execution_id in execution_ids`

回写频率：
- 每 20 或 50 个 definition 一次

文案示例：
- `正在构建 SQL lineage（definition 120 / 644, execution 2 / 3）`

### 2.2.5 Code lineage 开始
位置：
`_rebuild_lineage_assets(...)` 里，进入 `engine.build_lineage_assets(... workspace_root=...)` 前

回写：
- `28%`
- `开始构建 Code lineage`

### 2.2.6 Code lineage 批次进度
位置：
还是 `_rebuild_lineage_assets(...)` 的 definition 循环里  
因为现在是每个 definition 都调一次：
- `engine.build_lineage_assets(...)`

回写频率：
- 每 20 或 50 个 definition 一次

文案示例：
- `正在构建 Code lineage（214 / 644）`

这是最重要的点。

### 2.2.7 单个 definition 的 workspace 扫描细节
位置：
[engine.py](/D:/code/BugSeek/backend/app/domains/data_impact/engine.py) 的 `build_lineage_assets(...)`  
再往下是 [lineage_service.py](/D:/code/BugSeek/backend/app/domains/data_impact/lineage_service.py) 的 `build_and_persist_code_lineage_from_workspace(...)`

这里**不建议再写数据库级任务进度**，只建议写日志或回传 summary。  
原因：
- 文件级扫描太细
- 如果每个文件都回写任务，会把 DB 打爆

这里更适合聚合成返回值，例如：
- `scanned_files`
- `created_edges`
- `fallback_files`

然后由 `_rebuild_lineage_assets(...)` 汇总后再统一回写。

### 2.2.8 重建完成汇总
位置：
`_rebuild_lineage_assets(...)` 结束前

回写：
- `35%`
- `血缘资产重建完成：SQL edges=0，Code edges=913`

---

## 2.3 提取字段规格并做规则评分
位置：
`run()` 里 `extract_field_specs` 后半段，以及 `_mark_stage_complete(... Stage.RULE_SCORING ...)`

### 推荐补点

#### 开始字段规格整理
- `37%`
- `开始整理字段规格`

#### 字段规格处理中
如果能在 `extract_field_specs_step.run(...)` 内按 field batch 回写：
- `39%`
- `字段规格处理中（2200 / 4747）`

#### 规则评分完成
保留现有完成点，但改文案：
- `45%`
- `字段规格已提取，规则评分完成`

---

# 3. 智能筛选阶段
文件：
[services.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/services.py)

## 当前入口
- `build_recall_artifacts(...)`
- `rank_recall_items(...)`

## 推荐回写点

### 3.1 开始召回
位置：
调用 `build_recall_artifacts(...)` 前

回写：
- `50%`
- `开始召回候选映射`

### 3.2 召回分层进度
位置：
`build_recall_artifacts_step.run(...)` 内部

建议按证据源分点：
- `53%`：`正在构建 lexical / history 候选`
- `58%`：`正在注入 SQL / Code / Runtime evidence`
- `63%`：`正在聚合召回候选`

### 3.3 排序
位置：
`rank_recall_items(...)` 前后

回写：
- `67%`
- `正在排序候选`
- `70%`
- `智能筛选完成`

---

# 4. AI 优化阶段
文件：
[services.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/services.py)

## 当前入口
- `optimize_ranked_items(...)`

## 推荐回写点

### 4.1 开始 AI 筛选
位置：
进入 `optimize_ranked_items(...)` 前

回写：
- `73%`
- `开始筛选需要 AI 优化的字段`

### 4.2 AI 批处理
位置：
`optimize_ranked_items_step.run(...)` 内部  
如果是按批次调用模型，就按 batch 回写

示例：
- `77%`
- `AI 优化中（batch 3 / 12）`

### 4.3 AI 跳过情况
如果 `use_ai=false` 或没达到阈值

回写：
- `80%`
- `无字段达到 AI 优化条件，跳过 AI 阶段`

### 4.4 AI 完成
现有 `_mark_stage_complete(... Stage.AI_OPTIMIZATION ...)` 保留，但文案更统一：
- `85%`
- `AI 优化完成`

---

# 5. 结果合并阶段
文件：
[job_runner.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/orchestration/job_runner.py)  
[services.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/services.py)

## 当前入口
- `build_suggestions_from_ranked_items(...)`
- `save_artifact(...)`
- `_mark_stage_complete(... Stage.RESULT_MERGE ...)`

## 推荐回写点

### 5.1 开始生成建议
位置：
`build_suggestions_from_ranked_items(...)` 前

回写：
- `89%`
- `开始生成最终建议`

### 5.2 suggestions 生成中
如果 `build_suggestions_step.run(...)` 可按批处理

回写：
- `93%`
- `正在生成最终建议（2400 / 4747）`

### 5.3 落库 artifacts / result payload
位置：
`save_artifact(... final_suggestions / result_payload ...)` 之间

回写：
- `97%`
- `正在写入最终产物与统计信息`

### 5.4 结果合并完成
保留现有最终点：
- `100%`
- `最终建议已生成`

---

# 6. 具体应该在哪些函数里加

## 最适合新增“统一回写 helper”的地方
文件：
[job_runner.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/orchestration/job_runner.py)

建议新增一个统一 helper，概念上类似：
- `self._update_progress(task, progress, message, stage_num=None, stage_progress=None, child_step=None)`

因为这条 runner 已经是阶段编排中心，最适合做统一进度落库。

---

## 按函数落位

### [job_runner.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/orchestration/job_runner.py)
- `run()`
  - 阶段切换
  - 各大步骤开始/结束
- `_rebuild_lineage_assets(...)`
  - 最关键的子进度回写
  - definition 批次进度
  - SQL/Code 分支状态
- `_resolve_workspace_root(...)`
  - workspace 准备完成提示

### [engine.py](/D:/code/BugSeek/backend/app/domains/data_impact/engine.py)
- `build_lineage_assets(...)`
  - 返回单个 definition 的汇总统计
  - 不建议直接频繁写任务进度
  - 建议把结果返回给 `job_runner`

### [lineage_service.py](/D:/code/BugSeek/backend/app/domains/data_impact/lineage_service.py)
- `build_and_persist_code_lineage_from_workspace(...)`
  - 继续保留日志
  - 可增加返回 summary：
    - `scanned_files`
    - `created`
    - `code_edges`
    - `fallback_files`
  - 不建议直接写 `AsyncTask`

### [services.py](/D:/code/BugSeek/backend/app/domains/field_mapping_engine/services.py)
- `extract_field_specs(...)`
- `build_recall_artifacts(...)`
- `rank_recall_items(...)`
- `optimize_ranked_items(...)`
- `build_suggestions_from_ranked_items(...)`

这些函数最适合接受一个可选的 `progress_callback`，然后在 step 内部按批次回调。  
这样不会把进度逻辑散到太多层里。

---

# 7. 最小可落地版本

如果你现在只想补最值的一版，不要一次铺满，我建议只做这 6 个点：

1. `开始提取字段规格`
2. `开始重建血缘资产`
3. `正在构建 Code lineage（x / total）`
4. `血缘资产重建完成`
5. `开始召回候选`
6. `开始生成最终建议`

只做这 6 个点，像任务 42 这种“为什么一直 20%”的问题，基本就能解释清楚。

---

一句话总结：

**进度回写的主控应该放在 `job_runner.py`，而 `DataImpactEngine` 和 `LineageService` 负责返回重建统计，`FieldMappingAppService` 负责字段处理类步骤的批次回调。**  
**最关键的落位是 `_rebuild_lineage_assets(...)`，因为现在最重、最不透明的卡点就在这里。**

如果你愿意，我下一步可以继续给你一版更具体的：
**“阶段 / 子步骤 / progress 百分比 / progress_message / stage_results.children” 数据结构设计稿。**