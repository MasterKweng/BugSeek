# 场景执行改动清单

这份清单不是概念说明，而是按“可以落到代码改造”的方式整理的整改项。

目标不是继续加新功能，而是先把场景执行模块从：

能跑

提升到：

状态正确、结果一致、执行边界清晰、并发安全、历史可复盘

涉及的核心代码点：

- `backend/app/execution/engine.py`
- `backend/app/execution/worker.py`
- `backend/app/api/v1/scenarios.py`
- `backend/app/api/v1/execution_triggers.py`
- `backend/app/celery/tasks/ai_tasks.py`
- `backend/app/api/v1/task_status.py`

---

## 总原则

这次整改先统一三件事，再做具体实现：

### 1. 统一状态模型

后续所有同步接口、异步接口、任务状态、回调 payload 都统一使用：

- `status`：生命周期状态
  - `pending / running / completed / failed / cancelled`
- `result_status`：测试结果状态
  - `passed / failed / partial_failed / skipped`
- `success`：布尔值，供前端和上层调用方快速判断
- `error_message`：结构化失败描述的入口字段

### 2. 先明确 execution 实体边界

当前系统里同时存在：

- 场景级 `TestExecution`
- 节点执行时产生的 `single` 类型 `TestExecution`
- 场景级 `TestExecutionResult`

这次整改必须先明确采用哪种模型。

推荐采用：

**保留父子 execution 结构。**

即：

- 场景执行只有 1 条父 `TestExecution(execution_type=scenario)`
- 每个节点对应 1 条子 `TestExecution(execution_type=single, parent_execution_id=父 execution.id)`
- 场景汇总页读父 execution
- 节点明细可从 `TestExecutionResult` 或子 execution 两种维度查看

原因：

- 当前 `CaseExecutor` 已经有完整单用例 execution 记录逻辑，直接废弃会改动过大
- 父子 execution 结构更利于复用现有单用例执行结果
- 也利于后续做节点重跑、断点续跑、执行链路追踪

但要收口到：

**一次场景执行只允许有 1 条父 scenario execution。**

### 3. 场景执行器不再自行新建“第二条 scenario execution”

同步触发和异步触发都先创建父 execution，然后 `ScenarioExecutor` 只负责更新这条 execution，并写入场景级结果。

---

## P0：必须先修

## P0-1 修正场景执行状态语义

### 问题

`ScenarioExecutor.execute_scenario()` 当前把 `final_status` 固定写成 `"completed"`。

### 代码点

- `backend/app/execution/engine.py`
  - `ScenarioExecutor.execute_scenario`
  - `_aggregate_result_status`

### 改动

1. 把状态拆成两层：
   - `status`
   - `result_status`
2. 增加 `success` 字段
3. 规则统一为：
   - 执行链正常结束，但有断言失败或节点失败：
     - `status=completed`
     - `result_status=failed 或 partial_failed`
     - `success=false`
   - 图校验失败、环境不存在、执行器异常、执行前准备失败：
     - `status=failed`
     - `success=false`
   - 全部通过：
     - `status=completed`
     - `result_status=passed`
     - `success=true`

### 验收

- 场景中某个节点断言失败时，不再把它误判成成功执行
- 上层调用不再依赖 `summary.failed == 0` 这种绕路判断

---

## P0-2 收口父 scenario execution

### 问题

异步触发时已经先创建了一条 scenario execution，但 `ScenarioExecutor` 内部又会再新建一条 scenario execution。

### 代码点

- `backend/app/api/v1/execution_triggers.py`
- `backend/app/celery/tasks/ai_tasks.py`
- `backend/app/execution/engine.py`

### 改动

1. 同步执行入口也改成“先创建父 execution，再执行”。
2. `ScenarioExecutor.execute_scenario()` 增加参数：
   - `execution_id: Optional[int] = None`
3. `ScenarioExecutor` 不再新建 scenario execution。
4. `_save_scenario_execution_record()` 改成：
   - 如果传入 `execution_id`，则更新已有父 execution
   - 同时重建或刷新该 execution 下的场景级 `TestExecutionResult`
5. 异步和同步统一走一套父 execution 生命周期。

### 明确保留

- 节点执行过程中，`CaseExecutor.execute_case()` 仍然可以保留 child execution
- 但 child execution 的 `parent_execution_id` 必须指向父 scenario execution

### 验收

- 一次场景执行只产生 1 条父 scenario execution
- 同步入口、异步入口、回调、任务状态查询都指向同一父 execution

---

## P0-3 打通父子 execution 关系

### 问题

当前节点执行产生的 `single` execution，没有挂到父 scenario execution 上。

### 代码点

- `backend/app/execution/engine.py`
- `backend/app/execution/worker.py`

### 改动

1. `ScenarioExecutor.execute_scenario()` 接收父 `execution_id`
2. `_execute_single_node()` 调用 `CaseExecutor.execute_case()` 时传入：
   - `parent_execution_id=execution_id`
   - 必要时传入 `source_execution_id`
3. 场景级 `TestExecutionResult.target_id` 继续对应 `ScenarioNode.id`
4. 子 execution 保留自己完整的单用例请求响应信息

### 验收

- 可以从父 execution 追到所有节点对应的 child execution
- 执行历史不会再出现“父记录和节点单用例记录彼此割裂”

---

## P0-4 同步统一状态模型到外围接口

### 问题

就算修了执行器，如果不同接口还继续按旧逻辑消费 `status`，状态语义还是会乱。

### 代码点

- `backend/app/api/v1/scenarios.py`
- `backend/app/api/v1/execution_triggers.py`
- `backend/app/celery/tasks/ai_tasks.py`
- `backend/app/api/v1/task_status.py`

### 改动

统一所有接口返回字段：

- `status`
- `result_status`
- `success`
- `summary`
- `error_message`

具体要求：

1. 同步执行接口返回这套字段
2. 异步触发接口返回这套字段
3. Celery callback payload 返回这套字段
4. `task_status` 统一展示这套字段
5. 前端不要再用 `status == completed && failed == 0` 自己推导成功

### 验收

- 前端和调用方只需要一套解释逻辑
- 场景任务状态和执行详情不再互相矛盾

---

## P0-5 真正落地 `execution_mode`

### 问题

当前 `execution_mode` 只是字段存在，执行器统一按 DAG level 并发。

### 代码点

- `backend/app/execution/engine.py`

### 改动

新增统一计划函数，例如：

```python
def _get_execution_plan(self, scenario, nodes) -> List[List[Dict[str, Any]]]:
```

规则：

- `sequential`
  - 按 `step_order, id` 串行执行
  - 不允许 level 内并发
  - `depends_on` 仍用于合法性校验
- `dag`
  - 继续按依赖构建 level
  - 同一 level 内允许并发

同时返回：

- `execution_mode_effective`

### 验收

- 同一批无依赖节点在 `sequential` 下不并发
- 同一批无依赖节点在 `dag` 下可并发

---

## P0-6 落地场景级 / 节点级 timeout 与 retry

### 问题

字段存在，但执行时没有真正生效。

### 代码点

- `backend/app/execution/engine.py`
- `backend/app/execution/worker.py`

### 改动

#### timeout

- 场景级 timeout：包住整个 `execute_scenario`
- 节点级 timeout：包住 `_execute_single_node`

#### retry

- 在 `_execute_single_node` 外层增加 `_run_node_with_retry`
- retry 次数优先级：
  - 节点配置优先
  - 否则取场景级配置

#### 失败分类

- 可重试：
  - 网络错误
  - 连接失败
  - timeout
  - 429
  - 临时 5xx
- 不可重试：
  - 断言失败
  - 变量缺失
  - 图结构错误
  - 环境/配置错误

### 验收

- 配置 `retry_count=2` 的节点，最多执行 3 次
- 断言失败默认不重试
- timeout 在结果里有明确分类

---

## P0-7 让节点级 override 真正生效

### 问题

节点有 `assertion_overrides` 和 `extract_rules`，但目前执行时没有真正覆盖 `ApiCase` 默认配置。

### 代码点

- `backend/app/execution/engine.py`
- `backend/app/execution/worker.py`

### 改动

采用运行态覆盖，不改数据库原始 `ApiCase`：

1. `_execute_single_node()` 中构造 `execution_case`
2. 如果节点上有：
   - `assertion_overrides`，覆盖 `execution_case.assertion_rules`
   - `extract_rules`，覆盖 `execution_case.extraction_rules`
3. `CaseExecutor.execute_case()` 使用这个 `execution_case`

### 验收

- 同一个 `ApiCase` 被不同场景节点引用时，可以带不同断言和提取规则
- 覆盖只影响本次执行

---

## P0-8 并发节点使用独立 Session

### 问题

同一 level 并发执行时，共享同一个 `Session`，而 `CaseExecutor` 内部会频繁 `commit/rollback`。

### 代码点

- `backend/app/execution/engine.py`
- `backend/app/execution/worker.py`

### 改动

1. 场景级汇总仍保留主 session
2. 每个并发节点执行时内部创建独立 session
3. 节点结束后关闭自己的 session
4. 场景汇总在主 session 上统一更新父 execution

### 验收

- 单节点 rollback 不影响其他并发节点
- 不再出现 session 并发污染风险

---

## P1：应尽快补

## P1-1 保存场景时做设计时校验

### 问题

当前场景创建和更新基本是直接写库，很多错误会在执行时才暴露。

### 代码点

- `backend/app/api/v1/scenarios.py`

### 改动

抽出公共校验函数，例如：

```python
def validate_scenario_nodes(...)
```

保存前校验内容至少包括：

- `node_key` 非空
- `node_key` 唯一
- `depends_on` 引用存在
- 无自环
- 无环
- 至少一个启用节点
- `ref_type` 合法
- `ref_id` 可解析
- 目标对象属于同项目
- 环境可用且属于同项目

### 验收

- 非法图不能保存
- 非法目标不能保存
- 项目和环境错配在保存时就被拦住

---

## P1-2 明确 `api_definition` 节点如何选 case

### 问题

当前默认选第一条 active case，执行目标不确定。

### 代码点

- `backend/app/execution/engine.py`

### 改动

推荐二选一：

#### 方案 A

场景节点只允许 `ref_type=api_case`

#### 方案 B

保留 `api_definition`，但要求明确选择策略，例如：

- `preferred_case_id`
- `case_selection_strategy`

默认不允许 silent fallback。

### 验收

- 场景设计者能明确知道节点最终跑哪条 case
- 后续新增 case 不会悄悄改变历史场景行为

---

## P1-3 标准化失败类型

### 问题

现在大多只有 `error_message`，不利于聚类、重试和报表。

### 代码点

- `backend/app/execution/engine.py`
- `backend/app/execution/worker.py`

### 改动

统一增加：

- `error_type`
- `error_category`

推荐分类：

- `validation_error`
- `environment_error`
- `dependency_error`
- `auth_error`
- `network_error`
- `timeout_error`
- `assertion_error`
- `system_error`

### 验收

- 节点结果里有结构化错误类型
- 可直接支持后续 retry 和 RCA

---

## P1-4 增加可复盘的执行快照

### 问题

当前已有基础结果记录，但缺少“当次到底执行了什么”的 effective snapshot。

### 代码点

- `backend/app/execution/engine.py`

### 改动

在父 execution 上保存：

- scenario snapshot
- nodes snapshot
- resolved environment
- initial variables
- effective execution mode

在节点结果上保存：

- effective case id
- effective definition id
- rendered input mapping
- effective assertion rules
- effective extraction rules
- retry_count_effective
- timeout_effective

### 验收

- 历史 execution 可独立复盘
- 即使场景定义后来改了，也能知道当时到底执行了什么

---

## P2：后续增强

## P2-1 节点重跑 / 断点续跑

### 改动

- 支持从某个 `node_key` 继续执行
- 支持只重跑失败节点
- 支持注入已有 context snapshot

## P2-2 场景预检接口

### 改动

新增类似：

- `POST /scenarios/{id}/validate`

执行前做 dry-run：

- 图是否合法
- 节点目标是否可解析
- 环境是否存在
- 变量占位符是否缺失
- case 是否 active

## P2-3 场景版本冻结

### 改动

增加场景版本或发布快照机制：

- `draft`
- `published`
- `archived`

执行时优先跑发布态快照，而不是总跑最新编辑态。

## P2-4 场景变更影响分析

### 改动

结合资产关系和场景定义，识别：

- 变更了哪些节点
- 影响了哪些 case / definition
- 是否影响已有回归基线

---

## 推荐实施顺序

### 第一波

- P0-1 状态语义
- P0-2 收口父 scenario execution
- P0-3 打通父子 execution
- P0-4 统一外围状态模型
- P0-5 `execution_mode` 落地

### 第二波

- P0-6 timeout / retry
- P0-7 节点 override 生效
- P0-8 独立 session

### 第三波

- P1-1 保存前设计时校验
- P1-2 明确节点 target
- P1-3 标准化失败类型
- P1-4 执行快照

### 第四波

- 全部 P2

---

## 最终落地建议

这次场景执行整改，最关键的不是再往上堆能力，而是先把底层边界做硬：

- 一次场景执行只有 1 条父 execution
- 节点 execution 和父 execution 的关系是明确的
- `status` 和 `result_status` 的语义是稳定的
- 节点配置真的能影响执行
- 并发执行不会互相污染
- 历史结果可以复盘

只有先把这些补齐，场景执行模块才真正适合作为 V2.0 的主引擎继续演进。
