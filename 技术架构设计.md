# BugSeek 项目架构（个人使用 + PostgreSQL 版本）

## 一、整体架构

```
┌─────────────────────────────────────────────────────────┐
│                        前端层                            │
│              React + TypeScript + Ant Design            │
│                   (Vite 构建)                            │
└────────────────────────┬────────────────────────────────┘
                         │ HTTP/WebSocket
┌────────────────────────┴────────────────────────────────┐
│                        网关层                            │
│                    FastAPI 应用                          │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────┐
│                      业务模块层                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │需求洞察  │ │代码质量  │ │接口集成  │ │UI 测试   │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│  ┌──────────┐ ┌──────────┐                             │
│  │流程编排  │ │基础设施  │                             │
│  └──────────┘ └──────────┘                             │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────┐
│                      服务层                              │
│  AI 服务 │ Git 服务 │ 文件服务 │ 日志服务              │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────┐
│                      数据层                              │
│              PostgreSQL + 本地文件存储                   │
└─────────────────────────────────────────────────────────┘
```

## 二、项目目录结构

```
BugSeek/
├── frontend/                          # 前端项目
│   ├── public/
│   │   └── favicon.ico
│   ├── src/
│   │   ├── assets/                   # 静态资源
│   │   ├── components/               # 通用组件
│   │   │   ├── Layout/
│   │   │   ├── Header/
│   │   │   ├── Sidebar/
│   │   │   └── common/
│   │   ├── pages/                    # 页面
│   │   │   ├── Requirement/
│   │   │   ├── CodeQuality/
│   │   │   ├── ApiIntegration/
│   │   │   ├── UiTest/
│   │   │   ├── Orchestrator/
│   │   │   └── Dashboard/
│   │   ├── services/                 # API 调用
│   │   │   ├── api.ts
│   │   │   └── types.ts
│   │   ├── utils/                    # 工具函数
│   │   ├── hooks/                    # 自定义 Hooks
│   │   ├── router/                   # 路由配置
│   │   ├── store/                    # 状态管理
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── .env
│   ├── .env.local
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   └── index.html
│
├── backend/                           # 后端项目
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                   # FastAPI 入口
│   │   ├── config.py                 # 配置管理
│   │   ├── dependencies.py           # 依赖注入
│   │   │
│   │   ├── api/                      # API 路由
│   │   │   ├── __init__.py
│   │   │   ├── v1/
│   │   │   │   ├── __init__.py
│   │   │   │   ├── requirement.py
│   │   │   │   ├── code_quality.py
│   │   │   │   ├── api_integration.py
│   │   │   │   ├── ui_test.py
│   │   │   │   ├── orchestrator.py
│   │   │   │   └── dashboard.py
│   │   │   └── deps.py
│   │   │
│   │   ├── modules/                  # 业务模块
│   │   │   ├── __init__.py
│   │   │   ├── requirement/          # 需求洞察
│   │   │   │   ├── __init__.py
│   │   │   │   ├── service.py
│   │   │   │   ├── schemas.py
│   │   │   │   └── models.py
│   │   │   ├── code_quality/         # 代码质量
│   │   │   │   ├── __init__.py
│   │   │   │   ├── service.py
│   │   │   │   ├── schemas.py
│   │   │   │   └── models.py
│   │   │   ├── api_integration/      # 接口集成
│   │   │   │   ├── __init__.py
│   │   │   │   ├── service.py
│   │   │   │   ├── schemas.py
│   │   │   │   └── models.py
│   │   │   ├── ui_test/              # UI 测试
│   │   │   │   ├── __init__.py
│   │   │   │   ├── service.py
│   │   │   │   ├── schemas.py
│   │   │   │   └── models.py
│   │   │   ├── orchestrator/         # 流程编排
│   │   │   │   ├── __init__.py
│   │   │   │   ├── service.py
│   │   │   │   ├── schemas.py
│   │   │   │   └── models.py
│   │   │   └── infra/                # 基础设施
│   │   │       ├── __init__.py
│   │   │       ├── service.py
│   │   │       ├── schemas.py
│   │   │       └── models.py
│   │   │
│   │   ├── services/                 # 核心服务
│   │   │   ├── __init__.py
│   │   │   ├── ai_service.py         # AI 服务
│   │   │   ├── git_service.py        # Git 服务
│   │   │   ├── file_service.py       # 文件服务
│   │   │   └── log_service.py        # 日志服务
│   │   │
│   │   ├── db/                       # 数据库
│   │   │   ├── __init__.py
│   │   │   ├── base.py               # Base 模型
│   │   │   ├── session.py            # 数据库会话
│   │   │   └── init_db.py            # 初始化脚本
│   │   │
│   │   ├── core/                     # 核心功能
│   │   │   ├── __init__.py
│   │   │   ├── security.py           # 安全相关
│   │   │   ├── config.py             # 配置类
│   │   │   └── exceptions.py         # 异常处理
│   │   │
│   │   └── utils/                    # 工具函数
│   │       ├── __init__.py
│   │       ├── logger.py             # 日志工具
│   │       ├── file_utils.py         # 文件工具
│   │       └── common.py             # 通用工具
│   │
│   ├── tests/                        # 测试
│   │   ├── unit/
│   │   ├── integration/
│   │   └── conftest.py
│   │
│   ├── scripts/                      # 脚本
│   │   ├── init_db.py
│   │   └── seed_data.py
│   │
│   ├── logs/                         # 日志目录
│   ├── uploads/                      # 上传文件目录
│   ├── data/                         # 数据目录
│   │
│   ├── requirements.txt              # Python 依赖
│   ├── pyproject.toml                # 项目配置
│   ├── .env.example                  # 环境变量示例
│   └── README.md
│
├── docker/                           # Docker 配置
│   ├── Dockerfile.backend
│   ├── Dockerfile.frontend
│   └── docker-compose.yml
│
├── docs/                             # 文档（可选）
│   └── architecture.md
│
├── .gitignore
├── README.md
└── docker-compose.yml
```

## 三、技术选型

### 前端技术栈
| 类型 | 技术选型 | 说明 |
|------|---------|------|
| 框架 | React 18 | UI 框架 |
| 语言 | TypeScript 5.x | 类型安全 |
| 构建工具 | Vite 5.x | 快速构建 |
| UI 组件库 | Ant Design 5.x | 企业级组件 |
| 路由 | React Router 6.x | 路由管理 |
| 状态管理 | Zustand / Jotai | 轻量级状态管理 |
| HTTP 客户端 | Axios | API 请求 |
| 图表 | ECharts | 数据可视化 |
| 流程图 | React Flow | 流程图展示 |
| 代码编辑器 | Monaco Editor | 代码编辑 |

### 后端技术栈
| 类型 | 技术选型 | 说明 |
|------|---------|------|
| 框架 | FastAPI | 高性能 API 框架 |
| 语言 | Python 3.10+ | 后端语言 |
| 数据库 | PostgreSQL 15+ | 关系型数据库 |
| ORM | SQLAlchemy 2.x | 数据库 ORM |
| 迁移工具 | Alembic | 数据库迁移 |
| 异步任务 | FastAPI BackgroundTasks | 简单异步处理 |
| AI 引擎 | LangChain + DeepSeek/Ollama | LLM 集成 |
| 测试框架 | pytest | 单元测试 |
| UI 自动化 | Playwright | UI 测试 |
| 日志 | loguru | 日志管理 |
| 文档解析 | python-docx, PyPDF2 | 文档处理 |
| 代码分析 | ruff, ast | 代码分析 |
| 依赖图 | networkx | 图算法 |

## 四、数据库设计（PostgreSQL）

**核心表结构**：
```
projects                    # 项目表
requirements                # 需求表
test_cases                  # 测试用例表
code_reviews                # 代码审查表
api_endpoints               # 接口表
api_test_scripts            # 接口测试脚本表
ui_test_scripts             # UI 测试脚本表
test_executions             # 测试执行记录表
test_results                # 测试结果表
pipelines                   # 流水线表
pipeline_runs               # 流水线执行记录表
quality_gates               # 质量门禁表
mock_services               # Mock 服务表
test_data                   # 测试数据表
logs                        # 日志表
```

## 五、核心依赖清单

### 后端依赖
```txt
# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 数据库
sqlalchemy>=2.0.23
asyncpg>=0.29.0
alembic>=1.12.0

# AI & LLM
langchain>=0.1.0
openai>=1.3.0
ollama>=0.1.0

# 文档解析
python-docx>=1.1.0
PyPDF2>=3.0.1

# 代码分析
ruff>=0.1.0
sqlparse>=0.4.4

# 测试框架
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
playwright>=1.40.0

# 工具库
faker>=20.1.0
loguru>=0.7.2
networkx>=3.2.1
jinja2>=3.1.2
httpx>=0.25.0
python-multipart>=0.0.6

# 其他
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
```

### 前端依赖
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "antd": "^5.12.0",
    "axios": "^1.6.0",
    "echarts": "^5.4.0",
    "reactflow": "^11.10.0",
    "@monaco-editor/react": "^4.6.0",
    "zustand": "^4.4.0",
    "@tanstack/react-query": "^5.12.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.3.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0"
  }
}
```

## 六、环境配置

### 后端环境变量
```bash
# .env
DATABASE_URL=postgresql://bugseek:bugseek@localhost:5432/bugseek
SECRET_KEY=your-secret-key-here
AI_API_KEY=your-ai-api-key
AI_API_BASE=https://api.deepseek.com
UPLOAD_DIR=./uploads
LOG_DIR=./logs
```

### 前端环境变量
```bash
# .env
VITE_API_BASE_URL=http://localhost:8000/api/v1
```

## 七、启动方式

### 方式一：本地开发（推荐）

```bash
# 1. 启动 PostgreSQL (Docker)
docker run -d \
  --name bugseek-postgres \
  -e POSTGRES_DB=bugseek \
  -e POSTGRES_USER=bugseek \
  -e POSTGRES_PASSWORD=bugseek \
  -p 5432:5432 \
  postgres:15

# 2. 后端启动
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 3. 前端启动（新终端）
cd frontend
npm install
npm run dev
```

### 方式二：Docker Compose

```bash
docker-compose up -d
```

## 八、架构特点

| 特性 | 说明 |
|------|------|
| **前后端分离** | React + FastAPI，独立开发部署 |
| **RESTful API** | 统一 API 设计，自动生成文档 |
| **类型安全** | TypeScript + Pydantic 双重类型保障 |
| **异步支持** | FastAPI 原生异步，高并发处理 |
| **模块化设计** | 清晰的模块划分，易于扩展 |
| **零配置启动** | 本地开发一键启动 |
| **生产就绪** | 支持 Docker 部署 |