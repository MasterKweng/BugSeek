# BugSeek V2.0 补齐清单

基于当前仓库代码与 `V2.0 技术架构设计.md` 的差异分析，按优先级整理详细开发任务清单。

目标原则：
- 先修阻断主链路的问题，再补平台能力。
- 先打通 `意图 -> 场景 -> 执行 -> 影响分析`，再补智能化和可视化。
- 每个任务都尽量明确到代码位置、产出物、验收标准。

---

## P0：修复阻断主链路的问题

### P0-1 意图工作台前后端协议对齐

#### 问题
- 前端生成场景后读取 `response.data.draft`，但后端当前返回结构不是 `draft`。
- 前端确认保存时传的是 `{ draft: generatedScenario }`，后端确认接口实际按 `scenario_data.get("scenario") / get("nodes")` 读取。

#### 涉及文件
- `frontend/src/pages/scenario/IntentWorkbench.tsx`
- `backend/app/api/v1/intent_workbench.py`
- 如有需要：`frontend/src/types/scenario.ts`

#### 开发任务
1. 统一 `generate-scenario` 返回结构。
2. 推荐后端返回：
   - `data.draft.scenario`
   - `data.draft.nodes`
   - `data.draft.reasoning`
   - `data.draft.candidate_apis`
3. 统一 `confirm-scenario` 入参结构。
4. 前端确认保存时直接提交 `draft` 对象，或后端兼容 `{ draft: ... }`。
5. 清理前端里对 `scenario.id` 的错误依赖。
   - 当前生成阶段拿到的是草稿，不一定有数据库 `scenario_id`。
6. 明确字段映射任务是在“草稿阶段启动”还是“确认保存后启动”。
   - 推荐放到“确认保存后启动”，避免使用不存在的 `scenario.id`。

#### 产出物
- 对齐后的接口响应与请求结构。
- 前端生成、预览、确认保存流程可用。

#### 验收标准
- 在页面输入自然语言后，能成功展示场景草稿。
- 点击“确认保存”后，场景成功入库。
- 不出现 `draft`、`scenario` 字段不一致导致的空数据或报错。

---

### P0-2 修复 `ScenarioGenerator` 成功路径逻辑

#### 问题
- `ScenarioGenerator.generate()` 中，AI 成功后解析与返回逻辑存在缩进异常，成功路径可能无法正常返回。

#### 涉及文件
- `backend/app/domains/ai_testing/scenario_generator.py`
- `backend/tests/test_intent_to_scenario.py`

#### 开发任务
1. 梳理 `generate()` 的完整流程：
   - 项目校验
   - API 召回
   - AI 选择 API
   - AI 生成场景
   - 结果解析
   - 结构校验
   - 返回 `ScenarioDraft`
2. 修复 `if not ai_result.get("success")` 后的代码块缩进。
3. 增加失败分支与成功分支的单元测试。
4. 增加 AI 返回 markdown/json/string 三种场景的解析测试。
5. 增加“候选 API 全部被过滤后”的测试。

#### 产出物
- 可稳定返回 `ScenarioDraft` 的 `ScenarioGenerator`。
- 对应测试用例。

#### 验收标准
- `generate()` 在 AI 成功返回时，必然返回合法 `ScenarioDraft`。
- `test_intent_to_scenario` 覆盖成功/失败/脏数据三类输入。

---

### P0-3 修复 CI/CD 触发接口与执行模型不一致

#### 问题
- `execution_triggers.py` 往 `TestExecution` 写入了当前模型中不存在的字段，如 `scenario_id`、`created_by`、`celery_task_id`、`duration_ms`、`summary`、`results` 等上下文字段使用不一致。

#### 涉及文件
- `backend/app/api/v1/execution_triggers.py`
- `backend/app/platform/db/base.py`
- `backend/app/celery/tasks/ai_tasks.py`
- `backend/app/api/v1/scenarios.py`

#### 开发任务
1. 统一 `TestExecution` 模型用途。
   - 明确 `target_id + execution_type` 是否为唯一标准。
2. 修正触发接口中的执行记录创建逻辑。
3. 决定是否需要在模型中新增：
   - `celery_task_id`
   - `error_message`
   - `summary`
   - `results`
4. 如果不新增字段，则重构接口返回结构，改为查询 `test_execution_results` 聚合。
5. 修复 `get_trigger_result()` 中读取不存在字段的问题。
6. 为同步执行和异步执行分别补测试。

#### 产出物
- 可用的 CI/CD 触发接口。
- 与数据库模型一致的执行记录结构。

#### 验收标准
- 同步触发可成功执行场景并返回结果。
- 异步触发可创建任务、记录 Celery Task ID、轮询获得状态。
- 触发接口不再因字段不匹配报错。

---

### P0-4 修复 Knowledge Graph API 测试失败

#### 问题
- 当前图谱 API 相关测试存在 2 个失败，说明 API 层、查询引擎、测试桩之间未对齐。

#### 涉及文件
- `backend/app/domains/knowledge_graph/query_engine.py`
- `backend/app/api/v1/knowledge_graph.py`
- `backend/tests/test_knowledge_graph_api.py`
- `backend/tests/test_knowledge_graph_query_engine.py`

#### 开发任务
1. 梳理 `GraphQueryEngine` 的输入输出契约。
2. 修复 API 测试中使用 `GraphNode` 代替 `GraphEdge` 的问题，或增强测试桩。
3. 增加 `get_tables_by_api`、`get_apis_by_table` 的边界测试：
   - 无边
   - 单边
   - 多边
4. 增加 API 层返回结构断言。
5. 将当前失败用例纳入固定回归集合。

#### 产出物
- 全绿的 Knowledge Graph 基础测试。

#### 验收标准
- `test_knowledge_graph_api.py` 全部通过。
- `test_knowledge_graph_query_engine.py` 全部通过。

---

### P0-5 建立端到端主链路冒烟测试

#### 问题
- 当前测试偏模块级，缺少真正覆盖 V2 核心流程的一条可执行冒烟链。

#### 涉及文件
- `backend/tests/test_intent_to_scenario.py`
- `backend/tests/test_scenario_executor.py`
- 新增建议：`backend/tests/test_v2_main_flow_smoke.py`

#### 开发任务
1. 增加最小冒烟流程：
   - 输入意图
   - 生成场景草稿
   - 保存场景
   - 执行场景
   - 获取执行结果
2. 如条件允许，再增加：
   - Data Impact 分析
   - Knowledge Graph 回写校验
3. 使用 mock 隔离 LLM 与外部依赖。
4. 统一测试运行方式，补 `PYTHONPATH` 或 `pytest.ini` 配置，避免 `ModuleNotFoundError: app`。

#### 产出物
- 主链路冒烟测试文件。
- 测试运行说明或配置文件。

#### 验收标准
- 本地和 CI 中都能稳定运行最小 V2 冒烟链。

---

## P1：打通 V2 主闭环

### P1-1 打通意图到场景的完整链路

#### 涉及文件
- `backend/app/api/v1/intent_workbench.py`
- `backend/app/domains/ai_testing/engine.py`
- `backend/app/domains/ai_testing/scenario_generator.py`
- `backend/app/domains/api_hub/retrieval/service.py`
- `frontend/src/pages/scenario/IntentWorkbench.tsx`

#### 开发任务
1. 明确“意图工作台”和“AI Testing API”的职责边界。
2. 避免重复实现两套“生成场景”逻辑。
3. 推荐统一：
   - `intent_workbench` 负责工作台交互与落库流程
   - `domains/ai_testing` 负责 AI 能力编排
4. 抽象共享 DTO，避免前后端字段名漂移。
5. 在返回结构中明确：
   - 候选 API
   - 推理说明
   - 草稿场景
   - 可保存标记

#### 验收标准
- 一个入口即可稳定完成意图生成场景。
- 不再存在两套接口返回结构差异。

---

### P1-2 强化场景设计器，支持完整节点编辑

#### 涉及文件
- `frontend/src/pages/scenario/ScenarioDesigner.tsx`
- `frontend/src/pages/scenario/ScenarioDetail.tsx`
- `frontend/src/pages/scenario/ScenarioExecution.tsx`
- `backend/app/api/v1/scenarios.py`

#### 开发任务
1. 让前端设计器支持以下字段编辑：
   - `node_key`
   - `node_name`
   - `ref_type`
   - `ref_id`
   - `depends_on`
   - `input_mapping`
   - `extract_rules`
   - `assertion_overrides`
   - `timeout_seconds`
   - `retry_count`
   - `continue_on_failure`
   - `is_enabled`
2. 支持节点排序与 DAG 依赖校验提示。
3. 支持场景级执行配置编辑。
4. 统一前端场景详情页与设计器的数据模型。

#### 验收标准
- 前端能完整编辑后端支持的场景节点结构。
- 保存后重新打开，数据不丢失、不变形。

---

### P1-3 打通执行结果与报告展示

#### 涉及文件
- `backend/app/api/v1/scenarios.py`
- `backend/app/api/v1/reports.py`
- `backend/app/platform/db/base.py`
- `frontend/src/pages/scenario/ScenarioExecution.tsx`

#### 开发任务
1. 统一执行详情查询接口结构。
2. 前端展示：
   - 执行概览
   - 节点状态
   - 请求/响应
   - 提取变量
   - 断言结果
   - 错误信息
3. 报告模块先做 HTML 版可用输出。
4. PDF 报告保留在 P3。

#### 验收标准
- 从场景列表进入执行详情可完整查看一次执行过程。

---

### P1-4 打通 Data Impact 与执行链

#### 涉及文件
- `backend/app/execution/worker.py`
- `backend/app/domains/data_impact/engine.py`
- `backend/app/domains/data_impact/impact_analyzer.py`
- `backend/app/api/v1/data_impact.py`

#### 开发任务
1. 明确 Data Impact 的触发条件：
   - 单接口执行
   - 场景节点执行
2. 统一 `execution_id` 的生成和透传。
3. 明确快照采集前后时机。
4. 明确同步模式和异步模式行为。
5. 对接前端执行详情页，支持查看影响表和影响字段。

#### 验收标准
- 一次接口/场景执行后可查询对应数据影响结果。

---

### P1-5 打通 Knowledge Graph 自动回写

#### 涉及文件
- `backend/app/domains/knowledge_graph/graph_service.py`
- `backend/app/execution/worker.py`
- `backend/app/api/v1/db_schemas.py`
- `backend/app/api/v1/field_mappings.py`
- `backend/app/celery/tasks/knowledge_graph_tasks.py`

#### 开发任务
1. 保证以下来源都可回写图谱：
   - OpenAPI / API 文档
   - DB Schema
   - 字段映射结果
   - 执行数据影响结果
2. 统一 node/edge 去重规则。
3. 补充回写后的查询验证。

#### 验收标准
- 导入 API、上传 DB schema、运行字段映射、执行接口后，图谱中都能看到对应节点/边。

---

## P2：补齐智能能力

### P2-1 完成 AI 自动断言生成

#### 涉及文件
- `backend/app/execution/engine.py`
- `backend/app/domains/ai_testing/assertion_generator.py`
- `backend/app/api/v1/ai_testing.py`
- `backend/tests/test_phase4_ai_testing_engine.py`

#### 开发任务
1. 将 `AIAssistedExecutor.auto_generate_assertions()` 从预留改为实装。
2. 优先支持两类断言：
   - 状态码断言
   - 关键字段存在性/等值断言
3. 将生成结果转成当前系统可落库结构。
4. 增加基于 response schema 的兜底规则。

#### 验收标准
- 给定 API 定义或实际响应，可返回可执行断言集合。

---

### P2-2 完成 AI 失败根因分析

#### 涉及文件
- `backend/app/execution/engine.py`
- `backend/app/domains/ai_testing/failure_analyzer.py`
- `backend/app/api/v1/ai_testing.py`

#### 开发任务
1. 打通执行结果上下文注入。
2. 输出结构统一为：
   - `failure_type`
   - `root_cause`
   - `suggested_fix`
   - `confidence`
3. 先做规则化基础分析：
   - 状态码异常
   - 断言失败
   - 变量缺失
   - 上游节点失败传播
4. 再叠加 LLM 增强解释。

#### 验收标准
- 一次失败执行可得到结构化 RCA 结果。

---

### P2-3 完成 AI 自动变量映射

#### 涉及文件
- `backend/app/execution/engine.py`
- `backend/app/domains/ai_testing/context_builder.py`
- `backend/app/domains/ai_testing/scenario_generator.py`

#### 开发任务
1. 完成 `auto_map_variables()`。
2. 支持根据：
   - 上游响应字段
   - 下游请求字段
   - API schema
   - 字段映射结果
   生成推荐连线。
3. 输出映射建议和置信度。
4. 可与场景设计器联动，支持人工确认。

#### 验收标准
- 上下游节点之间能自动生成一批高置信度变量映射建议。

---

### P2-4 强化 Knowledge Graph 实体和关系

#### 涉及文件
- `backend/app/domains/knowledge_graph/entity_models.py`
- `backend/app/domains/knowledge_graph/builder.py`
- `backend/app/domains/knowledge_graph/graph_service.py`
- `backend/app/platform/db/base.py`

#### 开发任务
1. 在现有 `API/TABLE/FIELD` 基础上扩展：
   - `TEST_CASE`
   - `SCENARIO`
   - `SERVICE`
   - `UI_PAGE`
2. 增加关系类型：
   - `TESTS`
   - `CALLS`
   - `IMPLEMENTS`
   - `CONTAINS`
3. 场景保存或执行后，同步创建场景节点。
4. API 用例创建后，同步创建测试用例节点。

#### 验收标准
- 图谱不再只表达 API 与表关系，而能表达测试资产关系。

---

### P2-5 升级 Vector Search 为可组合检索

#### 涉及文件
- `backend/app/platform/vector/base.py`
- `backend/app/platform/vector/api_strategy.py`
- `backend/app/platform/vector/db_strategy.py`
- `backend/app/platform/vector/factory.py`

#### 开发任务
1. 明确向量检索层标准接口。
2. 增加关键词检索与语义检索的统一封装。
3. 增加简单 hybrid rerank。
4. 明确 `api` 与 `db` 两类索引的生命周期与缓存刷新策略。

#### 验收标准
- API 检索和 DB 字段检索都支持“关键词 + 语义”的组合策略。

---

## P3：补齐平台化体验

### P3-1 重做 Knowledge Graph 前端可视化

#### 涉及文件
- `frontend/src/pages/KnowledgeGraph.tsx`
- `frontend/src/services/knowledgeGraph.ts`
- `frontend/src/components/MainLayout.tsx`

#### 开发任务
1. 从“手动输入 node_id 查询”升级为图形化浏览。
2. 第一版只做三层：
   - API
   - TABLE
   - FIELD
3. 支持：
   - 节点点击查看详情
   - 按类型过滤
   - 查询 API 写表链路
   - 查询表被哪些 API 操作
4. 如果引入 G6，则保持最小可用，不做过度动画。

#### 验收标准
- 用户无需手动输入 UUID，就能浏览图谱关系。

---

### P3-2 增加任务状态实时反馈

#### 涉及文件
- `backend/app/api/v1/field_mappings_async.py`
- `backend/app/api/v1/execution_triggers.py`
- `backend/app/api/v1/sync_tasks.py`
- `frontend/src/pages/FieldMappingSuggestions.tsx`
- `frontend/src/pages/scenario/ScenarioExecution.tsx`

#### 开发任务
1. 先统一轮询接口。
2. 再决定是否补 WebSocket。
3. 支持以下任务状态展示：
   - 字段映射
   - 场景执行
   - 图谱构建
   - 文档同步

#### 验收标准
- 用户可持续看到异步任务进度，不需要刷新整个页面。

---

### P3-3 完成 UI Automation 最小可用版

#### 涉及文件
- 新增建议：`backend/app/domains/ui_testing/`
- 新增建议：`backend/app/api/v1/ui_testing.py`
- `frontend/src/App.tsx`

#### 开发任务
1. 新建 UI Automation 后端模块。
2. 最小版只支持：
   - Playwright 执行单条 UI 用例
   - 回传步骤日志
   - 保存执行结果
3. 前端入口从 `ComingSoon` 改为最小版工作台。
4. 第二阶段再考虑 UI -> API -> DB 联动追踪。

#### 验收标准
- 能运行一条基础 UI 自动化脚本并查看结果。

---

### P3-4 补齐基础设施能力

#### 涉及文件
- `backend/app/core/auth_service.py`
- `backend/app/core/reporting/generator.py`
- `backend/app/platform/config/settings.py`

#### 开发任务
1. 将鉴权缓存从 TODO 变为真实 Redis 读写。
2. 完成 PDF 报告生成。
3. 评估是否需要对象存储承载报告、执行附件、图谱导出。
4. 补充相关配置与运维说明。

#### 验收标准
- 报告、缓存、异步任务、制品存储形成稳定基础设施能力。

---

## 建议执行顺序

### 第一阶段：一周内完成
1. P0-1 意图工作台协议对齐
2. P0-2 修复 `ScenarioGenerator`
3. P0-3 修复 CI/CD 触发接口
4. P0-4 修复 Knowledge Graph API 测试
5. P0-5 建立主链路冒烟测试

### 第二阶段：一到两周完成
1. P1-1 打通意图到场景
2. P1-2 强化场景设计器
3. P1-3 打通执行详情与报告
4. P1-4 打通 Data Impact
5. P1-5 打通 Knowledge Graph 回写

### 第三阶段：两周左右完成
1. P2-1 AI 断言生成
2. P2-2 AI 失败根因分析
3. P2-3 AI 自动变量映射
4. P2-4 图谱实体关系扩展
5. P2-5 检索能力升级

### 第四阶段：按产品节奏推进
1. P3-1 图谱可视化
2. P3-2 实时状态反馈
3. P3-3 UI Automation
4. P3-4 基础设施补齐

---

## 建议先落地的 Top 10 任务

1. 对齐 `intent_workbench` 前后端协议
2. 修复 `ScenarioGenerator.generate()` 成功路径
3. 修复 `execution_triggers` 与 `TestExecution` 模型不一致
4. 修复 Knowledge Graph API 测试失败
5. 建立 `intent -> scenario -> execute` 冒烟测试
6. 强化场景设计器字段编辑能力
7. 打通 Data Impact 查询与前端展示
8. 打通 Knowledge Graph 回写链路
9. 实装 AI 断言生成
10. 实装 AI 失败根因分析

---

## 备注

- 当前最重要的不是继续扩模块，而是先把已有能力收口。
- 从投资回报比看，优先级应始终是：
  - 主链路稳定性
  - 数据模型一致性
  - 测试覆盖
  - 智能能力增强
  - 平台化体验









• 已完成。

  本轮补齐

  - P3-3 UI Automation
      - 新增后端执行与持久化：backend/app/domains/ui_testing/service.py
      - 新增请求/结果模型：backend/app/domains/ui_testing/schemas.py
      - 新增接口：
          - backend/app/api/v1/ui_testing.py
          - POST /api/v1/ui-testing/executions
          - GET /api/v1/ui-testing/executions/{execution_id}
      - 前端工作台替换 ComingSoon：
          - frontend/src/pages/ui-automation/UIAutomationWorkbench.tsx
          - frontend/src/services/uiAutomation.ts
          - frontend/src/App.tsx
  - P3-4 基础设施补齐
      - 鉴权缓存改为真实 Redis 读写，带内存兜底：
          - backend/app/core/auth_service.py
      - PDF 报告完成：
          - backend/app/core/reporting/generator.py
          - 先走 Playwright HTML->PDF，失败时降级为内置 PDF 生成
      - 报告接口按现有 TestExecution / TestExecutionResult 模型修复：
          - backend/app/api/v1/reports.py
      - 对象存储配置补齐：
          - backend/app/platform/config/settings.py
      - 运维说明补齐：
          - docs/infrastructure_operations.md
      - 基础设施前端页替换 ComingSoon：
          - frontend/src/pages/InfrastructureOverview.tsx
  - 之前建议项也一起补齐了
      - Knowledge Graph 增加 API -> FIELD、TABLE -> FIELD、FIELD -> API/TABLE 浏览链路：
          - backend/app/domains/knowledge_graph/graph_service.py
          - backend/app/domains/knowledge_graph/query_engine.py
          - backend/app/api/v1/knowledge_graph.py
          - frontend/src/pages/KnowledgeGraph.tsx
          - frontend/src/services/knowledgeGraph.ts

  新增测试

  - backend/tests/test_ui_testing_api.py
  - backend/tests/test_infra_foundation.py
  - backend/tests/test_reports_api.py
  - backend/tests/test_knowledge_graph_api.py

  主流程测试结果
  已执行：

  pytest backend/tests/test_v2_main_flow_smoke.py backend/tests/test_p0_regression.py
  backend/tests/test_p1_flow.py backend/tests/test_intent_to_scenario.py backend/tests/
  test_scenarios_api.py backend/tests/test_scenario_executor.py backend/tests/
  test_task_status_api.py backend/tests/test_p2_ai_features.py backend/tests/
  test_p2_platform_extensions.py backend/tests/test_knowledge_graph_api.py backend/tests/
  test_ui_testing_api.py backend/tests/test_infra_foundation.py backend/tests/
  test_reports_api.py -q
  npm --prefix frontend run build

  结果：

  - 后端：68 passed
  - 前端：build 成功

  测试结论

  - 主链路 intent -> confirm -> execute 回归通过。
  - 新增的 UI Automation、报告、知识图谱字段链路、基础设施能力测试通过。
  - 没有阻断性失败。

  剩余注意项

  - 测试里仍有现存 warning：
      - SQLAlchemy declarative_base 迁移 warning
      - Pydantic v1 @validator deprecation warning
      - 若干 pytest collection warning
      - 前端 build 仍有 chunk size warning
  - 工作区里还有测试/build 产物：
      - __pycache__
      - frontend/dist

  如果你要，我下一步可以继续帮你做一次工作区整理，只保留源码改动。
