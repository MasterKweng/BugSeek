  阶段 0：准备与基线（1~2 天）
  - 明确“当前版本中心”使用的项目/版本上下文传参方式，确保建议接口也用同一套上下文规则
  - 固化字段标准化规则（camel→snake、同义词表）
  - 定义候选评分权重与阈值（默认：≥0.85 自动，0.60~0.85 人工确认）
  阶段 1：最小可用（规则打分 + UI 建议列表）

  1. 后端

  - 输入：API 定义 + schema_snapshot
  - 输出：候选列表（含 score 与 reasons）

  2. 前端

  - 展示建议列表（接口、字段路径、推荐表/字段、置信度、原因）
  - 支持单条确认/驳回

  阶段 2：审核闭环（批量确认 + 状态管理）
  1. 数据结构



  - 新增批量应用接口：POST /field-mappings/batch-apply
  - 新增状态更新接口：PUT /field-mappings/{id}/status
  - 新增待审核列表接口：GET /field-mappings/pending

  3. 前端

  - 加“批量确认 ≥0.85”
  - 加“待审核筛选”（status=proposed）

  阶段 3：引入 AI 候选（低风险接入）

  1. 后端

  - 增加 AI 生成候选能力（从 API 语义 + schema 给出候选）
  - 与规则打分合并，AI 只提供候选池

  2. 前端

  - 建议列表增加“候选详情抽屉”
  - 显示 AI 置信度 + 规则评分

  阶段 4：版本继承与字典复用

  1. 后端

  - 版本映射复制接口：POST /field-mappings/clone
  - 全局字典表（项目级 field_dictionary）

  2. 前端

  - “继承上版本映射”按钮
  - 字典管理小面板（可选）

  阶段 5：自动化与闭环优化

  - 高置信度自动写入（≥0.85）
  - 低置信度进入待审
  - 从历史“确认映射”中学习（提高推荐置信度）