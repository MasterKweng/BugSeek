# V2.0 模块保留 / 延后 / 删除清单

这份清单的目标，不是评价某个模块“高级不高级”，而是回答三个更实际的问题：

1. 现在哪些模块值得继续投入。
2. 哪些模块应该延后，等主链路稳定后再做。
3. 哪些模块应该删除或停止包装，避免继续分散资源和制造错误预期。

当前判断标准按四项来看：

1. 是否已经接入真实执行链路。
2. 是否已经具备最小可用闭环。
3. 是否能直接降低测试设计、执行、定位、回归成本。
4. 是否会带来明显的维护成本、认知成本，或“演示大于落地”的风险。

---

## 一、当前结论总览

### 保留并继续投入

- 场景执行模块
- Data Impact Engine
- UI Automation 最小执行器
- 意图工作台，但仅保留为“草稿辅助入口”

### 延后投入

- Knowledge Graph 的独立展示能力
- AI assertion generation
- AI variable mapping
- AI failure analysis

### 已删除或已停止包装

- `AI full run / auto-fix` 这层自动闭环叙事
- `backend/app/execution/assertion_engine.py`
- `backend/app/execution/scheduler.py`
- `backend/app/execution/context_bus.py`
- `backend/app/execution/result_store.py`
- `AI test optimize` 这层“AI 优化引擎”包装
- `sync_tasks` 里的 `ai-auto-fix` 功能入口

### 当前仍有少量残留，但已不影响功能

- `backend/app/ai/prompts.py` 中仍保留未使用的 `auto_fix` prompt 模板

说明：

- 上面“已删除或已停止包装”的内容，代码层面已经完成清理。
- `prompts.py` 的残留目前没有入口引用，不影响运行，但建议后续做一次统一的废弃 prompt 清理。

---

## 二、已完成的清理动作

### 1. 已物理删除的文件

- `backend/app/execution/assertion_engine.py`
- `backend/app/execution/scheduler.py`
- `backend/app/execution/context_bus.py`
- `backend/app/execution/result_store.py`
- `backend/app/domains/ai_testing/test_optimizer.py`

### 2. 已删除的后端接口和包装层

- `backend/app/api/v1/ai_testing.py` 中的 `/ai/test/run-full`
- `backend/app/api/v1/ai_testing.py` 中的 `/ai/test/optimize`
- `backend/app/domains/ai_testing/engine.py` 中的 `run_full_flow` 及相关 auto-fix 闭环实现
- `backend/app/api/v1/sync_tasks.py` 中的 `/sync-tasks/{task_id}/ai-auto-fix`

### 3. 已删除的前端调用和入口

- `frontend/src/services/operations.ts` 中的 `runAiFullFlow`
- `frontend/src/services/operations.ts` 中的 `optimizeAiTests`
- `frontend/src/pages/api-hub/SyncTasksList.tsx` 中的“AI 自动修复”按钮和请求逻辑
- `frontend/src/i18n/messages.ts` 中不再使用的 `autoFix` 文案键

### 4. 清理后的结果

- 前后端已经不再暴露 `AI full run`
- 前后端已经不再暴露 `AI test optimize`
- `sync_tasks` 页面不再展示“AI 自动修复”
- 占位执行模块不再保留在代码结构中

---

## 三、保留并继续投入

### 1. 场景执行模块

结论：保留，并作为 V2.0 主链路核心。

原因：

- 它已经接入真实执行链路，不是孤立模块。
- 已支持 DAG、依赖校验、上下文变量传递、节点结果落库、同步/异步触发。
- 它直接承接“业务流回归”这一测试平台最核心的使用场景。

为什么优先级最高：

- 单接口执行再强，也只能解决点状问题。
- 场景执行能把接口资产真正组织成回归资产。
- 后续很多能力都应该挂在它周围，而不是绕开它单独发展。

继续投入方向：

- 补齐场景执行结果页、失败定位、变量链路展示。
- 增强场景调试能力，例如单节点重跑、从失败节点续跑、上下文快照查看。
- 完善节点级配置，例如超时、重试、断言覆盖、节点跳过策略。

进入下一阶段前的门槛：

- 至少能稳定承接核心业务流回归。
- 场景失败时，能快速定位到具体节点、变量和断言。

### 2. Data Impact Engine

结论：保留，并作为差异化能力继续投入。

原因：

- 它已经接到原子执行前后，不是概念层模块。
- 能为接口测试补上数据库影响面的证据链。
- 它和“测试是否真的验证了数据正确性”直接相关。

为什么要保留：

- 很多团队接口测试失败，不是 HTTP 失败，而是写脏、漏写、误写。
- 这块如果做稳，平台就不只是“请求发送器”，而是“数据正确性验证工具”。

继续投入方向：

- 提高 impact 结果的稳定性和可解释性。
- 把影响表、影响字段、快照差异更清晰地展示到执行结果页。
- 把 Data Impact 的结果沉淀到回归分析和断言补全场景里。

进入下一阶段前的门槛：

- 至少在一类典型业务中稳定可用。
- 结果必须做到“可读、可信、可追溯”。

### 3. UI Automation 最小执行器

结论：保留，但严格控制边界，不扩张叙事。

原因：

- 它已经是可运行能力，不是演示页面。
- 能覆盖少量高价值核心链路，例如登录、下单、支付、搜索、提单。
- 和接口、场景能力组合后，可以承担最小冒烟职责。

为什么只做“最小执行器”：

- UI 自动化天然维护成本高。
- 过早把 UI 测试做成大平台，会稀释主链路建设资源。
- 当前更适合把它定位成“核心路径巡检工具”。

继续投入方向：

- 重点提升稳定性，而不是堆功能。
- 增加截图、失败页信息、步骤回放等诊断能力。
- 只补最常见动作和断言，不做大而全 DSL。

进入下一阶段前的门槛：

- 能稳定跑通核心冒烟链路。
- 失败时能提供足够定位信息。

### 4. 意图工作台

结论：保留，但降级为“草稿辅助入口”，不作为自动化主叙事。

原因：

- 它已经具备“召回 API -> AI 选 API -> AI 生成草稿 -> 人确认保存”的链路。
- 对新人、复杂链路初稿、场景搭建提速是有帮助的。

为什么要限制定位：

- 当前更适合做“加速起稿”，不适合承诺“自动设计高质量测试场景”。
- 一旦把它包装成自动测试架构师，预期会远高于现有实现。

继续投入方向：

- 提高草稿可编辑性和可解释性。
- 给出候选 API、生成理由、依赖关系说明。
- 优先优化“草稿可用率”，而不是追求“全自动闭环执行”。

进入下一阶段前的门槛：

- 草稿结构稳定。
- 人工确认成本足够低。
- 不把低质量草稿直接送入自动执行主链路。

---

## 四、延后投入

### 1. Knowledge Graph 独立展示能力

结论：延后，不作为 V2.0 主卖点。

原因：

- 图谱底层能力和查询代码是存在的，不是假模块。
- 但它现在更适合作为底层证据层，而不是单独包装成主要功能。
- 同时底层模型还缺少充分的项目和版本隔离能力，存在资产污染风险。

延后策略：

- 保留底层能力。
- 不优先做复杂图谱浏览页、运营化展示和概念包装。
- 优先服务场景执行、数据影响、资产关联等已有链路。

### 2. AI assertion generation

结论：延后，先做辅助建议，不做核心卖点。

原因：

- 这类能力理论上有价值，但当前平台更缺的是“执行结果可信、调试体验顺滑”。
- 如果执行链路还没补稳，AI 断言生成很容易变成额外噪声。

更合适的落点：

- 先作为建议工具存在。
- 由人工确认后写回用例。
- 不进入强自动链路。

### 3. AI variable mapping

结论：延后。

原因：

- 它确实可能提升跨节点变量拼接效率。
- 但前提是场景执行、变量可视化、上下文调试已经足够稳定。
- 如果上下文链路本身还在持续变化，过早强化 AI 映射只会提高复杂度。

更合适的落点：

- 先服务于场景编辑器中的“建议映射”。
- 不作为平台当前核心差异化卖点。

### 4. AI failure analysis

结论：延后。

原因：

- 失败分析本身有价值，但必须建立在结果采集完整、错误分类明确、证据充分之上。
- 否则生成出来的分析，只会停留在泛化描述，无法真正帮助定位。

更合适的落点：

- 先补执行日志、错误分类、上下文快照。
- 再把 AI failure analysis 建在结构化证据之上。

---

## 五、删除或停止包装

### 1. AI full run / auto-fix

状态：已完成清理。

处理结果：

- 已删除后端路由。
- 已删除前端服务调用。
- 已删除域层 `run_full_flow` 相关实现。
- 已停止作为 V2.0 对外能力描述。

原因：

- 当前状态流没有闭合。
- 这层包装最容易制造“平台已经能自动闭环修复测试”的错误认知。

后续建议：

- 在真正补完状态流、结果流、修复流之前，不恢复这类包装。

### 2. `backend/app/execution/assertion_engine.py`

状态：已删除。

原因：

- 当前没有实际能力。
- 保留只会增加架构理解成本。

### 3. `backend/app/execution/scheduler.py`

状态：已删除。

原因：

- 当前没有真实调度逻辑。
- 异步执行已经由其他链路承接。

### 4. `backend/app/execution/context_bus.py`

状态：已删除。

原因：

- 当前实现只是极简容器。
- 真正的上下文逻辑已经在执行器内部维护。

### 5. `backend/app/execution/result_store.py`

状态：已删除。

原因：

- 当前结果落库逻辑已经在真实执行器内部。
- 这个文件本身没有独立能力，也没有实际接入。

### 6. AI test optimize 包装

状态：已完成清理。

处理结果：

- `/ai/test/optimize` 已删除。
- 前端 `optimizeAiTests` 已删除。
- `TestOptimizer` 实现文件已删除。

原因：

- 当前实现只是简单排序截断。
- 不值得继续以“AI 优化引擎”名义保留。

### 7. `sync_tasks` 中的 `ai-auto-fix`

状态：已完成清理。

处理结果：

- 后端 `/sync-tasks/{task_id}/ai-auto-fix` 已删除。
- `SyncTasksList` 页面中的按钮和请求逻辑已删除。
- 前端相关文案键已删除。

原因：

- 这条链路不属于主执行链路。
- 继续保留只会延续“自动修复”叙事，而当前平台并不适合继续强化这个方向。

残留说明：

- `backend/app/ai/prompts.py` 中仍有未使用的 `auto_fix` prompt 模板。
- 这不影响运行，但建议在后续统一清理废弃 prompt。

---

## 六、建议的 V2.0 资源分配

如果 V2.0 只能重点投入 100% 资源，建议这样分配：

- 40% 给场景执行模块
- 30% 给 Data Impact Engine
- 15% 给 UI Automation 最小执行器
- 10% 给意图工作台
- 5% 用于统一清理残留、命名、文档和错误叙事

不建议优先投向：

- 自动闭环修复类包装层
- Knowledge Graph 独立展示层
- 更多尚未落地的 AI 子模块

---

## 七、推荐执行顺序

### 第一阶段：先把主链路做稳

- 场景执行模块
- Data Impact Engine
- UI Automation 最小执行器

目标：

- 先让平台具备稳定的“接口 -> 场景 -> 数据影响 -> UI 冒烟”基础闭环。

### 第二阶段：再补辅助提效能力

- 意图工作台
- AI assertion generation
- AI variable mapping
- AI failure analysis

目标：

- 在主链路稳定后，再把 AI 放到“辅助提效”的位置，而不是替代主链路。

### 第三阶段：最后处理平台化展示能力

- Knowledge Graph 独立展示
- 更复杂的调度、编排、自动闭环包装

目标：

- 等底层数据和主链路足够稳定后，再做更高阶的平台包装。

---

## 八、更新后的落地建议

V2.0 应围绕“场景执行 + 数据影响 + 最小 UI 冒烟 + 草稿辅助”收敛，而不是继续扩张“全自动 AI 测试闭环”的叙事。

目前和这个方向相冲突的包装层已经基本清理完成。接下来更值得投入的，不是再增加新的 AI 概念模块，而是把保留下来的主链路做稳、做清楚、做出可验证价值。
