下面是 **BugSeek V2 阶段三：Data Impact Engine（数据库影响分析）完整工程方案**。
这一阶段是整个 **AI 自动测试平台的“自动断言能力核心”**。

一句话总结 Data Impact Engine 的作用：

> **让系统知道“一个 API 调用到底改变了数据库什么”。**

没有这一步，AI 只能生成：

```
status_code == 200
```

有了这一步，AI 才能生成：

```
orders.status = PAID
inventory.stock -= 1
payment.status = SUCCESS
```

这才是**真正的自动化测试断言**。

下面从 **工程架构 → 数据模型 → 执行机制 → 实现细节** 完整展开。

---

# 一、Data Impact Engine 的核心目标

系统要实现：

```
API调用
   ↓
数据库变化检测
   ↓
识别影响的表
   ↓
识别影响的字段
   ↓
生成数据变化记录
```

最终输出：

```
API → TABLE → FIELD → CHANGE
```

例如：

```
POST /api/order/pay

影响：

orders.status       NEW → PAID
orders.paid_time    null → timestamp
payments.status     INIT → SUCCESS
```

---

# 二、Data Impact Engine 在系统架构中的位置

架构位置：

```
Scenario Engine
      │
      │
Execution Engine
      │
      │
Data Impact Engine
      │
      │
Knowledge Graph
      │
      │
AI Testing Engine
```

执行顺序：

```
API执行
   ↓
SQL trace
   ↓
Data impact analysis
   ↓
Graph update
   ↓
AI assertion suggestion
```

---

# 三、Data Impact Engine 模块结构

模块路径：

```
domains/data_impact/
```

完整结构：

```
data_impact/

engine.py
sql_tracer.py
change_detector.py
impact_analyzer.py
snapshot_manager.py
schema_mapper.py
impact_repository.py
```

模块职责：

| 模块               | 作用     |
| ---------------- | ------ |
| engine           | 主入口    |
| sql_tracer       | 捕获SQL  |
| change_detector  | 数据变化检测 |
| impact_analyzer  | 分析影响   |
| snapshot_manager | 数据快照   |
| schema_mapper    | 字段映射   |
| repository       | 持久化    |

---

# 四、数据模型设计

新增核心表。

---

## 1 API Execution Trace

记录 API 执行：

```
api_execution_traces

id
execution_id
api_id
start_time
end_time
status
```

---

## 2 SQL Trace

记录执行 SQL：

```
sql_traces

id
trace_id
sql_text
operation_type
table_name
timestamp
```

operation_type：

```
INSERT
UPDATE
DELETE
SELECT
```

---

## 3 Table Impact

记录影响的表：

```
table_impacts

id
execution_id
api_id
table_name
operation
row_count
```

operation：

```
INSERT
UPDATE
DELETE
```

---

## 4 Field Impact

记录字段变化：

```
field_impacts

id
table_impact_id
field_name
old_value
new_value
change_type
```

change_type：

```
CREATED
UPDATED
DELETED
```

---

# 五、SQL 捕获机制（SQL Tracer）

核心问题：

**如何捕获 API 执行产生的 SQL？**

有三种方式。

---

## 方案1（推荐）

数据库日志解析。

PostgreSQL 开启：

```
log_statement = 'all'
```

优点：

* 不侵入代码
* 简单

缺点：

日志量大。

---

## 方案2

代理数据库连接。

例如：

```
pg_stat_statements
```

---

## 方案3

ORM hook。

如果系统使用：

```
SQLAlchemy
```

可以：

```
event.listen(engine, "before_cursor_execute")
```

捕获 SQL。

---

**BugSeek 推荐方案：**

```
SQLAlchemy Hook + SQL Parser
```

---

# 六、SQL Parser

捕获 SQL 后需要解析。

工具：

```
sqlparse
```

解析：

```
UPDATE orders SET status='PAID' WHERE id=123
```

得到：

```
operation = UPDATE
table = orders
fields = status
```

模块：

```
sql_parser.py
```

---

# 七、数据变化检测

SQL 只能知道操作。

不能知道：

```
old_value
new_value
```

所以必须做：

```
数据快照
```

---

# 八、Snapshot Manager

执行 API 前：

```
snapshot_before
```

执行后：

```
snapshot_after
```

结构：

```
snapshots

id
execution_id
table_name
data_json
snapshot_time
```

---

# 九、变化检测算法

流程：

```
before snapshot
      ↓
after snapshot
      ↓
diff algorithm
```

示例：

```
orders

before:
id=1 status=NEW

after:
id=1 status=PAID
```

变化：

```
status NEW → PAID
```

算法：

```
dict diff
```

---

# 十、Impact Analyzer

模块：

```
impact_analyzer.py
```

流程：

```
SQL traces
   ↓
affected tables
   ↓
snapshot diff
   ↓
field changes
```

输出：

```
ImpactResult
```

结构：

```
{
  table: orders
  changes: [
      status NEW→PAID
  ]
}
```

---

# 十一、Knowledge Graph 更新

Impact Engine 需要更新 Graph。

例如：

```
API createOrder
   ↓
WRITES
   ↓
orders table
```

Graph Edge：

```
API → WRITES → TABLE
```

---

# 十二、自动断言生成

Impact Engine 可以自动生成断言。

示例：

```
orders.status = PAID
```

转为：

```
assert orders.status == "PAID"
```

断言模板：

```
SELECT status FROM orders WHERE id={{order_id}}
```

---

# 十三、Impact 缓存

影响分析不需要每次运行。

可以缓存：

```
api_table_impacts
```

结构：

```
api_table_impacts

api_id
table_name
confidence
```

AI 使用：

```
confidence > 0.7
```

---

# 十四、Impact Engine API

提供接口：

```
GET /impact/api/{api_id}

GET /impact/execution/{execution_id}
```

---

# 十五、Impact Engine 与 Scenario Engine

场景执行：

```
CreateOrder
    ↓
PayOrder
    ↓
QueryOrder
```

Impact Engine 可以分析：

```
跨 API 数据变化
```

---

# 十六、Impact Engine 性能优化

优化策略：

### 1 限制快照表

只监控：

```
高价值表
```

---

### 2 增量快照

只记录：

```
changed rows
```

---

### 3 异步分析

Impact 分析放到：

```
Celery task
```

---

# 十七、Celery 任务

任务：

```
analyze_data_impact
```

流程：

```
API execution
     ↓
enqueue task
     ↓
impact analysis
```

---

# 十八、Impact Engine 输出

最终系统会拥有：

```
API → TABLE → FIELD → CHANGE
```

例：

```
createOrder

orders
    status
    amount

order_items
    product_id
    quantity
```

---

# 十九、AI 使用 Data Impact

AI 可以生成：

```
assert orders.status == "PAID"
assert payments.status == "SUCCESS"
```

---

# 二十、阶段三完成后的能力

系统拥有：

```
自动数据库断言能力
```

AI 能：

```
理解 API 行为
```

这是：

**AI 自动测试的核心能力之一。**

---

如果你愿意，我可以继续给你写 **阶段四：AI Testing Engine（AI 自动测试引擎）完整工程方案**。

那部分会真正实现：

```
自然语言 → 自动测试场景
```

并且我会详细设计：

* AI Agent
* Prompt 架构
* Test generation pipeline
* Root cause analysis engine

这是 **整个 BugSeek AI 自动化测试平台的最后一块核心拼图**。
