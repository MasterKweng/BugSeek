# 场景执行优化

我把当前“场景执行模块”按真实调用链完整读了一遍，范围包括：

- `backend/app/api/v1/scenarios.py`
- `backend/app/api/v1/execution_triggers.py`
- `backend/app/execution/engine.py`
- `backend/app/execution/worker.py`
- `backend/app/celery/tasks/ai_tasks.py`
- `backend/app/api/v1/task_status.py`

先给总判断：

当前场景执行模块已经不是 demo，也不是“不能跑”的状态。它已经具备了场景 CRUD、同步执行、异步触发、DAG 依赖分层、上下文变量透传、节点结果落库这些核心骨架。

但它距离“可长期稳定承载核心回归”的生产级场景执行系统，还差几块非常关键的基础能力：

- 执行状态语义不够正确
- 场景 execution 与节点 execution 的边界不够清晰
- 执行模式、超时、重试、节点 override 这些配置还没有真正进入执行语义
- 并发执行时的数据库会话边界有风险
- 场景保存时缺少设计时校验
- 历史执行结果还不够可复盘

换句话说：

这个模块现在最不缺的是“能执行”。
它最缺的是“执行契约、状态一致性、生产化硬约束”。

---

## 一、当前代码已经具备的能力

### 1. 场景执行主链路已存在

- `ScenarioExecutor.execute_scenario()` 会从场景定义构图，校验图结构，按依赖构建层级，然后逐层执行节点。
- 执行结果会汇总为一个 scenario execution，并写入 `TestExecution` / `TestExecutionResult`。
- 节点执行通过 `CaseExecutor.execute_case()` 完成，已经具备真实 HTTP 请求、断言、变量提取、Data Impact 集成。

这说明：

场景执行模块不是空壳，已经是实际工作链路的一部分。

### 2. DAG 形态已经初步落地

- 当前执行器会对 `depends_on` 做图校验。
- 会把节点构造成 level，并在同一 level 内并发执行。

这说明：

“依赖驱动执行”不是概念设计，而是已经进入实现。

### 3. 上下文变量链路已经存在

- 场景初始变量会进入 `context`
- 节点 `input_mapping` 会基于上下文渲染
- 节点提取出的变量会回写到 `context["vars"]` 和 `context["node"]`

这说明：

跨节点变量传递已经具备雏形。

---

## 二、当前最关键的问题

## 1. 执行状态语义不正确

当前 `ScenarioExecutor.execute_scenario()` 在汇总结果后，`final_status` 被固定写成 `"completed"`。

这意味着：

- 只要执行流程走完，无论中间节点是否失败，`status` 都是 `completed`
- 真正的通过/失败，只体现在 `result_status` 和 `summary.failed`

这会直接影响：

- 同步执行接口的返回解释
- 异步触发接口的状态判断
- `task_status` 的任务状态聚合
- 后续 failure analysis 或自动决策逻辑

更准确的语义应该拆成两层：

- `status`：生命周期状态，例如 `pending / running / completed / failed / cancelled`
- `result_status`：测试结果状态，例如 `passed / failed / partial_failed / skipped`

否则平台会持续输出一种危险状态：

测试失败了，但平台说“执行完成了”。

## 2. execution 实体边界不够清晰

这里不是单一问题，而是两个层次混在一起了。

### 第一层：异步触发会先创建一条 scenario execution

`execution_triggers.py` 在异步触发时，会先创建一条 `TestExecution(status=pending)`，然后把 `execution_id` 传给 Celery。

### 第二层：`ScenarioExecutor` 内部又会新建一条 scenario execution

`ScenarioExecutor._save_scenario_execution_record()` 会再次创建一条 `execution_type=scenario` 的 `TestExecution`。

这会导致：

- 一次异步场景执行产生两条 scenario execution
- 触发返回的 `execution_id` 和最终结果所在的 `execution_id` 不是同一条
- 查询、回调、执行历史很容易错位

### 第三层：每个节点执行时还会继续落子 execution

`ScenarioExecutor` 调用 `CaseExecutor.execute_case()` 时，后者还会为每个节点创建一条 `execution_type=single` 的 `TestExecution`。

这本身不一定是错的，但当前文档和代码都没有把关系说清楚：

- 这到底是“场景 execution + 子 execution”的父子结构
- 还是“应该只保留场景 execution，节点只保留 result 明细”

所以当前真正的问题不是“多了一条记录”这么简单，而是：

**execution 模型没有明确收口。**

后续必须先明确设计：

1. 保留父子 execution 结构
2. 或者取消节点级 execution，只保留 scenario execution + result 明细

不先定这个，后面的清理和状态统一会继续打架。

## 3. `execution_mode` 只存了，没有真正生效

模型和接口层都支持：

- `sequential`
- `dag`

但当前执行器不按 `execution_mode` 分支。

它的真实行为始终是：

- 先校验图
- 再按依赖生成 levels
- 同一 level 内直接 `asyncio.gather`

所以当前真实语义是：

- `dag` 有点像真的
- `sequential` 只是字段存在，没有被真正尊重

这会导致：

如果一个场景配置成 `sequential`，但节点之间没有显式 `depends_on`，它们还是会并发执行。

## 4. timeout / retry 还是“模型占位”，不是执行能力

场景级和节点级都有：

- `timeout_seconds`
- `retry_count`

但当前实际执行时：

- `ScenarioExecutor` 没按这些字段做控制
- `CaseExecutor` 的 HTTP client 固定 `timeout=30.0`
- retry 也没有真正进入节点执行策略

所以当前 timeout / retry 基本还是“存了数据”，不是真实执行保障。

## 5. 节点级 override 没有真正生效

节点模型有：

- `input_mapping`
- `extract_rules`
- `assertion_overrides`

当前真正生效的主要只有：

- `input_mapping`

而 `assertion_overrides`、节点级 `extract_rules` 并没有在执行时覆盖 `ApiCase` 的默认规则。

这会导致：

场景层无法真正控制节点执行行为。

而这本来应该是场景执行模块的核心价值之一。

## 6. 并发节点共享同一个 `Session`

同一 level 内的节点通过 `asyncio.gather` 并发执行，但传给每个节点执行的都是同一个 `db: Session`。

而 `CaseExecutor.execute_case()` 内部会频繁使用这个 session：

- 前后置 SQL
- Data Impact
- 执行记录持久化
- 各类 commit / rollback

这意味着：

在真实 DAG 并发场景下，存在明显的会话污染和事务边界风险。

## 7. 缺少保存前的设计时校验

当前 `create_scenario` / `update_scenario` 基本都是直接写入 `ApiScenario + ScenarioNode`。

缺少的不是一两个字段校验，而是一整套设计时校验：

- `node_key` 唯一性
- `depends_on` 引用合法性
- 自环 / 环检测
- 至少一个启用节点
- `ref_type/ref_id` 是否可解析
- 目标对象是否属于同项目
- 环境是否可用

当前很多错误会变成：

场景能保存，但一执行才炸。

## 8. `api_definition` 节点的执行目标不确定

当前如果节点 `ref_type="api_definition"`，执行器会取该 definition 下第一条 active 的 `ApiCase` 来跑。

这会造成一个很危险的隐式耦合：

- 场景节点没有显式绑定最终执行用例
- 后续新增、调整 case 后，场景行为可能悄悄变化

这类问题最容易产生：

“我没改场景，但结果变了。”

## 9. 缺少可复盘的执行快照

当前不是完全没有记录，系统已经保存了：

- summary
- 节点 result
- 请求响应
- 断言结果
- 提取变量

但它还缺少真正能支持复盘的“effective snapshot”，例如：

- 当次执行用到的场景节点快照
- 当次渲染后的 input mapping
- 最终解析到的 case / definition
- effective assertion rules / extraction rules
- effective execution mode

这会影响：

- 回放
- 复盘
- 历史审计
- 结果可复现

---

## 三、优先级建议

### P0：必须先补

这些不补，系统会持续输出脏状态或脏记录：

1. 修正状态语义
2. 明确 execution 实体模型并收口
3. 真正落地 `execution_mode`
4. 真正落实 timeout / retry
5. 让节点级 override 生效
6. 并发节点不要共享同一个 `Session`
7. 同步 / 异步 / task_status 的状态模型同步调整

### P1：应尽快补

这些不一定马上出事故，但会不断提高维护成本：

1. 保存场景时做设计时校验
2. 明确 `api_definition` 节点如何选择 case
3. 标准化失败类型
4. 增加可复盘的执行快照

### P2：后续生产增强

1. 节点重跑 / 断点续跑
2. 场景预检接口
3. 场景版本冻结
4. 变更影响分析

---

## 四、最终结论

当前场景执行模块已经值得继续投入，而且应该作为平台主链路之一来建设。

但它下一阶段最不该做的，不是再加更多 AI 包装，而是先把这些“无聊但决定上线质量”的基础能力补齐：

- 一次执行的状态语义必须绝对一致
- execution 实体边界必须说清楚
- 节点配置必须真正控制执行
- 并发执行必须有明确的会话边界
- 历史结果必须可以复盘

做到这一步，它才真正配得上“生产级场景执行引擎”这个定位。
