  # 一、路径语义规则（Path Semantics Rules）
  目标：通过 URL 路径推断“主资源表”，提高映射准确率。
  ## 1) 路径分段解析

  /orders/{order_id}/items/{item_id}
  segments = ["orders","{order_id}","items","{item_id}"]
  ## 2) 规则集
  规则 R1：资源名 → 表名
  - 如果段是复数名词（orders/users/items），优先匹配表名 orders/users/items

  规则 R2：路径参数 → 最近资源

  - {order_id} 优先映射到最近出现的资源段 orders

  规则 R3：子资源关系

  - orders/{order_id}/items
      - order_id 倾向 orders.id
      - item_id 倾向 items.id

  规则 R4：动词段忽略
  - create/update/batch/export/import/login 等动词段不用于表匹配


  - 距离最近资源段的权重最大
      - {order_id} 距离 orders 最近 → 权重 +0.2

  ———

  ## 3) 评分加权示例

  score_path = 0.1 if matches second nearest resource

  ———

  # 二、AI 提示词模板（Prompt Template）

  目标：生成候选映射 + 解释 + 置信度。

  ## 模板（System）

  你是数据库字段映射专家，任务是将 API 请求字段映射到数据库表字段。
  ## 模板（User）

  - method: {method}
  - path: {path}
  - summary: {summary}
  待映射字段:
  - api_field_path: {api_field_path}

  数据库结构:
  {schema_snapshot}
  已有字段字典:
  {field_dictionary}

  2. 给出理由 (如路径语义、字段名匹配、主键优先)
  4. 若无合适映射，返回空列表
  {
    "candidates": [
      {
        "db_table": "...",
        "db_column": "...",
        "confidence": 0.86,
        "reasons": ["path匹配","字段名相似"]
      }
    ]
  }

  ———

  # 三、数据库结构索引建议（DB Schema Index Rules）

  目标：提升预查询 pre_sql 的效率。

  ## 1) ID 字段索引

  - 所有 id 字段必须是 PK（自动索引）
  - 所有 *_id 字段必须有索引（FK 索引）

  ## 2) 组合索引建议

  若接口常通过组合过滤：

  - tenant_id + user_id
  - order_id + item_id

  建议在 schema 中注明组合索引，供匹配算法利用。

  ## 3) 可选结构约束

  - 在 schema_snapshot 中保留 primary_key / foreign_key
  - AI & 校验算法优先参考 PK/FK

  ———

  # 总结

  - 路径语义规则：决定“字段 → 哪张表”的语义优先级
  - AI 提示词模板：生成候选映射 + 理由 + 置信度
  - 数据库结构索引建议：提高 pre_sql 查询效率和匹配准确度