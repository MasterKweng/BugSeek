你的这套 **BugSeek 当前数据库 + pgvector + Redis + Celery 设计，其实已经是一个非常成熟的基础设施层**。
所以先给你一个**客观结论**：

> **阶段一架构重构不会推翻现有数据库体系，但会进行结构升级和模块化拆分。**

换句话说：

* **80%数据库可以保留**
* **20%需要结构升级**
* **新增约10~15张核心表**

原因很简单：

当前数据库是为 **API管理 + 执行平台**设计的
但 **BugSeek V2 是 AI测试系统**

所以需要新增三类数据：

```
1 AI数据
2 图数据
3 影响分析数据
```

下面我给你 **完整工程级修改方案**。

---

# 一、阶段一架构重构对数据库的总体影响

整体变化如下：

| 模块        | 变化   |
| --------- | ---- |
| 用户系统      | 不变   |
| 项目系统      | 不变   |
| API管理     | 轻微修改 |
| 测试用例      | 轻微修改 |
| 场景系统      | 升级   |
| 字段映射      | 升级   |
| 数据库schema | 升级   |
| 向量系统      | 升级   |
| 任务系统      | 升级   |
| AI系统      | 新增   |
| 知识图谱      | 新增   |
| 数据影响分析    | 新增   |

总结：

```
原表数量：23
新增表：12~15
最终表数：35~40
```

这是 **AI平台正常规模**。

---

# 二、需要新增的核心系统

阶段一重构后数据库分为 **9大模块**：

```
1 identity
2 project
3 api
4 testing
5 scenario
6 schema
7 mapping
8 ai
9 graph
10 impact
```

下面逐个说明。

---

# 三、原有表保留（无需修改）

以下表 **可以100%保留**：

### 用户系统

```
users
user_contexts
```

---

### 项目系统

```
projects
versions
environments
global_vars
```

---

### API管理

```
api_definitions
api_endpoint_groups
api_cases
sync_tasks
```

---

### 鉴权系统

```
auth_configs
auth_input_mappings
auth_extract_rules
project_auth_templates
```

---

# 四、需要修改的表

以下表需要升级。

---

# 1 api_cases 表升级

当前：

```
api_cases
```

新增字段：

```
ai_generated
ai_confidence
ai_strategy
ai_prompt
ai_context
```

完整新增：

```
ALTER TABLE api_cases ADD COLUMN ai_strategy VARCHAR(50);
ALTER TABLE api_cases ADD COLUMN ai_prompt TEXT;
ALTER TABLE api_cases ADD COLUMN ai_context JSON;
```

作用：

AI生成测试时记录策略。

---

# 2 api_scenarios 表升级

新增：

```
scenario_type
ai_generated
graph_source
```

例如：

```
manual
ai_generated
graph_inferred
```

---

# 3 scenario_nodes 表升级

新增：

```
node_key
node_role
node_metadata
```

支持：

```
login_node
payment_node
verification_node
```

---

# 五、向量数据库升级

目前：

```
pgvector + 本地pkl缓存
```

问题：

```
无法支持复杂语义搜索
```

建议升级：

新增 **vector_store 表**

```
vector_store

id
entity_type
entity_id
vector
metadata
created_at
```

entity_type：

```
api
db_column
scenario
test_case
knowledge
```

向量维度：

```
vector(384)
```

（MiniLM默认384维）

---

# 六、知识图谱表（新增）

新增模块：

```
knowledge_graph
```

新增表：

### graph_nodes

```
graph_nodes

id
node_type
node_key
name
metadata
created_at
```

node_type：

```
API
TABLE
COLUMN
SCENARIO
TEST
```

---

### graph_edges

```
graph_edges

id
source_id
target_id
relation
weight
metadata
```

relation：

```
CALLS
WRITES
READS
DEPENDS
VALIDATES
```

---

# 七、Data Impact Engine 表（新增）

新增：

### api_execution_traces

```
api_execution_traces

id
execution_id
api_id
start_time
end_time
status
```

---

### sql_traces

```
sql_traces

id
trace_id
sql_text
operation_type
table_name
timestamp
```

---

### table_impacts

```
table_impacts

id
execution_id
api_id
table_name
operation
row_count
```

---

### field_impacts

```
field_impacts

id
table_impact_id
field_name
old_value
new_value
change_type
```

---

# 八、AI系统表（新增）

AI需要记忆和学习。

新增：

### ai_memories

```
ai_memories

id
memory_type
context
result
embedding
created_at
```

memory_type：

```
test_generation
failure_analysis
scenario_generation
```

---

### ai_tasks

AI任务管理：

```
ai_tasks

id
task_type
input
context
status
result
created_at
```

---

# 九、测试执行系统升级

现有：

```
test_executions
test_execution_results
```

建议新增：

### test_assertions

```
test_assertions

id
case_id
assertion_type
expression
expected_value
```

---

### test_variables

```
test_variables

id
execution_id
var_key
var_value
source
```

---

# 十、Redis 修改方案

你的 Redis 目前只用于：

```
Celery
```

这是 **合理的**。

但 BugSeek V2 需要新增用途：

| 用途      | 说明     |
| ------- | ------ |
| 任务队列    | Celery |
| 缓存      | API缓存  |
| Session | 用户会话   |
| Graph缓存 | 图查询缓存  |

新增 key：

```
bugseek:cache:api:{id}
bugseek:graph:neighbors:{node}
bugseek:ai:context:{id}
```

---

# 十一、Redis DB 规划

建议：

```
DB0  celery queue
DB1  celery result
DB2  system cache
DB3  ai cache
```

---

# 十二、Celery任务扩展

新增任务：

```
generate_ai_tests
build_knowledge_graph
analyze_data_impact
generate_scenario
ai_failure_analysis
```

---

# 十三、数据库最终结构（重构后）

模块结构：

```
identity
  users
  user_contexts

project
  projects
  versions
  environments

api
  api_definitions
  api_endpoint_groups
  api_cases

scenario
  api_scenarios
  scenario_nodes

testing
  test_executions
  test_execution_results
  test_assertions
  test_variables

schema
  db_schema_versions
  database_configs

mapping
  api_field_mappings
  field_mapping_suggestions
  field_mapping_traces

vector
  vector_store

graph
  graph_nodes
  graph_edges

impact
  api_execution_traces
  sql_traces
  table_impacts
  field_impacts

ai
  ai_tasks
  ai_memories
```

---

# 十四、是否需要重建数据库？

答案：

**不需要。**

升级方式：

```
Alembic migration
```

步骤：

```
1 新增表
2 修改字段
3 构建索引
4 数据迁移
```

---

# 十五、性能影响评估

你的服务器：

```
8核
16线程
16GB
```

完全足够。

预计容量：

```
API: 5000
Tables: 200
Cases: 20000
Executions/day: 50000
```

PostgreSQL完全可以支撑。

---

# 十六、最重要的建议

你的 **数据库设计其实已经非常优秀**，我只建议三件事：

### 1 新增 Graph

这是 AI能力核心。

---

### 2 新增 Impact Engine 表

这是自动断言核心。

---

### 3 新增 AI Memory

这是 AI学习能力。

---

# 十七、最关键的结论

你的 BugSeek 当前数据库：

> **已经完成 70% 的 AI测试平台基础设施。**

真正缺的只有：

```
Knowledge Graph
Data Impact Engine
AI Memory
```

加上这三块：

BugSeek 就会从：

```
API测试工具
```

升级为：

```
AI测试平台
```

---

如果你愿意，我可以继续帮你做一件**非常关键的事情**：

**给你画出 BugSeek V2 最终数据库 ER 图（40+表完整关系图）。**

那张图会直接成为：

> **你整个系统未来 3 年的数据库蓝图。**
