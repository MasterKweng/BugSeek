目前最主要的问题
下面这些不是“没做”，而是“做了，但还没收口”。
1. 执行状态和结果状态还是混了
这是我认为当前最大的问题。
在 worker.py 里：
• 单用例执行如果断言失败，TestExecution.status 会写成 FAILED
• 批量执行只要子项里有失败，父执行 status 也会写成 FAILED
同时你又单独有 result_status。
worker
这会导致语义冲突：
正确的语义应该是
• status 表示“执行流程是否完成”
• result_status 表示“结果是否通过”
但你现在实际上是：
• 有断言失败，就把执行本身也记成失败
这会影响：
• 报告统计
• 前端状态展示
• 后续场景/套件接入
我建议
把语义改成：
• 执行完了，不管断言是否通过，status = completed
• 结果通过/失败/部分失败，放到 result_status
也就是：
复制
status       = completed
result_status = passed / failed / partial_failed
而不是现在这种“断言失败 == 执行失败”。
￼
2. started_at / finished_at 不够准确
在 _save_execution_record() 里，单用例执行的 started_at 和 finished_at 都是在保存时用 datetime.utcnow() 写的。
worker
这会导致：
• 时间看起来有了
• 但不是真实的执行开始/结束时间
特别是你后面如果要做：
• 时序分析
• 执行耗时排行
• 并发执行轨迹
这个偏差会很明显。
我建议
在执行开始前记录真实 started_at，在执行结束后记录真实 finished_at，不要在 save 时统一生成。
￼
3. 报告模块命名和职责有点乱
你现在实际上有两个“报告”模块：
• reports.py：是场景报告 / HTML/PDF / RCA 报告，偏场景工作室 
reports
• execution_reports.py：才是这次要的执行报表中心
execution_reports
而 __init__.py 里两个都挂了。
__init__
这会造成两个问题：
• OpenAPI/前端接入时容易误解
• 后面团队接手时很难一眼判断“哪个是执行报表，哪个是场景报告”
我建议
命名直接收口：
• reports.py 保留给执行中心
或者
• scenario_reports.py 明确场景报告职责
• execution_reports.py 明确执行中心职责
总之别再让两个“reports”并存而语义不同。
￼
4. 查询层没有真正拆 service / repository
我没有在当前分支定位到你之前设计里那种：
• execution_schemas.py
• execution_query_service.py
• report_service.py
• execution_repository.py
至少按这些路径我没有找到。
当前逻辑基本还是直接写在路由文件里，尤其 executions.py 和 execution_reports.py 自己在拼查询和 DTO。
这意味着：
功能完成了
但架构还没收口。
短期没问题，长期会带来：
• 路由文件变大
• 查询逻辑重复
• 后面场景/套件/CI 接入时复用困难
我建议
这不是现在必须返工，但应该列入下一轮收敛任务。
￼
5. 报表能力“可用”，但还不是完整报表中心
你现在的 execution_reports.py 已经有核心聚合能力，这点是实的。
execution_reports
但它目前更像：
一组报表接口
而不是
一个完整的报表层
因为还缺：
• 统一 DTO 结构层
• 统一聚合 service
• 更清晰的指标定义
• 更细的错误/超时/断言失败分层统计
所以这块我会评为：
功能完成，工程层未收口。
￼
6. 关键字查询还有一点不够完整
executions.py 的 list_executions() 里，keyword 搜索默认主要查：
• TestExecution.title
• TestExecution.triggered_by
而 TestExecutionResult.target_name 只有在 case_id 或 definition_id 过滤时才进入搜索条件。
executions
这意味着：
用户搜接口名/用例名时
并不总能搜到。
我建议
把 target_name 的搜索做成统一 join 逻辑，而不是只在特定过滤条件下启用。
￼
7. 重跑能力目前只支持 single / batch
executions.py 里的 rerun_execution() 目前只支持：
• single
• batch
对 scenario / suite 直接拒绝。
executions
这不算 bug，因为你现在重点是这三块。
但如果你想把“执行中心”定义成统一入口，那这块以后一定要补。