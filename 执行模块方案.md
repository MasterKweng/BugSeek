可以。下面我给你的是一版**完整版功能设计**，目标不是“补几个查询接口”，而是把这三块真正做成一个完整闭环：

1. **执行记录查询**
2. **报告聚合**
3. **执行结果展示**

这版设计会严格基于你当前代码现实来扩展，不脱离现有实现。

当前代码现实是：

* 执行入口已经存在，单用例和批量执行都从 `api_cases.py` 发起 
* 执行内核已经存在，`CaseExecutor` 负责执行、断言、提取变量、保存执行记录 
* 执行表和执行结果表已经存在，`TestExecution` / `TestExecutionResult` 已建模 
* 但主路由中没有 execution / report 读侧模块，说明当前只有写侧，没有产品化读侧 

所以，这次不是重做执行系统，而是把现有执行能力提升为**可查询、可聚合、可展示、可运营**的完整执行中心。

---

# 一、产品目标

## 1.1 总目标

建立一个完整的“执行中心”，覆盖三层能力：

### 第一层：执行记录查询

解决“执行过什么、什么时候执行、谁触发、执行结果如何”。

### 第二层：报告聚合

解决“整体质量怎样、趋势怎样、失败集中在哪、性能波动怎样”。

### 第三层：执行结果展示

解决“某一次执行的具体请求、响应、断言、提取变量、失败原因是什么”。

---

## 1.2 功能边界

这次设计只聚焦这三块，不扩展到：

* 场景设计器
* 套件设计器
* CI/CD 编排
* AI 失败分析
* 自动修复

但设计会**为它们预留结构**，避免以后返工。

---

# 二、完整功能范围

## 2.1 执行记录查询

要包含：

* 执行历史列表
* 执行详情页
* 子执行列表
* 单条执行结果详情
* 按项目/版本/环境/类型/状态筛选
* 按时间范围筛选
* 按关键字搜索
* 分页
* 排序
* 批量执行父子关系查看

---

## 2.2 报告聚合

要包含：

* 概览统计
* 趋势图
* 通过率趋势
* 失败率趋势
* 平均响应时间趋势
* 失败接口排行
* 失败用例排行
* 响应时间排行
* 状态码分布
* 环境对比
* 版本对比

---

## 2.3 执行结果展示

要包含：

* 基础信息
* 请求信息
* 响应信息
* 断言结果
* 提取变量
* 错误信息
* 原始文本 / JSON 双视图
* 明细级别的通过/失败标记
* 失败定位能力

---

# 三、核心业务对象设计

你现在已经有两个核心表：

* `TestExecution`
* `TestExecutionResult` 

但按完整功能来看，它们还不够，需要补充。

---

## 3.1 TestExecution 的完整职责

它不应该只是“执行落库表”，而应该成为**执行中心的主表**。

### 建议职责

记录一次完整执行任务的“头部信息”：

* 执行是谁
* 执行什么
* 在哪个项目/版本/环境下
* 怎么触发
* 执行总时长
* 执行统计
* 是否有子执行
* 执行摘要是什么

---

## 3.2 TestExecutionResult 的完整职责

它不应该只是“存 response_body 的地方”，而应该成为**展示层的原子结果表**。

### 建议职责

记录执行中的每一个目标结果：

* 单用例结果
* 场景中的节点结果
* 套件中的成员结果

未来场景/套件接进来，也继续复用这个表，不另起炉灶。

---

# 四、数据库完整设计

---

## 4.1 `test_executions` 字段增强设计

当前表字段有：

* `project_id`
* `execution_type`
* `target_id`
* `environment_id`
* `execution_mode`
* `triggered_by`
* `status`
* `started_at`
* `finished_at`
* `duration`
* `total/passed/failed/skipped`
* Jenkins/Webhook 预留字段 

### 建议新增字段

#### 1）`version_id`

用途：报告按版本统计、执行查询按版本过滤。

```sql id="7zjioh"
version_id INTEGER NULL REFERENCES versions(id)
```

#### 2）`parent_execution_id`

用途：支持批量执行、场景执行、套件执行的父子层级。

```sql id="twwh9p"
parent_execution_id INTEGER NULL REFERENCES test_executions(id)
```

#### 3）`title`

用途：给前端列表直接展示，不再运行时拼接。

```sql id="8jhh9o"
title VARCHAR(255) NULL
```

#### 4）`summary_json`

用途：执行摘要结构化存储。

```sql id="prrryw"
summary_json JSON NULL
```

建议内容：

```json id="x28d0y"
{
  "target_name": "获取用户列表",
  "method": "GET",
  "path": "/api/users",
  "batch_size": 5
}
```

#### 5）`version_snapshot_tag`

用途：未来支持“执行时的版本标签”冗余展示，可选。

#### 6）`operator_user_id`

用途：记录真实操作人，和 `triggered_by` 区分。

---

## 4.2 `test_execution_results` 字段增强设计

当前有：

* `execution_id`
* `target_type`
* `target_id`
* `status`
* `response_time`
* `response_code`
* `response_body`
* `request_body`
* `assertion_results`
* `extracted_variables`
* `error_message` 

### 建议新增字段

#### 1）`case_id`

因为当前 `execution.target_id` 更偏执行对象，而 `result.target_id` 存的是 endpoint/definition 语义，不够清楚。

```sql id="34vuta"
case_id INTEGER NULL REFERENCES api_cases(id)
```

#### 2）`definition_id`

建议显式保留，不靠 `target_id` 模糊表达。

#### 3）`target_name`

前端展示直接用。

```sql id="krv0l9"
target_name VARCHAR(255) NULL
```

#### 4）`request_headers`

#### 5）`response_headers`

方便问题排查。

#### 6）`request_display_type`

#### 7）`response_display_type`

值：

* `json`
* `text`
* `html`
* `binary`
* `unknown`

#### 8）`assertion_passed_count`

#### 9）`assertion_total_count`

避免列表页每次现算。

#### 10）`sort_order`

为场景/套件结果展示预留顺序字段。

---

## 4.3 索引设计

建议新增：

```sql id="otzd90"
CREATE INDEX ix_test_executions_version_id ON test_executions(version_id);
CREATE INDEX ix_test_executions_parent_execution_id ON test_executions(parent_execution_id);
CREATE INDEX ix_test_executions_project_version_env ON test_executions(project_id, version_id, environment_id);
CREATE INDEX ix_test_execution_results_case_id ON test_execution_results(case_id);
CREATE INDEX ix_test_execution_results_definition_id ON test_execution_results(definition_id);
CREATE INDEX ix_test_execution_results_execution_status ON test_execution_results(execution_id, status);
```

---

# 五、执行状态与结果状态的统一规范

当前代码里，执行表状态和结果状态语义混在一起的风险很大。 

这次必须统一。

---

## 5.1 执行级状态（Execution Status）

用于 `TestExecution.status`

值定义：

* `pending`
* `running`
* `completed`
* `failed`
* `cancelled`

### 含义

这是**任务过程状态**，不是断言状态。

---

## 5.2 结果级状态（Result Status）

用于 `TestExecutionResult.status`

值定义：

* `passed`
* `failed`
* `error`
* `skipped`

### 含义

这是**结果判定状态**。

---

## 5.3 执行列表的展示状态

前端列表页建议展示两个状态：

### 执行状态

例如：

* 已完成
* 运行中
* 失败

### 结果状态

例如：

* 通过
* 失败
* 部分失败

对于父执行：

* 如果 `failed > 0 and passed > 0`，展示 `partial_failed`
* 如果 `failed == 0 and passed > 0`，展示 `passed`
* 如果 `passed == 0 and failed > 0`，展示 `failed`

---

# 六、完整后端模块设计

---

## 6.1 新增模块一：`executions.py`

职责：执行记录查询与执行结果展示。

### 路由设计

#### 1）获取执行列表

`GET /executions`

筛选参数：

* `project_id`
* `version_id`
* `environment_id`
* `execution_type`
* `status`
* `result_status`
* `triggered_by`
* `keyword`
* `started_after`
* `started_before`
* `page`
* `page_size`
* `sort_by`
* `sort_order`

#### 2）获取执行详情

`GET /executions/{execution_id}`

返回：

* 执行头信息
* 统计
* 环境信息
* 版本信息
* 子执行数量
* 子执行摘要

#### 3）获取执行明细列表

`GET /executions/{execution_id}/results`

支持：

* `status`
* `keyword`
* `page`
* `page_size`

#### 4）获取单条执行结果详情

`GET /execution-results/{result_id}`

返回：

* request
* response
* assertions
* extracted_variables
* error_message

#### 5）获取子执行列表

`GET /executions/{execution_id}/children`

用于批量/场景/套件。

---

## 6.2 新增模块二：`reports.py`

职责：报告聚合。

### 路由设计

#### 1）概览统计

`GET /reports/summary`

返回：

* total_executions
* total_results
* pass_rate
* fail_rate
* avg_response_time
* today_count
* recent_7d_count

#### 2）趋势统计

`GET /reports/trends`

参数：

* `group_by=day|week|month`
* `metric=executions|pass_rate|fail_rate|avg_response_time`

#### 3）失败分布

`GET /reports/failures`

返回：

* by_definition
* by_case
* by_status_code
* by_error_message

#### 4）性能排行

`GET /reports/performance`

返回：

* slowest_cases
* slowest_definitions
* timeout_like_cases

#### 5）环境对比

`GET /reports/environment-comparison`

#### 6）版本对比

`GET /reports/version-comparison`

---

## 6.3 新增模块三：`execution_schemas.py`

职责：统一 DTO。

### 必要模型

* `ExecutionListItem`
* `ExecutionDetailResponse`
* `ExecutionResultListItem`
* `ExecutionResultDetailResponse`
* `ReportSummaryResponse`
* `TrendPoint`
* `FailureDistributionResponse`
* `PerformanceRankingResponse`

---

## 6.4 新增模块四：`execution_query_service.py`

职责：统一查询、聚合、DTO 转换。

### 不建议直接在路由里写 SQLAlchemy 逻辑

否则：

* `executions.py`
* `reports.py`
* 未来场景/套件页
  会重复一堆聚合逻辑。

### service 里至少拆这些方法

* `list_executions()`
* `get_execution_detail()`
* `list_execution_results()`
* `get_execution_result_detail()`
* `get_report_summary()`
* `get_report_trends()`
* `get_failure_distribution()`
* `get_performance_ranking()`

---

# 七、写侧完整改造设计

---

## 7.1 单用例执行写侧改造

当前单用例执行由 `api_cases.py` 调 `CaseExecutor.execute_case()`。

### 要补的内容

在执行前获取：

* `current_project_id`
* `current_version_id`
* 当前用户 ID

并传入 `CaseExecutor`。

### 执行落库时

写入：

* `version_id`
* `operator_user_id`
* `title`
* `summary_json`

---

## 7.2 批量执行写侧改造

当前批量执行只是 `execute_batch()` 循环调 `execute_case()`。 

这必须改。

### 建议新逻辑

#### 第一步：创建父执行

创建一条 `TestExecution` 作为批次头：

* `execution_type = batch`
* `title = "批量执行：{n} 个用例"`
* `total = n`
* `status = running`

#### 第二步：每个用例执行写子记录

每个子执行写自己的 `TestExecution`

* `parent_execution_id = parent.id`
* `execution_type = single`

#### 第三步：父执行聚合更新

所有子执行结束后，回填父记录：

* `passed`
* `failed`
* `skipped`
* `duration`
* `status`

---

## 7.3 执行结果写入标准化

当前 `CaseExecutor` 直接把 request/response raw 包进 JSON。

建议改成标准化写入：

### request

```json id="hyxq1y"
{
  "raw": "{\"name\":\"test\"}",
  "json": {"name": "test"},
  "display_type": "json"
}
```

### response

```json id="4p6izv"
{
  "raw": "{\"code\":0,\"data\":{}}",
  "json": {"code": 0, "data": {}},
  "display_type": "json"
}
```

如果解析失败：

```json id="9q8s3x"
{
  "raw": "<html>...</html>",
  "json": null,
  "display_type": "html"
}
```

---

# 八、前端完整页面设计

你虽然这次重点是后端，但这三块功能如果不把页面模型一起设计，后端很容易做偏。

---

## 8.1 页面一：执行记录列表页

### 展示维度

表格列建议：

* 执行 ID
* 标题
* 执行类型
* 执行状态
* 结果状态
* 项目
* 版本
* 环境
* 触发方式
* 总数
* 通过
* 失败
* 时长
* 开始时间
* 操作

### 筛选区

* 项目
* 版本
* 环境
* 执行类型
* 状态
* 时间范围
* 关键字

### 操作

* 查看详情
* 查看报告
* 查看结果明细

---

## 8.2 页面二：执行详情页

### 顶部卡片

展示：

* 标题
* 执行状态
* 结果状态
* 版本
* 环境
* 触发方式
* 时间
* 持续时长

### 中部统计区

* total / passed / failed / skipped
* pass_rate
* avg_response_time

### 下部 Tab

* 结果明细
* 子执行
* 原始日志（未来预留）

---

## 8.3 页面三：执行结果详情抽屉 / 详情页

### 左侧

* 请求信息
* 请求头
* 请求体

### 右侧

* 响应状态码
* 响应头
* 响应体

### 下方

* 断言结果表
* 提取变量表
* 错误信息

---

## 8.4 页面四：报告页

### 模块区

1. 概览卡片
2. 趋势图
3. 失败排行
4. 性能排行
5. 状态码分布
6. 环境/版本对比

---

# 九、权限与过滤设计

当前很多模块的权限校验还比较弱，你这块要至少做到“读执行记录不能串项目”。这次不能再放松。前面多个管理模块就是这个问题。   

---

## 9.1 执行记录查询权限

所有查询必须至少按：

* 当前用户可访问的 `project_id`
  做限制。

## 9.2 版本过滤

如果传了 `version_id`，必须校验版本属于项目。

## 9.3 环境过滤

如果传了 `environment_id`，必须校验环境属于项目。

---

# 十、性能设计

这三块功能一旦上线，查询量会很快变大，所以这次必须考虑性能。

---

## 10.1 列表页不返回大字段

`GET /executions`
和
`GET /executions/{id}/results`
都**不要返回大 request_body/response_body**。

只返回摘要。

大字段只在：
`GET /execution-results/{result_id}`
里返回。

---

## 10.2 报告聚合尽量走数据库聚合

不要把 10000 条结果拉到 Python 里统计。

建议：

* pass/fail/count/avg 全部 SQL 聚合
* 趋势走 `date_trunc`
* TopN 排行走 group by + count/order by

---

## 10.3 列表接口必须分页

默认：

* page_size = 20
* 最大 100

---

# 十一、完整接口清单

## Execution APIs

* `GET /executions`
* `GET /executions/{execution_id}`
* `GET /executions/{execution_id}/children`
* `GET /executions/{execution_id}/results`
* `GET /execution-results/{result_id}`

## Report APIs

* `GET /reports/summary`
* `GET /reports/trends`
* `GET /reports/failures`
* `GET /reports/performance`
* `GET /reports/environment-comparison`
* `GET /reports/version-comparison`

---

# 十二、接口响应结构完整版

---

## 12.1 执行列表项

```json id="re3u1t"
{
  "id": 101,
  "parent_execution_id": null,
  "title": "批量执行：5 个用例",
  "execution_type": "batch",
  "execution_status": "completed",
  "result_status": "partial_failed",
  "project_id": 1,
  "version_id": 12,
  "environment_id": 3,
  "triggered_by": "manual",
  "total": 5,
  "passed": 4,
  "failed": 1,
  "skipped": 0,
  "duration": 1243,
  "started_at": "2026-03-27T10:00:00Z",
  "finished_at": "2026-03-27T10:00:01Z",
  "summary": {
    "batch_size": 5
  }
}
```

---

## 12.2 执行详情

```json id="ex9l5h"
{
  "id": 101,
  "title": "批量执行：5 个用例",
  "execution_type": "batch",
  "execution_status": "completed",
  "result_status": "partial_failed",
  "project": {"id": 1, "name": "BugSeek"},
  "version": {"id": 12, "version_number": "V2.0"},
  "environment": {"id": 3, "name": "Test"},
  "triggered_by": "manual",
  "stats": {
    "total": 5,
    "passed": 4,
    "failed": 1,
    "skipped": 0,
    "pass_rate": 80.0
  },
  "children_count": 5,
  "started_at": "...",
  "finished_at": "...",
  "duration": 1243
}
```

---

## 12.3 执行结果详情

```json id="gv5j6j"
{
  "result_id": 999,
  "execution_id": 101,
  "case_id": 88,
  "definition_id": 22,
  "target_name": "获取用户列表",
  "status": "failed",
  "response_time": 132,
  "response_code": 200,
  "request": {
    "display_type": "json",
    "raw": "{\"page\":1}",
    "json": {"page": 1}
  },
  "response": {
    "display_type": "json",
    "raw": "{\"code\":0,\"data\":[]}",
    "json": {"code": 0, "data": []}
  },
  "assertions": {
    "passed": 2,
    "total": 3,
    "items": [...]
  },
  "extracted_variables": {},
  "error_message": "断言 $.data.total == 1 失败"
}
```

---

# 十三、代码级改动文件清单

## 修改

* `backend/app/db/base.py` 
* `backend/app/core/test_execution.py` 
* `backend/app/api/v1/api_cases.py` 
* `backend/app/api/v1/__init__.py` 

## 新增

* `backend/app/api/v1/executions.py`
* `backend/app/api/v1/reports.py`
* `backend/app/api/v1/execution_schemas.py`
* `backend/app/services/execution_query_service.py`

---

# 十四、开发实施顺序

## M1：执行记录查询

完成：

* 表字段扩充
* `GET /executions`
* `GET /executions/{id}`
* `GET /executions/{id}/results`
* `GET /execution-results/{id}`

## M2：批量执行父子关系

完成：

* `parent_execution_id`
* 批量执行父记录
* 子执行关联

## M3：报告聚合

完成：

* `GET /reports/summary`
* `GET /reports/trends`
* `GET /reports/failures`
* `GET /reports/performance`

## M4：结果展示协议优化

完成：

* request/response JSON 结构化
* assertion 统计预计算
* target_name / summary_json 落库

---

# 十五、最终工程判断

这三块如果做“完整版”，正确方向不是只补两个接口，而是做成：

### 一个完整的执行中心后端

它的核心结构应该是：

* **Execution Head**：执行头部
* **Execution Result**：执行原子结果
* **Execution Query Service**：统一读侧服务
* **Report Aggregate Layer**：统一聚合层
* **Display DTO Protocol**：统一展示协议

这样后面无论你接：

* 场景执行
* 套件执行
* CI 回归
* 定时任务
* AI 失败分析

都不用推翻。