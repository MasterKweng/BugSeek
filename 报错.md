你好。作为算法老兵，我将你原本的 **12 项修复清单** 与团队补充的 **5 项关键建议** 进行了深度融合。

这是一份**最终定稿、无死角**的实施清单。它将工程鲁棒性（Robustness）提升到了与算法准确性同等的高度。

---

### 🔴 P0：致命架构与一致性修复 (Critical)
*必须优先修复，否则会导致数据损坏、任务无法停止或状态欺骗。*

#### 1. 修复：batch-apply 强制精确命中，禁止模糊回退
*   **问题**：接口目前允许只传 `task_id` 进行模糊匹配，并发下极易误改其他建议。
*   **最终方案**：
    *   **强制校验**：入参必须包含 `suggestion_id`。如果前端仅传了坐标（task_id + path），则必须是**完全精确匹配**。
    *   **移除回退**：彻底删除“按相似度查找”或“取第一条”的回退分支。匹配不到即报错 `404 Not Found` 或 `400 Bad Request`。
*   **涉及文件**：`field_mappings.py`

#### 2. 修复：双写失败的“部分成功”状态与补写机制
*   **问题**：Celery 任务成功但 DB 写入失败（被吞），导致前端“假成功”。
*   **最终方案**：
    *   **状态扩展**：引入 `PARTIAL_SUCCESS` 任务状态。
    *   **事务控制**：Worker 中执行 `bulk_insert`。如果全量失败，Task 置为 `FAILURE`。
    *   **部分失败**：如果因特殊字符等原因导致部分写入失败，捕获异常，记录失败条目 ID，将 Task 置为 `PARTIAL_SUCCESS`。
    *   **补写接口**：提供 `/retry-failed` 接口，仅重试写入失败的建议。
*   **涉及文件**：`tasks.py`, `processor.py`

#### 3. 修复：取消任务必须是“硬取消 + 软清理”
*   **问题**：仅修改 DB 状态无法停止正在运行的 AI 线程或死循环。
*   **最终方案**：
    *   **Hard Stop**：API 调用 `celery.control.revoke(task_id, terminate=True)`，强制杀掉 Worker 进程（需配合 DB 连接池回收）。
    *   **Soft Check**：保留 `Processor` 中的 `_check_cancellation()` 检查点，用于在非暴力终止下优雅退出并释放资源。
*   **涉及文件**：`field_mappings_async.py`, `processor.py`

#### 4. 修复：恢复数据缺 Schema 版本导致的反序列化失败
*   **问题**：Checkpoint 数据结构变更后，旧任务无法恢复。
*   **最终方案**：
    *   **版本控制**：保存 `{"version": 1, "data": ...}`。
    *   **兼容策略**：读取时校验 Version。版本不匹配则**丢弃旧状态**，强制重跑（Fail-safe），防止 Crash。
*   **涉及文件**：`processor.py`

#### 5. 修复：DB Column 类型迁移
*   **问题**：`current_stage` 字段类型不匹配（VARCHAR vs Integer）。
*   **最终方案**：应用启动自检 + 迁移脚本。
*   **涉及文件**：`base.py`, `migrations/`

---

### 🟠 P1：核心算法逻辑与精度修正 (High)
*修复后将显著提升映射的准确率和智能度。*

#### 6. 重构：阶段1“去重粒度过粗”导致的上下文丢失 (核心算法修正)
*   **问题**：全局聚合导致 `path.id` 和 `body.id` 混淆，且丢失 API 维度上下文，致使重心表失效。
*   **最终方案**：
    *   **取消物理聚合**：阶段1 生成扁平的 `APIFieldInstance` 列表（保留 `api_id`, `source_type`）。
    *   **逻辑去重**：仅在向量搜索环节做 `field_name` 缓存。
    *   **复合键**：缓存键使用 `normalized_name + source_type`。
    *   **上下文回填**：搜索结果回填给每个 API 实例，确保每个 API 拥有独立的重心计算上下文。
*   **涉及文件**：`processor.py`

#### 7. 优化：阶段2“单一向量检索”导致的短词匹配差
*   **问题**：`id`, `qty` 等短词向量噪音大。
*   **最终方案**：**混合检索策略**。
    *   **关键词匹配**：精确/前缀匹配 DB 列名，Score=1.0。
    *   **向量检索**：Embeddings 找 Top 20。
    *   **合并**：取并集。
*   **涉及文件**：`processor.py`, `vector_index.py`

#### 8. 修复：阶段3“优先级覆盖”Bug
*   **问题**：低优先级规则覆盖高优先级规则。
*   **最终方案**：`current_priority = max(current_priority, new_rule_priority)`。
*   **涉及文件**：`processor.py`

#### 9. 优化：阶段5“融合策略”引入来源权威性
*   **问题**：AI 结果被规则结果平权处理。
*   **最终方案**：
    *   **AI 锁定**：AI 选中的候选，Score 锁定 `0.99` 并置顶。
    *   **规则降权**：非 AI 候选 Score 乘 `0.9`。
*   **涉及文件**：`processor.py`

#### 10. 修复：语义冲突规则误报
*   **问题**：启发式规则误报率高。
*   **最终方案**：改用 **重心表离群检测** —— 候选表不属于当前 API 重心表的，视为冲突。
*   **涉及文件**：`processor.py`

---

### 🟡 P2：工程稳定性与可观测性 (Medium)
*提升系统的长期健康度和可维护性。*

#### 11. 新增：可观测性与审计链路 (Audit Trace)
*   **问题**：无法排查映射错误的根本原因。
*   **最终方案**：
    *   **结构化日志**：`FieldMappingSuggestion` 表增加 JSON 字段 `decision_trace`。
    *   **内容**：记录 `vector_score`, `gravity_boost`, `ai_selected`, `rule_hit`, `schema_version`。
*   **涉及文件**：`models.py`, `processor.py`

#### 12. 优化：AI 批处理线程模型
*   **问题**：频繁创建线程池开销大。
*   **最终方案**：全局单例 `ThreadPoolExecutor` 或使用 `AsyncClient`。
*   **涉及文件**：`ai/service.py`

#### 13. 新增：明确回归测试矩阵 (Test Matrix)
*   **问题**：测试覆盖不全面。
*   **最终方案**：建立自动化/手动测试用例集：
    1.  **取消中断**：验证 Worker 进程是否真正终止。
    2.  **双写一致性**：模拟 DB 故障，验证 Task 状态是否变更为 `PARTIAL_SUCCESS/FAILURE`。
    3.  **并发隔离**：多任务并行，验证建议不串岗。
    4.  **精确命中**：验证不传 ID 时接口报错。
    5.  **重试恢复**：验证 Crash 后从 Checkpoint 恢复。

---

### 📅 最终执行建议顺序

1.  **Day 1**: 修复 **P0-2 (双写)**, **P0-3 (取消)**, **P0-1 (精确命中)** —— *先把门关紧，防止生产事故。*
2.  **Day 2**: 重构 **P1-6 (去重)**, **P0-4 (Schema)** —— *修正核心骨架。*
3.  **Day 3**: 实现 **P1-7 (混合检索)**, **P1-9 (融合策略)**, **P2-11 (审计)** —— *提升大脑智商。*
4.  **Day 4**: 修复 **P1-8 (优先级)**, **P1-10 (冲突检测)** —— *优化细节。*
5.  **Day 5**: 执行 **P2-13 (测试矩阵)** —— *验收交付。*