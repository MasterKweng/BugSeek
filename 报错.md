





   根据 Postman 的界面设计模式，我来给出来源配置的改动方案：

  Postman 界面分析

  1. Basic Auth
  界面布局：
   - 左侧：下拉选择 "Basic Auth"
   - 右侧：两个字段
     - Username 输入框
     - Password 输入框

  关键特点：
   - ✅ 只需要两个输入字段
   - ✅ 不需要配置参数位置（固定为 Authorization header）
   - ✅ 不需要配置注入键名（固定为 Authorization）
   - ✅ 不需要值模板（固定格式：Basic base64(username:password)）

  2. Bearer Token
  界面布局：
   - 左侧：下拉选择 "Bearer Token"
   - 中间：标签 "Token"
   - 右侧：Token 输入框

  关键特点：
   - ✅ 只需要一个输入字段
   - ✅ 不需要配置参数位置（固定为 Authorization header）
   - ✅ 不需要配置注入键名（固定为 Authorization）
   - ✅ 不需要值模板（固定格式：Bearer {token}）

  3. API Key
  界面布局（推测）：
   - Key: 参数名称输入框（如 X-API-Key）
   - Value: API Key 值输入框
   - Add to: 选择添加位置（Header/Query Params）

  关键特点：
   - ✅ 需要配置参数名称
   - ✅ 需要配置添加位置

  ---

  来源配置改动方案

  核心原则
  根据 login_auth_type 的选择，动态调整"参数映射"区域的显示方式和必填字段。

  方案设计

  保持不变的部分
   - ✅ 来源模式：静态 / 动态
   - ✅ 登录接口 ID：动态模式下需要
   - ✅ 提取规则：保持不变

  需要改动的部分

  参数映射区域根据 login_auth_type 动态调整：


  ┌───────────────┬──────────────────┬─────────────────────────────────┬──────────┐
  │ login_auth_type │ 参数映射显示方式 │ 必填字段                        │ 自动填充 │
  ├───────────────┼──────────────────┼─────────────────────────────────┼──────────┤
  │ None          │ 完全自定义模式   │ 用户自己配置所有字段            │ 无       │
  │ Basic         │ 简化模式         │ Username, Password              │ 无       │
  │ Bearer        │ 简化模式         │ Token                           │ 无       │
  │ API Key       │ 简化模式         │ API Key, Add to（Header/Query） │ 无       │
  │ Session       │ 简化模式         │ Session ID                      │ 无       │
  │ Custom        │ 完全自定义模式   │ 用户自己配置所有字段            │ 无       │
  └───────────────┴──────────────────┴─────────────────────────────────┴──────────┘

  具体实现方案

  1. None（自定义）
  显示：完整的参数映射表单
   参数映射：
   +---------+----------+----------+
   | 位置     | 键名     | 值       |
   +---------+----------+----------+
   | header  |          |          |
   +---------+----------+----------+
   | body    |          |          |
   +---------+----------+----------+
   | query   |          |          |
   +---------+----------+----------+
   [添加参数映射]

  2. Basic
  显示：简化为两个固定字段
   参数映射（Basic 认证）：
   +----------+----------+
   | Username | [输入框] |
   +----------+----------+
   | Password | [输入框] |
   +----------+----------+
  后端逻辑：
   - 自动构建为：Authorization: Basic base64(username:password)

  3. Bearer
  显示：简化为一个固定字段
   参数映射（Bearer 认证）：
   +----------+----------+
   | Token    | [输入框] |
   +----------+----------+
  后端逻辑：
   - 自动构建为：Authorization: Bearer {token}

  4. API Key
  显示：简化为三个固定字段
   参数映射（API Key 认证）：
   +----------+-------------------+
   | Key      | [输入框]           |
   +----------+-------------------+
   | Value    | [输入框]           |
   +----------+-------------------+
   | Add to   | [Header/Query]      |
   +----------+-------------------+
  后端逻辑：
   - 根据选择的位置自动构建

  5. Session
  显示：简化为一个固定字段
   参数映射（Session 认证）：
   +-----------+----------+
   | Session ID | [输入框]  |
   +-----------+----------+
   | Cookie名   | [输入框]  |
   +-----------+----------+
  后端逻辑：
   - 自动构建为 Cookie header

  6. Custom
  显示：同 None（完全自定义）
   参数映射：
   +---------+----------+----------+
   | 位置     | 键名     | 值       |
   +---------+----------+----------+
   | header  |          |          |
   +---------+----------+----------+
   | body    |          |          |
   +---------+----------+----------+
   | query   |          |          |
   +---------+----------+----------+
   [添加参数映射]

  前端改动点

  位置：AuthConfigForm.tsx 组件，来源配置区域的参数映射部分

  改动逻辑：
   1. 监听 login_auth_type 字段的变化
   2. 根据选择的值，渲染不同的表单：
      - Basic/Bearer/API Key/Session：渲染简化版表单
      - None/Custom：渲染完整的参数映射表单（支持位置、键名、值的自由配置）

  后端改动点

  位置：auth_config.py 的 _acquire_token_from_template 函数

  改动逻辑：
   - Basic：从简化的 Username/Password 字段获取数据，编码为 Basic 认证
   - Bearer：从简化的 Token 字段获取数据，构建 Bearer 认证
   - API Key：从简化的 Key/Value/Add to 字段获取数据，按指定位置添加
   - Session：从 Session ID 和 Cookie 名字段获取数据，构建 Cookie
   - None/Custom：保持现有的完整参数映射逻辑

  ---

  总结

  Postman 的核心设计理念：根据认证类型只暴露必要的配置项，减少用户的认知负担和配置错误。

  我们的方案：
   - 标准认证类型（Basic/Bearer/API Key/Session）：简化参数映射，只暴露必要字段
   - 自定义认证类型：保持完全灵活的参数映射
   - 向后兼容：不影响现有的 None/Custom 模式

✦ 这样既符合 Postman 的设计标准，又保持了系统的灵活性。