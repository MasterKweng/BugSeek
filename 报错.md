异常 1：向量检索和 RRF 算法“离奇失踪”
看日志：
[阶段一] 关键词召回完成: found=122 candidates -> 直接跳到了 -> [阶段二] 语义重排...
老兵诊断：
这说明代码根本没有执行我们辛辛苦苦写的 _vector_search 和 _merge_recall_results_rrf！
根本原因： 在你上一步的修改中，你在 retrieve_apis_by_intent 里加入了混合检索。但是！intent_workbench.py 现在调用的是你新写的 retrieve_apis_by_intent_lite。你大概率是把旧的关键词检索逻辑复制到了 lite 方法里，却忘记把向量搜索和 RRF 融合逻辑也加进去了。
异常 2：致命的“关键词投毒（Keyword Poisoning）”
看日志：
提取关键词: ['POST', '测试登录流程']
随后，一个完全不相干的库存 API（/api/stock/location-type/）被打出了 5.0 的高分排在第一！
老兵诊断：
你的意图是“测试登录流程”，但系统却把 HTTP 方法名 POST 当成了关键词去搜索！因为库存 API 也是个 POST 接口，它命中了 POST 这个词，导致计算出了高分。这解释了为什么只查一个登录，却找出了惊人的 122 个候选接口。
异常 3：“轻量检索”一点都不轻，依然携带了大量冗余信息
看日志：
在传递给大模型（AI 输入数据）的 Prompt 里，赫然出现了：
描述: API endpoint for a list of StockLocationType objects...
标签: ['stock']
老兵诊断：
我们明明叫它 lite，就是想去掉冗长的描述和标签。之所以它还存在，是因为底层虽然返回了轻量数据，但你仍然在调用旧的 _semantic_rerank（语义重排）大模型任务，而这个旧任务的 Prompt 模板（api_retrieval_ranking）里硬编码了去读取 描述 和 标签。