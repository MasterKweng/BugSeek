✦ 📋 完整改动方案（建议表架构 + 工程细节）

  ---

  一、核心架构设计

  数据流向图

   ┌─────────────────────────────────────────────────────────────────┐
   │                    Celery Task 执行流程                          │
   └─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │  processor.process() 返回 result                                 │
   │  {                                                               │
   │    "status": "success",                                          │
   │    "total": 4320,                                                │
   │    "suggestions": [...]  ← 4320 条建议                           │
   │  }                                                               │
   └─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │  celery/tasks.py: execute_field_mapping_task()                  │
   │                                                                  │
   │  1. 保存 JSON 备份（向后兼容）                                    │
   │     task.result = result                                         │
   │                                                                  │
   │  2. 清理旧建议（幂等性保证）                                      │
   │     DELETE FROM field_mapping_suggestions                       │
   │     WHERE task_id = {task_id}                                   │
   │                                                                  │
   │  3. 批量插入新建议（双写策略）                                    │
   │     INSERT INTO field_mapping_suggestions (...)                 │
   │     VALUES (...)                                                │
   │                                                                  │
   │  4. 提交事务（原子性保证）                                        │
   │     db.commit()                                                 │
   └─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
   ┌─────────────────────────────────────────────────────────────────┐
   │  数据存储结构                                                    │
   │                                                                  │
   │  async_tasks.result (JSON)           field_mapping_suggestions   │
   │  ┌─────────────────────────┐         ┌─────────────────────────┐  │
   │  │ {                       │         │ id │ task_id │ status │  │
   │  │   "suggestions": [...]  │ ←─────→ │ 1  │ 10      │ pending│  │
   │  │ }                       │         │ 2  │ 10      │ pending│  │
   │  └─────────────────────────┘         └─────────────────────────┘  │
   └─────────────────────────────────────────────────────────────────┘

  ---

  二、数据库表结构设计

  新建表：field_mapping_suggestions

  文件位置：backend/app/db/base.py

  表结构定义：

   class FieldMappingSuggestion(Base, TimestampMixin):
       """字段映射建议表（业务工作台）"""
       __tablename__ = "field_mapping_suggestions"

       # 主键
       id = Column(BigInteger, primary_key=True, index=True)

       # 关联字段
       task_id = Column(Integer, ForeignKey("async_tasks.id"), nullable=False, index=True)
       project_id = Column(Integer, ForeignKey("projects.id"), nullable=False, index=True)

       # API 坐标
       definition_id = Column(Integer, ForeignKey("api_definitions.id"), nullable=False, index=True)
       api_field_path = Column(String(255), nullable=False, index=True)

       # 核心数据（候选列表）
       # PostgreSQL JSONB 类型，支持索引和查询
       candidates = Column(JSONB, nullable=False)

       # 状态管理（关键！）
       status = Column(String(50), nullable=False, default="pending", index=True)
       # pending: 待审核
       # accepted: 已接受
       # ignored: 已忽略
       # modified: 已修改

       # 关联已确认的映射（Optional）
       mapping_id = Column(BigInteger, ForeignKey("api_field_mappings.id"), nullable=True)

       # 时间戳
       created_at = Column(DateTime, default=datetime.utcnow, nullable=False)
       updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, nullable=False)

       # 关系定义
       task = relationship("AsyncTask", foreign_keys=[task_id])
       project = relationship("Project", foreign_keys=[project_id])
       definition = relationship("ApiDefinition", foreign_keys=[definition_id])
       mapping = relationship("ApiFieldMapping", foreign_keys=[mapping_id])

       __table_args__ = (
           # 复合索引：任务+状态
           Index('ix_field_mapping_suggestions_task_status', 'task_id', 'status'),
           # 复合索引：项目+定义+字段路径（用于查询某个字段的建议）
           Index('ix_field_mapping_suggestions_project_definition_field',
                 'project_id', 'definition_id', 'api_field_path'),
           # 唯一约束：防止同一任务同一字段重复建议
           UniqueConstraint(
               'task_id', 'definition_id', 'api_field_path',
               name='uq_suggestion_task_definition_field'
           ),
       )

  关键设计说明：

   1. JSONB 类型：PostgreSQL 特有，支持索引和查询，比 JSON 性能更好
   2. 唯一约束：uq_suggestion_task_definition_field 防止数据重复
   3. 复合索引：优化常用查询场景
   4. mapping_id：建立建议和映射的双向关联

  ---

  三、后端改动方案

  改动1：修改 Celery Task（双写策略 + 幂等性保证）

  文件位置：backend/app/celery/tasks.py

  改动内容：

   @celery_app.task(bind=True, name="app.celery.tasks.execute_field_mapping_task")
   def execute_field_mapping_task(self, task_id: int):
       """
       执行字段映射建议任务（支持建议表双写）
       """
       trace_id = get_trace_id()
       db: Session = SessionLocal()

       try:
           # ... 查询任务信息 ...

           # 执行字段映射处理
           processor = FieldMappingProcessor(db, task)
           result = asyncio.run(processor.process())

           # 更新任务状态为完成
           task.status = "completed"
           task.finished_at = datetime.now()

           # 保存结果到数据库（双写策略）
           if result and result.get('success'):
               # 1. 保存 JSON 备份（向后兼容）
               task.result = result

               # 2. 双写到建议表（新增逻辑）
               _save_suggestions_to_db(db, task_id, result)

           db.commit()

           return result
       except Exception as e:
           db.rollback()
           raise
       finally:
           db.close()


   def _save_suggestions_to_db(db: Session, task_id: int, result: Dict[str, Any]):
       """
       将建议数据保存到建议表（幂等性保证）

       遵循后端代码规范：
       - 幂等性保证：先删除旧数据再插入
       - 事务原子性：整个操作在一个事务中
       - 批量插入：使用 bulk_save_objects 提升性能

       Args:
           db: 数据库会话
           task_id: 任务ID
           result: 任务结果
       """
       from app.db.base import FieldMappingSuggestion

       suggestions_list = result.get("suggestions", [])

       if not suggestions_list:
           return

       logger.info(f"[{get_trace_id()}] 开始保存建议数据到数据库: task_id={task_id}, count={len(suggestions_list)}")

       try:
           # 1. 清理旧建议（幂等性保证）
           # 如果任务重试，先删除之前的建议，防止数据重复
           deleted_count = db.query(FieldMappingSuggestion).filter(
               FieldMappingSuggestion.task_id == task_id
           ).delete()

           if deleted_count > 0:
               logger.info(f"[{get_trace_id()}] 清理旧建议数据: {deleted_count} 条")

           # 2. 准备新数据
           suggestion_objects = []

           for item in suggestions_list:
               # 确保 candidates 是可序列化的纯字典/列表
               candidates = item.get("candidates", [])

               # 序列化检查：确保不包含不可序列化的对象
               if not isinstance(candidates, list):
                   logger.warning(f"[{get_trace_id()}] candidates 不是列表: {type(candidates)}")
                   candidates = []

               obj = FieldMappingSuggestion(
                   task_id=task_id,
                   project_id=item.get("project_id", 1),  # 从 task_params 获取
                   definition_id=item.get("definition_id"),
                   api_field_path=item.get("api_field_path"),
                   candidates=candidates,  # JSONB 字段，SQLAlchemy 自动序列化
                   status="pending"
               )
               suggestion_objects.append(obj)

           # 3. 批量插入（性能优化）
           if suggestion_objects:
               db.bulk_save_objects(suggestion_objects)
               logger.info(f"[{get_trace_id()}] 批量插入建议数据: {len(suggestion_objects)} 条")

       except Exception as e:
           logger.error(f"[{get_trace_id()}] 保存建议数据失败: {str(e)}", exc_info=True)
           raise

  关键改动点：

   1. 双写策略：同时写入 JSON 和建议表
   2. 幂等性保证：先删除旧数据，再插入新数据
   3. 序列化检查：确保 candidates 是可序列化的纯字典/列表
   4. 批量插入：使用 bulk_save_objects 提升性能

  ---

  改动2：修改读取接口（真分页 + 状态筛选）

  文件位置：backend/app/api/v1/field_mappings_async.py

  改动内容：

   @router.get("/field-mappings/suggestions", response_model=ApiResponse)
   async def get_suggestions(
       task_id: int,
       status: Optional[str] = Query(None, description="状态筛选"),
       page: int = Query(1, ge=1, description="页码"),
       size: int = Query(20, ge=1, le=100, description="每页数量"),
       db: Session = Depends(get_db),
       current_user: User = Depends(get_current_user)
   ):
       """
       获取字段映射建议结果（建议表版本）

       遵循后端代码规范：
       - 真分页：数据库层面分页
       - 状态筛选：支持 status 参数
       - 水平越权校验（IDOR）：检查资源归属人
       """
       trace_id = get_trace_id()

       # 查询任务
       task = db.query(AsyncTask).filter(AsyncTask.id == task_id).first()
       if not task:
           raise HTTPException(status_code=404, detail=f"任务不存在：{task_id}")

       # 权限校验
       if task.user_id != current_user.id:
           raise HTTPException(status_code=403, detail="无权访问该任务")

       # 构建查询
       from app.db.base import FieldMappingSuggestion

       query = db.query(FieldMappingSuggestion).filter(
           FieldMappingSuggestion.task_id == task_id
       )

       # 状态筛选
       if status:
           query = query.filter(FieldMappingSuggestion.status == status)

       # 获取总数
       total = query.count()

       # 分页查询
       items = query.order_by(FieldMappingSuggestion.id).offset(
           (page - 1) * size
       ).limit(size).all()

       # 转换为响应格式
       items_data = []
       for item in items:
           items_data.append({
               "id": item.id,
               "definition_id": item.definition_id,
               "definition_method": item.definition.method if item.definition else None,
               "definition_path": item.definition.path if item.definition else None,
               "api_field_path": item.api_field_path,
               "candidates": item.candidates,  # JSONB 自动反序列化
               "status": item.status,
               "mapping_id": item.mapping_id
           })

       return ApiResponse(
           code=0,
           message="查询成功",
           data={
               "items": items_data,  # 关键修正：重命名为 items
               "total": total,
               "page": page,
               "size": size
           }
       )

  关键改动点：

   1. 真分页：数据库层面分页，性能更好
   2. 状态筛选：支持 status 参数
   3. JSONB 自动反序列化：item.candidates 自动从 JSONB 转为 Python 对象
   4. 响应格式：统一返回 { items, total, page, size }

  ---

  改动3：修改审核接口（原子性保证）

  文件位置：backend/app/api/v1/field_mappings.py

  改动内容：

   @router.post("/field-mappings/batch-apply", response_model=ApiResponse)
   async def batch_apply_field_mappings(
       request: FieldMappingBatchApplyRequest,
       project_id: Optional[int] = Query(None),
       version_id: Optional[int] = Query(None),
       db: Session = Depends(get_db),
       current_user: User = Depends(get_current_user)
   ):
       """
       批量应用字段映射（原子性保证）

       遵循后端代码规范：
       - 事务原子性：整个操作在一个事务中
       - 双写策略：同时更新 api_field_mappings 和 field_mapping_suggestions
       - 幂等性：如果映射已存在，更新而不是插入
       """
       trace_id = get_trace_id()
       ctx = _get_project_and_version(db, current_user, project_id, version_id)

       from app.db.base import FieldMappingSuggestion

       try:
           created_count = 0
           updated_suggestion_ids = []

           for item in request.items:
               # 1. 验证API定义存在性
               definition = db.query(ApiDefinition).filter(
                   ApiDefinition.id == item.definition_id,
                   ApiDefinition.project_id == ctx["project_id"]
               ).first()

               if not definition:
                   logger.warning(f"[{trace_id}] API定义不存在或无权限: {item.definition_id}")
                   continue

               # 2. 检查是否已存在相同的映射
               existing = db.query(ApiFieldMapping).filter(
                   ApiFieldMapping.project_id == ctx["project_id"],
                   ApiFieldMapping.version_id == ctx["version_id"],
                   ApiFieldMapping.definition_id == item.definition_id,
                   ApiFieldMapping.api_field_path == item.api_field_path,
                   ApiFieldMapping.db_table == item.db_table,
                   ApiFieldMapping.db_column == item.db_column
               ).first()

               mapping = None

               if existing:
                   # 更新现有映射
                   existing.relation_type = item.relation_type
                   existing.source = item.source
                   existing.updated_by = current_user.id
                   mapping = existing
               else:
                   # 创建新映射
                   mapping = ApiFieldMapping(
                       project_id=ctx["project_id"],
                       version_id=ctx["version_id"],
                       definition_id=item.definition_id,
                       api_field_path=item.item.api_field_path,
                       db_table=item.db_table,
                       db_column=item.db_column,
                       relation_type=item.relation_type,
                       source=item.source,
                       status="confirmed" if request.mode == "confirm" else "proposed",
                       created_by=current_user.id,
                       updated_by=current_user.id
                   )
                   db.add(mapping)
                   db.flush()  # 获取 mapping.id

               # 3. 更新建议表状态（新增逻辑）
               # 查找对应的建议记录
               suggestion = db.query(FieldMappingSuggestion).filter(
                   FieldMappingSuggestion.project_id == ctx["project_id"],
                   FieldMappingSuggestion.definition_id == item.definition_id,
                   FieldMappingSuggestion.api_field_path == item.api_field_path,
                   FieldMappingSuggestion.status == "pending"
               ).first()

               if suggestion:
                   # 更新建议状态和关联映射
                   suggestion.status = "accepted"
                   suggestion.mapping_id = mapping.id
                   updated_suggestion_ids.append(suggestion.id)

               created_count += 1

           # 4. 提交事务（原子性保证）
           db.commit()

           logger.info(
               f"[{trace_id}] 批量应用字段映射成功: "
               f"created_count={created_count}, updated_suggestions={len(updated_suggestion_ids)}"
           )

           return ApiResponse(
               code=0,
               message=f"批量应用成功，处理了 {created_count} 个映射",
               data={
                   "processed_count": created_count,
                   "updated_suggestion_count": len(updated_suggestion_ids)
               }
           )

       except Exception as e:
           # 回滚事务（原子性保证）
           db.rollback()
           logger.error(f"[{trace_id}] 批量应用字段映射失败: {str(e)}", exc_info=True)
           raise

  关键改动点：

   1. 事务原子性：整个操作在一个事务中，失败则回滚
   2. 双写策略：同时更新 api_field_mappings 和 field_mapping_suggestions
   3. 关联更新：通过 mapping_id 建立建议和映射的关联
   4. 状态同步：建议表的状态反映用户的审核行为

  ---

  四、前端改动方案

  改动1：适配分页参数

  文件位置：frontend/src/pages/FieldMappingSuggestions.tsx

  改动内容：

   // 添加分页状态
   const [pagination, setPagination] = useState({
     current: 1,
     pageSize: 20,
     total: 0
   });

   // 修改加载函数，传入分页参数
   const loadTaskResults = async (taskId: number, page = 1, size = 20) => {
     try {
       const response = await fieldMappingService.getFieldMappingSuggestions(taskId, {
         page,
         page_size: size
       });

       setSuggestions(response.data?.items || []);
       setPagination({
         current: page,
         pageSize: size,
         total: response.data?.total || 0
       });
     } catch (error: any) {
       console.error('加载任务结果失败:', error);
       message.error(error.message || '加载结果失败');
     }
   };

   // Table 添加分页配置
   <Table
     dataSource={suggestions}
     pagination={{
       current: pagination.current,
       pageSize: pagination.pageSize,
       total: pagination.total,
       showSizeChanger: true,
       showTotal: (total) => `共 ${total} 条`,
       onChange: (page, pageSize) => {
         loadTaskResults(taskId, page, pageSize);
       }
     }}
     // ...
   />

  关键改动点：

   1. 分页状态管理：管理 current、pageSize、total
   2. 服务器分页：调用 API 时传入分页参数
   3. Table 分页配置：Ant Design Table 自动处理分页 UI

  ---

  五、数据库迁移脚本

  文件位置：backend/migrations/add_field_mapping_suggestions_table.py

  脚本内容：

   """
   创建字段映射建议表
   """
   from sqlalchemy import text
   from app.db.session import engine
   from app.db.base import Base
   from app.db.base import FieldMappingSuggestion


   def upgrade():
       """
       创建字段映射建议表
       """
       print("开始创建字段映射建议表...")

       # 创建表
       FieldMappingSuggestion.__table__.create(engine, checkfirst=True)

       # 创建索引
       with engine.connect() as conn:
           # 复合索引：任务+状态
           conn.execute(text("""
               CREATE INDEX IF NOT EXISTS ix_field_mapping_suggestions_task_status
               ON field_mapping_suggestions(task_id, status)
           """))

           # 复合索引：项目+定义+字段路径
           conn.execute(text("""
               CREATE INDEX IF NOT EXISTS ix_field_mapping_suggestions_project_definition_field
               ON field_mapping_suggestions(project_id, definition_id, api_field_path)
           """))

       print("字段映射建议表创建成功！")


   def downgrade():
       """
       删除字段映射建议表
       """
       print("开始删除字段映射建议表...")

       FieldMappingSuggestion.__table__.drop(engine, checkfirst=True)

       print("字段映射建议表删除成功！")


   if __name__ == "__main__":
       import sys

       if len(sys.argv) > 1 and sys.argv[1] == "downgrade":
           downgrade()
       else:
           upgrade()

  ---

  六、改动总结


  ┌──────────┬───────────────────────────────────────────────┬──────────┬───────────────────────────────┐
  │ 改动项   │ 文件位置                                      │ 改动类型 │ 关键点                        │
  ├──────────┼───────────────────────────────────────────────┼──────────┼───────────────────────────────┤
  │ 新建表   │ app/db/base.py                                │ 新增     │ FieldMappingSuggestion 表结构 │
  │ 迁移脚本 │ migrations/add_field_mapping_suggestions_table.py │ 新增     │ 创建表和索引                  │
  │ 双写逻辑 │ celery/tasks.py                               │ 修改     │ 先删除旧数据，再插入新数据    │
  │ 读取接口 │ api/v1/field_mappings_async.py                  │ 修改     │ 真分页 + 状态筛选             │
  │ 审核接口 │ api/v1/field_mappings.py                      │ 修改     │ 事务原子性 + 双写             │
  │ 前端分页 │ pages/FieldMappingSuggestions.tsx             │ 修改     │ 服务器分页适配                │
  └──────────┴───────────────────────────────────────────────┴──────────┴───────────────────────────────┘

  ---

  七、工程细节补充

  1. 幂等性保证

  问题：任务重试导致数据重复

  解决方案：
   # 先删除旧数据
   db.query(FieldMappingSuggestion).filter(
       FieldMappingSuggestion.task_id == task_id
   ).delete()

   # 再插入新数据
   db.bulk_save_objects(suggestion_objects)

  ---

  2. JSON 序列化陷阱

  问题：candidates 字段包含不可序列化的对象

  解决方案：
   # 确保 candidates 是纯字典/列表
   candidates = item.get("candidates", [])

   if not isinstance(candidates, list):
       logger.warning(f"candidates 不是列表: {type(candidates)}")
       candidates = []

  PostgreSQL JSONB：自动处理序列化，无需手动 json.dumps

  ---

  3. 关联更新的原子性

  问题：映射写入成功，但建议表更新失败

  解决方案：
   try:
       # 1. 插入映射
       db.add(mapping)
       db.flush()

       # 2. 更新建议状态
       suggestion.status = "accepted"
       suggestion.mapping_id = mapping.id

       # 3. 提交事务
       db.commit()
   except:
       # 回滚事务
       db.rollback()
       raise

  ---

  八、执行顺序

   1. 创建表：运行迁移脚本创建 field_mapping_suggestions 表
   2. 修改后端：
      - 修改 celery/tasks.py 添加双写逻辑
      - 修改 api/v1/field_mappings_async.py 改为查表
      - 修改 api/v1/field_mappings.py 添加事务更新
   3. 修改前端：适配服务器分页
   4. 测试：创建新任务，验证建议列表显示

  ---

  九、架构优势总结


  ┌────────────┬────────────────┬──────────────┐
  │ 维度       │ JSON 方案      │ 建议表方案   │
  ├────────────┼────────────────┼──────────────┤
  │ 查询性能   │ 差（全量传输） │ 优（真分页） │
  │ 状态管理   │ ❌ 不支持      │ ✅ 支持      │
  │ 复杂查询   │ ❌ 不支持      │ ✅ 支持      │
  │ 幂等性     │ ❌ 不支持      │ ✅ 支持      │
  │ 原子性     │ N/A            │ ✅ 支持      │
  │ 扩展性     │ 差             │ 优           │
  │ 架构清晰度 │ 差             │ 优           │
  └────────────┴────────────────┴──────────────┘


✦ 这是最正统的"企业级"做法！ 🚀