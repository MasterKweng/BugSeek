---

# 🚀 字段映射引擎最终实施方案 (生产增强版)

本方案在保留现有五阶段代码骨架的前提下，通过注入“安全网”、“领域约束”和“白盒审计”，实现系统的平滑且高价值演进。

## 🛠️ 第一层：AI 安全与柔性护栏 (解决 Issue 1, 2)
**目标**：防止 AI 幻觉（造假），同时保留 AI 纠正规则错误的权力。

### 1. 强制 Prompt 输出规范 (解决 Issue 2)
在 `_build_ai_prompt_v2` 中，必须明确要求 AI 输出严格的 JSON 格式，并附带置信度：
```json
// Prompt 约束：
"请严格按照以下 JSON 格式输出，不要包含任何其他字符：
{
  "table": "数据库中真实存在的表名",
  "column": "数据库中真实存在的列名",
  "confidence": 0.85  // 0.0到1.0之间的置信度分数
}"
```
*在代码解析中增加防御：如果 AI 没有返回 `confidence`，代码默认赋值 `0.5`。*

### 2. 双层安全校验 (解决 Issue 1)
在 `_priority_ai_calling` 接收到 AI 结果后，实施**双层校验**：

*   **第一层：物理存在校验（必须通过，否则硬拒绝）**
    *   **逻辑**：`table` 是否在 `db_schema` 中？`column` 是否在该表的列中？
    *   **动作**：如果不存在，记录 `AI_HALLUCINATION`，直接丢弃，回退到规则 Top1。
*   **第二层：域合理性校验（软校验，防误杀）**
    *   **逻辑**：检查 `ai_table` 是否在 `graph_context['valid_tables']` 中。
    *   **动作**：如果**不在**，说明 AI 的判断与规则引擎的图谱推断发生冲突。此时**不拒绝** AI，而是记录 Warning，并将 AI 的 `confidence` 分数打个折扣（例如 `confidence * 0.8`），然后交由后续合并逻辑公平竞争。保留了 AI 纠错的可能性。

---

## 👁️ 第二层：结构增强与精准域推断 (解决 Issue 3, 4)
**目标**：在进入向量搜索前，精准圈定 `allowed_tables`，并防止公共词污染。

### 1. 权重分离的域推断 (解决 Issue 3)
创建 `DomainInferer` 类，采用分离权重策略：
```python
def infer_domain(self, api_path: str, fields: List[str]) -> str:
    # 1. 路径推断 (权重 0.7) - 从 "/api/purchase_orders/create" 提取 "purchase_orders"
    path_domain = self.extract_from_path(api_path) 
    
    # 2. 字段前缀统计 (权重 0.3)
    field_prefixes = self.count_field_prefixes(fields) # 忽略 common_words
    
    # 加权合并，得出 Top 域
    return self.weighted_merge(path_domain, field_prefixes)
```

### 2. 表名标准化处理 (解决 Issue 4)
不能直接使用 `in` 进行模糊匹配，必须引入 `normalize_name`。
```python
import inflect
p = inflect.engine()

def normalize_name(self, name: str) -> str:
    # 1. 转小写
    name = name.lower()
    # 2. 去除 ERP 常见前缀 (t_, sys_, erp_)
    name = re.sub(r'^(t_|sys_|erp_)', '', name)
    # 3. 单词单复数归一化 (orders -> order)
    singular = p.singular_noun(name)
    return singular if singular else name
```
*基于规范化后的名字，去匹配数据库表，圈定 `allowed_tables`。*

### 3. 向量搜索施加护栏
在 `processor.py` 中，调用向量搜索时：
```python
# 仅在 DomainInferer 圈定的 allowed_tables 中进行向量检索
candidates = vector_manager.search_in_tables(field_name, allowed_tables)
```

---

## 📝 第三层：全息可追溯体系 (解决 Issue 5, 6)
**目标**：建立完整的审计日志，让系统未来具备调参和自学习的能力。

### 1. `field_mapping_traces` 表结构升级
在原有的基础上，必须包含归因字段和域评分字段：

| 字段名 | 类型 | 说明 (解决的问题) |
| :--- | :--- | :--- |
| `id` | PK | 主键 |
| `field_key` | STR | 关联的字段（如 `11:body.goods_id`） |
| `candidate` | STR | 候选表和列 (`order_items.goods_id`) |
| `stage` | STR | 处于哪个阶段 (Rule_Scoring, AI_Optimized) |
| `decision_source` | STR | **(Issue 5) 来源：`rule`, `ai`, `fallback`, `manual`** |
| `in_allowed_tables`| BOOL | **(Issue 6) 是否在 DomainInferer 圈定范围内** |
| `is_anchor_table` | BOOL | 是否属于图谱计算出的核心表 |
| `s_vector` | FLOAT| 向量原始得分 |
| `s_exact` | FLOAT| 精确匹配得分 |
| `s_graph` | FLOAT| 图谱连通得分 |
| `final_score` | FLOAT| 最终加权得分 |

### 2. 埋点写入时机
*   在 `calculate_final_score_v2` 后批量写入规则的 Traces (`decision_source = 'rule'`)。
*   在 `_priority_ai_calling` 后写入 AI 的 Traces (`decision_source = 'ai' 或 'fallback'`)。

---

## 📅 最终实施执行顺序 (Actionable Steps)

为了最快见效且最小化风险，请按以下顺序敲代码：

1.  **Step 1 (P0): AI 防护网**
    *   修改 `parse_json_safely` 确保提取 confidence。
    *   在 `_priority_ai_calling` 中增加物理校验 (`_column_exists`) 和逻辑软校验。
2.  **Step 2 (P1): 数据库与 Trace 埋点**
    *   创建包含 `decision_source` 和 `in_allowed_tables` 的 `traces` 表。
    *   在当前流程中注入写库代码（先跑起来，看当前系统真实长什么样）。
3.  **Step 3 (P1): 表名标准化工具**
    *   实现 `normalize_name` 工具函数（引入 `inflect`），写单元测试确保 `t_purchase_orders` 能匹配到 `purchase_order`。
4.  **Step 4 (P2): 域约束注入**
    *   实现带权重的 `DomainInferer`。
    *   将其生成的 `allowed_tables` 传给 Vector Index 进行预过滤。

**最后确认**：这份包含 6 大核心补丁的方案，已经彻底闭环。它既不破坏你现有的 5 阶段流转，又完美堵住了 AI 幻觉、域污染、表名匹配失败和审计黑盒等一系列生产级大坑。