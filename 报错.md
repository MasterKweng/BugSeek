  调整后的方案一B：左侧可交互列表 + 右侧动态展示

   ┌─────────────────────────────────────────────────────────────────────┐
   │  模块分析                                                           │
   │  [刷新] [分析所有] [分析选中] [分析模块间依赖]                       │
   ├────────────────────┬────────────────────────────────────────────────┤
   │  左侧：模块列表    │  右侧：选中模块的详细信息                        │
   │  ┌──────────────┐ │  ┌────────────────────────────────────────────┐ │
   │  │ 模块1        │ │  │ 📋 模块：模块2                            │ │
   │  │   依赖:3 🔗  │ │  ├────────────────────────────────────────────┤ │
   │  │   输入:5 📥  │ │  │ 🔗 依赖关系                               │ │
   │  │   输出:8 📤  │ │  │   - 模块A -> 模块B (强度: 0.8)            │ │
   │  │ 模块2 (选中) │ │  │   - 模块C -> 模块B (强度: 0.6)            │ │
   │  │   依赖:3 🔗  │ │  │   - 模块D -> 模块B (强度: 0.5)            │ │
   │  │   输入:5 📥  │ │  │                                            │ │
   │  │   输出:8 📤  │ │  │ [分析模块间依赖]                           │ │
   │  │ 模块3        │ │  └────────────────────────────────────────────┘ │
   │  │   依赖:2 🔗  │                                             │
   │  │   输入:3 📥  │                                             │
   │  │   输出:4 📤  │                                             │
   │  │ ...          │ │                                             │
   │  └──────────────┘ │                                             │
   └───────────────────┴─────────────────────────────────────────────┘

   点击模块2下的"依赖"后：
   ┌─────────────────────────────────────────────────────────────────────┐
   │  模块分析                                                           │
   │  [刷新] [分析所有] [分析选中] [分析模块间依赖]                       │
   ├────────────────────┬────────────────────────────────────────────────┤
   │  左侧：模块列表    │  右侧：选中模块的详细信息                        │
   │  ┌──────────────┐ │  ┌────────────────────────────────────────────┐ │
   │  │ 模块1        │ │  │ 📋 模块：模块2                            │ │
   │  │   依赖:3 🔗  │ │  ├────────────────────────────────────────────┤ │
   │  │   输入:5 📥  │ │  │ 🔗 依赖关系 (3个)                        │ │
   │  │   输出:8 📤  │ │  │   - 模块A -> 模块B (强度: 0.8)            │ │
   │  │ 模块2 (选中) │ │  │   - 模块C -> 模块B (强度: 0.6)            │ │
   │  │   依赖:3 🔗 ⬅ │  │    - 模块D -> 模块B (强度: 0.5)            │ │
   │  │   输入:5 📥  │ │  │                                            │ │
   │  │   输出:8 📤  │ │  │ [分析模块间依赖]                           │ │
   │  │ 模块3        │ │  └────────────────────────────────────────────┘ │
   │  │   依赖:2 🔗  │                                             │
   │  │   输入:3 📥  │                                             │
   │  │   输出:4 📤  │                                             │
   │  │ ...          │ │                                             │
   │  └──────────────┘ │                                             │
   └───────────────────┴─────────────────────────────────────────────┘

   点击模块2下的"输入"后：
   ┌─────────────────────────────────────────────────────────────────────┐
   │  模块分析                                                           │
   │  [刷新] [分析所有] [分析选中] [分析模块间依赖]                       │
   ├────────────────────┬────────────────────────────────────────────────┤
   │  左侧：模块列表    │  右侧：选中模块的详细信息                        │
   │  ┌──────────────┐ │  ┌────────────────────────────────────────────┐ │
   │  │ 模块1        │ │  │ 📋 模块：模块2                            │ │
   │  │   依赖:3 🔗  │ │  ├────────────────────────────────────────────┤ │
   │  │   输入:5 📥  │ │  │ 📥 输入接口 (5个)                        │ │
   │  │   输出:8 📤  │ │  │   - GET /api/users                        │ │
   │  │ 模块2 (选中) │ │  │   - POST /api/login                       │ │
   │  │   依赖:3 🔗  │ │  │   - GET /api/config                       │ │
   │  │   输入:5 📥 ⬅ │  │   - GET /api/settings                     │ │
   │  │   输出:8 📤  │ │  │   - POST /api/auth/refresh                │ │
   │  │ 模块3        │ │  │                                            │ │
   │  │   依赖:2 🔗  │ │  └────────────────────────────────────────────┘ │
   │  │   输入:3 📥  │                                             │
   │  │   输出:4 📤  │                                             │
   │  │ ...          │ │                                             │
   │  └──────────────┘ │                                             │
   └───────────────────┴─────────────────────────────────────────────┘

   点击模块2下的"输出"后：
   ┌─────────────────────────────────────────────────────────────────────┐
   │  模块分析                                                           │
   │  [刷新] [分析所有] [分析选中] [分析模块间依赖]                       │
   ├────────────────────┬────────────────────────────────────────────────┤
   │  左侧：模块列表    │  右侧：选中模块的详细信息                        │
   │  ┌──────────────┐ │  ┌────────────────────────────────────────────┐ │
   │  │ 模块1        │ │  │ 📋 模块：模块2                            │ │
   │  │   依赖:3 🔗  │ │  ├────────────────────────────────────────────┤ │
   │  │   输入:5 📥  │ │  │ 📤 输出接口 (8个)                        │ │
   │  │   输出:8 📤  │ │  │   - GET /api/profile                      │ │
   │  │ 模块2 (选中) │ │  │   - POST /api/logout                      │ │
   │  │   依赖:3 🔗  │ │  │   - PUT /api/users/{id}                   │ │
   │  │   输入:5 📥  │ │  │   - DELETE /api/users/{id}                │ │
   │  │   输出:8 📤 ⬅ │  │   - GET /api/users/{ id}/permissions        │ │
   │  │ 模块3        │ │  │   - POST /api/users/{id}/permissions        │ │
   │  │   依赖:2 🔗  │ │  │   - GET /api/users/{id}/roles            │ │
   │  │   输入:3 📥  │ │  │   - PUT /api/users/{id}/roles             │ │
   │  │   输出:4 📤  │ │  │                                            │ │
   │  │ ...          │ │  └────────────────────────────────────────────┘ │
   │  └──────────────┘ │                                             │
   └───────────────────┴─────────────────────────────────────────────┘

  具体实现方案

  左侧模块列表（可交互）

   const moduleColumns: ColumnsType<Module> = [
     {
       title: '模块名称',
       dataIndex: 'name',
       key: 'name',
       ellipsis: true,
     },
     {
       title: '详细信息',
       key: 'details',
       render: (_, record) => (
         <Space size="small">
           <Tooltip title="点击查看依赖关系">
             <Button
               type="text"
               size="small"
               icon={<LinkOutlined />}
               onClick={(e) => {
                 e.stopPropagation();
                 handleShowDependencies(record.id);
               }}
               style={{
                 color: '#1890ff',
                 fontWeight: selectedModuleId === record.id && selectedDetailType === 'dependencies' ? 'bold' : 'normal'
               }}
             >
               依赖: {record.dependency_count}
             </Button>
           </Tooltip>
           <Tooltip title="点击查看输入接口">
             <Button
               type="text"
               size="small"
               icon={<ArrowDownOutlined />}
               onClick={(e) => {
                 e.stopPropagation();
                 handleShowInputEndpoints(record.id);
               }}
               style={{
                 color: '#52c41a',
                 fontWeight: selectedModuleId === record.id && selectedDetailType === 'input' ? 'bold' : 'normal'
               }}
             >
               输入: {record.input_endpoint_count}
             </Button>
           </Tooltip>
           <Tooltip title="点击查看输出接口">
             <Button
               type="text"
               size="small"
               icon={<ArrowUpOutlined />}
               onClick={(e) => {
                 e.stopPropagation();
                 handleShowOutputEndpoints(record.id);
               }}
               style={{
                 color: '#fa8c16',
                 fontWeight: selectedModuleId === record.id && selectedDetailType === 'output' ? 'bold' : 'normal'
               }}
             >
               输出: {record.output_endpoint_count}
             </Button>
           </Tooltip>
         </Space>
       ),
     },
     {
       title: '状态',
       dataIndex: 'status',
       key: 'status',
       width: 100,
       render: (status: string) => getStatusTag(status),
     },
   ];

  右侧详情面板（动态展示）

   const ModuleDetailPanel: React.FC<{
     moduleId: number | null;
     projectId: number | undefined;
     detailType: 'dependencies' | 'input' | 'output' | null;
   }> = ({ moduleId, projectId, detailType }) => {
     const [data, setData] = useState<any[]>([]);
     const [loading, setLoading] = useState(false);

     // 根据选中的详情类型加载数据
     useEffect(() => {
       if (moduleId && projectId && detailType) {
         loadDetailData(moduleId, projectId, detailType);
       }
     }, [moduleId, projectId, detailType]);

     const loadDetailData = async (mid: number, pid: number, type: string) => {
       setLoading(true);
       try {
         let response;
         switch (type) {
           case 'dependencies':
             response = await api.get(`/api-integration/modules/dependencies?project_id=${pid}`);
             setData(response.data.dependencies?.filter((d: any) => d.target_group_id === mid) || []);
             break;
           case 'input':
             response = await api.get(`/api-integration/modules/${mid}/status`);
             setData(response.data.input_endpoints || []);
             break;
           case 'output':
             response = await api.get(`/api-integration/modules/${mid}/status`);
             setData(response.data.output_endpoints || []);
             break;
         }
       } catch (error) {
         console.error('加载详情失败:', error);
       } finally {
         setLoading(false);
       }
     };

     if (!moduleId) {
       return (
         <Card>
           <Empty description="请选择一个模块查看详情" />
         </Card>
       );
     }

     const module = modules.find(m => m.id === moduleId);
     const getTitle = () => {
       switch (detailType) {
         case 'dependencies':
           return `🔗 依赖关系 (${data.length}个)`;
         case 'input':
           return `📥 输入接口 (${data.length}个)`;
         case 'output':
           return `📤 输出接口 (${data.length}个)`;
         default:
           return `📋 模块：${module?.name}`;
       }
     };

     const getColumns = () => {
       switch (detailType) {
         case 'dependencies':
           return dependencyColumns;
         case 'input':
         case 'output':
           return endpointColumns;
         default:
           return [];
       }
     };

     return (
       <Card
         title={getTitle()}
         loading={loading}
       >
         {detailType ? (
           <Table
             columns={getColumns()}
             dataSource={data}
             rowKey="id"
             size="small"
             pagination={false}
             scroll={{ y: 500 }}
           />
         ) : (
           <Empty description="请点击左侧模块的详细信息查看对应内容" />
         )}
       </Card>
     );
   };

  主布局

   const Scenarios: React.FC = () => {
     const [selectedModuleId, setSelectedModuleId] = useState<number | null>(null);
     const [selectedDetailType, setSelectedDetailType] = useState<'dependencies' | 'input' | 'output' | null>(null);

     // 处理显示依赖关系
     const handleShowDependencies = (moduleId: number) => {
       setSelectedModuleId(moduleId);
       setSelectedDetailType('dependencies');
     };

     // 处理显示输入接口
     const handleShowInputEndpoints = (moduleId: number) => {
       setSelectedModuleId(moduleId);
       setSelectedDetailType('input');
     };

     // 处理显示输出接口
     const handleShowOutputEndpoints = (moduleId: number) => {
       setSelectedModuleId(moduleId);
       setSelectedDetailType('output');
     };

     return (
       <Row gutter={16}>
         {/* 左侧：模块列表 */}
         <Col span={10}>
           <Card
             title="模块列表"
             extra={
               <Space>
                 <Button icon={<ReloadOutlined />} onClick={loadModules} loading={loadingModules}>
                   刷新
                 </Button>
                 <Button type="primary" icon={<PlayCircleOutlined />} onClick={analyzeAllModules} loading={analyzing}>
                   分析所有模块
                 </Button>
               </Space>
             }
           >
             <Table
               columns={moduleColumns}
               dataSource={modules}
               rowKey="id"
               scroll={{ y: 700 }}
               pagination={false}
               rowSelection={{
                 type: 'checkbox',
                 selectedRowKeys: selectedModuleIds,
                 onChange: (selectedKeys) => setSelectedModuleIds(selectedKeys as number[]),
               }}
               onRow={(record) => ({
                 onClick: () => setSelectedModuleId(record.id),
                 style: selectedModuleId === record.id ? { backgroundColor: '#e6f7ff' } : {},
               })}
             />
           </Card>
         </Col>

         {/* 右侧：模块详情 */}
         <Col span={14}>
           <ModuleDetailPanel
             moduleId={selectedModuleId}
             projectId={currentProject?.id}
             detailType={selectedDetailType}
           />
         </Col>
       </Row>
     );
   };

  优点

   1. ✅ 交互清晰：左侧的每个项目都可以点击，右侧动态展示对应内容
   2. ✅ 信息精准：用户点击什么，右侧就展示什么，不会混淆
   3. ✅ 视觉反馈：左侧选中的项目会高亮显示，用户清楚当前在看什么
   4. ✅ 空间高效：右侧只展示用户选择的内容，不会浪费空间
   5. ✅ 操作便捷：用户可以通过点击快速切换不同的详细信息