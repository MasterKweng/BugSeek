• 可直接贴 Jira/飞书的文件级任务清单（不改代码版）

  下面按 Epic -> Task 给出，每条都含输入/输出/验收标准。
  任务编号可直接用作 Jira Key 后缀（例如 BSK-SC-001）。

  1. BSK-SC-001 基线差异盘点（场景相关）
     文件: base.py, base_backup.py, change_detector.py
     输入: 当前主模型、备份模型、场景影响分析代码
     输出: docs/scenario_baseline_diff.md（字段差异、缺失模型、冲突引用）
     验收标准: 明确列出 ApiScenario 缺失点、ApiEndpoint/ApiDefinition 命名冲突、修复优先级
  2. BSK-SC-002 异步任务链路盘点（sync/field-mapping）
     文件: sync_tasks.py, tasks.py, manager.py
     输入: 同步任务路由、Celery任务、本地异步管理器
     输出: docs/async_task_gap.md（缺失任务定义、双轨状态风险）
     验收标准: 明确 execute_sync_task 缺口和统一状态机方案
  3. BSK-SC-003 场景数据模型设计冻结
     文件: base.py
     输入: 方案文档 + BSK-SC-001 盘点结果
     输出: docs/scenario_schema_v1.md（api_scenarios、scenario_nodes、索引、约束）
     验收标准: 包含 DAG 依赖字段、变量映射字段、执行策略字段
  4. BSK-SC-004 场景表迁移脚本（建表）
     文件: backend/migrations（新增 add_scenario_v1_tables.py）
     输入: scenario_schema_v1.md
     输出: 可执行迁移脚本（create table/index/constraint）
     验收标准: 空库执行成功；重复执行幂等；回滚脚本可用
  5. BSK-SC-005 兼容迁移策略（老数据/历史链路）
     文件: add_scenario_tables.py, add_chain_scenarios_table.py
     输入: 历史迁移脚本
     输出: docs/scenario_migration_compat.md（保留/废弃字段映射）
     验收标准: 明确 scenario_endpoints 与 scenario_nodes 的兼容策略
  6. BSK-SC-006 主模型接入场景实体
     文件: base.py
     输入: 迁移完成后的表结构
     输出: ORM 模型定义与关系定义
     验收标准: ApiScenario 可被 import；关系查询正常；无循环依赖异常
  7. BSK-SC-007 修复场景影响分析引用
     文件: change_detector.py
     输入: 新模型可用性
     输出: 使用主模型实体的影响分析逻辑
     验收标准: 执行 scan_impact 不抛模型不存在错误
  8. BSK-SC-008 场景执行器实现（DAG校验）
     文件: test_execution.py
     输入: 场景节点结构
     输出: ScenarioExecutor 的图校验逻辑（无环、依赖合法、孤点校验）
     验收标准: 非法图返回结构化错误；合法图可进入执行阶段
  9. BSK-SC-009 场景执行器实现（拓扑分层执行）
     文件: test_execution.py
     输入: DAG 校验通过图
     输出: 分层执行器（层内并发、层间串行）
     验收标准: 并行节点可同时执行；依赖节点严格后执行
  10. BSK-SC-010 Context Bus 实现与变量命名规范
     文件: test_execution.py
     输入: 节点输出提取规则
     输出: context 写入/读取、命名空间规范文档 docs/context_bus_spec.md
     验收标准: 下游节点可消费上游变量；冲突变量有覆盖规则
  11. BSK-SC-011 场景执行记录落库
     文件: base.py, test_execution.py
     输入: 节点执行结果
     输出: execution 主记录 + 节点级结果（扩表或扩字段）
     验收标准: 可按 scenario_id 查询完整执行链
  12. BSK-SC-012 场景 API（CRUD）
     文件: backend/app/api/v1（新增 scenarios.py）, init.py
     输入: 场景 ORM
     输出: create/list/detail/update/archive 接口
     验收标准: 接口遵循 {code,message,data}；IDOR 校验通过
  13. BSK-SC-013 场景 API（执行与执行历史）
     文件: backend/app/api/v1（scenarios.py）
     输入: ScenarioExecutor
     输出: POST /scenarios/{id}/execute, GET /scenarios/{id}/executions, GET /executions/{id}
     验收标准: 执行成功返回 execution_id；历史可分页查询
  14. BSK-SC-014 AI任务类型扩展（意图生成场景）
     文件: prompts.py, service.py, gateway.py
     输入: 意图文本、候选API集合
     输出: intent_scenario_generation 任务类型与结构化输出 JSON
     验收标准: 输出可直接落库为 scenario + nodes
  15. BSK-SC-015 API 检索编排（RAG入口）
     文件: vector_index.py, api_definitions.py
     输入: 意图关键词/语义向量
     输出: 候选 API 召回与重排接口
     验收标准: 返回候选列表含评分和理由
  16. BSK-SC-016 意图工作台后端接口
     文件: backend/app/api/v1（新增 intent_workbench.py）, init.py
     输入: 意图文本、项目上下文
     输出: 生成草案接口、确认落库接口
     验收标准: 一次确认后产生可执行场景
  17. BSK-SC-017 字段映射处理器支持场景子集（JIT）
     文件: processor.py
     输入: definition_ids 子集参数
     输出: 仅对场景接口集合执行映射
     验收标准: 子集任务耗时显著低于全量；结果数量与输入接口集合一致
  18. BSK-SC-018 字段映射异步接口支持子集参数
     文件: field_mappings_async.py, tasks.py
     输入: 场景ID或 definition_ids
     输出: 任务创建、轮询、结果查询全链路支持子集
     验收标准: task_params 持久化包含子集范围；结果可追溯 scenario_id
  19. BSK-SC-019 全局映射入口降级策略
     文件: field_mappings.py, FieldMappingSuggestions.tsx
     输入: 现有全量入口
     输出: UI/接口层“默认场景级、全量为高级模式”
     验收标准: 用户默认流程不再触发项目全量计算
  20. BSK-SC-020 前端路由接入新页面
     文件: App.tsx, MainLayout.tsx
     输入: 新后端路由
     输出: 新菜单与路由（意图工作台/场景编排/场景执行/报告）
     验收标准: 菜单可达、鉴权守卫正常
  21. BSK-SC-021 前端场景状态层重构
     文件: scenario.ts, scenario.ts
     输入: 旧 /api-integration/* 约定
     输出: 新 API 契约状态层（场景/节点/执行/报告）
     验收标准: 不再依赖不存在的旧接口路径
  22. BSK-SC-022 意图工作台前端页
     文件: frontend/src/pages（新增 IntentWorkbench.tsx）
     输入: 意图输入 + 候选 API
     输出: 生成草案、人工确认、落库入口
     验收标准: 端到端可创建场景草案
  23. BSK-SC-023 场景编排前端页（DAG可视化）
     文件: frontend/src/pages（新增 ScenarioDesigner.tsx）
     输入: scenario + nodes
     输出: 节点编辑、依赖边编辑、变量连线检查
     验收标准: 编辑后保存成功且后端图校验通过
  24. BSK-SC-024 场景执行前端页
     文件: frontend/src/pages（新增 ScenarioExecutions.tsx）
     输入: execution API
     输出: 实时状态、节点结果、错误定位
     验收标准: 可查看执行中与执行后详情
  25. BSK-SC-025 场景级映射确认联动
     文件: FieldMappingSuggestions.tsx, fieldMapping.ts
     输入: 场景子集建议
     输出: 批量确认后回写场景节点映射
     验收标准: 确认后可直接触发场景执行
  26. BSK-SC-026 CI/CD触发接口
     文件: backend/app/api/v1（新增 execution_triggers.py）, init.py
     输入: scenario_id、environment_id、token
     输出: webhook/trigger API
     验收标准: 可被 Jenkins/GitLab/GHA 调用并返回可轮询 execution_id
  27. BSK-SC-027 CLI触发脚本
     文件: backend（新增 scripts/trigger_scenario.py）
     输入: 命令行参数（base_url/token/scenario/environment）
     输出: 标准退出码（0成功/非0失败）
     验收标准: 可直接用于流水线 gate
  28. BSK-SC-028 报告聚合服务
     文件: backend/app/core（新增 reporting/）, trace.py
     输入: execution + node results + trace
     输出: 结构化报告数据对象
     验收标准: 含通过率、耗时、失败节点、根因输入上下文
  29. BSK-SC-029 报告输出接口（HTML/PDF）
     文件: backend/app/api/v1（新增 reports.py）
     输入: 报告数据对象
     输出: HTML 报告下载/查看接口（PDF可选）
     验收标准: 场景执行后可获取报告 URL
  30. BSK-SC-030 AI RCA 结果注入报告
     文件: service.py, prompts.py, reporting/*
     输入: 失败节点日志、断言差异、trace
     输出: RCA 文本（原因+建议）
     验收标准: 失败报告包含 RCA，成功报告不强制生成
  31. BSK-SC-031 测试覆盖：场景执行主链
     文件: backend/tests（新增 test_scenario_executor.py）
     输入: 场景样例（串行/并行/失败重试）
     输出: 自动化测试
     验收标准: 覆盖 DAG 校验、Context 传递、失败策略
  32. BSK-SC-032 测试覆盖：意图与JIT映射链
     文件: backend/tests（新增 test_intent_to_scenario.py, test_jit_field_mapping.py）
     输入: mock AI 输出、映射任务输入
     输出: 自动化测试
     验收标准: 意图生成结构合法；JIT 子集范围正确
  33. BSK-SC-033 前端回归用例
     文件: frontend/src/pages, frontend/src/store
     输入: 新页面交互流程
     输出: 关键交互测试（至少 smoke）
     验收标准: 从意图输入到场景执行的主流程不崩溃
  34. BSK-SC-034 观测与日志规范补齐
     文件: logging_config.py, test_execution.py
     输入: 执行链路节点事件
     输出: 统一日志字段规范文档 docs/observability_spec.md
     验收标准: 每次执行具备 trace_id、scenario_id、node_key
  35. BSK-SC-035 发布策略与开关
     文件: config.py, frontend/src/constants
     输入: 灰度上线需求
     输出: feature flags（scenario_v2_enabled 等）
     验收标准: 可按项目/环境灰度启用并可回滚

  ———

  建议的 Jira Epic 划分

  1. EPIC-A 场景数据与执行主干（任务 001-013）
  2. EPIC-B 意图工作台与 JIT 映射（任务 014-025）
  3. EPIC-C CI/CD 与报告闭环（任务 026-030）
  4. EPIC-D 测试与发布治理（任务 031-035）

  如果你要，我可以下一条直接给你“飞书多维表可粘贴 CSV 版本”（含负责人、工期、优先级、前置依赖列）。