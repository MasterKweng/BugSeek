# 阶段化保存+断点续传功能设计方案

## 一、总体架构设计

### 1.1 设计目标

| 目标 | 描述 |
|------|------|
| 阶段化保存 | 每个阶段完成后立即保存结果到数据库 |
| 断点续传 | 任务中断后可从失败阶段继续执行 |
| 独立重试 | 支持按阶段单独重试 |
| 用户控制 | 用户可查看中间结果并确认继续 |
| 数据不丢失 | 任何阶段失败，已完成数据不丢失 |

### 1.2 架构对比

**当前架构（全有或全无）**:
```
[开始] → 阶段1 → 阶段2 → 阶段3 → 阶段4 → 阶段5 → [保存到数据库]
                    ↓ 失败 → 所有数据丢失
```

**改进架构（阶段化保存）**:
```
[开始] → 阶段1 → [保存] → 阶段2 → [保存] → 阶段3 → [保存] → 阶段4 → [保存] → 阶段5 → [保存]
                    ↓ 失败          ↓ 失败          ↓ 失败          ↓ 失败
                已保存数据保留   已保存数据保留   已保存数据保留   已保存数据保留
```

---

## 二、数据库设计

### 2.1 方案选择

**推荐方案**: 扩展 `AsyncTask` 表，添加阶段结果字段

**理由**:
- 避免创建新表，简化数据关联
- 数据一致性更好（都在同一事务中）
- 查询效率高（不需要JOIN）
- 便于现有代码迁移

### 2.2 表结构扩展

#### 2.2.1 AsyncTask 表新增字段

```sql
ALTER TABLE async_tasks ADD COLUMN current_stage INTEGER DEFAULT 0;
ALTER TABLE async_tasks ADD COLUMN stage_results JSONB DEFAULT '{}';

COMMENT ON COLUMN async_tasks.current_stage IS '当前执行到的阶段 (0=未开始, 1=字段提取, 2=规则评分, 3=智能筛选, 4=AI优化, 5=结果合并)';
COMMENT ON COLUMN async_tasks.stage_results IS '各阶段结果数据: {"stage1": {...}, "stage2": {...}, ...}';
```

#### 2.2.2 stage_results 数据结构

```json
{
  "stage1": {
    "name": "字段提取",
    "status": "completed",
    "progress": 100,
    "completed_at": "2026-02-11 16:30:00",
    "data": {
      "total_fields": 595,
      "unique_fields": 595,
      "field_count_by_type": {
        "path": 120,
        "query": 80,
        "body": 395
      },
      "sample_fields": ["order_id", "user_id", "product_id"]
    }
  },
  "stage2": {
    "name": "规则评分",
    "status": "completed",
    "progress": 100,
    "completed_at": "2026-02-11 16:32:00",
    "data": {
      "processed_fields": 595,
      "success_fields": 595,
      "failed_fields": 0,
      "avg_score": 0.72,
      "avg_candidates_per_field": 3.2
    }
  },
  "stage3": {
    "name": "智能筛选",
    "status": "completed",
    "progress": 100,
    "completed_at": "2026-02-11 16:33:00",
    "data": {
      "auto_confirm": 120,
      "ai_high": 80,
      "ai_medium": 200,
      "ai_low": 195
    }
  },
  "stage4": {
    "name": "AI优化",
    "status": "completed",
    "progress": 100,
    "completed_at": "2026-02-11 16:35:00",
    "data": {
      "total_batches": 8,
      "processed_batches": 8,
      "ai_optimized_fields": 275,
      "ai_failed_fields": 0
    }
  },
  "stage5": {
    "name": "结果合并",
    "status": "completed",
    "progress": 100,
    "completed_at": "2026-02-11 16:36:00",
    "data": {
      "total_suggestions": 4320,
      "unique_fields_covered": 595
    }
  }
}
```

---

## 三、API接口设计

### 3.1 接口清单

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 创建任务 | POST | `/api/v1/field-mappings/suggest` | 创建新的映射建议任务 |
| 查询任务 | GET | `/api/v1/async-tasks/{task_id}` | 查询任务详情（含阶段结果） |
| 查询阶段结果 | GET | `/api/v1/field-mappings/tasks/{task_id}/stage/{stage_num}` | 查询指定阶段的详细结果 |
| 继续执行 | POST | `/api/v1/field-mappings/tasks/{task_id}/resume` | 从当前阶段继续执行 |
| 重试阶段 | POST | `/api/v1/field-mappings/tasks/{task_id}/retry/{stage_num}` | 重试指定阶段 |
| 重置任务 | POST | `/api/v1/field-mappings/tasks/{task_id}/reset` | 重置任务到初始状态 |
| 确认阶段 | POST | `/api/v1/field-mappings/tasks/{task_id}/confirm/{stage_num}` | 用户确认当前阶段，继续下一阶段 |

---

## 四、后端逻辑设计

### 4.1 阶段常量定义

```python
# backend/app/field_mapping/constants.py
from enum import IntEnum

class Stage(IntEnum):
    """阶段枚举"""
    NOT_STARTED = 0
    FIELD_EXTRACTION = 1      # 字段提取
    RULE_SCORING = 2           # 规则评分
    INTELLIGENT_SCREENING = 3  # 智能筛选
    AI_OPTIMIZATION = 4        # AI优化
    RESULT_MERGE = 5           # 结果合并

class StageStatus(IntEnum):
    """阶段状态枚举"""
    NOT_STARTED = "not_started"
    RUNNING = "running"
    COMPLETED = "completed"
    FAILED = "failed"
    SKIPPED = "skipped"

# 阶段信息配置
STAGE_CONFIG = {
    Stage.FIELD_EXTRACTION: {
        "name": "字段提取",
        "description": "从所有API定义中提取字段并去重",
        "progress_range": (0, 10)
    },
    Stage.RULE_SCORING: {
        "name": "规则评分",
        "description": "使用规则引擎对字段进行数据库匹配评分",
        "progress_range": (15, 35)
    },
    Stage.INTELLIGENT_SCREENING: {
        "name": "智能筛选",
        "description": "根据规则评分结果对字段进行优先级分类",
        "progress_range": (40, 40)
    },
    Stage.AI_OPTIMIZATION: {
        "name": "AI优化",
        "description": "使用AI服务对筛选出的字段进行智能推荐优化",
        "progress_range": (45, 95)
    },
    Stage.RESULT_MERGE: {
        "name": "结果合并",
        "description": "合并规则候选和AI候选，生成最终建议列表",
        "progress_range": (98, 100)
    }
}
```

### 4.2 阶段结果保存方法

```python
# backend/app/field_mapping/processor.py

def _save_stage_result(
    self,
    stage_num: int,
    stage_data: Dict[str, Any],
    status: str = "completed"
):
    """
    保存阶段结果到数据库
    
    Args:
        stage_num: 阶段编号 (1-5)
        stage_data: 阶段结果数据
        status: 阶段状态
    """
    stage_key = f"stage{stage_num}"
    
    if not self.task.stage_results:
        self.task.stage_results = {}
    
    # 更新阶段结果
    self.task.stage_results[stage_key] = {
        "name": STAGE_CONFIG[stage_num]["name"],
        "status": status,
        "progress": 100 if status == "completed" else 0,
        "completed_at": datetime.now().isoformat() if status == "completed" else None,
        "data": stage_data
    }
    
    # 更新当前阶段
    self.task.current_stage = stage_num
    
    self.db.commit()
    logger.info(f"[{self.trace_id}] 阶段{stage_num}结果已保存: {status}")
```

### 4.3 断点续传逻辑

```python
# backend/app/field_mapping/processor.py

async def execute(self) -> Dict[str, Any]:
    """
    执行字段映射任务（支持断点续传）
    """
    
    # 获取当前阶段（断点续传）
    current_stage = self.task.current_stage or 0
    
    logger.info(f"[{self.trace_id}] 开始执行任务，当前阶段: {current_stage}")
    
    # 阶段1: 字段提取和去重
    if current_stage < Stage.FIELD_EXTRACTION:
        self._execute_stage_1()
        self._save_stage_result(Stage.FIELD_EXTRACTION, {
            "total_fields": len(field_registry),
            "unique_fields": len(field_registry),
            "field_count_by_type": self._count_by_type(field_registry)
        })
    
    # 从数据库加载阶段1结果（断点续传）
    if current_stage >= Stage.FIELD_EXTRACTION:
        field_registry = self._load_stage_result(Stage.FIELD_EXTRACTION)
    
    # 阶段2: 规则评分
    if current_stage < Stage.RULE_SCORING:
        self._execute_stage_2(field_registry)
        self._save_stage_result(Stage.RULE_SCORING, {
            "processed_fields": len(rule_results),
            "success_fields": len([r for r in rule_results.values() if r]),
            "avg_score": self._calculate_avg_score(rule_results)
        })
    
    # 从数据库加载阶段2结果（断点续传）
    if current_stage >= Stage.RULE_SCORING:
        rule_results = self._load_stage_result(Stage.RULE_SCORING)
    
    # 阶段3: 智能筛选
    if current_stage < Stage.INTELLIGENT_SCREENING:
        categories = self._execute_stage_3(field_registry, rule_results)
        self._save_stage_result(Stage.INTELLIGENT_SCREENING, {
            "auto_confirm": len(categories.get('auto_confirm', [])),
            "ai_high": len(categories.get('ai_high', [])),
            "ai_medium": len(categories.get('ai_medium', [])),
            "ai_low": len(categories.get('ai_low', []))
        })
    
    # 从数据库加载阶段3结果（断点续传）
    if current_stage >= Stage.INTELLIGENT_SCREENING:
        categories = self._load_stage_result(Stage.INTELLIGENT_SCREENING)
    
    # 阶段4: AI优化
    if current_stage < Stage.AI_OPTIMIZATION:
        ai_results = await self._execute_stage_4(field_registry, categories)
        self._save_stage_result(Stage.AI_OPTIMIZATION, {
            "total_batches": total_batches,
            "processed_batches": completed_batches,
            "ai_optimized_fields": len(ai_results)
        })
    
    # 从数据库加载阶段4结果（断点续传）
    if current_stage >= Stage.AI_OPTIMIZATION:
        ai_results = self._load_stage_result(Stage.AI_OPTIMIZATION)
    
    # 阶段5: 结果合并
    if current_stage < Stage.RESULT_MERGE:
        suggestions = self._execute_stage_5(field_registry, ai_results)
        self._save_stage_result(Stage.RESULT_MERGE, {
            "total_suggestions": len(suggestions),
            "unique_fields_covered": len(field_registry)
        })
    
    return {
        "status": "success",
        "total": len(suggestions),
        "suggestions": [s.model_dump() for s in suggestions]
    }
```

### 4.4 加载阶段结果

```python
def _load_stage_result(self, stage_num: int) -> Any:
    """
    从数据库加载阶段结果
    
    Args:
        stage_num: 阶段编号
    
    Returns:
        阶段结果数据
    """
    stage_key = f"stage{stage_num}"
    
    if not self.task.stage_results:
        raise ValueError(f"阶段{stage_num}结果不存在")
    
    stage_result = self.task.stage_results.get(stage_key)
    
    if not stage_result:
        raise ValueError(f"阶段{stage_num}结果不存在")
    
    if stage_result.get("status") != "completed":
        raise ValueError(f"阶段{stage_num}未完成")
    
    logger.info(f"[{self.trace_id}] 从数据库加载阶段{stage_num}结果")
    
    return stage_result.get("data")
```

---

## 五、前端UI设计

### 5.1 阶段进度展示组件

```typescript
// frontend/src/components/FieldMappingStageProgress.tsx
interface StageResult {
  name: string;
  status: 'not_started' | 'running' | 'completed' | 'failed';
  progress: number;
  completed_at?: string;
  data?: any;
}

interface TaskProgress {
  current_stage: number;
  stage_results: {
    stage1: StageResult;
    stage2: StageResult;
    stage3: StageResult;
    stage4: StageResult;
    stage5: StageResult;
  };
}

export const FieldMappingStageProgress: React.FC<{
  taskId: number;
  progress: TaskProgress;
  onStageClick?: (stageNum: number) => void;
  onRetry?: (stageNum: number) => void;
}> = ({ taskId, progress, onStageClick, onRetry }) => {
  const stages = [
    { num: 1, name: "字段提取", icon: "📋" },
    { num: 2, name: "规则评分", icon: "📊" },
    { num: 3, name: "智能筛选", icon: "🔍" },
    { num: 4, name: "AI优化", icon: "🤖" },
    { num: 5, name: "结果合并", icon: "📝" }
  ];

  const getStageStatus = (stageNum: number) => {
    const stageKey = `stage${stageNum}`;
    const stage = progress.stage_results?.[stageKey];
    return stage?.status || 'not_started';
  };

  const getStageProgress = (stageNum: number) => {
    const stageKey = `stage${stageNum}`;
    const stage = progress.stage_results?.[stageKey];
    return stage?.progress || 0;
  };

  return (
    <div className="space-y-4">
      {stages.map((stage) => {
        const status = getStageStatus(stage.num);
        const stageProgress = getStageProgress(stage.num);
        const isCurrentStage = progress.current_stage === stage.num;
        const isFailed = status === 'failed';
        
        return (
          <div
            key={stage.num}
            className={`p-4 border rounded-lg ${
              isCurrentStage ? 'border-blue-500 bg-blue-50' : 'border-gray-200'
            }`}
          >
            <div className="flex items-center justify-between">
              <div className="flex items-center space-x-3">
                <span className="text-2xl">{stage.icon}</span>
                <div>
                  <div className="font-medium">{stage.name}</div>
                  <div className="text-sm text-gray-500">
                    状态: {getStatusLabel(status)}
                  </div>
                </div>
              </div>
              
              <div className="flex items-center space-x-2">
                <Progress percent={stageProgress} size="small" />
                
                {status === 'completed' && (
                  <Button
                    type="link"
                    size="small"
                    onClick={() => onStageClick?.(stage.num)}
                  >
                    查看结果
                  </Button>
                )}
                
                {isFailed && (
                  <Button
                    type="primary"
                    danger
                    size="small"
                    onClick={() => onRetry?.(stage.num)}
                  >
                    重试
                  </Button>
                )}
              </div>
            </div>
          </div>
        );
      })}
    </div>
  );
};
```

---

## 六、执行步骤

### 6.1 第一阶段：数据库迁移（预计1小时）

| 步骤 | 任务 | 验证方式 |
|------|------|----------|
| 1.1 | 创建迁移脚本 `add_field_mapping_stage_results.py` | 代码审查 |
| 1.2 | 执行迁移脚本 | 检查数据库表结构 |
| 1.3 | 验证字段添加成功 | 查询 `async_tasks` 表结构 |

### 6.2 第二阶段：后端核心逻辑改造（预计4小时）

| 步骤 | 任务 | 验证方式 |
|------|------|----------|
| 2.1 | 创建 `constants.py` 定义阶段常量 | 单元测试 |
| 2.2 | 在 `processor.py` 添加 `_save_stage_result` 方法 | 单元测试 |
| 2.3 | 在 `processor.py` 添加 `_load_stage_result` 方法 | 单元测试 |
| 2.4 | 改造 `execute` 方法支持断点续传 | 单元测试 |
| 2.5 | 在每个阶段完成后调用保存方法 | 集成测试 |
| 2.6 | 添加异常处理，确保失败时保存状态 | 集成测试 |

### 6.3 第三阶段：API接口开发（预计3小时）

| 步骤 | 任务 | 验证方式 |
|------|------|----------|
| 3.1 | 添加 `get_stage_result` 接口 | API测试 |
| 3.2 | 添加 `resume_task` 接口 | API测试 |
| 3.3 | 添加 `retry_stage` 接口 | API测试 |
| 3.4 | 添加 `reset_task` 接口 | API测试 |
| 3.5 | 更新 `get_async_task_progress` 接口返回阶段结果 | API测试 |

### 6.4 第四阶段：前端UI开发（预计4小时）

| 步骤 | 任务 | 验证方式 |
|------|------|----------|
| 4.1 | 创建 `FieldMappingStageProgress` 组件 | 组件测试 |
| 4.2 | 创建 `StageResultModal` 组件 | 组件测试 |
| 4.3 | 创建各阶段结果展示子组件 | 组件测试 |
| 4.4 | 在主页面集成阶段进度展示 | UI测试 |
| 4.5 | 添加控制按钮（继续、重试、重置） | 功能测试 |

### 6.5 第五阶段：集成测试（预计2小时）

| 步骤 | 任务 | 验证方式 |
|------|------|----------|
| 5.1 | 测试完整流程（5个阶段全部成功） | 端到端测试 |
| 5.2 | 测试断点续传（中途中断后继续） | 端到端测试 |
| 5.3 | 测试阶段重试（AI优化失败后重试） | 端到端测试 |
| 5.4 | 测试任务重置（清除所有结果） | 端到端测试 |
| 5.5 | 测试异常场景（网络断开、服务异常） | 容错测试 |

### 6.6 第六阶段：文档和发布（预计1小时）

| 步骤 | 任务 | 交付物 |
|------|------|--------|
| 6.1 | 更新API文档 | API文档 |
| 6.2 | 更新用户手册 | 用户手册 |
| 6.3 | 代码审查 | 审查记录 |
| 6.4 | 合并代码 | Git提交 |

---

## 七、风险评估

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| 数据库迁移失败 | 高 | 低 | 备份数据库，支持回滚 |
| 阶段结果数据过大 | 中 | 中 | 对大字段进行压缩或分片存储 |
| 断点续传逻辑复杂 | 高 | 中 | 充分的单元测试和集成测试 |
| 前端兼容性问题 | 低 | 低 | 使用渐进式增强，保持向后兼容 |
| 性能下降 | 中 | 低 | 添加数据库索引，优化查询 |

---

## 八、预期收益

| 指标 | 当前 | 改进后 | 提升 |
|------|------|--------|------|
| 任务失败后恢复时间 | 100%重新执行 | 只需重试失败阶段 | **降低70%** |
| AI调用成本 | 失败全部浪费 | 已完成阶段保留 | **降低50%** |
| 用户体验 | 无法查看中间结果 | 可查看每个阶段 | **显著提升** |
| 系统可靠性 | 全有或全无 | 阶段化保存 | **显著提升** |

---

## 九、总结

本设计方案通过以下核心改进实现阶段化保存和断点续传：

1. **数据库层面**: 扩展 `AsyncTask` 表，添加 `current_stage` 和 `stage_results` 字段
2. **后端层面**: 每个阶段完成后立即保存结果，支持从任意阶段继续执行
3. **API层面**: 提供阶段查询、继续、重试、重置等控制接口
4. **前端层面**: 展示阶段进度，支持查看中间结果和手动控制执行流程

预计总开发时间：**15小时**（1-2个工作日）