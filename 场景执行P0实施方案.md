# 场景执行 P0 实施方案

这份文档只聚焦 P0 改造，不再重复问题背景，直接回答三件事：

1. 具体改哪些类和函数
2. 入参、返回结构怎么调整
3. 以什么顺序改，才能减少回归风险

目标：

- 一次场景执行只有 1 条父 scenario execution
- 节点 child execution 能挂到父 execution 下
- `status / result_status / success` 语义统一
- `execution_mode / timeout / retry / override` 真正进入执行语义
- 并发节点不共享同一个 `Session`

---

## 一、P0 改造范围

### 主要文件

- `backend/app/execution/engine.py`
- `backend/app/execution/worker.py`
- `backend/app/api/v1/scenarios.py`
- `backend/app/api/v1/execution_triggers.py`
- `backend/app/celery/tasks/ai_tasks.py`
- `backend/app/api/v1/task_status.py`

### P0 拆分

- P0-A：统一状态模型
- P0-B：收口父 execution
- P0-C：打通父子 execution
- P0-D：执行计划真实生效
- P0-E：timeout / retry 落地
- P0-F：节点 override 生效
- P0-G：并发 session 隔离

---

## 二、统一状态模型

## 1. 返回结构目标

后续所有场景执行相关返回统一为：

```json
{
  "execution_id": 123,
  "scenario_id": 456,
  "status": "completed",
  "result_status": "failed",
  "success": false,
  "summary": {
    "total": 3,
    "passed": 2,
    "failed": 1,
    "skipped": 0,
    "duration_ms": 1540
  },
  "error_message": null,
  "results": []
}
```

### 状态定义

- `status`
  - 生命周期状态
  - 合法值：`pending / running / completed / failed / cancelled`
- `result_status`
  - 测试结果状态
  - 合法值：`passed / failed / partial_failed / skipped`
- `success`
  - 快速判断字段
  - 规则：仅当 `status == completed` 且 `result_status == passed` 时为 `true`

---

## 2. `engine.py` 签名改造

### 当前

`ScenarioExecutor.execute_scenario(...) -> Dict[str, Any]`

### 建议改为

```python
async def execute_scenario(
    self,
    scenario_id: int,
    graph_data: Optional[Dict[str, Any]],
    variables: Optional[Dict[str, Any]],
    db: Session,
    environment_id: Optional[int] = None,
    version_id: Optional[int] = None,
    operator_user_id: Optional[int] = None,
    source_execution_id: Optional[int] = None,
    execution_id: Optional[int] = None,
    triggered_by: str = "manual",
) -> Dict[str, Any]:
```

### 关键新增参数

- `execution_id`
  - 父 scenario execution 的 id
  - 场景执行器只更新它，不再自行新建第二条父 execution

---

## 3. 新增内部结果对象约定

建议在 `engine.py` 内统一场景层结果结构：

```python
scenario_result = {
    "scenario_id": scenario_id,
    "execution_id": execution_id,
    "status": "completed",
    "result_status": "failed",
    "success": False,
    "error_message": None,
    "execution_mode_effective": "dag",
    "summary": {...},
    "results": [...],
    "context": {...},
}
```

### 规则

- 执行器内部异常：
  - `status=failed`
  - `result_status=failed`
  - `success=false`
  - `error_message` 写执行级错误
- 正常执行结束但节点有失败：
  - `status=completed`
  - `result_status=failed / partial_failed`
  - `success=false`

---

## 三、收口父 execution

## 1. 新增统一创建函数

建议在 `scenarios.py` 和 `execution_triggers.py` 都不要手写 `TestExecution(...)`，统一抽一个函数：

```python
def create_scenario_execution(
    db: Session,
    scenario: ApiScenario,
    environment_id: int,
    operator_user_id: Optional[int],
    triggered_by: str,
    webhook_url: Optional[str] = None,
    source_execution_id: Optional[int] = None,
) -> TestExecution:
```

### 创建规则

- `execution_type = "scenario"`
- `target_id = scenario.id`
- `status = "pending"`
- `result_status = None`
- `total / passed / failed / skipped = 0`
- `execution_mode = scenario.execution_mode`

### 好处

- 同步和异步入口完全复用
- 不会再出现两套 execution 初始化逻辑

---

## 2. 同步执行入口改造

### 文件

- `backend/app/api/v1/scenarios.py`

### 当前问题

- 直接调用 `executor.execute_scenario(...)`
- 没有先创建父 execution

### 改造后流程

1. 校验场景与环境
2. `create_scenario_execution(...)`
3. 调用 `executor.execute_scenario(..., execution_id=execution.id, ...)`
4. 返回统一结果结构

### 伪代码

```python
execution = create_scenario_execution(...)
result = await executor.execute_scenario(
    scenario_id=scenario_id,
    graph_data=None,
    variables=variables or {},
    environment_id=resolved_environment_id,
    version_id=scenario.version_id,
    operator_user_id=current_user.id,
    execution_id=execution.id,
    triggered_by="manual",
    db=db,
)
```

---

## 3. 异步执行入口改造

### 文件

- `backend/app/api/v1/execution_triggers.py`
- `backend/app/celery/tasks/ai_tasks.py`

### 改造后流程

#### 触发接口

1. 创建父 execution
2. 把 `execution.id` 传给 Celery
3. 立即返回 `pending`

#### Celery 任务

1. 查询该 execution
2. 标记为 `running`
3. 调用 `executor.execute_scenario(..., execution_id=execution.id, ...)`
4. 根据返回结果更新 `status / result_status / success / summary`
5. 回调统一 payload

### 关键要求

Celery 任务里不能再允许执行器创建第二条 scenario execution。

---

## 四、打通父子 execution

## 1. 节点执行新增父 execution 透传

### 文件

- `backend/app/execution/engine.py`

### `_execute_single_node` 签名建议

```python
async def _execute_single_node(
    self,
    scenario: ApiScenario,
    node: Dict[str, Any],
    context: Dict[str, Any],
    environment_id: Optional[int],
    version_id: Optional[int],
    operator_user_id: Optional[int],
    execution_id: int,
    triggered_by: str,
    db: Session,
) -> Dict[str, Any]:
```

### 改造点

调用 `CaseExecutor.execute_case()` 时增加：

```python
parent_execution_id=execution_id
```

如后续需要更强追溯，也可以继续传：

```python
source_execution_id=execution_id
```

但两者不要混用不清。

推荐语义：

- `parent_execution_id`
  - 表示节点 child execution 属于哪个父 scenario execution
- `source_execution_id`
  - 仅用于“由哪个已有执行派生/重试/复用”

---

## 2. 保持场景级 `TestExecutionResult`

场景执行仍然保留场景级 `TestExecutionResult`，因为它直接对应 `ScenarioNode.id`，方便：

- 展示节点结果
- 做 stage 进度图
- 做节点级状态聚合

建议关系如下：

- 父 execution
  - `TestExecution(execution_type=scenario)`
- 节点概览
  - `TestExecutionResult(execution_id=父 execution.id, target_id=ScenarioNode.id)`
- 节点详情
  - `TestExecution(execution_type=single, parent_execution_id=父 execution.id)`

---

## 五、执行计划真实生效

## 1. 新增 `_get_execution_plan`

### 文件

- `backend/app/execution/engine.py`

### 建议签名

```python
def _get_execution_plan(
    self,
    scenario: ApiScenario,
    nodes: List[Dict[str, Any]],
) -> Tuple[List[List[Dict[str, Any]]], str]:
```

返回：

- 执行批次列表
- `execution_mode_effective`

### 规则

#### sequential

- 按 `(step_order, id)` 排序
- 每个节点单独成为一个 batch
- `depends_on` 仍做校验，但不再决定调度批次

#### dag

- 继续使用 `_build_execution_levels(nodes)`

### 结果

`execute_scenario()` 内不再直接调 `_build_execution_levels()`，统一改为：

```python
execution_plan, execution_mode_effective = self._get_execution_plan(scenario, nodes)
```

---

## 六、timeout / retry 落地

## 1. 节点级执行包装

### 建议新增函数

```python
async def _run_node_with_retry(...)
```

### 职责

- 计算 effective timeout
- 计算 effective retry_count
- 用 `asyncio.wait_for()` 包住节点执行
- 根据错误类型决定是否重试

### effective 规则

- 节点级优先
- 否则取场景级
- 都没有则使用默认值

例如：

```python
node_timeout = node.get("timeout_seconds") or scenario.timeout_seconds or 600
node_retry = node.get("retry_count") if node.get("retry_count") is not None else scenario.retry_count
```

---

## 2. `worker.py` 中的 HTTP timeout

当前 `CaseExecutor` 使用固定：

```python
httpx.AsyncClient(timeout=30.0)
```

建议两步走：

### 第一步

先保留 client 默认 timeout，但允许 `execute_case()` 接收：

```python
request_timeout: Optional[float] = None
```

### 第二步

请求发出时优先使用节点 effective timeout：

```python
timeout = request_timeout or 30.0
```

这样不用一次性重构整个 `CaseExecutor` 生命周期。

---

## 七、节点 override 生效

## 1. `engine.py` 内做运行态覆盖

### 建议新增函数

```python
def _build_execution_case(
    self,
    case: ApiCase,
    node: Dict[str, Any],
) -> ApiCase:
```

### 行为

- 对 `case` 做浅复制
- 如果节点有 `assertion_overrides`，覆盖 `assertion_rules`
- 如果节点有 `extract_rules`，覆盖 `extraction_rules`

### 注意

不要修改 ORM 挂载的原始对象，避免误写回数据库。

更稳的做法是构造一个轻量运行态对象，或者复制后只作为内存对象传给 `CaseExecutor`。

---

## 八、并发 session 隔离

## 1. 不再把共享 `db` 直接交给并发节点

### 文件

- `backend/app/execution/engine.py`

### 建议改法

`execute_scenario()` 保留主 session 做：

- 查场景
- 更新父 execution
- 保存场景级结果

而 `_execute_single_node()` 内部自己创建 session：

```python
node_db = SessionLocal()
try:
    ...
finally:
    node_db.close()
```

### 重要点

- `_resolve_node_target()` 使用 `node_db`
- `CaseExecutor.execute_case()` 使用 `node_db`
- 节点级 commit / rollback 只影响该节点

---

## 九、外围接口改造

## 1. `execution_triggers.py`

### 需要改的点

- 同步接口成功判断不要再写：

```python
result["status"] == "completed" and result["summary"]["failed"] == 0
```

统一改成：

```python
result["success"] is True
```

### 返回结构统一

```json
{
  "execution_id": 1,
  "scenario_id": 2,
  "status": "completed",
  "result_status": "failed",
  "success": false,
  "async_mode": false,
  "summary": {...},
  "error_message": null
}
```

---

## 2. `task_status.py`

### 需要改的点

- `UnifiedTaskStatus.status` 继续保留生命周期状态
- `summary` 中补 `result_status`
- `detail` 中补 `success`
- stage 状态继续根据 node result 转换

### 建议补充字段

```python
summary = {
    "total": ...,
    "passed": ...,
    "failed": ...,
    "skipped": ...,
    "duration_ms": ...,
    "result_status": execution.result_status,
    "success": execution.result_status == "passed" and execution.status == "completed",
}
```

---

## 十、推荐实施顺序

## 第 1 步

只改 execution 收口和状态模型，不碰 timeout/retry/override：

- 增加 `create_scenario_execution`
- 给 `execute_scenario()` 增加 `execution_id`
- 让执行器不再新建第二条 scenario execution
- 打通 child execution 的 `parent_execution_id`
- 统一同步/异步/task_status 的 `status/result_status/success`

这是最小闭环，也是收益最高的一步。

## 第 2 步

落地执行语义：

- `_get_execution_plan`
- `sequential/dag`
- `_run_node_with_retry`
- 节点 timeout / retry

## 第 3 步

落地节点控制力和并发安全：

- `_build_execution_case`
- override 生效
- 节点独立 session

---

## 十一、P0 完成后的验收标准

满足下面这些，才能算 P0 真正完成：

1. 同步执行和异步执行都只产生 1 条父 scenario execution。
2. 每个节点产生的 child execution 都能通过 `parent_execution_id` 追到父 execution。
3. 所有入口返回都统一为 `status/result_status/success`。
4. `sequential` 模式下无依赖节点不再并发。
5. 节点 timeout/retry 会真实影响执行。
6. 节点 `assertion_overrides / extract_rules` 会真实生效。
7. 并发节点不再共享同一个 `Session`。

---

## 十二、最终建议

P0 最值得优先做的，不是把所有能力一次改完，而是先完成“execution 收口 + 状态模型统一”这一个小闭环。

只要这一步完成，场景执行模块的很多脏状态、脏历史、错位查询问题就会立刻下降，而且后面的 timeout、retry、override、断点续跑也会更容易接上去。
